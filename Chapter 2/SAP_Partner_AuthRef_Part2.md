# 🌐 SAP Partner Namespaces — Part 2 (P to Z)
> **Source:** USOBT_C table — SAP S/4HANA system
> **Date:** March 2026

---

## Namespace Reference

| Namespace | SAP System / Solution | T-codes |
|-----------|----------------------|---------|
| `/PMG/` | SAP Portfolio Management | 60 |
| `/PRA/` | SAP Production & Revenue Accounting (Oil & Gas) | 693 |
| `/QOS/` | SAP Quality of Service | 4 |
| `/RPM/` | SAP Resource & Portfolio Management | 21 |
| `/S4PPM/` | SAP S/4HANA Portfolio & Project Management | 6 |
| `/SAM4U/` | SAP Asset Management for Utilities | 9 |
| `/SAPAPO/` | SAP Advanced Planning & Optimization (APO) | 526 |
| `/SAPBOQ/` | SAP BOQ — Bill of Quantities | 5 |
| `/SAPCE/` | SAP Construction Engineering | 63 |
| `/SAPCEM/` | SAP Construction Engineering & Management | 40 |
| `/SAPCND/` | SAP Condition Contract Management | 58 |
| `/SAPDII/` | SAP Digital Integration Infrastructure | 19 |
| `/SAPDMC/` | SAP Digital Manufacturing Cloud | 1 |
| `/SAPF15/` | SAP Financial Closing (F15) | 13 |
| `/SAPMP/` | SAP Master Data Management (MDM) | 20 |
| `/SAPNEA/` | SAP Near East & Africa Localization | 8 |
| `/SAPPCE/` | SAP PCE — Process Control Engineering | 66 |
| `/SAPPO/` | SAP Portfolio & Project Management (PPM) | 41 |
| `/SAPPSPRO/` | SAP Project System Professional | 13 |
| `/SAPSLL/` | SAP Global Trade Services (GTS) | 114 |
| `/SAPSMOSS/` | SAP Service & Asset Intelligence | 19 |
| `/SAPTRX/` | SAP Event Management — Tracking | 35 |
| `/SCA/` | SAP Supply Chain Analytics | 49 |
| `/SCDL/` | SAP Delivery (SCM) | 39 |
| `/SCF/` | SAP Supply Chain Foundation | 56 |
| `/SCMB/` | SAP SCM Basis | 197 |
| `/SCMTMS/` | SAP Transportation Management (TM) | 436 |
| `/SCTM/` | SAP Contract & Time Management | 60 |
| `/SCWM/` | SAP Extended Warehouse Management (EWM) | 1107 |
| `/SDF/` | SAP System Diagnostics & Focused Run | 58 |
| `/SEHS/` | SAP Environment Health & Safety (EHS) | 82 |
| `/SHCM/` | SAP Healthcare Management | 14 |
| `/SMB/` | SAP SMB Solutions | 33 |
| `/SMFND/` | SAP Service Management Foundation | 10 |
| `/SOMO/` | SAP Social Media Order Management | 1 |
| `/SPE/` | SAP Shipment Processing & Event Management | 115 |
| `/SRMERP/` | SAP SRM ERP Integration | 9 |
| `/SRMSMC/` | SAP SRM Supplier Management | 124 |
| `/SSF/` | SAP Secure Store & Forward | 5 |
| `/STMC/` | SAP Store Management & Commerce | 4 |
| `/STTPEC/` | SAP Time & Attendance Management | 12 |
| `/SYCLO/` | SAP Syclo — Mobile Applications | 18 |
| `/TMF/` | SAP TM Foundation | 45 |
| `/VSO/` | SAP Vehicle Scheduling & Optimization | 21 |

---


## `/PMG/` — SAP Portfolio Management
> T-codes: **60**


### 1. `/PMG/45000001`

```
T-Code: /PMG/45000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMBTXACT
```

### 2. `/PMG/45000004`

```
T-Code: /PMG/45000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMP_VTD
```

### 3. `/PMG/45000008`

```
T-Code: /PMG/45000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_V_GEN_MD
```

### 4. `/PMG/45000011`

```
T-Code: /PMG/45000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_POVE_OPT
```

### 5. `/PMG/45000013`

```
T-Code: /PMG/45000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_ORG_LVLV
```

### 6. `/PMG/45000014`

```
T-Code: /PMG/45000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_ORG_ASGV
```

### 7. `/PMG/45000019`

```
T-Code: /PMG/45000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_MCA_PGEN
```

### 8. `/PMG/45000021`

```
T-Code: /PMG/45000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMP_PSCR
```

### 9. `/PMG/45000022`

```
T-Code: /PMG/45000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMP_SCR
```

### 10. `/PMG/59000003`

```
T-Code: /PMG/59000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMCOR
```

### 11. `/PMG/59000007`

```
T-Code: /PMG/59000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMPLN
```

### 12. `/PMG/59000008`

```
T-Code: /PMG/59000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMPCT
```

### 13. `/PMG/59000017`

```
T-Code: /PMG/59000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMPOL
```

### 14. `/PMG/59000018`

```
T-Code: /PMG/59000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_VSELEX
```

### 15. `/PMG/67000002`

```
T-Code: /PMG/67000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_FRQ_RCOR
```

### 16. `/PMG/75000393`

```
T-Code: /PMG/75000393
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PPPDIS
```

### 17. `/PMG/75000394`

```
T-Code: /PMG/75000394
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_P_AUTH
```

### 18. `/PMG/75000407`

```
T-Code: /PMG/75000407
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PMP_CAT
```

### 19. `/PMG/75000408`

```
T-Code: /PMG/75000408
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PAPLN
```

### 20. `/PMG/75000413`

```
T-Code: /PMG/75000413
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_PGRP
```

### 21. `/PMG/98000052`

```
T-Code: /PMG/98000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PMG/GE_VDFLT
```

### 22. `/PMG/ABT_CREATE_MPOL`

```
T-Code: /PMG/ABT_CREATE_MPOL
├── /PMG/MPO
    ├── /PM0/BPRC → CP
    └── /PMG/MPGRP → GBS
└── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 23. `/PMG/ABT_CREATE_SA`

```
T-Code: /PMG/ABT_CREATE_SA
└── /PMG/MPO
    ├── /PM0/BPRC → SA
    └── /PMG/MPGRP → GBS
```

### 24. `/PMG/ABT_GEN_ENTITY_LOOKUP`

```
T-Code: /PMG/ABT_GEN_ENTITY_LOOKUP
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 25. `/PMG/ABT_INPI_CREATE_SA`

```
T-Code: /PMG/ABT_INPI_CREATE_SA
├── /PMG/MPO
    ├── /PM0/BPRC → HA
    └── /PMG/MPGRP → GBS
└── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 26. `/PMG/ABT_LOOKUP_MP`

```
T-Code: /PMG/ABT_LOOKUP_MP
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 27. `/PMG/ABT_PROCESS_LOCK_SA`

```
T-Code: /PMG/ABT_PROCESS_LOCK_SA
└── /PMG/MPO
    ├── /PM0/BPRC → LA
    └── /PMG/MPGRP → GBS
```

### 28. `/PMG/ABT_READ_MPOL`

```
T-Code: /PMG/ABT_READ_MPOL
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 29. `/PMG/ABT_READ_MPOL_PROD_MOD`

```
T-Code: /PMG/ABT_READ_MPOL_PROD_MOD
├── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
└── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 30. `/PMG/ABT_READ_SLAVE_MPOL`

```
T-Code: /PMG/ABT_READ_SLAVE_MPOL
└── /PMG/MPO
    ├── /PM0/BPRC → SA
    └── /PMG/MPGRP → GBS
```

### 31. `/PMG/ABT_REVERSE_MPOL`

```
T-Code: /PMG/ABT_REVERSE_MPOL
├── /PM0/ABBTX
    ├── /PM0/BPRC → SA
    ├── /PM0/BTXID → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 32. `/PMG/ABT_SVC_APL_A_EXECUTE_RFC`

```
T-Code: /PMG/ABT_SVC_APL_A_EXECUTE_RFC
└── /PMG/MPO
    ├── /PM0/BPRC → SA
    └── /PMG/MPGRP → GBS
```

### 33. `/PMG/ABT_SVC_REJ_EXECUTE`

```
T-Code: /PMG/ABT_SVC_REJ_EXECUTE
└── /PMG/MPO
    ├── /PM0/BPRC → SA
    └── /PMG/MPGRP → GBS
```

### 34. `/PMG/ABT_SYNC_MMP_CMP`

```
T-Code: /PMG/ABT_SYNC_MMP_CMP
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 35. `/PMG/ABT_UPDATE_MPOL`

```
T-Code: /PMG/ABT_UPDATE_MPOL
├── /PM0/ABBTX
    ├── /PM0/BPRC → UP
    ├── /PM0/BTXID → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 36. `/PMG/DCOI`

```
T-Code: /PMG/DCOI
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PMG/DCOI
```

### 37. `/PMG/GEMA`

```
T-Code: /PMG/GEMA
├── S_ALV_LAYO
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYR
    ├── ACTVT → (empty — maintain in PFCG)
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → (empty — maintain in PFCG)
├── S_DOKU_AUT
    ├── DOKU_ACT → (empty — maintain in PFCG)
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → (empty — maintain in PFCG)
├── S_SCD0
    └── ACTVT → (empty — maintain in PFCG)
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → (empty — maintain in PFCG)
```

### 38. `/PMG/GERES`

```
T-Code: /PMG/GERES
└── S_NUMBER
    ├── ACTVT → 17, 2, 3
    └── NROBJ → (empty — maintain in PFCG)
```

### 39. `/PMG/GE_ABG_MASS_SIMU_PACK`

```
T-Code: /PMG/GE_ABG_MASS_SIMU_PACK
└── /PMG/MPO
    ├── /PM0/BPRC → MS
    └── /PMG/MPGRP → GBS
```

### 40. `/PMG/GE_ABG_MASS_SIMU_PACK2`

```
T-Code: /PMG/GE_ABG_MASS_SIMU_PACK2
└── /PMG/MPO
    ├── /PM0/BPRC → MS
    └── /PMG/MPGRP → GBS
```

### 41. `/PMG/GE_CHN_LOG`

```
T-Code: /PMG/GE_CHN_LOG
├── P_RUNTIME
    ├── P_ACTVT_FW → (empty — maintain in PFCG)
    ├── P_ACTVT_RT → (empty — maintain in PFCG)
    └── P_APP_ID → (empty — maintain in PFCG)
├── S_ALV_LAYO
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYR
    ├── ACTVT → (empty — maintain in PFCG)
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 42. `/PMG/GE_COR_POL_COLLECT`

```
T-Code: /PMG/GE_COR_POL_COLLECT
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 43. `/PMG/GE_CREATE_MP`

```
T-Code: /PMG/GE_CREATE_MP
└── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 44. `/PMG/GE_END_POLICY`

```
T-Code: /PMG/GE_END_POLICY
├── S_CTS_ADMI
    └── CTS_ADMFCT → (empty — maintain in PFCG)
├── S_CTS_SADM
    ├── CTS_ADMFCT → (empty — maintain in PFCG)
    ├── DESTSYS → (empty — maintain in PFCG)
    └── DOMAIN → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → (empty — maintain in PFCG)
    └── TTYPE → (empty — maintain in PFCG)
```

### 45. `/PMG/GE_FILE_UPLOAD`

```
T-Code: /PMG/GE_FILE_UPLOAD
├── /PM0/ABACT
    ├── /PM0/ACTN → (empty — maintain in PFCG)
    └── /PM0/ITEM → (empty — maintain in PFCG)
├── /PM0/APMPG
    ├── /PM0/BPRC → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
├── /PM0/COMP
    └── /PM0/CMPID → $/PM0/CMPID
├── B_BUP_PCPT
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYO
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYR
    ├── ACTVT → (empty — maintain in PFCG)
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_CTS_ADMI
    └── CTS_ADMFCT → (empty — maintain in PFCG)
├── S_CTS_SADM
    ├── CTS_ADMFCT → (empty — maintain in PFCG)
    ├── DESTSYS → (empty — maintain in PFCG)
    └── DOMAIN → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → (empty — maintain in PFCG)
    └── TTYPE → (empty — maintain in PFCG)
```

### 46. `/PMG/GE_FPP_OAPPL`

```
T-Code: /PMG/GE_FPP_OAPPL
├── /PM0/ABCOR
    ├── ACTVT → 43
    └── COTYP_KK → (empty — maintain in PFCG)
├── /PM0/FPP
    └── ACTVT → 1, 16, 32, 6, 69
├── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → FS-PM
    └── ACTVT → 2, 3, 6, A8, B2
└── B_BUP_PCPT
    └── ACTVT → 3
```

### 47. `/PMG/GE_IBC_MPOL`

```
T-Code: /PMG/GE_IBC_MPOL
├── /PM0/IBCT
    ├── /PM0/APPLI → /PMG/GE_MP
    ├── /PM0/ENTIT → (empty — maintain in PFCG)
    ├── /PM0/OPERA → CHG, CRT, DEL, REL, SHO, TRM
    ├── /PM0/TMPID → (empty — maintain in PFCG)
    └── /PM0/TMPTY → (empty — maintain in PFCG)
└── S_TABU_CLI
    └── CLIIDMAINT → (empty — maintain in PFCG)
```

### 48. `/PMG/GE_IOB_CRE`

```
T-Code: /PMG/GE_IOB_CRE
├── /PM0/ABACT
    ├── /PM0/ACTN → (empty — maintain in PFCG)
    └── /PM0/ITEM → (empty — maintain in PFCG)
├── B_BUP_PCPT
    └── ACTVT → (empty — maintain in PFCG)
├── S_CTS_ADMI
    └── CTS_ADMFCT → (empty — maintain in PFCG)
├── S_CTS_SADM
    ├── CTS_ADMFCT → (empty — maintain in PFCG)
    ├── DESTSYS → (empty — maintain in PFCG)
    └── DOMAIN → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → (empty — maintain in PFCG)
    └── TTYPE → (empty — maintain in PFCG)
```

### 49. `/PMG/GE_MPE_GET_AC_DATA`

```
T-Code: /PMG/GE_MPE_GET_AC_DATA
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 50. `/PMG/GE_MPE_GET_CM_DATA`

```
T-Code: /PMG/GE_MPE_GET_CM_DATA
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → (empty — maintain in PFCG)
```

### 51. `/PMG/GE_MPE_GET_CS_DATA`

```
T-Code: /PMG/GE_MPE_GET_CS_DATA
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 52. `/PMG/GE_MPE_GET_JOURNAL_DATA`

```
T-Code: /PMG/GE_MPE_GET_JOURNAL_DATA
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 53. `/PMG/GE_MPE_GET_RI_DATA`

```
T-Code: /PMG/GE_MPE_GET_RI_DATA
└── /PMG/MPO
    ├── /PM0/BPRC → GD
    └── /PMG/MPGRP → GBS
```

### 54. `/PMG/GE_REL_REJ`

```
T-Code: /PMG/GE_REL_REJ
├── /PM0/ABACT
    ├── /PM0/ACTN → (empty — maintain in PFCG)
    └── /PM0/ITEM → (empty — maintain in PFCG)
├── /PM0/ABPST
    ├── /PM0/BILXP → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /PM0/APMPG
    ├── /PM0/BPRC → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
├── /PM0/COMP
    └── /PM0/CMPID → $/PM0/CMPID
├── /PM0/DUCON
    ├── /PM0/PPGRP → (empty — maintain in PFCG)
    └── /PM0/SVGRP → (empty — maintain in PFCG)
├── B_BUP_PCPT
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYO
    └── ACTVT → (empty — maintain in PFCG)
├── S_ALV_LAYR
    ├── ACTVT → (empty — maintain in PFCG)
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → (empty — maintain in PFCG)
├── S_CTS_ADMI
    └── CTS_ADMFCT → (empty — maintain in PFCG)
├── S_CTS_SADM
    ├── CTS_ADMFCT → (empty — maintain in PFCG)
    ├── DESTSYS → (empty — maintain in PFCG)
    └── DOMAIN → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
├── S_DOKU_AUT
    ├── DOKU_ACT → (empty — maintain in PFCG)
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
├── S_TRANSLAT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── TLANGUAGE → (empty — maintain in PFCG)
    └── TRANOBJ → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → (empty — maintain in PFCG)
    └── TTYPE → (empty — maintain in PFCG)
```

### 55. `/PMG/GE_RMC`

```
T-Code: /PMG/GE_RMC
└── S_BTCH_ADM
    └── BTCADMIN → (empty — maintain in PFCG)
```

### 56. `/PMG/GE_SA_TO_POLICY`

```
T-Code: /PMG/GE_SA_TO_POLICY
├── /PM0/ABACT
    ├── /PM0/ACTN → (empty — maintain in PFCG)
    └── /PM0/ITEM → (empty — maintain in PFCG)
├── /PM0/ABPST
    ├── /PM0/BILXP → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /PM0/APMPG
    ├── /PM0/BPRC → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
├── /PM0/COMP
    └── /PM0/CMPID → $/PM0/CMPID
├── B_BUP_PCPT
    └── ACTVT → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 57. `/PMG/GE_SA_VIA_POLIC`

```
T-Code: /PMG/GE_SA_VIA_POLIC
├── /PM0/ABACT
    ├── /PM0/ACTN → (empty — maintain in PFCG)
    └── /PM0/ITEM → (empty — maintain in PFCG)
├── /PM0/ABPST
    ├── /PM0/BILXP → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /PM0/APMPG
    ├── /PM0/BPRC → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
├── /PM0/COMP
    └── /PM0/CMPID → $/PM0/CMPID
├── B_BUP_PCPT
    └── ACTVT → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 58. `/PMG/GE_SELECTION`

```
T-Code: /PMG/GE_SELECTION
├── /PM0/APMPG
    ├── /PM0/BPRC → (empty — maintain in PFCG)
    └── /PM0/PRELG → (empty — maintain in PFCG)
├── /PM0/COMP
    └── /PM0/CMPID → $/PM0/CMPID
├── /PMG/MPO
    ├── /PM0/BPRC → MP
    └── /PMG/MPGRP → GBS
└── S_TRANSPRT
    ├── ACTVT → 2, 3, 43, 5, 6
    └── TTYPE → PIEC, TASK
```

### 59. `/PMG/GE_SEL_FPP`

```
T-Code: /PMG/GE_SEL_FPP
└── /PM0/FPP
    └── ACTVT → 1, 16, 32, 6, 69
```

### 60. `/PMG/GE_TMF_PLAN_DATE`

```
T-Code: /PMG/GE_TMF_PLAN_DATE
└── /PMG/MPO
    ├── /PM0/BPRC → MP
    └── /PMG/MPGRP → GBS
```

## `/PRA/` — SAP Production & Revenue Accounting (Oil & Gas)
> T-codes: **693**


### 61. `/PRA/35000001`

```
T-Code: /PRA/35000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T515_EN
```

### 62. `/PRA/35000002`

```
T-Code: /PRA/35000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T515FIRST
```

### 63. `/PRA/35000003`

```
T-Code: /PRA/35000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T515_COMP
```

### 64. `/PRA/35000004`

```
T-Code: /PRA/35000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T515BUKRS
```

### 65. `/PRA/35000005`

```
T-Code: /PRA/35000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T515_PDCD
```

### 66. `/PRA/35000010`

```
T-Code: /PRA/35000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_IND
```

### 67. `/PRA/35000017`

```
T-Code: /PRA/35000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_WH_REC_CD
```

### 68. `/PRA/35000036`

```
T-Code: /PRA/35000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_EN_FN
```

### 69. `/PRA/35000038`

```
T-Code: /PRA/35000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_OIUY6_WY_VTCD
```

### 70. `/PRA/35000040`

```
T-Code: /PRA/35000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_GNES_ENBL
```

### 71. `/PRA/35000041`

```
T-Code: /PRA/35000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_GNES_GRP
```

### 72. `/PRA/35000042`

```
T-Code: /PRA/35000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_GNES_COMP
```

### 73. `/PRA/35000043`

```
T-Code: /PRA/35000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_GNES_SUS
```

### 74. `/PRA/35000044`

```
T-Code: /PRA/35000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_GENES_STT
```

### 75. `/PRA/35000046`

```
T-Code: /PRA/35000046
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_GEN_TAB2
```

### 76. `/PRA/35000047`

```
T-Code: /PRA/35000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_GEN_TAB
```

### 77. `/PRA/35000050`

```
T-Code: /PRA/35000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_SUS_CD
```

### 78. `/PRA/35000053`

```
T-Code: /PRA/35000053
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_GNES_GRPC
```

### 79. `/PRA/49000011`

```
T-Code: /PRA/49000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FF_UCA
```

### 80. `/PRA/49000012`

```
T-Code: /PRA/49000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FF_DSP
```

### 81. `/PRA/49000013`

```
T-Code: /PRA/49000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FF_CTYP
```

### 82. `/PRA/49000014`

```
T-Code: /PRA/49000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/UTX_UT_TH
```

### 83. `/PRA/49000016`

```
T-Code: /PRA/49000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_V_FIRST
```

### 84. `/PRA/49000017`

```
T-Code: /PRA/49000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_V_EN
```

### 85. `/PRA/49000018`

```
T-Code: /PRA/49000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_V_COMP
```

### 86. `/PRA/49000019`

```
T-Code: /PRA/49000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_V_BUKRS
```

### 87. `/PRA/49000020`

```
T-Code: /PRA/49000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_PROC_ID
```

### 88. `/PRA/49000022`

```
T-Code: /PRA/49000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_ENTY_CON
```

### 89. `/PRA/49000023`

```
T-Code: /PRA/49000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_PDCXRF
```

### 90. `/PRA/49000024`

```
T-Code: /PRA/49000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_VTCXRF
```

### 91. `/PRA/49000025`

```
T-Code: /PRA/49000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_MSTR_CON
```

### 92. `/PRA/49000026`

```
T-Code: /PRA/49000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_PDC
```

### 93. `/PRA/49000027`

```
T-Code: /PRA/49000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_V_PDCD
```

### 94. `/PRA/49000028`

```
T-Code: /PRA/49000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_REJ_CD
```

### 95. `/PRA/49000030`

```
T-Code: /PRA/49000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_VTC
```

### 96. `/PRA/49000031`

```
T-Code: /PRA/49000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_PROC_TAX
```

### 97. `/PRA/49000032`

```
T-Code: /PRA/49000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_MKT_CD
```

### 98. `/PRA/49000033`

```
T-Code: /PRA/49000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/GT_MKT_XREF
```

### 99. `/PRA/59000009`

```
T-Code: /PRA/59000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/R3_MAT_TH
```

### 100. `/PRA/59000014`

```
T-Code: /PRA/59000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_PPA_ST
```

### 101. `/PRA/59000015`

```
T-Code: /PRA/59000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_PPA_SD
```

### 102. `/PRA/92000116`

```
T-Code: /PRA/92000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/SN_NR_CONF
```

### 103. `/PRA/92000185`

```
T-Code: /PRA/92000185
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ACCT_COA
```

### 104. `/PRA/92000189`

```
T-Code: /PRA/92000189
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_AR_CFG
```

### 105. `/PRA/92000190`

```
T-Code: /PRA/92000190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TP_CFG
```

### 106. `/PRA/92000204`

```
T-Code: /PRA/92000204
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_FRML
```

### 107. `/PRA/92000205`

```
T-Code: /PRA/92000205
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_FED_RSV
```

### 108. `/PRA/92000206`

```
T-Code: /PRA/92000206
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_ST_STAT
```

### 109. `/PRA/92000207`

```
T-Code: /PRA/92000207
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_ACCT_PD
```

### 110. `/PRA/92000208`

```
T-Code: /PRA/92000208
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_AUT_PST
```

### 111. `/PRA/92000209`

```
T-Code: /PRA/92000209
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_OPTION
```

### 112. `/PRA/92000210`

```
T-Code: /PRA/92000210
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/INT_AP_OVRD
```

### 113. `/PRA/92000230`

```
T-Code: /PRA/92000230
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/TRANS_INFO
```

### 114. `/PRA/92000231`

```
T-Code: /PRA/92000231
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/NAVG_MAP_IN
```

### 115. `/PRA/92000234`

```
T-Code: /PRA/92000234
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/NAVG_DSP_OR
```

### 116. `/PRA/92000275`

```
T-Code: /PRA/92000275
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/BP_CUST_BPC
```

### 117. `/PRA/92000276`

```
T-Code: /PRA/92000276
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/BP_VEND_BPC
```

### 118. `/PRA/92000277`

```
T-Code: /PRA/92000277
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_BLMT_BPC
```

### 119. `/PRA/92000278`

```
T-Code: /PRA/92000278
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_CONT_BPC
```

### 120. `/PRA/92000279`

```
T-Code: /PRA/92000279
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_DOIX_BPC
```

### 121. `/PRA/92000280`

```
T-Code: /PRA/92000280
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_MGD_BPC
```

### 122. `/PRA/92000281`

```
T-Code: /PRA/92000281
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_PBAX_BPC
```

### 123. `/PRA/92000282`

```
T-Code: /PRA/92000282
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_PBA_BPC
```

### 124. `/PRA/92000283`

```
T-Code: /PRA/92000283
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_RETM_BPC
```

### 125. `/PRA/92000284`

```
T-Code: /PRA/92000284
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_SPFS_BPC
```

### 126. `/PRA/92000285`

```
T-Code: /PRA/92000285
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_SPO_BPC
```

### 127. `/PRA/92000286`

```
T-Code: /PRA/92000286
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_TRNS_BPC
```

### 128. `/PRA/92000287`

```
T-Code: /PRA/92000287
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_BPJV_BPC
```

### 129. `/PRA/92000288`

```
T-Code: /PRA/92000288
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_DIDO_BPC
```

### 130. `/PRA/92000289`

```
T-Code: /PRA/92000289
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_DP_C_BPC
```

### 131. `/PRA/92000290`

```
T-Code: /PRA/92000290
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_JV_BPC
```

### 132. `/PRA/92000291`

```
T-Code: /PRA/92000291
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_JV_P_BPC
```

### 133. `/PRA/92000292`

```
T-Code: /PRA/92000292
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_NOJV_BPC
```

### 134. `/PRA/92000293`

```
T-Code: /PRA/92000293
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_OWNR_BPC
```

### 135. `/PRA/92000294`

```
T-Code: /PRA/92000294
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_UTP_BPC
```

### 136. `/PRA/92000295`

```
T-Code: /PRA/92000295
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_DMG_BPC
```

### 137. `/PRA/92000296`

```
T-Code: /PRA/92000296
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_DNAP_BPC
```

### 138. `/PRA/92000297`

```
T-Code: /PRA/92000297
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_DND_BPC
```

### 139. `/PRA/92000298`

```
T-Code: /PRA/92000298
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_DNND_BPC
```

### 140. `/PRA/92000299`

```
T-Code: /PRA/92000299
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_DN_BPC
```

### 141. `/PRA/92000300`

```
T-Code: /PRA/92000300
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_FLD_BPC
```

### 142. `/PRA/92000301`

```
T-Code: /PRA/92000301
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_MPAP_BPC
```

### 143. `/PRA/92000302`

```
T-Code: /PRA/92000302
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_MPD_BPC
```

### 144. `/PRA/92000303`

```
T-Code: /PRA/92000303
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_MPVL_BPC
```

### 145. `/PRA/92000304`

```
T-Code: /PRA/92000304
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_MP_BPC
```

### 146. `/PRA/92000305`

```
T-Code: /PRA/92000305
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_MWT_BPC
```

### 147. `/PRA/92000306`

```
T-Code: /PRA/92000306
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_RESR_BPC
```

### 148. `/PRA/92000307`

```
T-Code: /PRA/92000307
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_TCM_BPC
```

### 149. `/PRA/92000308`

```
T-Code: /PRA/92000308
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_WCDT_BPC
```

### 150. `/PRA/92000309`

```
T-Code: /PRA/92000309
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_WCD_BPC
```

### 151. `/PRA/92000310`

```
T-Code: /PRA/92000310
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_WCTS_BPC
```

### 152. `/PRA/92000311`

```
T-Code: /PRA/92000311
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_WC_BPC
```

### 153. `/PRA/92000312`

```
T-Code: /PRA/92000312
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/PR_WL_BPC
```

### 154. `/PRA/92000313`

```
T-Code: /PRA/92000313
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RV_TCD_BPC
```

### 155. `/PRA/92000314`

```
T-Code: /PRA/92000314
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/VL_STRR_BPC
```

### 156. `/PRA/99000001`

```
T-Code: /PRA/99000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PP_EN
```

### 157. `/PRA/99000002`

```
T-Code: /PRA/99000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PP_GRP
```

### 158. `/PRA/99000003`

```
T-Code: /PRA/99000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_GRPCCX
```

### 159. `/PRA/99000004`

```
T-Code: /PRA/99000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_BANK
```

### 160. `/PRA/99000005`

```
T-Code: /PRA/99000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_GRPCX
```

### 161. `/PRA/99000008`

```
T-Code: /PRA/99000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_TM
```

### 162. `/PRA/99000009`

```
T-Code: /PRA/99000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_MKTXRF
```

### 163. `/PRA/99000010`

```
T-Code: /PRA/99000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_TAXXRF
```

### 164. `/PRA/99000012`

```
T-Code: /PRA/99000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CA_GS
```

### 165. `/PRA/99000013`

```
T-Code: /PRA/99000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CM_UOM
```

### 166. `/PRA/99000014`

```
T-Code: /PRA/99000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_OTHXRF
```

### 167. `/PRA/99000018`

```
T-Code: /PRA/99000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WELL_SIDT
```

### 168. `/PRA/99000019`

```
T-Code: /PRA/99000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_SP
```

### 169. `/PRA/99000020`

```
T-Code: /PRA/99000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_WH
```

### 170. `/PRA/99000021`

```
T-Code: /PRA/99000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_CFG
```

### 171. `/PRA/99000022`

```
T-Code: /PRA/99000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_REJ
```

### 172. `/PRA/99000023`

```
T-Code: /PRA/99000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PP_REJACT
```

### 173. `/PRA/99000024`

```
T-Code: /PRA/99000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_MINPAY
```

### 174. `/PRA/99000025`

```
T-Code: /PRA/99000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_UOMXRF
```

### 175. `/PRA/99000029`

```
T-Code: /PRA/99000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_SUMOVR
```

### 176. `/PRA/99000031`

```
T-Code: /PRA/99000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_DREJ
```

### 177. `/PRA/99000032`

```
T-Code: /PRA/99000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PP_REJACC
```

### 178. `/PRA/99000035`

```
T-Code: /PRA/99000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_ACTWHX
```

### 179. `/PRA/99000041`

```
T-Code: /PRA/99000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PP_RMBXRF
```

### 180. `/PRA/99000042`

```
T-Code: /PRA/99000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_FP_EN
```

### 181. `/PRA/99000043`

```
T-Code: /PRA/99000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_PAYOR
```

### 182. `/PRA/99000045`

```
T-Code: /PRA/99000045
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_PDCXRF
```

### 183. `/PRA/99000046`

```
T-Code: /PRA/99000046
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_MKTXRF
```

### 184. `/PRA/99000047`

```
T-Code: /PRA/99000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_FP_GRP
```

### 185. `/PRA/99000048`

```
T-Code: /PRA/99000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_DIS_CD
```

### 186. `/PRA/99000049`

```
T-Code: /PRA/99000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_OGXRF
```

### 187. `/PRA/99000050`

```
T-Code: /PRA/99000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/MIG_GRPPX
```

### 188. `/PRA/99000052`

```
T-Code: /PRA/99000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_TLP
```

### 189. `/PRA/99000053`

```
T-Code: /PRA/99000053
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_ERR_CD
```

### 190. `/PRA/99000054`

```
T-Code: /PRA/99000054
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CM_MIG_ITEM
```

### 191. `/PRA/99000055`

```
T-Code: /PRA/99000055
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_EDT_CD
```

### 192. `/PRA/99000064`

```
T-Code: /PRA/99000064
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RSP_AS, /PRA/RSP_AS_AT_V
```

### 193. `/PRA/99000065`

```
T-Code: /PRA/99000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_COMP
```

### 194. `/PRA/99000066`

```
T-Code: /PRA/99000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_TRNEDT
```

### 195. `/PRA/99000068`

```
T-Code: /PRA/99000068
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RSP_FUNCE_V
```

### 196. `/PRA/99000069`

```
T-Code: /PRA/99000069
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_PP_TH_SP
```

### 197. `/PRA/99000070`

```
T-Code: /PRA/99000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_MIG
```

### 198. `/PRA/99000072`

```
T-Code: /PRA/99000072
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_DF_UOM
```

### 199. `/PRA/99000074`

```
T-Code: /PRA/99000074
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WH_EN
```

### 200. `/PRA/99000075`

```
T-Code: /PRA/99000075
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WH_GRP
```

### 201. `/PRA/99000076`

```
T-Code: /PRA/99000076
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_RPT_CF
```

### 202. `/PRA/99000080`

```
T-Code: /PRA/99000080
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/KS_PRDXRF
```

### 203. `/PRA/99000087`

```
T-Code: /PRA/99000087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CF_PKSZ
```

### 204. `/PRA/99000088`

```
T-Code: /PRA/99000088
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/KS_PRDXRF
```

### 205. `/PRA/99000092`

```
T-Code: /PRA/99000092
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_PDCXRF
```

### 206. `/PRA/99000093`

```
T-Code: /PRA/99000093
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_TRCXRF
```

### 207. `/PRA/99000094`

```
T-Code: /PRA/99000094
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_FLD
```

### 208. `/PRA/99000095`

```
T-Code: /PRA/99000095
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_PLT
```

### 209. `/PRA/99000100`

```
T-Code: /PRA/99000100
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_REJ
```

### 210. `/PRA/99000101`

```
T-Code: /PRA/99000101
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_REJC
```

### 211. `/PRA/99000103`

```
T-Code: /PRA/99000103
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_REJ
```

### 212. `/PRA/99000104`

```
T-Code: /PRA/99000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/OW_RQS_DFLT
```

### 213. `/PRA/99000108`

```
T-Code: /PRA/99000108
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ACTCTG_ACR
```

### 214. `/PRA/99000109`

```
T-Code: /PRA/99000109
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ACC_DOC
```

### 215. `/PRA/99000110`

```
T-Code: /PRA/99000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIUHS_JE_ACC_ID, OIUHS_JE_ACC_TYP, OIUHS_JE_DBI, OIUHS_JE_DPK
```

### 216. `/PRA/99000111`

```
T-Code: /PRA/99000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ACCT_CYC
```

### 217. `/PRA/99000114`

```
T-Code: /PRA/99000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_TRC
```

### 218. `/PRA/99000116`

```
T-Code: /PRA/99000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WH_PKSZ
```

### 219. `/PRA/99000117`

```
T-Code: /PRA/99000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_WH_ACC
```

### 220. `/PRA/99000118`

```
T-Code: /PRA/99000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WH_IN_TYP
```

### 221. `/PRA/99000119`

```
T-Code: /PRA/99000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CM_COMPC
```

### 222. `/PRA/99000120`

```
T-Code: /PRA/99000120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIU_V_T100C
```

### 223. `/PRA/99000121`

```
T-Code: /PRA/99000121
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_RPT_CF
```

### 224. `/PRA/99000123`

```
T-Code: /PRA/99000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_PCXREF
```

### 225. `/PRA/99000124`

```
T-Code: /PRA/99000124
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_TCWC_XRF
```

### 226. `/PRA/99000125`

```
T-Code: /PRA/99000125
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_TA_ENTTYX
```

### 227. `/PRA/99000126`

```
T-Code: /PRA/99000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_TA_PCXREF
```

### 228. `/PRA/99000127`

```
T-Code: /PRA/99000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_REJ
```

### 229. `/PRA/99000128`

```
T-Code: /PRA/99000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_REJC
```

### 230. `/PRA/99000129`

```
T-Code: /PRA/99000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_T9_VTCXRF
```

### 231. `/PRA/99000130`

```
T-Code: /PRA/99000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_POOL_CD
```

### 232. `/PRA/99000131`

```
T-Code: /PRA/99000131
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_POST_CD
```

### 233. `/PRA/99000132`

```
T-Code: /PRA/99000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_RPTY
```

### 234. `/PRA/99000133`

```
T-Code: /PRA/99000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_PERIOD
```

### 235. `/PRA/99000135`

```
T-Code: /PRA/99000135
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_EN
```

### 236. `/PRA/99000136`

```
T-Code: /PRA/99000136
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_PROCID
```

### 237. `/PRA/99000137`

```
T-Code: /PRA/99000137
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_COMP
```

### 238. `/PRA/99000138`

```
T-Code: /PRA/99000138
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_BUKRS
```

### 239. `/PRA/99000139`

```
T-Code: /PRA/99000139
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_FIRST
```

### 240. `/PRA/99000140`

```
T-Code: /PRA/99000140
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/AR_CFG
```

### 241. `/PRA/99000142`

```
T-Code: /PRA/99000142
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T9_PDCD
```

### 242. `/PRA/99000143`

```
T-Code: /PRA/99000143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_WH_1099TY
```

### 243. `/PRA/99000144`

```
T-Code: /PRA/99000144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_WELL_COD
```

### 244. `/PRA/99000147`

```
T-Code: /PRA/99000147
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/ND_MKTXRF
```

### 245. `/PRA/99000148`

```
T-Code: /PRA/99000148
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_REJ
```

### 246. `/PRA/99000149`

```
T-Code: /PRA/99000149
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_OTPAY_XRF
```

### 247. `/PRA/99000150`

```
T-Code: /PRA/99000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_WH_RUN_YE
```

### 248. `/PRA/99000152`

```
T-Code: /PRA/99000152
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_EN
```

### 249. `/PRA/99000153`

```
T-Code: /PRA/99000153
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_FIRST
```

### 250. `/PRA/99000154`

```
T-Code: /PRA/99000154
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_COMP
```

### 251. `/PRA/99000155`

```
T-Code: /PRA/99000155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_BUKRS
```

### 252. `/PRA/99000157`

```
T-Code: /PRA/99000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_PDCD
```

### 253. `/PRA/99000158`

```
T-Code: /PRA/99000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/TP_CFG
```

### 254. `/PRA/99000160`

```
T-Code: /PRA/99000160
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_TAB
```

### 255. `/PRA/99000161`

```
T-Code: /PRA/99000161
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_PROF
```

### 256. `/PRA/99000162`

```
T-Code: /PRA/99000162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_INT
```

### 257. `/PRA/99000172`

```
T-Code: /PRA/99000172
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_INT
```

### 258. `/PRA/99000173`

```
T-Code: /PRA/99000173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IEN
```

### 259. `/PRA/99000174`

```
T-Code: /PRA/99000174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IFIRST
```

### 260. `/PRA/99000175`

```
T-Code: /PRA/99000175
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_ICOMP
```

### 261. `/PRA/99000176`

```
T-Code: /PRA/99000176
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IBUKRS
```

### 262. `/PRA/99000178`

```
T-Code: /PRA/99000178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IPDCD
```

### 263. `/PRA/99000179`

```
T-Code: /PRA/99000179
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_TA_MKTXRF
```

### 264. `/PRA/99000180`

```
T-Code: /PRA/99000180
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_TA_OGXREF
```

### 265. `/PRA/99000181`

```
T-Code: /PRA/99000181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IREJ
```

### 266. `/PRA/99000182`

```
T-Code: /PRA/99000182
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_TA_IREJC
```

### 267. `/PRA/99000183`

```
T-Code: /PRA/99000183
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RP_SPLIT
```

### 268. `/PRA/99000199`

```
T-Code: /PRA/99000199
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CMP_GAS, /PRA/V_COMB_CMP
```

### 269. `/PRA/99000201`

```
T-Code: /PRA/99000201
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_REG_OUT, V_OIU_REP_AGENCY, V_OIU_REP_FIELDS, V_OIU_REP_REPORT, V_OIU_REP_STRUC, V_OIU_REP_VALTAB
```

### 270. `/PRA/99000202`

```
T-Code: /PRA/99000202
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T3_EN
```

### 271. `/PRA/99000203`

```
T-Code: /PRA/99000203
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T3_FIRST
```

### 272. `/PRA/99000204`

```
T-Code: /PRA/99000204
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T3_COMP
```

### 273. `/PRA/99000205`

```
T-Code: /PRA/99000205
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T3_BUKRS
```

### 274. `/PRA/99000206`

```
T-Code: /PRA/99000206
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T3_PDCD
```

### 275. `/PRA/99000207`

```
T-Code: /PRA/99000207
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CI_PRC_TOL
```

### 276. `/PRA/99000208`

```
T-Code: /PRA/99000208
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CI_RCV_DEC
```

### 277. `/PRA/99000209`

```
T-Code: /PRA/99000209
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CI_MISC_ACC
```

### 278. `/PRA/99000212`

```
T-Code: /PRA/99000212
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CH_BUKRS, /PRA/V_SB_BUKRS
```

### 279. `/PRA/99000220`

```
T-Code: /PRA/99000220
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PN_SWTCH
```

### 280. `/PRA/99000221`

```
T-Code: /PRA/99000221
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ACTLK_EXP, /PRA/V_ACTLK_EXU
```

### 281. `/PRA/99000223`

```
T-Code: /PRA/99000223
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_TA_INCACC
```

### 282. `/PRA/99000226`

```
T-Code: /PRA/99000226
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T5_EN
```

### 283. `/PRA/99000227`

```
T-Code: /PRA/99000227
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T5_FIRST
```

### 284. `/PRA/99000228`

```
T-Code: /PRA/99000228
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T5_COMP
```

### 285. `/PRA/99000229`

```
T-Code: /PRA/99000229
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T5_BUKRS
```

### 286. `/PRA/99000230`

```
T-Code: /PRA/99000230
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T5_PDCD
```

### 287. `/PRA/99000231`

```
T-Code: /PRA/99000231
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T6_EN
```

### 288. `/PRA/99000232`

```
T-Code: /PRA/99000232
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T6_FIRST
```

### 289. `/PRA/99000233`

```
T-Code: /PRA/99000233
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T6_COMP
```

### 290. `/PRA/99000234`

```
T-Code: /PRA/99000234
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T6_BUKRS
```

### 291. `/PRA/99000235`

```
T-Code: /PRA/99000235
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T6_PDCD
```

### 292. `/PRA/99000239`

```
T-Code: /PRA/99000239
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T8_EN
```

### 293. `/PRA/99000240`

```
T-Code: /PRA/99000240
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T8_FIRST
```

### 294. `/PRA/99000241`

```
T-Code: /PRA/99000241
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T8_COMP
```

### 295. `/PRA/99000242`

```
T-Code: /PRA/99000242
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T8_BUKRS
```

### 296. `/PRA/99000243`

```
T-Code: /PRA/99000243
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_T8_PDCD
```

### 297. `/PRA/99000244`

```
T-Code: /PRA/99000244
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R3_EN
```

### 298. `/PRA/99000245`

```
T-Code: /PRA/99000245
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R3_FIRST
```

### 299. `/PRA/99000246`

```
T-Code: /PRA/99000246
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R3_COMP
```

### 300. `/PRA/99000247`

```
T-Code: /PRA/99000247
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R3_BUKRS
```

### 301. `/PRA/99000248`

```
T-Code: /PRA/99000248
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R3_PDCD
```

### 302. `/PRA/99000249`

```
T-Code: /PRA/99000249
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R6_EN
```

### 303. `/PRA/99000250`

```
T-Code: /PRA/99000250
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R6_FIRST
```

### 304. `/PRA/99000251`

```
T-Code: /PRA/99000251
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R6_COMP
```

### 305. `/PRA/99000252`

```
T-Code: /PRA/99000252
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R6_BUKRS
```

### 306. `/PRA/99000253`

```
T-Code: /PRA/99000253
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_R6_PDCD
```

### 307. `/PRA/99000254`

```
T-Code: /PRA/99000254
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_EN
```

### 308. `/PRA/99000255`

```
T-Code: /PRA/99000255
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_FIRST
```

### 309. `/PRA/99000256`

```
T-Code: /PRA/99000256
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_COMP
```

### 310. `/PRA/99000257`

```
T-Code: /PRA/99000257
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_BUKRS
```

### 311. `/PRA/99000258`

```
T-Code: /PRA/99000258
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_KS_PDCD
```

### 312. `/PRA/99000260`

```
T-Code: /PRA/99000260
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_EN
```

### 313. `/PRA/99000261`

```
T-Code: /PRA/99000261
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_FIRST
```

### 314. `/PRA/99000262`

```
T-Code: /PRA/99000262
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_COMP
```

### 315. `/PRA/99000263`

```
T-Code: /PRA/99000263
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_BUKRS
```

### 316. `/PRA/99000264`

```
T-Code: /PRA/99000264
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_ND_PDCD
```

### 317. `/PRA/99000265`

```
T-Code: /PRA/99000265
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_FP_S6ROVR
```

### 318. `/PRA/99000266`

```
T-Code: /PRA/99000266
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_VL_DA_CTY
```

### 319. `/PRA/99000271`

```
T-Code: /PRA/99000271
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_VL_DACALC
```

### 320. `/PRA/99000276`

```
T-Code: /PRA/99000276
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIUCM_VEND_KTOKK
```

### 321. `/PRA/99000277`

```
T-Code: /PRA/99000277
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIUCM_CUST_KTOKD
```

### 322. `/PRA/99000278`

```
T-Code: /PRA/99000278
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIUCM_ADDR_TYPE
```

### 323. `/PRA/99000282`

```
T-Code: /PRA/99000282
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIU_CM_EDIT
```

### 324. `/PRA/99000283`

```
T-Code: /PRA/99000283
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → OIU_CM_EDIT
```

### 325. `/PRA/99000284`

```
T-Code: /PRA/99000284
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CM_TTYINT
```

### 326. `/PRA/99000285`

```
T-Code: /PRA/99000285
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/C_RV_TAXTYP
```

### 327. `/PRA/99000286`

```
T-Code: /PRA/99000286
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/VL_TAXCONF
```

### 328. `/PRA/ACCTDOC_MANUAL`

```
T-Code: /PRA/ACCTDOC_MANUAL
├── /PRA/RADM
    ├── ACTVT → 1, 10, 2, 3, 48, 6, 90, G5, UL
    └── BUKRS → $BUKRS
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → ACCT_DOC
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 329. `/PRA/ACCT_ANLYSIS_RP`

```
T-Code: /PRA/ACCT_ANLYSIS_RP
├── /PRA/RADM
    ├── ACTVT → 90
    └── BUKRS → $BUKRS
└── /PRA/RADRL
    ├── /PRA/ACTTY → AP, AR, EX, IC, IN, LS, MS, OP, ST, TP
    └── BUKRS → $BUKRS
```

### 330. `/PRA/ACCT_ANLYS_DEL`

```
T-Code: /PRA/ACCT_ANLYS_DEL
├── /PRA/RADM
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
└── /PRA/RADRL
    ├── /PRA/ACTTY → AP, AR, EX, IC, IN, LS, MS, OP, ST, TP
    └── BUKRS → $BUKRS
```

### 331. `/PRA/ACCT_AWTO`

```
T-Code: /PRA/ACCT_AWTO
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 332. `/PRA/ACCT_CRTREJ`

```
T-Code: /PRA/ACCT_CRTREJ
└── /PRA/RADM
    ├── ACTVT → 10, 48, 96, G5, G6
    └── BUKRS → $BUKRS
```

### 333. `/PRA/ACCT_CYC_CLOSE`

```
T-Code: /PRA/ACCT_CYC_CLOSE
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 334. `/PRA/ACCT_DATA_CLNUP`

```
T-Code: /PRA/ACCT_DATA_CLNUP
├── /PRA/RADM
    ├── ACTVT → 10
    └── BUKRS → $BUKRS
└── /PRA/RADRL
    ├── /PRA/ACTTY → AP, AR, EX, IC, IN, LS, MS, OP, ST, TP
    └── BUKRS → $BUKRS
```

### 335. `/PRA/ACCT_DFLT_CFG`

```
T-Code: /PRA/ACCT_DFLT_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 336. `/PRA/ACCT_ENTRY`

```
T-Code: /PRA/ACCT_ENTRY
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, UL
    └── OIU_TCODE → /PRA/ACCT_ENTRY
```

### 337. `/PRA/ACCT_POSTBLK_VW`

```
T-Code: /PRA/ACCT_POSTBLK_VW
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ACCT_POSTBLK_VW
```

### 338. `/PRA/ACCT_QUEUE_MON`

```
T-Code: /PRA/ACCT_QUEUE_MON
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ACCT_QUEUE_MON
```

### 339. `/PRA/ACC_DAAG`

```
T-Code: /PRA/ACC_DAAG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/CT_ACC_DAAG
```

### 340. `/PRA/ADV`

```
T-Code: /PRA/ADV
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/ADV
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 341. `/PRA/ADV_DOC`

```
T-Code: /PRA/ADV_DOC
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ADV_DOC
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIU
    └── ALG_SUBOBJ → ADV
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 342. `/PRA/ADV_TRAN`

```
T-Code: /PRA/ADV_TRAN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ADV_TRAN
```

### 343. `/PRA/ARESC_SFI`

```
T-Code: /PRA/ARESC_SFI
└── /PRA/OWN
    └── /PRA/OWN_F → (empty — maintain in PFCG)
```

### 344. `/PRA/AR_AUTO_WO`

```
T-Code: /PRA/AR_AUTO_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/AR_AUTO_WO
```

### 345. `/PRA/AR_CNTRL_RPT`

```
T-Code: /PRA/AR_CNTRL_RPT
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/AR_CNTRL_RPT
```

### 346. `/PRA/AR_ES_WORKPLACE`

```
T-Code: /PRA/AR_ES_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/AR_ES_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 347. `/PRA/AR_UPLOAD_CMMT`

```
T-Code: /PRA/AR_UPLOAD_CMMT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/AR_UPLOAD_CMMT
```

### 348. `/PRA/AR_WOCONFIG`

```
T-Code: /PRA/AR_WOCONFIG
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/AR_WOCONFIG
```

### 349. `/PRA/AR_WORKPLACE`

```
T-Code: /PRA/AR_WORKPLACE
└── O_OIU_TXN
    ├── ACTVT → 16, 3, 31, 40, 49, 6, 69, 71, A9
    └── OIU_TCODE → /PRA/AR_WORKPLACE
```

### 350. `/PRA/BADI_IMP`

```
T-Code: /PRA/BADI_IMP
└── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 351. `/PRA/CA_GA_COMPONENT`

```
T-Code: /PRA/CA_GA_COMPONENT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_CMP_GAS, /PRA/V_COMB_CMP
```

### 352. `/PRA/CA_GA_WP2`

```
T-Code: /PRA/CA_GA_WP2
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, UL
    └── OIU_TCODE → /PRA/CA_GA_WP2
```

### 353. `/PRA/CA_GA_WP3`

```
T-Code: /PRA/CA_GA_WP3
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, UL
    └── OIU_TCODE → /PRA/CA_GA_WP3
```

### 354. `/PRA/CA_MAN_UPLOAD`

```
T-Code: /PRA/CA_MAN_UPLOAD
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/CA_MAN_UPLOAD
```

### 355. `/PRA/CA_SPF_UPLOAD`

```
T-Code: /PRA/CA_SPF_UPLOAD
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/CA_SPF_UPLOAD
```

### 356. `/PRA/CA_SPO_KW`

```
T-Code: /PRA/CA_SPO_KW
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/CA_KW_MST
```

### 357. `/PRA/CA_SPO_UPLOAD`

```
T-Code: /PRA/CA_SPO_UPLOAD
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/CA_SPO_UPLOAD
```

### 358. `/PRA/CDEX_PRODXREF`

```
T-Code: /PRA/CDEX_PRODXREF
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3
    └── OIU_TCODE → O3UCI_PRDX
```

### 359. `/PRA/CI_ACCT_CFG`

```
T-Code: /PRA/CI_ACCT_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 360. `/PRA/CI_ARVAR`

```
T-Code: /PRA/CI_ARVAR
└── O_OIU_TXN
    ├── ACTVT → 16
    └── OIU_TCODE → /PRA/CI_ARVAR
```

### 361. `/PRA/CI_CDEX`

```
T-Code: /PRA/CI_CDEX
└── O_OIU_TXN
    ├── ACTVT → 1, 3, 6, 71, UL
    └── OIU_TCODE → /PRA/CI_CDEX
```

### 362. `/PRA/CI_CDEXREM_X`

```
T-Code: /PRA/CI_CDEXREM_X
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, F4
    └── OIU_TCODE → /PRA/CI_CDEXREM_X
```

### 363. `/PRA/CI_CHECK_LAYOUT`

```
T-Code: /PRA/CI_CHECK_LAYOUT
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/CI_CHECK_LAYOUT
```

### 364. `/PRA/CI_COPY_REVERSE`

```
T-Code: /PRA/CI_COPY_REVERSE
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 16
    └── OIU_TCODE → /PRA/CI_COPY_REVERSE
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
└── V_KNA1_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 365. `/PRA/CI_MASS`

```
T-Code: /PRA/CI_MASS
├── O_OIU_TXN
    ├── ACTVT → 16
    └── OIU_TCODE → /PRA/CI_MASS
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 366. `/PRA/CI_PDX`

```
T-Code: /PRA/CI_PDX
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, F4, UL
    └── OIU_TCODE → /PRA/CI_PDX
├── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/CI_PDXCHNG
└── V_KNA1_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 367. `/PRA/CI_PD_PRC_CFG`

```
T-Code: /PRA/CI_PD_PRC_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 368. `/PRA/CI_PPD`

```
T-Code: /PRA/CI_PPD
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/CI_PPD
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
└── V_KNA1_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 369. `/PRA/CI_PROCESSRULES`

```
T-Code: /PRA/CI_PROCESSRULES
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/CI_PROCESSRULES
```

### 370. `/PRA/CI_PUR_REM_XREF`

```
T-Code: /PRA/CI_PUR_REM_XREF
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/CI_PUR_REM_XREF
└── V_KNA1_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 371. `/PRA/CI_REC_DEC_CFG`

```
T-Code: /PRA/CI_REC_DEC_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 372. `/PRA/CI_REM_LAYOUT_X`

```
T-Code: /PRA/CI_REM_LAYOUT_X
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/CI_REM_LAYOUT_X
```

### 373. `/PRA/CI_SUSP`

```
T-Code: /PRA/CI_SUSP
└── O_OIU_TXN
    ├── ACTVT → 16
    └── OIU_TCODE → /PRA/CI_SUSP
```

### 374. `/PRA/CI_TRANSLATIONS`

```
T-Code: /PRA/CI_TRANSLATIONS
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/CI_TRANSLATIONS
```

### 375. `/PRA/COA`

```
T-Code: /PRA/COA
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, UL
    └── OIU_TCODE → /PRA/COA
```

### 376. `/PRA/COLLECTIVE_DOI`

```
T-Code: /PRA/COLLECTIVE_DOI
├── O_OIUOW_DO
    ├── OIU_ACTVT → 1, 2, 3, 4, 5, 6, 7, 8, 9
    ├── OIU_BUKRS → (empty — maintain in PFCG)
    ├── OIU_DOINO → (empty — maintain in PFCG)
    └── OIU_JVNAME → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 31, 37, 40, 41, 48, 49, 6, 60, 61, 64, 69, 7, 71, 75, 84, 85, 86, 87, 93, 94, A5, A9, DL, F4, UL
    └── OIU_TCODE → /PRA/COLLECTIVE_DOI, /PRA/OW_COLLECT_DOI
└── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → OIU_BRGP, OIU_DI, OIU_DPUC
```

### 377. `/PRA/DC_NO`

```
T-Code: /PRA/DC_NO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/DC_NO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 378. `/PRA/DISP_KS_OOSWO`

```
T-Code: /PRA/DISP_KS_OOSWO
├── O_OIU_TXN
    ├── ACTVT → 16, 2, 3
    └── OIU_TCODE → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 379. `/PRA/DN_WORKPLACE_C`

```
T-Code: /PRA/DN_WORKPLACE_C
├── O_OIU_NSUB
    └── OIU_SUBSYS → CA
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → DNL
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 380. `/PRA/DN_WORKPLACE_P`

```
T-Code: /PRA/DN_WORKPLACE_P
├── O_OIU_NSUB
    └── OIU_SUBSYS → PR
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → DNL
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 381. `/PRA/DN_WORKPLACE_V`

```
T-Code: /PRA/DN_WORKPLACE_V
├── O_OIU_NSUB
    └── OIU_SUBSYS → VL
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → DNL
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 382. `/PRA/DOC_COMPARE`

```
T-Code: /PRA/DOC_COMPARE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/DOC_COMPARE
```

### 383. `/PRA/DPSEVT_OVRD`

```
T-Code: /PRA/DPSEVT_OVRD
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/DPSEVT_OVRD
```

### 384. `/PRA/DVOLOPT_BK`

```
T-Code: /PRA/DVOLOPT_BK
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/DVOLOPT_BK
```

### 385. `/PRA/DVOLOPT_REV`

```
T-Code: /PRA/DVOLOPT_REV
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/DVOLOPT_REV
```

### 386. `/PRA/DVOLOPT_TXM`

```
T-Code: /PRA/DVOLOPT_TXM
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/DVOLOPT_TXM
```

### 387. `/PRA/DVOLOPT_VDOIMKT`

```
T-Code: /PRA/DVOLOPT_VDOIMKT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/DVOLOPT_VDOIMKT
```

### 388. `/PRA/ESCH_DISP_HIST`

```
T-Code: /PRA/ESCH_DISP_HIST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/ESCH_DISP_HIST
```

### 389. `/PRA/ESCH_WORKPLACE`

```
T-Code: /PRA/ESCH_WORKPLACE
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/ESCH_WORKPLACE
```

### 390. `/PRA/ESCR_WORKPLACE`

```
T-Code: /PRA/ESCR_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ESCR_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 391. `/PRA/ESC_SFI`

```
T-Code: /PRA/ESC_SFI
├── /PRA/OWN
    └── /PRA/OWN_F → 1
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ESC_SFI
```

### 392. `/PRA/FF_CTR_VOL`

```
T-Code: /PRA/FF_CTR_VOL
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 31, 37, 40, 49, 6, 60, 61, 69, 71, A5, A9, DL, UL
    └── OIU_TCODE → /PRA/FF_CTR_VOLS
```

### 393. `/PRA/FF_CTR_VOL_UPLD`

```
T-Code: /PRA/FF_CTR_VOL_UPLD
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N, P, R
    └── /PRA/PNRCD → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FF_CTR_VOL_UPLD
```

### 394. `/PRA/FF_CTXRF`

```
T-Code: /PRA/FF_CTXRF
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FF_CTXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FF_CTXRF
```

### 395. `/PRA/FF_MAINT_RVWDS`

```
T-Code: /PRA/FF_MAINT_RVWDS
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FF_MAINT_RVWDS
```

### 396. `/PRA/FF_RESULTSA`

```
T-Code: /PRA/FF_RESULTSA
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 31, 6, 71, A5, A9, DL, UL
    └── OIU_TCODE → /PRA/FF_RESULTSA
```

### 397. `/PRA/FF_RESULTSE`

```
T-Code: /PRA/FF_RESULTSE
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 31, 49, 6, 60, 61, 69, 71, A5, A9, DL, UL
    └── OIU_TCODE → /PRA/FF_RESULTSE
```

### 398. `/PRA/FF_UCA_MAINT`

```
T-Code: /PRA/FF_UCA_MAINT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FF_UCA_MAINT
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FF_UCA
```

### 399. `/PRA/FF_UCA_UPLD`

```
T-Code: /PRA/FF_UCA_UPLD
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FF_UCA_UPLD
```

### 400. `/PRA/FP_A`

```
T-Code: /PRA/FP_A
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_A
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 401. `/PRA/FP_ADMIN_WP`

```
T-Code: /PRA/FP_ADMIN_WP
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_ADMIN_WP
```

### 402. `/PRA/FP_AE`

```
T-Code: /PRA/FP_AE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_AE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 403. `/PRA/FP_AGREE`

```
T-Code: /PRA/FP_AGREE
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_AGREE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_AGREE
```

### 404. `/PRA/FP_AK`

```
T-Code: /PRA/FP_AK
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_AK
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 405. `/PRA/FP_ARC`

```
T-Code: /PRA/FP_ARC
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_ARC
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_ARC
```

### 406. `/PRA/FP_ASU`

```
T-Code: /PRA/FP_ASU
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_ASU
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_ASU
```

### 407. `/PRA/FP_ASUE`

```
T-Code: /PRA/FP_ASUE
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_ASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_ASUE
```

### 408. `/PRA/FP_CTXRF`

```
T-Code: /PRA/FP_CTXRF
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_CTXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_CTXRF
```

### 409. `/PRA/FP_DISP_COM_REC`

```
T-Code: /PRA/FP_DISP_COM_REC
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_DISP_COM_REC
```

### 410. `/PRA/FP_DISP_FED_BAL`

```
T-Code: /PRA/FP_DISP_FED_BAL
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_DISP_FED_BAL
```

### 411. `/PRA/FP_DISP_INDXPRC`

```
T-Code: /PRA/FP_DISP_INDXPRC
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_DISP_INDXPRC
```

### 412. `/PRA/FP_DISP_INT_CAL`

```
T-Code: /PRA/FP_DISP_INT_CAL
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_DISP_RCP_HD
```

### 413. `/PRA/FP_DISP_RCP_HD`

```
T-Code: /PRA/FP_DISP_RCP_HD
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_DISP_RCP_HD
```

### 414. `/PRA/FP_DIS_HST_CMP`

```
T-Code: /PRA/FP_DIS_HST_CMP
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_DIS_HST_CMP
```

### 415. `/PRA/FP_DIS_OGOR_CMP`

```
T-Code: /PRA/FP_DIS_OGOR_CMP
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_DIS_OGOR_CMP
```

### 416. `/PRA/FP_EDTOVR`

```
T-Code: /PRA/FP_EDTOVR
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_EDTOVR
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_EDT_OVR
```

### 417. `/PRA/FP_EST`

```
T-Code: /PRA/FP_EST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_EST
```

### 418. `/PRA/FP_FIN_REC_POP`

```
T-Code: /PRA/FP_FIN_REC_POP
└── O_OIU_TXN
    ├── ACTVT → 1
    └── OIU_TCODE → /PRA/FP_FIN_REC_POP
```

### 419. `/PRA/FP_FIN_REC_RPT`

```
T-Code: /PRA/FP_FIN_REC_RPT
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_FIN_REC_RPT
```

### 420. `/PRA/FP_INDX_PRC`

```
T-Code: /PRA/FP_INDX_PRC
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_INDX_PRC
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_INDXPRC
```

### 421. `/PRA/FP_INDX_TOL`

```
T-Code: /PRA/FP_INDX_TOL
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_EDTOVR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/FP_INDX_TOL
```

### 422. `/PRA/FP_INT_RT`

```
T-Code: /PRA/FP_INT_RT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FP_CTXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_INT_RT
```

### 423. `/PRA/FP_LEASE`

```
T-Code: /PRA/FP_LEASE
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_LEASE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_LEASE
```

### 424. `/PRA/FP_MANUAL_2014`

```
T-Code: /PRA/FP_MANUAL_2014
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_MANUAL_2014
```

### 425. `/PRA/FP_MASTER`

```
T-Code: /PRA/FP_MASTER
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_MASTER
```

### 426. `/PRA/FP_MIGRATION`

```
T-Code: /PRA/FP_MIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_MIGRATION
```

### 427. `/PRA/FP_OOS_WO`

```
T-Code: /PRA/FP_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_OOS_WO
```

### 428. `/PRA/FP_OOS_WO2`

```
T-Code: /PRA/FP_OOS_WO2
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_OOS_WO2
```

### 429. `/PRA/FP_OOS_WO_RPT`

```
T-Code: /PRA/FP_OOS_WO_RPT
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_OOS_WO_RPT
```

### 430. `/PRA/FP_OOS_WO_RPT2`

```
T-Code: /PRA/FP_OOS_WO_RPT2
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_OOS_WO_RPT2
```

### 431. `/PRA/FP_OWN_GRP`

```
T-Code: /PRA/FP_OWN_GRP
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_OWN_GRP
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_OWN_GRP
```

### 432. `/PRA/FP_PD`

```
T-Code: /PRA/FP_PD
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_PD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 433. `/PRA/FP_PDCXRF`

```
T-Code: /PRA/FP_PDCXRF
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_PDCXRF
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /PRA/V_FP_PDCXRF
```

### 434. `/PRA/FP_PORTAL`

```
T-Code: /PRA/FP_PORTAL
└── O_OIU_TXN
    ├── ACTVT → 16, 3
    └── OIU_TCODE → O3UE2_PORTAL_004
```

### 435. `/PRA/FP_PRC_LMT`

```
T-Code: /PRA/FP_PRC_LMT
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/FP_PRC_LMT
```

### 436. `/PRA/FP_PRDTA`

```
T-Code: /PRA/FP_PRDTA
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_PRDTA
```

### 437. `/PRA/FP_PRDTE`

```
T-Code: /PRA/FP_PRDTE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_PRDTE
```

### 438. `/PRA/FP_RECON_RPT`

```
T-Code: /PRA/FP_RECON_RPT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FP_RECON_RPT
```

### 439. `/PRA/FP_RNT_CRD`

```
T-Code: /PRA/FP_RNT_CRD
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_RNT_CRD
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_RNT_CRD
```

### 440. `/PRA/FP_RPDT`

```
T-Code: /PRA/FP_RPDT
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_RPDT
```

### 441. `/PRA/FP_RPDTE`

```
T-Code: /PRA/FP_RPDTE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_RPDTE
```

### 442. `/PRA/FP_S`

```
T-Code: /PRA/FP_S
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_S
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 443. `/PRA/FP_S6RTOVR`

```
T-Code: /PRA/FP_S6RTOVR
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_LEASE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_S6RTOVR
```

### 444. `/PRA/FP_SE`

```
T-Code: /PRA/FP_SE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_SE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 445. `/PRA/FP_SOL`

```
T-Code: /PRA/FP_SOL
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_SOL
```

### 446. `/PRA/FP_SOLE`

```
T-Code: /PRA/FP_SOLE
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_SOLE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_SOLE
```

### 447. `/PRA/FP_SRC`

```
T-Code: /PRA/FP_SRC
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_SRC
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/FP_SRC
```

### 448. `/PRA/FP_TRAN_PRO`

```
T-Code: /PRA/FP_TRAN_PRO
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/FP_TRAN_PRO
```

### 449. `/PRA/FP_UPDATE_PRICE`

```
T-Code: /PRA/FP_UPDATE_PRICE
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_UPDATE_PRICE
```

### 450. `/PRA/FP_UPD_EST`

```
T-Code: /PRA/FP_UPD_EST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_UPD_EST
```

### 451. `/PRA/FP_UPD_HST_CMP`

```
T-Code: /PRA/FP_UPD_HST_CMP
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_UPD_HST_CMP
```

### 452. `/PRA/FP_UPD_INT_EST`

```
T-Code: /PRA/FP_UPD_INT_EST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_DISP_RCP_HD
```

### 453. `/PRA/FP_UPD_RPDT_HST`

```
T-Code: /PRA/FP_UPD_RPDT_HST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/FP_UPD_RPDT_HST
```

### 454. `/PRA/FP_UPL_LSE_AGR`

```
T-Code: /PRA/FP_UPL_LSE_AGR
└── O_OIU_TXN
    ├── ACTVT → 1
    └── OIU_TCODE → /PRA/FP_UPL_LSE_AGR
```

### 455. `/PRA/FP_UPL_RECOUP`

```
T-Code: /PRA/FP_UPL_RECOUP
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 6, DL, UL
    └── OIU_TCODE → /PRA/FP_UPL_DWN_RCP
```

### 456. `/PRA/FP_VAR_RPT_MO`

```
T-Code: /PRA/FP_VAR_RPT_MO
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_VAR_RPT_MO
```

### 457. `/PRA/FP_VAR_SAL_MO`

```
T-Code: /PRA/FP_VAR_SAL_MO
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/FP_VAR_SAL_MO
```

### 458. `/PRA/FP_WO`

```
T-Code: /PRA/FP_WO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/FP_WO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 459. `/PRA/FP_WORKPLACE`

```
T-Code: /PRA/FP_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/FP_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2
    └── ALG_SUBOBJ → STEP_LOG
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 460. `/PRA/FT_OWNER_BAL`

```
T-Code: /PRA/FT_OWNER_BAL
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 461. `/PRA/GEN_ESCROW_ADDR`

```
T-Code: /PRA/GEN_ESCROW_ADDR
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GEN_ESCROW_ADDR
└── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → ADRESSE
```

### 462. `/PRA/GEN_WORKPLACE`

```
T-Code: /PRA/GEN_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GEN_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2
    └── ALG_SUBOBJ → STEP_LOG
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 463. `/PRA/GNES_OCCNO_MAIN`

```
T-Code: /PRA/GNES_OCCNO_MAIN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GNES_OCCNO_MAIN
```

### 464. `/PRA/GNES_PROC_SEL`

```
T-Code: /PRA/GNES_PROC_SEL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GNES_PROC_SEL
└── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /PRA/GNES_PR_SL
```

### 465. `/PRA/GTA`

```
T-Code: /PRA/GTA
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/GT_A
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 466. `/PRA/GTAE`

```
T-Code: /PRA/GTAE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/GT_AE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 467. `/PRA/GTS`

```
T-Code: /PRA/GTS
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/GT_S
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 468. `/PRA/GTSE`

```
T-Code: /PRA/GTSE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/GT_SE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 469. `/PRA/GTWO`

```
T-Code: /PRA/GTWO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/GT_WO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 470. `/PRA/GT_ASU`

```
T-Code: /PRA/GT_ASU
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_ASU
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_ASU
```

### 471. `/PRA/GT_ASUE`

```
T-Code: /PRA/GT_ASUE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_ASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_ASUE
```

### 472. `/PRA/GT_DISP_OOSWO`

```
T-Code: /PRA/GT_DISP_OOSWO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_DISP_OOSWO
```

### 473. `/PRA/GT_DISP_RPTVAR`

```
T-Code: /PRA/GT_DISP_RPTVAR
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_DISP_RPTVAR
```

### 474. `/PRA/GT_DISP_TAXHST`

```
T-Code: /PRA/GT_DISP_TAXHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_DISP_TAXHST
```

### 475. `/PRA/GT_DISP_TAXTXN`

```
T-Code: /PRA/GT_DISP_TAXTXN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_DISP_TAXTXN
```

### 476. `/PRA/GT_ENTITY`

```
T-Code: /PRA/GT_ENTITY
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_ENTITY
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_ENTITY
```

### 477. `/PRA/GT_MASTER`

```
T-Code: /PRA/GT_MASTER
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_MASTER
```

### 478. `/PRA/GT_MIG_TAXHST`

```
T-Code: /PRA/GT_MIG_TAXHST
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 40, 41, 49, 6, DL, UL
    └── OIU_TCODE → /PRA/GT_MIG_TAXHST
├── S_BTCH_ADM
    └── BTCADMIN → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 479. `/PRA/GT_OOS_WO`

```
T-Code: /PRA/GT_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_OOS_WO
```

### 480. `/PRA/GT_SOL`

```
T-Code: /PRA/GT_SOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_SOL
```

### 481. `/PRA/GT_SOLE`

```
T-Code: /PRA/GT_SOLE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_SOLE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_SOLE
```

### 482. `/PRA/GT_TAXID`

```
T-Code: /PRA/GT_TAXID
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_TAXID
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_OPTAXID
```

### 483. `/PRA/GT_UPD_TAXHST`

```
T-Code: /PRA/GT_UPD_TAXHST
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_UPD_TAXHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_TAXHST
```

### 484. `/PRA/GT_UPD_TAXTXN`

```
T-Code: /PRA/GT_UPD_TAXTXN
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/GT_UPD_TAXTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/GT_TAXTXN
```

### 485. `/PRA/JEPURGE_OFF`

```
T-Code: /PRA/JEPURGE_OFF
└── O_OIU_TXN
    ├── ACTVT → 1
    └── OIU_TCODE → DELETE
```

### 486. `/PRA/KOO`

```
T-Code: /PRA/KOO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KOO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 487. `/PRA/KSA`

```
T-Code: /PRA/KSA
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KSA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 488. `/PRA/KSE`

```
T-Code: /PRA/KSE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KSE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 489. `/PRA/KSO`

```
T-Code: /PRA/KSO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KSO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 490. `/PRA/KSS`

```
T-Code: /PRA/KSS
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KSS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 491. `/PRA/KSTXH`

```
T-Code: /PRA/KSTXH
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/KSTXH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 492. `/PRA/KS_ASU`

```
T-Code: /PRA/KS_ASU
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/KS_ASU
```

### 493. `/PRA/KS_ASU1`

```
T-Code: /PRA/KS_ASU1
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_ASU1
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_ASU
```

### 494. `/PRA/KS_ASUE`

```
T-Code: /PRA/KS_ASUE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_ASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_ASUE
```

### 495. `/PRA/KS_LEASE`

```
T-Code: /PRA/KS_LEASE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_LEASE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_LEASE
```

### 496. `/PRA/KS_LEASE_UP_DWN`

```
T-Code: /PRA/KS_LEASE_UP_DWN
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, DL, UL
    └── OIU_TCODE → /PRA/KS_LEASE_UP_DWN
```

### 497. `/PRA/KS_MASTER`

```
T-Code: /PRA/KS_MASTER
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_MASTER
```

### 498. `/PRA/KS_MASTER_UPDWN`

```
T-Code: /PRA/KS_MASTER_UPDWN
└── O_OIU_TXN
    ├── ACTVT → 16
    └── OIU_TCODE → /PRA/KS_MASTER_UPDWN
```

### 499. `/PRA/KS_MIGRATION`

```
T-Code: /PRA/KS_MIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/KS_MIGRATION
```

### 500. `/PRA/KS_OOS_WO`

```
T-Code: /PRA/KS_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → 16, 2, 3
    └── OIU_TCODE → (empty — maintain in PFCG)
```

### 501. `/PRA/KS_SOL`

```
T-Code: /PRA/KS_SOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_SOL1
```

### 502. `/PRA/KS_SOLE`

```
T-Code: /PRA/KS_SOLE
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → O_OIU_TXN
```

### 503. `/PRA/KS_SOLE1`

```
T-Code: /PRA/KS_SOLE1
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_SOLE1
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_SOLE
```

### 504. `/PRA/KS_TAXHST`

```
T-Code: /PRA/KS_TAXHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_TAXHST
```

### 505. `/PRA/KS_TAXHT_UPDATE`

```
T-Code: /PRA/KS_TAXHT_UPDATE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/KS_TAXHT_UPDATE
```

### 506. `/PRA/KS_TAXRT`

```
T-Code: /PRA/KS_TAXRT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_TAXRT
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_TAXRATE
```

### 507. `/PRA/KS_TAXTXN`

```
T-Code: /PRA/KS_TAXTXN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_TAXTXN
```

### 508. `/PRA/KS_TAXTXN_UPLD`

```
T-Code: /PRA/KS_TAXTXN_UPLD
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, UL
    └── OIU_TCODE → /PRA/KS_TAXTXN_UPLD
```

### 509. `/PRA/KS_TXCL_XFR`

```
T-Code: /PRA/KS_TXCL_XFR
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/KS_TXCL_XFR
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/KS_TXCL_XF
```

### 510. `/PRA/LOCK_MAINT`

```
T-Code: /PRA/LOCK_MAINT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/LOCK_MAINT
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 511. `/PRA/LS_RECON_RPT`

```
T-Code: /PRA/LS_RECON_RPT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/LS_RECON_RPT
```

### 512. `/PRA/MIGRATION`

```
T-Code: /PRA/MIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/MIGRATION
```

### 513. `/PRA/MIG_BADI_RAD`

```
T-Code: /PRA/MIG_BADI_RAD
└── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 514. `/PRA/MIG_CFG`

```
T-Code: /PRA/MIG_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 515. `/PRA/MIG_R3_PRDT`

```
T-Code: /PRA/MIG_R3_PRDT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R3_PRDT
```

### 516. `/PRA/MIG_R3_ROYHST`

```
T-Code: /PRA/MIG_R3_ROYHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R3_ROYHST
```

### 517. `/PRA/MIG_R3_ROYSTG`

```
T-Code: /PRA/MIG_R3_ROYSTG
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R3_ROYSTG
```

### 518. `/PRA/MIG_R3_ROYTXN`

```
T-Code: /PRA/MIG_R3_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R3_ROYTXN
```

### 519. `/PRA/MIG_R6_PRDT`

```
T-Code: /PRA/MIG_R6_PRDT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R6_PRDT
```

### 520. `/PRA/MIG_R6_ROYHST`

```
T-Code: /PRA/MIG_R6_ROYHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R6_ROYHST
```

### 521. `/PRA/MIG_R6_ROYSTG`

```
T-Code: /PRA/MIG_R6_ROYSTG
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R6_ROYSTG
```

### 522. `/PRA/MIG_R6_ROYTXN`

```
T-Code: /PRA/MIG_R6_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/MIG_R6_ROYTXN
```

### 523. `/PRA/MIG_RECON_JERAD`

```
T-Code: /PRA/MIG_RECON_JERAD
└── S_GUI
    └── ACTVT → 61
```

### 524. `/PRA/NDA`

```
T-Code: /PRA/NDA
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 525. `/PRA/NDASE`

```
T-Code: /PRA/NDASE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDASE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 526. `/PRA/NDCHD`

```
T-Code: /PRA/NDCHD
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDCHD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 527. `/PRA/NDO`

```
T-Code: /PRA/NDO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 528. `/PRA/NDSE`

```
T-Code: /PRA/NDSE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDSE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 529. `/PRA/NDSS`

```
T-Code: /PRA/NDSS
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/NDSS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 530. `/PRA/ND_ASU`

```
T-Code: /PRA/ND_ASU
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_ASU
```

### 531. `/PRA/ND_ASUE`

```
T-Code: /PRA/ND_ASUE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_ASUE
```

### 532. `/PRA/ND_CUSTAXID`

```
T-Code: /PRA/ND_CUSTAXID
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_CUSTAXID
```

### 533. `/PRA/ND_DISP_VAR`

```
T-Code: /PRA/ND_DISP_VAR
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ND_DISP_VAR
```

### 534. `/PRA/ND_MASTER`

```
T-Code: /PRA/ND_MASTER
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_MASTER
```

### 535. `/PRA/ND_MASTER_UPDWN`

```
T-Code: /PRA/ND_MASTER_UPDWN
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_MASTER_UPDWN
```

### 536. `/PRA/ND_MIGRATION`

```
T-Code: /PRA/ND_MIGRATION
├── S_BTCH_ADM
    └── BTCADMIN → (empty — maintain in PFCG)
└── S_BTCH_JOB
    ├── JOBACTION → *
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 537. `/PRA/ND_SOL`

```
T-Code: /PRA/ND_SOL
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_SOL
```

### 538. `/PRA/ND_SOLE`

```
T-Code: /PRA/ND_SOLE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_SOLE
```

### 539. `/PRA/ND_TAXHST`

```
T-Code: /PRA/ND_TAXHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ND_TAXHST
```

### 540. `/PRA/ND_TAXHT_UPDATE`

```
T-Code: /PRA/ND_TAXHT_UPDATE
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 6, DL, UL
    └── OIU_TCODE → /PRA/ND_TAXHT_UPDATE
```

### 541. `/PRA/ND_TAXRT`

```
T-Code: /PRA/ND_TAXRT
├── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_TAXRT
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/ND_TAXRATE
```

### 542. `/PRA/ND_UPD_TAXTXN`

```
T-Code: /PRA/ND_UPD_TAXTXN
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/ND_UPD_TAXTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/ND_TAXTXN
```

### 543. `/PRA/ND_VEN_POOL_XRF`

```
T-Code: /PRA/ND_VEN_POOL_XRF
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/ND_VEN_POOL_XRF
```

### 544. `/PRA/NIMB_MASSUPLOAD`

```
T-Code: /PRA/NIMB_MASSUPLOAD
└── S_GUI
    └── ACTVT → 2, 4, 60, 61
```

### 545. `/PRA/NIMB_WORKPLACE`

```
T-Code: /PRA/NIMB_WORKPLACE
└── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → OIU_NIMB
```

### 546. `/PRA/OM_DOCUMENT`

```
T-Code: /PRA/OM_DOCUMENT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/OM_DOCUMENT
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/OM_MFM
```

### 547. `/PRA/OM_EXECUTE`

```
T-Code: /PRA/OM_EXECUTE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/OM_EXECUTE
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 548. `/PRA/OM_MF`

```
T-Code: /PRA/OM_MF
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/OM_MF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 549. `/PRA/OUTBOUND_CDEX`

```
T-Code: /PRA/OUTBOUND_CDEX
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/OUTBOUND_CDEX
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 550. `/PRA/OW_PREREQ_RPT`

```
T-Code: /PRA/OW_PREREQ_RPT
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/OW_PREREQ_RPT
```

### 551. `/PRA/OW_TRACK_RPT`

```
T-Code: /PRA/OW_TRACK_RPT
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/OW_TRACK_RPT
```

### 552. `/PRA/PN_DEFINE_TAG`

```
T-Code: /PRA/PN_DEFINE_TAG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PN_TAG
```

### 553. `/PRA/PN_DNL_PCTL`

```
T-Code: /PRA/PN_DNL_PCTL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PN_DNLPCT
```

### 554. `/PRA/PN_DNL_QCTL`

```
T-Code: /PRA/PN_DNL_QCTL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/V_PN_DNLQCT
```

### 555. `/PRA/PN_MANUAL`

```
T-Code: /PRA/PN_MANUAL
├── /PRA/PN
    ├── /PRA/PNACT → N
    └── /PRA/PNRCD → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 556. `/PRA/PN_WORKPLACE`

```
T-Code: /PRA/PN_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 557. `/PRA/PPN_WORKPLACE_C`

```
T-Code: /PRA/PPN_WORKPLACE_C
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N, P
    └── /PRA/PNRCD → (empty — maintain in PFCG)
├── O_OIU_NSUB
    └── OIU_SUBSYS → CA
└── S_GUI
    └── ACTVT → 61
```

### 558. `/PRA/PPN_WORKPLACE_M`

```
T-Code: /PRA/PPN_WORKPLACE_M
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N, P
    └── /PRA/PNRCD → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 559. `/PRA/PPN_WORKPLACE_P`

```
T-Code: /PRA/PPN_WORKPLACE_P
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N, P
    └── /PRA/PNRCD → (empty — maintain in PFCG)
├── O_OIU_NSUB
    └── OIU_SUBSYS → CA
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 560. `/PRA/PPN_WORKPLACE_V`

```
T-Code: /PRA/PPN_WORKPLACE_V
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N, P
    └── /PRA/PNRCD → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → DNL
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 561. `/PRA/PP_ADMIN_WP`

```
T-Code: /PRA/PP_ADMIN_WP
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_ADMIN_WP
```

### 562. `/PRA/PP_BANK_TAPE`

```
T-Code: /PRA/PP_BANK_TAPE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_BANK_TAPE
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 563. `/PRA/PP_CH`

```
T-Code: /PRA/PP_CH
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/PP_CH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 564. `/PRA/PP_CHECK_CLEAR`

```
T-Code: /PRA/PP_CHECK_CLEAR
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_CHECK_CLEAR
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → CHK_CLR
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 565. `/PRA/PP_CM`

```
T-Code: /PRA/PP_CM
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/PP_CM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 566. `/PRA/PP_DISP_CHK_HDR`

```
T-Code: /PRA/PP_DISP_CHK_HDR
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_DISP_CHK_HDR
```

### 567. `/PRA/PP_DISP_OWN_BAL`

```
T-Code: /PRA/PP_DISP_OWN_BAL
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_DISP_OWN_BAL
```

### 568. `/PRA/PP_JB`

```
T-Code: /PRA/PP_JB
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/PP_JB
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 569. `/PRA/PP_MIN_PAY`

```
T-Code: /PRA/PP_MIN_PAY
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_MIN_PAY
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/PP_MIN_PAY
```

### 570. `/PRA/PP_MKT_OVR`

```
T-Code: /PRA/PP_MKT_OVR
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_MKT_OVR
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/PP_MKT_OVR
```

### 571. `/PRA/PP_NRC_VD`

```
T-Code: /PRA/PP_NRC_VD
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_NRC_VD
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/PP_NRC_VD
```

### 572. `/PRA/PP_OUTPUT_FILE`

```
T-Code: /PRA/PP_OUTPUT_FILE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_OUTPUT_FILE
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 573. `/PRA/PP_PORTAL`

```
T-Code: /PRA/PP_PORTAL
└── O_OIU_TXN
    ├── ACTVT → 16, 3
    └── OIU_TCODE → O3UE2_PORTAL_004
```

### 574. `/PRA/PP_RECON_RPT`

```
T-Code: /PRA/PP_RECON_RPT
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_RECON_RPT
```

### 575. `/PRA/PP_REFUND_WH_TX`

```
T-Code: /PRA/PP_REFUND_WH_TX
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_REFUND_WH_TX
```

### 576. `/PRA/PP_TAX_OVR`

```
T-Code: /PRA/PP_TAX_OVR
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_TAX_OVR
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/PP_TAX_OVR
```

### 577. `/PRA/PP_VAR_SET`

```
T-Code: /PRA/PP_VAR_SET
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PP_VAR_SET
```

### 578. `/PRA/PP_WH_RT`

```
T-Code: /PRA/PP_WH_RT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_WH_RT
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/PP_WH_RT
```

### 579. `/PRA/PP_WORKPLACE`

```
T-Code: /PRA/PP_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/PP_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2
    └── ALG_SUBOBJ → STEP_LOG
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 580. `/PRA/PROC_SEL`

```
T-Code: /PRA/PROC_SEL
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/PROC_SEL
```

### 581. `/PRA/R3_DISP_BAL_RPT`

```
T-Code: /PRA/R3_DISP_BAL_RPT
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R3_DISP_BAL_RPT
```

### 582. `/PRA/R3_DISP_ROYTXN`

```
T-Code: /PRA/R3_DISP_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 6, A5, DL, UL
    └── OIU_TCODE → /PRA/R3_DISP_ROYTXN
```

### 583. `/PRA/R3_MASTER`

```
T-Code: /PRA/R3_MASTER
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R3_MASTER
```

### 584. `/PRA/R3_OWNOVR`

```
T-Code: /PRA/R3_OWNOVR
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 40, 6
    └── OIU_TCODE → /PRA/R3_OWNOVR
```

### 585. `/PRA/R3_OWNXRF`

```
T-Code: /PRA/R3_OWNXRF
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R3_OWNXRF
```

### 586. `/PRA/R3_PAY_RECON`

```
T-Code: /PRA/R3_PAY_RECON
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/R3_PAY_RECON
```

### 587. `/PRA/R3_RRC`

```
T-Code: /PRA/R3_RRC
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R3_RRC
```

### 588. `/PRA/R3_UPD_OOS_WO`

```
T-Code: /PRA/R3_UPD_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R3_UPD_OOS_WO
```

### 589. `/PRA/R3_UPD_ROYHST`

```
T-Code: /PRA/R3_UPD_ROYHST
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R3_UPD_ROYHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/R3_ROYHST
```

### 590. `/PRA/R3_UPD_ROYTXN`

```
T-Code: /PRA/R3_UPD_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 40, 6, A5, DL, UL
    └── OIU_TCODE → /PRA/R3_UPD_ROYTXN
```

### 591. `/PRA/R6_DISP_BAL_RPT`

```
T-Code: /PRA/R6_DISP_BAL_RPT
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_DISP_BAL_RPT
```

### 592. `/PRA/R6_DISP_OOSWO`

```
T-Code: /PRA/R6_DISP_OOSWO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_DISP_OOSWO
```

### 593. `/PRA/R6_DISP_ROYHST`

```
T-Code: /PRA/R6_DISP_ROYHST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_DISP_ROYHST
```

### 594. `/PRA/R6_DISP_ROYTXN`

```
T-Code: /PRA/R6_DISP_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_DISP_ROYTXN
```

### 595. `/PRA/R6_MASTER`

```
T-Code: /PRA/R6_MASTER
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R6_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → OIUY6_WY_MASTER
```

### 596. `/PRA/R6_PAY_RECON`

```
T-Code: /PRA/R6_PAY_RECON
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/R6_PAY_RECON
```

### 597. `/PRA/R6_UPD_OOS_WO`

```
T-Code: /PRA/R6_UPD_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_UPD_OOS_WO
```

### 598. `/PRA/R6_UPD_ROYHST`

```
T-Code: /PRA/R6_UPD_ROYHST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_UPD_ROYHST
```

### 599. `/PRA/R6_UPD_ROYTXN`

```
T-Code: /PRA/R6_UPD_ROYTXN
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/R6_UPD_ROYTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/R6_ROYTXN
```

### 600. `/PRA/RD_EF_RULE`

```
T-Code: /PRA/RD_EF_RULE
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/RD_EF_RULE
```

### 601. `/PRA/RD_RPT_BG`

```
T-Code: /PRA/RD_RPT_BG
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 602. `/PRA/RKS_P2_OOS_WO`

```
T-Code: /PRA/RKS_P2_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/RKS_P2_OOS_WO
```

### 603. `/PRA/ROY_GLO_DATA_UP`

```
T-Code: /PRA/ROY_GLO_DATA_UP
└── O_OIU_TXN
    ├── ACTVT → UL
    └── OIU_TCODE → /PRA/ROY_GLO_DATA_UP
```

### 604. `/PRA/ROY_UTX_DATA_UP`

```
T-Code: /PRA/ROY_UTX_DATA_UP
└── O_OIU_TXN
    ├── ACTVT → UL
    └── OIU_TCODE → /PRA/ROY_UTX_DATA_UP
```

### 605. `/PRA/RP_PORTAL`

```
T-Code: /PRA/RP_PORTAL
└── O_OIU_TXN
    ├── ACTVT → 16, 3
    └── OIU_TCODE → O3UE2_PORTAL_004
```

### 606. `/PRA/RP_PROF`

```
T-Code: /PRA/RP_PROF
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RP_PROF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/RP_PROF
```

### 607. `/PRA/RP_ROYOWN`

```
T-Code: /PRA/RP_ROYOWN
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RP_ROYOWN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/RP_ROYOWN
```

### 608. `/PRA/RP_WORKPLACE`

```
T-Code: /PRA/RP_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RP_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2, OIUX3
    └── ALG_SUBOBJ → STEP_LOG
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 609. `/PRA/RSP`

```
T-Code: /PRA/RSP
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 610. `/PRA/RSPFH`

```
T-Code: /PRA/RSPFH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RSP_FUNCH_V
```

### 611. `/PRA/RSPHR`

```
T-Code: /PRA/RSPHR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RSP_FUNH_V, /PRA/RSP_HIER_V
```

### 612. `/PRA/RSPLP`

```
T-Code: /PRA/RSPLP
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSPLP
```

### 613. `/PRA/RSPUG`

```
T-Code: /PRA/RSPUG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /PRA/RSP_UG_US_V, /PRA/RSP_UG_V
```

### 614. `/PRA/RSP_UPLD_ATT`

```
T-Code: /PRA/RSP_UPLD_ATT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_ATT
```

### 615. `/PRA/RSP_UPLD_ATT2`

```
T-Code: /PRA/RSP_UPLD_ATT2
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_ATT2
```

### 616. `/PRA/RSP_UPLD_DN`

```
T-Code: /PRA/RSP_UPLD_DN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_DN
```

### 617. `/PRA/RSP_UPLD_HIER`

```
T-Code: /PRA/RSP_UPLD_HIER
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_HIER
```

### 618. `/PRA/RSP_UPLD_LEASE`

```
T-Code: /PRA/RSP_UPLD_LEASE
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_LEASE
```

### 619. `/PRA/RSP_UPLD_MP`

```
T-Code: /PRA/RSP_UPLD_MP
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_MP
```

### 620. `/PRA/RSP_UPLD_TP`

```
T-Code: /PRA/RSP_UPLD_TP
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_TP
```

### 621. `/PRA/RSP_UPLD_USER`

```
T-Code: /PRA/RSP_UPLD_USER
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_USER
```

### 622. `/PRA/RSP_UPLD_VDMP`

```
T-Code: /PRA/RSP_UPLD_VDMP
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/RSP_UPLD_VDMP
```

### 623. `/PRA/R_PN_MIGRATION`

```
T-Code: /PRA/R_PN_MIGRATION
├── /PRA/PN
    ├── /PRA/PNACT → A, D, H, I, N
    └── /PRA/PNRCD → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 624. `/PRA/R_PN_VL_POST_ST`

```
T-Code: /PRA/R_PN_VL_POST_ST
├── /PRA/PN
    ├── /PRA/PNACT → P
    └── /PRA/PNRCD → (empty — maintain in PFCG)
└── O_OIU_NSUB
    └── OIU_SUBSYS → VL
```

### 625. `/PRA/R_PN_VOL_PPN`

```
T-Code: /PRA/R_PN_VOL_PPN
├── O_OIU_NSUB
    └── OIU_SUBSYS → CA, PR
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/R_PN_VOL_PPN
```

### 626. `/PRA/R_VL_SD_MIGRATE`

```
T-Code: /PRA/R_VL_SD_MIGRATE
└── S_GUI
    └── ACTVT → 61
```

### 627. `/PRA/R_VL_SD_STITCH`

```
T-Code: /PRA/R_VL_SD_STITCH
└── S_GUI
    └── ACTVT → 61
```

### 628. `/PRA/R_WH_REP_TH`

```
T-Code: /PRA/R_WH_REP_TH
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/R_WH_REP_TH
```

### 629. `/PRA/SS0`

```
T-Code: /PRA/SS0
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/SS0
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIU
    └── ALG_SUBOBJ → VL
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 630. `/PRA/T3_OO`

```
T-Code: /PRA/T3_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T3_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 631. `/PRA/T3_TAXID`

```
T-Code: /PRA/T3_TAXID
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3, 6
    └── OIU_TCODE → (empty — maintain in PFCG)
```

### 632. `/PRA/T515_OOS_WO`

```
T-Code: /PRA/T515_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T515_OOS_WO
```

### 633. `/PRA/T5_OO`

```
T-Code: /PRA/T5_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T5_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 634. `/PRA/T5_TAXID`

```
T-Code: /PRA/T5_TAXID
└── O_OIU_TXN
    ├── ACTVT → 1, 16, 2, 3
    └── OIU_TCODE → (empty — maintain in PFCG)
```

### 635. `/PRA/T6_OO`

```
T-Code: /PRA/T6_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T6_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 636. `/PRA/T6_OPID`

```
T-Code: /PRA/T6_OPID
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → /PRA/T6_OPID
```

### 637. `/PRA/T7_MIG_4X11HST`

```
T-Code: /PRA/T7_MIG_4X11HST
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T7_MIG_4X11HST
```

### 638. `/PRA/T8_OO`

```
T-Code: /PRA/T8_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T8_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 639. `/PRA/T8_OOS_WO`

```
T-Code: /PRA/T8_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T8_OOS_WO
```

### 640. `/PRA/T9_A`

```
T-Code: /PRA/T9_A
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_A
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 641. `/PRA/T9_AE`

```
T-Code: /PRA/T9_AE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_AE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 642. `/PRA/T9_ASU`

```
T-Code: /PRA/T9_ASU
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_ASU
```

### 643. `/PRA/T9_ASUE`

```
T-Code: /PRA/T9_ASUE
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_ASUE
```

### 644. `/PRA/T9_DIS_OGP_COMP`

```
T-Code: /PRA/T9_DIS_OGP_COMP
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_OGP_COMP
```

### 645. `/PRA/T9_DIS_OOSWO`

```
T-Code: /PRA/T9_DIS_OOSWO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T9_DIS_OOSWO
```

### 646. `/PRA/T9_DIS_RPGDTE`

```
T-Code: /PRA/T9_DIS_RPGDTE
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_RPGDTE
```

### 647. `/PRA/T9_DIS_RPODTD`

```
T-Code: /PRA/T9_DIS_RPODTD
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_RPODTD
```

### 648. `/PRA/T9_DIS_RPT_VAR`

```
T-Code: /PRA/T9_DIS_RPT_VAR
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_RPT_VAR
```

### 649. `/PRA/T9_DIS_TAXHST`

```
T-Code: /PRA/T9_DIS_TAXHST
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_TAXHST
```

### 650. `/PRA/T9_DIS_TAXTXN`

```
T-Code: /PRA/T9_DIS_TAXTXN
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_DIS_TAXTXN
```

### 651. `/PRA/T9_ISMASTER`

```
T-Code: /PRA/T9_ISMASTER
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/T9_ISMASTER
```

### 652. `/PRA/T9_LEASE`

```
T-Code: /PRA/T9_LEASE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_LEASE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_LEASE
```

### 653. `/PRA/T9_MASTER`

```
T-Code: /PRA/T9_MASTER
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_MASTER
```

### 654. `/PRA/T9_MIGRATION`

```
T-Code: /PRA/T9_MIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T9_MIGRATION
```

### 655. `/PRA/T9_OO`

```
T-Code: /PRA/T9_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 656. `/PRA/T9_OOSWO`

```
T-Code: /PRA/T9_OOSWO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T9_OOSWO
```

### 657. `/PRA/T9_OW`

```
T-Code: /PRA/T9_OW
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_OW
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 658. `/PRA/T9_P2_OOSWO`

```
T-Code: /PRA/T9_P2_OOSWO
└── O_OIU_TXN
    ├── ACTVT → 3
    └── OIU_TCODE → /PRA/T9_P2_OOSWO
```

### 659. `/PRA/T9_PTCXRF`

```
T-Code: /PRA/T9_PTCXRF
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_PTCXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_PTCXRF
```

### 660. `/PRA/T9_S`

```
T-Code: /PRA/T9_S
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_S
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 661. `/PRA/T9_SE`

```
T-Code: /PRA/T9_SE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_SE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 662. `/PRA/T9_SOL`

```
T-Code: /PRA/T9_SOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_SOL
```

### 663. `/PRA/T9_SOLE`

```
T-Code: /PRA/T9_SOLE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_SOLE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_SOLE
```

### 664. `/PRA/T9_TT`

```
T-Code: /PRA/T9_TT
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/T9_TT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 665. `/PRA/T9_UPD_TAXHST`

```
T-Code: /PRA/T9_UPD_TAXHST
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/T9_UPD_TAXHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/T9_TAXHST
```

### 666. `/PRA/T9_UPD_TAXTXN`

```
T-Code: /PRA/T9_UPD_TAXTXN
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T9_UPD_TAXTXN
```

### 667. `/PRA/TAO`

```
T-Code: /PRA/TAO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TAO
```

### 668. `/PRA/TAX_TX_DATA_UP`

```
T-Code: /PRA/TAX_TX_DATA_UP
└── O_OIU_TXN
    ├── ACTVT → UL
    └── OIU_TCODE → /PRA/TAX_TX_DATA_UP
```

### 669. `/PRA/TA_A`

```
T-Code: /PRA/TA_A
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_A
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 670. `/PRA/TA_AE`

```
T-Code: /PRA/TA_AE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_AE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 671. `/PRA/TA_ASU`

```
T-Code: /PRA/TA_ASU
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_ASU
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_ASU
```

### 672. `/PRA/TA_ASUE`

```
T-Code: /PRA/TA_ASUE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_ASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_ASUE
```

### 673. `/PRA/TA_DIS_IOOSWO`

```
T-Code: /PRA/TA_DIS_IOOSWO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/TA_DIS_IOOSWO
```

### 674. `/PRA/TA_DIS_ITAXHST`

```
T-Code: /PRA/TA_DIS_ITAXHST
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_DIS_ITAXHST
```

### 675. `/PRA/TA_DIS_ITAXTXN`

```
T-Code: /PRA/TA_DIS_ITAXTXN
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_DIS_ITAXTXN
```

### 676. `/PRA/TA_DIS_OOSWO`

```
T-Code: /PRA/TA_DIS_OOSWO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_DIS_OOSWO
```

### 677. `/PRA/TA_DIS_RPT_VAR`

```
T-Code: /PRA/TA_DIS_RPT_VAR
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_DIS_RPT_VAR
```

### 678. `/PRA/TA_DIS_TAXTXN`

```
T-Code: /PRA/TA_DIS_TAXTXN
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_DIS_TAXTXN
```

### 679. `/PRA/TA_I`

```
T-Code: /PRA/TA_I
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_I
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 680. `/PRA/TA_IASU`

```
T-Code: /PRA/TA_IASU
├── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_IASU
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_IASU
```

### 681. `/PRA/TA_IASUE`

```
T-Code: /PRA/TA_IASUE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_IASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_IASUE
```

### 682. `/PRA/TA_IE`

```
T-Code: /PRA/TA_IE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_IE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 683. `/PRA/TA_IMIGRATION`

```
T-Code: /PRA/TA_IMIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/TA_IMIGRATION
```

### 684. `/PRA/TA_IO`

```
T-Code: /PRA/TA_IO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_IO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 685. `/PRA/TA_IOOSWO`

```
T-Code: /PRA/TA_IOOSWO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_OOSWO
```

### 686. `/PRA/TA_IS`

```
T-Code: /PRA/TA_IS
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_IS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 687. `/PRA/TA_ISOL`

```
T-Code: /PRA/TA_ISOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_ISOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_ISOL
```

### 688. `/PRA/TA_ISOLE`

```
T-Code: /PRA/TA_ISOLE
└── O_OIU_TXN
    ├── ACTVT → 3, A5
    └── OIU_TCODE → /PRA/TA_ISOLE
```

### 689. `/PRA/TA_IT`

```
T-Code: /PRA/TA_IT
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_IT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 690. `/PRA/TA_IX`

```
T-Code: /PRA/TA_IX
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_IX
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 691. `/PRA/TA_LEVY`

```
T-Code: /PRA/TA_LEVY
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_LEVY
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_LEVY_RT
```

### 692. `/PRA/TA_MIGRATION`

```
T-Code: /PRA/TA_MIGRATION
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/TA_MIGRATION
```

### 693. `/PRA/TA_OO`

```
T-Code: /PRA/TA_OO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_OO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 694. `/PRA/TA_OOSWO`

```
T-Code: /PRA/TA_OOSWO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_OOSWO
```

### 695. `/PRA/TA_OPERXREF`

```
T-Code: /PRA/TA_OPERXREF
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_OPERXREF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_OPXREF
```

### 696. `/PRA/TA_OW`

```
T-Code: /PRA/TA_OW
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_OW
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 697. `/PRA/TA_S`

```
T-Code: /PRA/TA_S
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_S
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 698. `/PRA/TA_SE`

```
T-Code: /PRA/TA_SE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/TA_SE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 699. `/PRA/TA_SOL`

```
T-Code: /PRA/TA_SOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_SOL
```

### 700. `/PRA/TA_SOLE`

```
T-Code: /PRA/TA_SOLE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_SOLE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_SOLE
```

### 701. `/PRA/TA_UPD_ITAXHST`

```
T-Code: /PRA/TA_UPD_ITAXHST
├── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/T9_UPD_TAXHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_ITAXHST
```

### 702. `/PRA/TA_UPD_ITAXTXN`

```
T-Code: /PRA/TA_UPD_ITAXTXN
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_UPD_ITAXTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_ITAXTXN
```

### 703. `/PRA/TA_UPD_TAXHST`

```
T-Code: /PRA/TA_UPD_TAXHST
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_UPD_TAXHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_TAXHST
```

### 704. `/PRA/TA_UPD_TAXTXN`

```
T-Code: /PRA/TA_UPD_TAXTXN
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TA_UPD_TAXTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/TA_TAXTXN
```

### 705. `/PRA/TEMP_DIS_SHC`

```
T-Code: /PRA/TEMP_DIS_SHC
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TEMP_DIS_SHC
```

### 706. `/PRA/TEMP_PP_OWN`

```
T-Code: /PRA/TEMP_PP_OWN
├── /PRA/OWN
    └── /PRA/OWN_F → 1
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/TEMP_PP_OWN
```

### 707. `/PRA/TP_AUTO_WO`

```
T-Code: /PRA/TP_AUTO_WO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TP_AUTO_WO
```

### 708. `/PRA/TP_CNTRL_RPT`

```
T-Code: /PRA/TP_CNTRL_RPT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/TP_CNTRL_RPT
```

### 709. `/PRA/TP_SUMMRY`

```
T-Code: /PRA/TP_SUMMRY
└── OIUX4_WRK
    └── OIUX4_ACT → AW, CM, CT, CW, IW
```

### 710. `/PRA/TP_WO_SETUP`

```
T-Code: /PRA/TP_WO_SETUP
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6
    └── OIU_TCODE → (empty — maintain in PFCG)
```

### 711. `/PRA/UTXA`

```
T-Code: /PRA/UTXA
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/UTXA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 712. `/PRA/UTXAE`

```
T-Code: /PRA/UTXAE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/UTXAE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 713. `/PRA/UTXS`

```
T-Code: /PRA/UTXS
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/UTXS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 714. `/PRA/UTXSE`

```
T-Code: /PRA/UTXSE
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/UTXSE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 715. `/PRA/UTX_ASU`

```
T-Code: /PRA/UTX_ASU
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_ASU
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_ASU
```

### 716. `/PRA/UTX_ASUE`

```
T-Code: /PRA/UTX_ASUE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_ASUE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_ASUE
```

### 717. `/PRA/UTX_COMBINE_UT3`

```
T-Code: /PRA/UTX_COMBINE_UT3
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/UTX_COMBINE_UT3
```

### 718. `/PRA/UTX_DISP_BALRPT`

```
T-Code: /PRA/UTX_DISP_BALRPT
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_DISP_BALRPT
```

### 719. `/PRA/UTX_DISP_OOSWO`

```
T-Code: /PRA/UTX_DISP_OOSWO
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_DISP_OOSWO
```

### 720. `/PRA/UTX_DISP_ROYHST`

```
T-Code: /PRA/UTX_DISP_ROYHST
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_DISP_ROYHST
```

### 721. `/PRA/UTX_DISP_ROYTXN`

```
T-Code: /PRA/UTX_DISP_ROYTXN
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_DISP_ROYTXN
```

### 722. `/PRA/UTX_DISP_RPTVAR`

```
T-Code: /PRA/UTX_DISP_RPTVAR
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_DISP_RPTVAR
```

### 723. `/PRA/UTX_LEASE`

```
T-Code: /PRA/UTX_LEASE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_LEASE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_LEASE
```

### 724. `/PRA/UTX_MASTER`

```
T-Code: /PRA/UTX_MASTER
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_MASTER
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_MASTER
```

### 725. `/PRA/UTX_OOS_WO`

```
T-Code: /PRA/UTX_OOS_WO
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 2, 3
    └── OIU_TCODE → /PRA/UTX_OOS_WO
```

### 726. `/PRA/UTX_PAY_RECON`

```
T-Code: /PRA/UTX_PAY_RECON
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_PAY_RECON
```

### 727. `/PRA/UTX_PDCXRF`

```
T-Code: /PRA/UTX_PDCXRF
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_PDCXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_PDCXRF
```

### 728. `/PRA/UTX_PURXRF`

```
T-Code: /PRA/UTX_PURXRF
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_PURXRF
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_PURXRF
```

### 729. `/PRA/UTX_ROYTXN`

```
T-Code: /PRA/UTX_ROYTXN
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_ROYTXN
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_ROYTXN
```

### 730. `/PRA/UTX_RPDT_MIG`

```
T-Code: /PRA/UTX_RPDT_MIG
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/UTX_RPDT_MIG
```

### 731. `/PRA/UTX_RRC`

```
T-Code: /PRA/UTX_RRC
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_RRC
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_RRC
```

### 732. `/PRA/UTX_SOL`

```
T-Code: /PRA/UTX_SOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_SOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_SOL
```

### 733. `/PRA/UTX_SOLE`

```
T-Code: /PRA/UTX_SOLE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_SOLE
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_SOLE
```

### 734. `/PRA/UTX_ST_HIST_UPL`

```
T-Code: /PRA/UTX_ST_HIST_UPL
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/UTX_ST_HIST_UPL
```

### 735. `/PRA/UTX_ST_HIST_VER`

```
T-Code: /PRA/UTX_ST_HIST_VER
└── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_ST_HIST_VER
```

### 736. `/PRA/UTX_UNIT`

```
T-Code: /PRA/UTX_UNIT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_UNIT
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_UNIT
```

### 737. `/PRA/UTX_UPD_ROYHST`

```
T-Code: /PRA/UTX_UPD_ROYHST
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_UPD_ROYHST
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_RYHSTA
```

### 738. `/PRA/UTX_ZVOL`

```
T-Code: /PRA/UTX_ZVOL
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/UTX_ZVOL
└── S_SCD0_OBJ
    ├── ACTVT → (empty — maintain in PFCG)
    └── OBJECTCLAS → /PRA/UTX_ZVOL
```

### 739. `/PRA/VL_DA_LOG`

```
T-Code: /PRA/VL_DA_LOG
└── O_OIU_TXN
    ├── ACTVT → 3, 64, 71, A5, DL, F4, UL
    └── OIU_TCODE → /PRA/VL_DA_LOG
```

### 740. `/PRA/VL_SD_TEMPLATE`

```
T-Code: /PRA/VL_SD_TEMPLATE
├── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/VL_SD_TEMPLATE
└── S_GUI
    └── ACTVT → 61
```

### 741. `/PRA/VL_SD_WORKPLACE`

```
T-Code: /PRA/VL_SD_WORKPLACE
├── /PRA/PN
    ├── /PRA/PNACT → N
    └── /PRA/PNRCD → SD
├── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/VL_SD_WORKPLACE
└── S_GUI
    └── ACTVT → 61
```

### 742. `/PRA/VL_SD_WORKPL_M`

```
T-Code: /PRA/VL_SD_WORKPL_M
├── /PRA/PN
    ├── /PRA/PNACT → N
    └── /PRA/PNRCD → SD
├── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 37, 6
    └── OIU_TCODE → /PRA/VL_SD_WORKPLACE
└── S_GUI
    └── ACTVT → 61
```

### 743. `/PRA/WHNR`

```
T-Code: /PRA/WHNR
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/WHNR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 744. `/PRA/WHOCM`

```
T-Code: /PRA/WHOCM
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /PRA/WHOCM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 745. `/PRA/WH_BL_ENQ`

```
T-Code: /PRA/WH_BL_ENQ
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2
    └── ALG_SUBOBJ → STEP_LOG
└── S_GUI
    └── ACTVT → 60, 61
```

### 746. `/PRA/WH_EN_TYP_REP`

```
T-Code: /PRA/WH_EN_TYP_REP
└── O_OIU_TXN
    ├── ACTVT → 1
    └── OIU_TCODE → /PRA/WH_EN_TYP_REP
```

### 747. `/PRA/WH_MANUAL_DATA`

```
T-Code: /PRA/WH_MANUAL_DATA
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, DL, UL
    └── OIU_TCODE → /PRA/WH_MANUAL_DATA
```

### 748. `/PRA/WH_OWN_GRP`

```
T-Code: /PRA/WH_OWN_GRP
└── O_OIU_TXN
    ├── ACTVT → 1
    └── OIU_TCODE → /PRA/WH_OWN_GRP
```

### 749. `/PRA/WH_PROC_SEL`

```
T-Code: /PRA/WH_PROC_SEL
└── O_OIU_TXN
    ├── ACTVT → 1, 2, 3, 6, 7, UL
    └── OIU_TCODE → /PRA/WH_PROC_SEL
```

### 750. `/PRA/WH_RECON_RPT`

```
T-Code: /PRA/WH_RECON_RPT
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/WH_RECON_RPT
├── S_APPL_LOG
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ALG_OBJECT → /PRA/
    └── ALG_SUBOBJ → ACCT_DOC, CHK_CLR
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → /PRA/R_RECON_RPT_BG
```

### 751. `/PRA/WH_STR_WELL`

```
T-Code: /PRA/WH_STR_WELL
└── O_OIU_TXN
    ├── ACTVT → 1, 16
    └── OIU_TCODE → /PRA/WH_STR_WELL
```

### 752. `/PRA/WH_WORKPLACE`

```
T-Code: /PRA/WH_WORKPLACE
├── O_OIU_TXN
    ├── ACTVT → (empty — maintain in PFCG)
    └── OIU_TCODE → /PRA/WH_WORKPLACE
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → OIUE2
    └── ALG_SUBOBJ → STEP_LOG
├── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → (empty — maintain in PFCG)
```

### 753. `/PRA/WORKPLACE`

```
T-Code: /PRA/WORKPLACE
└── O_OIU_TXN
    ├── ACTVT → 1, 10, 16, 2, 3, 37, 6, 60, 61, 69, 71, A5, DL, UL
    └── OIU_TCODE → /PRA/WORKPLACE
```

## `/QOS/` — SAP Quality of Service
> T-codes: **4**


### 754. `/QOS/09000464`

```
T-Code: /QOS/09000464
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /QOS/V_QC, /QOS/V_QR
```

### 755. `/QOS/09000465`

```
T-Code: /QOS/09000465
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /QOS/V_ALGOD, /QOS/V_RDE, /QOS/V_RDEL
```

### 756. `/QOS/09000466`

```
T-Code: /QOS/09000466
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /QOS/V_ALGOO, /QOS/V_ROF
```

### 757. `/QOS/09000467`

```
T-Code: /QOS/09000467
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /QOS/V_CPROFILE, /QOS/V_CPROFILEA, /QOS/V_CPROFILEB
```

## `/RPM/` — SAP Resource & Portfolio Management
> T-codes: **21**


### 758. `/RPM/36000018`

```
T-Code: /RPM/36000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_DP_DP_MP, /RPM/V_DP_STA_MP, /RPM/V_ITEM_TYPE_RO, /RPM/V_ITM_ST_MP, /RPM/V_ITM_TY_MP
```

### 759. `/RPM/36000024`

```
T-Code: /RPM/36000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_DASHBOARD, /RPM/V_DASHB_P, /RPM/V_DASH_ROLE, /RPM/V_DB_COLHDR, /RPM/V_DB_R_MET, /RPM/V_PORTTYP
```

### 760. `/RPM/36000025`

```
T-Code: /RPM/36000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_DASH_PERS, /RPM/V_DB_R_PER
```

### 761. `/RPM/36000026`

```
T-Code: /RPM/36000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_DB_SCOPE
```

### 762. `/RPM/36000027`

```
T-Code: /RPM/36000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_LTXT_ITYP, /RPM/V_OBJ_LTXT
```

### 763. `/RPM/36000038`

```
T-Code: /RPM/36000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_ACT_GRP_G
```

### 764. `/RPM/36000039`

```
T-Code: /RPM/36000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_ACT_GRP_C
```

### 765. `/RPM/47000026`

```
T-Code: /RPM/47000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_SUBST_OBJ
```

### 766. `/RPM/47000027`

```
T-Code: /RPM/47000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_CAP_DISP, /RPM/V_FIN_DISP
```

### 767. `/RPM/47000029`

```
T-Code: /RPM/47000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_SCENTYPES
```

### 768. `/RPM/47000030`

```
T-Code: /RPM/47000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_CAP_CHART, /RPM/V_CAP_L_CD, /RPM/V_CAP_VW_MP, /RPM/V_FIN_CHART, /RPM/V_FIN_L_CD, /RPM/V_FIN_VW_MP, /RPM/V_PFICAP_SP, /RPM/V_PORTTYP_R, /RPM/V_PORT_CAPG, /RPM/V_PORT_C_UI, /RPM/V_PORT_FI_G, /RPM/V_PORT_F_UI
```

### 769. `/RPM/47000032`

```
T-Code: /RPM/47000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_SCENCLASS
```

### 770. `/RPM/51000012`

```
T-Code: /RPM/51000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_FI_HIERAR
```

### 771. `/RPM/51000065`

```
T-Code: /RPM/51000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/VCPM_AT_MAP, /RPM/VCPM_ST_MAP, /RPM/V_CPDP_MAP, /RPM/V_CPITPT, /RPM/V_CPIT_MAP2, /RPM/V_ITPP_MAP, /RPM/V_NADP_MAP, /RPM/V_PSITPT, /RPM/V_PTIT_MAP, /RPM/V_WBSDP_MAP
```

### 772. `/RPM/51000071`

```
T-Code: /RPM/51000071
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_ITCT_MAP, /RPM/V_ITPT_MAP, /RPM/V_TDPCP_MAP, /RPM/V_TDPNA, /RPM/V_TDPWBS
```

### 773. `/RPM/51000073`

```
T-Code: /RPM/51000073
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_TAB_FLD, /RPM/V_TAB_GROUP
```

### 774. `/RPM/51000074`

```
T-Code: /RPM/51000074
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_OBJ_FORMS
```

### 775. `/RPM/51000076`

```
T-Code: /RPM/51000076
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_CHANGEDOC
```

### 776. `/RPM/59000003`

```
T-Code: /RPM/59000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /RPM/V_DB_METCOL
```

### 777. `/RPM/CLNTCOPY`

```
T-Code: /RPM/CLNTCOPY
└── S_GUI
    └── ACTVT → 2, 4, 60, 61
```

### 778. `/RPM/CUST_COPY`

```
T-Code: /RPM/CUST_COPY
└── S_GUI
    └── ACTVT → 2, 4, 60, 61
```

## `/S4PPM/` — SAP S/4HANA Portfolio & Project Management
> T-codes: **6**


### 779. `/S4PPM/52000591`

```
T-Code: /S4PPM/52000591
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /S4PPM/V_STATAM
```

### 780. `/S4PPM/52000595`

```
T-Code: /S4PPM/52000595
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /S4PPM/V_TSK_TYP
```

### 781. `/S4PPM/52001928`

```
T-Code: /S4PPM/52001928
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /S4PPM/V_CHG_DOC
```

### 782. `/S4PPM/97000001`

```
T-Code: /S4PPM/97000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /S4PPM/VSUP_AUTH
```

### 783. `/S4PPM/HCHECK_PROJ7`

```
T-Code: /S4PPM/HCHECK_PROJ7
└── /S4PPM/PRJ
    ├── ACTVT → 3
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── KOSTL → (empty — maintain in PFCG)
    ├── PRCTR → $PRCTR
    ├── PRJ_CONF → (empty — maintain in PFCG)
    ├── PRJ_ORG → (empty — maintain in PFCG)
    └── PRJ_PRFL → (empty — maintain in PFCG)
```

### 784. `/S4PPM/HCHECK_PROJCT`

```
T-Code: /S4PPM/HCHECK_PROJCT
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /S4PPM/PROJECT
```

## `/SAM4U/` — SAP Asset Management for Utilities
> T-codes: **9**


### 785. `/SAM4U/BEX_CON1_CENTRAL`

```
T-Code: /SAM4U/BEX_CON1_CENTRAL
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BEX_CON1_CENTRAL
    └── RFC_TYPE → FUNC
└── S_USER_GRP
    ├── ACTVT → 3
    └── CLASS → (empty — maintain in PFCG)
```

### 786. `/SAM4U/BEX_DATASET_RUN`

```
T-Code: /SAM4U/BEX_DATASET_RUN
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BEX_DATASET_RUN
    └── RFC_TYPE → FUNC
```

### 787. `/SAM4U/BEX_READ_STAD_WRAPPER`

```
T-Code: /SAM4U/BEX_READ_STAD_WRAPPER
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BEX_READ_STAD_WRAPPER
    └── RFC_TYPE → FUNC
└── S_TOOLS_EX
    └── AUTH → S_TOOLS_EX_A
```

### 788. `/SAM4U/BTL_GET_DESTINATIONS`

```
T-Code: /SAM4U/BTL_GET_DESTINATIONS
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_GET_DESTINATIONS
    └── RFC_TYPE → FUNC
```

### 789. `/SAM4U/BTL_GET_SYSTEM`

```
T-Code: /SAM4U/BTL_GET_SYSTEM
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_GET_SYSTEM
    └── RFC_TYPE → FUNC
```

### 790. `/SAM4U/BTL_IMPORT_HANA_EE`

```
T-Code: /SAM4U/BTL_IMPORT_HANA_EE
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_IMPORT_HANA_EE
    └── RFC_TYPE → FUNC
```

### 791. `/SAM4U/BTL_IMPORT_SAM`

```
T-Code: /SAM4U/BTL_IMPORT_SAM
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_IMPORT_SAM
    └── RFC_TYPE → FUNC
```

### 792. `/SAM4U/BTL_IMPORT_STAR`

```
T-Code: /SAM4U/BTL_IMPORT_STAR
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_IMPORT_STAR
    └── RFC_TYPE → FUNC
```

### 793. `/SAM4U/BTL_RUN_WORKER`

```
T-Code: /SAM4U/BTL_RUN_WORKER
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAM4U/BTL_RUN_WORKER
    └── RFC_TYPE → FUNC
```

## `/SAPAPO/` — SAP Advanced Planning & Optimization (APO)
> T-codes: **526**


### 794. `/SAPAPO/09000093`

```
T-Code: /SAPAPO/09000093
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/EOGL_CHR
```

### 795. `/SAPAPO/09000143`

```
T-Code: /SAPAPO/09000143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CNT_GR
```

### 796. `/SAPAPO/09000144`

```
T-Code: /SAPAPO/09000144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 797. `/SAPAPO/09000692`

```
T-Code: /SAPAPO/09000692
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VTPAR
```

### 798. `/SAPAPO/12000018`

```
T-Code: /SAPAPO/12000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VOECSUB, /SAPAPO/VOECSUBH
```

### 799. `/SAPAPO/41000249`

```
T-Code: /SAPAPO/41000249
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/EOGL_ILC
```

### 800. `/SAPAPO/48000009`

```
T-Code: /SAPAPO/48000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPEPPRHD, /SAPAPO/VPEPPRPB, /SAPAPO/VPEPPRSL
```

### 801. `/SAPAPO/48000010`

```
T-Code: /SAPAPO/48000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPE_STG, /SAPAPO/VPE_STG2
```

### 802. `/SAPAPO/48000011`

```
T-Code: /SAPAPO/48000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPEPRCPR
```

### 803. `/SAPAPO/52000341`

```
T-Code: /SAPAPO/52000341
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_APPPRF
```

### 804. `/SAPAPO/52000342`

```
T-Code: /SAPAPO/52000342
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/APPUSERV
```

### 805. `/SAPAPO/52000343`

```
T-Code: /SAPAPO/52000343
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TPUSR
```

### 806. `/SAPAPO/52000618`

```
T-Code: /SAPAPO/52000618
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/COMB_ORD
```

### 807. `/SAPAPO/60000272`

```
T-Code: /SAPAPO/60000272
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_KSCHL
```

### 808. `/SAPAPO/72000069`

```
T-Code: /SAPAPO/72000069
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPESRVUI
```

### 809. `/SAPAPO/86000017`

```
T-Code: /SAPAPO/86000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CIFPUB1V
```

### 810. `/SAPAPO/AC03`

```
T-Code: /SAPAPO/AC03
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 0
```

### 811. `/SAPAPO/AC04`

```
T-Code: /SAPAPO/AC04
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 0
```

### 812. `/SAPAPO/AC05`

```
T-Code: /SAPAPO/AC05
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 0
```

### 813. `/SAPAPO/AC06`

```
T-Code: /SAPAPO/AC06
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 2
```

### 814. `/SAPAPO/AC08`

```
T-Code: /SAPAPO/AC08
└── C_APO_ATP
    ├── ACTVT → 1
    └── APO_ATP_AR → S
```

### 815. `/SAPAPO/AC42`

```
T-Code: /SAPAPO/AC42
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 5
```

### 816. `/SAPAPO/ACTVERCREATE`

```
T-Code: /SAPAPO/ACTVERCREATE
├── C_APO_MOD
    ├── ACTVT → 1
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 1
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 817. `/SAPAPO/ADVM`

```
T-Code: /SAPAPO/ADVM
├── C_APO_ADV
    ├── ACTVT → 16, 2
    ├── APO_PLBK → (empty — maintain in PFCG)
    └── APO_VIEW → (empty — maintain in PFCG)
└── S_APO_CUST
    └── ACTVT → 2
```

### 818. `/SAPAPO/AGT_HEURVAR`

```
T-Code: /SAPAPO/AGT_HEURVAR
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 819. `/SAPAPO/ALRT_CUST_PP`

```
T-Code: /SAPAPO/ALRT_CUST_PP
└── C_APO_AMON
    └── ACTVT → 2, 3
```

### 820. `/SAPAPO/AMOCFG`

```
T-Code: /SAPAPO/AMOCFG
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 821. `/SAPAPO/AMON0`

```
T-Code: /SAPAPO/AMON0
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 822. `/SAPAPO/AMON1`

```
T-Code: /SAPAPO/AMON1
├── C_APO_AMON
    └── ACTVT → 16, 3
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 0
```

### 823. `/SAPAPO/AMON1_OLD`

```
T-Code: /SAPAPO/AMON1_OLD
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 824. `/SAPAPO/AMON2`

```
T-Code: /SAPAPO/AMON2
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 825. `/SAPAPO/AMON3`

```
T-Code: /SAPAPO/AMON3
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 826. `/SAPAPO/AMONMSG`

```
T-Code: /SAPAPO/AMONMSG
└── C_APO_AMON
    └── ACTVT → 16
```

### 827. `/SAPAPO/AMONMSG_SEND`

```
T-Code: /SAPAPO/AMONMSG_SEND
├── C_APO_AMON
    └── ACTVT → 16
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → AM
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 828. `/SAPAPO/AMON_AT_DESC`

```
T-Code: /SAPAPO/AMON_AT_DESC
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 829. `/SAPAPO/AMON_REORG`

```
T-Code: /SAPAPO/AMON_REORG
└── C_APO_AMON
    └── ACTVT → 6
```

### 830. `/SAPAPO/AMON_SETTING`

```
T-Code: /SAPAPO/AMON_SETTING
└── C_APO_AMON
    └── ACTVT → 16, 2, 3
```

### 831. `/SAPAPO/AMON_SET_DC`

```
T-Code: /SAPAPO/AMON_SET_DC
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 832. `/SAPAPO/AMO_BATCH`

```
T-Code: /SAPAPO/AMO_BATCH
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 833. `/SAPAPO/AP`

```
T-Code: /SAPAPO/AP
├── S_ADMI_FCD
    └── S_ADMI_FCD → ' '
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_DEVELOP
    ├── ACTVT → L0
    ├── DEVCLASS → /SAPAPO/VMI_PO_NR
    ├── OBJNAME → /SAPAPO/SAPMMAPO
    ├── OBJTYPE → DIAL
    └── P_GROUP → X
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE, FINAL_VERS, RAW_VERS
    ├── DOKU_DEVCL → /SAPAPO/VMI_PO_NR
    └── DOKU_MODE → MAINTAIN, TRANSLATE
├── S_GUI
    └── ACTVT → 2, 4
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → DTRA, TASK, TRAN
```

### 834. `/SAPAPO/APNTYP_MAINT`

```
T-Code: /SAPAPO/APNTYP_MAINT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/APNTYPE
```

### 835. `/SAPAPO/ASB_SIMLN_CONTROL_TASK`

```
T-Code: /SAPAPO/ASB_SIMLN_CONTROL_TASK
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAPAPO/ASB_SIMLN_CONTROL
    └── RFC_TYPE → FUGR
```

### 836. `/SAPAPO/ATP2PPDS`

```
T-Code: /SAPAPO/ATP2PPDS
└── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 837. `/SAPAPO/ATPC03`

```
T-Code: /SAPAPO/ATPC03
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 838. `/SAPAPO/ATPCH01`

```
T-Code: /SAPAPO/ATPCH01
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 839. `/SAPAPO/ATPCLEAN02`

```
T-Code: /SAPAPO/ATPCLEAN02
└── C_APO_ATP
    ├── ACTVT → 6
    └── APO_ATP_AR → 2
```

### 840. `/SAPAPO/ATPLOG`

```
T-Code: /SAPAPO/ATPLOG
└── C_APO_ATP
    ├── ACTVT → 2
    └── APO_ATP_AR → C
```

### 841. `/SAPAPO/ATPLOG_DEL`

```
T-Code: /SAPAPO/ATPLOG_DEL
└── C_APO_ATP
    ├── ACTVT → 6
    └── APO_ATP_AR → C
```

### 842. `/SAPAPO/ATYPES_SDP`

```
T-Code: /SAPAPO/ATYPES_SDP
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 843. `/SAPAPO/A_ASS_SP`

```
T-Code: /SAPAPO/A_ASS_SP
├── S_PROJECT
    ├── ACTVT → 2, 23, 3, 6, 71, 76, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 844. `/SAPAPO/A_ASS_SP_BAD`

```
T-Code: /SAPAPO/A_ASS_SP_BAD
└── S_DEVELOP
    ├── ACTVT → 1, 16, 2, 3, 40, 41, 42, 6, 7, 70, 90, 94, L0, MA
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 845. `/SAPAPO/BLRG01`

```
T-Code: /SAPAPO/BLRG01
└── C_APO_PPL
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
```

### 846. `/SAPAPO/BOD001`

```
T-Code: /SAPAPO/BOD001
└── C_APO_RELH
    └── ACTVT → 1, 2, 3, 6
```

### 847. `/SAPAPO/BOP`

```
T-Code: /SAPAPO/BOP
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 848. `/SAPAPO/BOPI`

```
T-Code: /SAPAPO/BOPI
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 849. `/SAPAPO/BOPIN`

```
T-Code: /SAPAPO/BOPIN
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 850. `/SAPAPO/BOPI_LOG`

```
T-Code: /SAPAPO/BOPI_LOG
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 851. `/SAPAPO/BOPI_LOG_03`

```
T-Code: /SAPAPO/BOPI_LOG_03
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → B
```

### 852. `/SAPAPO/BOP_COMPARE`

```
T-Code: /SAPAPO/BOP_COMPARE
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 853. `/SAPAPO/BOP_DELETE`

```
T-Code: /SAPAPO/BOP_DELETE
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 854. `/SAPAPO/BOP_MONITOR`

```
T-Code: /SAPAPO/BOP_MONITOR
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 855. `/SAPAPO/BOP_RESULT`

```
T-Code: /SAPAPO/BOP_RESULT
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 856. `/SAPAPO/BOP_RUNTIME`

```
T-Code: /SAPAPO/BOP_RUNTIME
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 857. `/SAPAPO/BOP_UPDATE`

```
T-Code: /SAPAPO/BOP_UPDATE
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 858. `/SAPAPO/BOP_WORKLIST`

```
T-Code: /SAPAPO/BOP_WORKLIST
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → E
```

### 859. `/SAPAPO/BP3`

```
T-Code: /SAPAPO/BP3
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → 10001, 10002, 10003, 10004, 10009, 10014, 10015, 10017, 11201, 11411, 11412, 11413, 11414, 11415
    └── RFC_TYPE → FUGR
```

### 860. `/SAPAPO/C1`

```
T-Code: /SAPAPO/C1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 861. `/SAPAPO/C2`

```
T-Code: /SAPAPO/C2
├── B_ALE_LSYS
    └── LOGSYS → ' '
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 862. `/SAPAPO/C4`

```
T-Code: /SAPAPO/C4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CIFGPAR1
```

### 863. `/SAPAPO/C41`

```
T-Code: /SAPAPO/C41
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CIFGPARV
```

### 864. `/SAPAPO/C5`

```
T-Code: /SAPAPO/C5
└── S_TABU_DIS
    ├── ACTVT → 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 865. `/SAPAPO/CALENDAR`

```
T-Code: /SAPAPO/CALENDAR
└── C_APO_CAL
    └── ACTVT → 1, 2, 3, 6
```

### 866. `/SAPAPO/CAPREV_REL`

```
T-Code: /SAPAPO/CAPREV_REL
└── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
```

### 867. `/SAPAPO/CAPRIO`

```
T-Code: /SAPAPO/CAPRIO
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 868. `/SAPAPO/CBCLPCONF`

```
T-Code: /SAPAPO/CBCLPCONF
├── S_CTS_ADMI
    └── CTS_ADMFCT → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 869. `/SAPAPO/CBMM`

```
T-Code: /SAPAPO/CBMM
├── C_KLAH_BKL
    ├── ACTVT → (empty — maintain in PFCG)
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → (empty — maintain in PFCG)
    └── BGRKP → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → (empty — maintain in PFCG)
├── C_TCLS_BER
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── S_ALV_LAYO
    └── ACTVT → (empty — maintain in PFCG)
```

### 870. `/SAPAPO/CC_PERF_SAVE`

```
T-Code: /SAPAPO/CC_PERF_SAVE
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 871. `/SAPAPO/CDORDGDL`

```
T-Code: /SAPAPO/CDORDGDL
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_SCD0
    └── ACTVT → 8
```

### 872. `/SAPAPO/CDPRTCAT`

```
T-Code: /SAPAPO/CDPRTCAT
└── S_SCD0
    └── ACTVT → 12, 8
```

### 873. `/SAPAPO/CDPRTLOC`

```
T-Code: /SAPAPO/CDPRTLOC
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_SCD0
    └── ACTVT → 8
```

### 874. `/SAPAPO/CDPRTPROD`

```
T-Code: /SAPAPO/CDPRTPROD
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_SCD0
    └── ACTVT → 8
```

### 875. `/SAPAPO/CDPS0`

```
T-Code: /SAPAPO/CDPS0
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 876. `/SAPAPO/CDPS0_HTML`

```
T-Code: /SAPAPO/CDPS0_HTML
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 877. `/SAPAPO/CDPS1`

```
T-Code: /SAPAPO/CDPS1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 878. `/SAPAPO/CDPS2`

```
T-Code: /SAPAPO/CDPS2
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 879. `/SAPAPO/CDPS3`

```
T-Code: /SAPAPO/CDPS3
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 880. `/SAPAPO/CDPSB0`

```
T-Code: /SAPAPO/CDPSB0
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 881. `/SAPAPO/CDPSB1`

```
T-Code: /SAPAPO/CDPSB1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 882. `/SAPAPO/CDPSB2`

```
T-Code: /SAPAPO/CDPSB2
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → APO, SF_LOG_OBJ
    └── ALG_SUBOBJ → PPDS, SF_LOG_SUB
```

### 883. `/SAPAPO/CDPSB3`

```
T-Code: /SAPAPO/CDPSB3
└── S_APPL_LOG
    ├── ACTVT → 6
    ├── ALG_OBJECT → APO, SF_LOG_OBJ
    └── ALG_SUBOBJ → PPDS, SF_LOG_SUB
```

### 884. `/SAPAPO/CDPSC0`

```
T-Code: /SAPAPO/CDPSC0
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_OVRPRF
```

### 885. `/SAPAPO/CDPSC1`

```
T-Code: /SAPAPO/CDPSC1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 886. `/SAPAPO/CDPSC10`

```
T-Code: /SAPAPO/CDPSC10
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 887. `/SAPAPO/CDPSC11`

```
T-Code: /SAPAPO/CDPSC11
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/DISCOUNT, /SAPAPO/SNP14, /SAPAPO/V_BOPST, /SAPAPO/V_BOPSTD, /SAPAPO/V_SECTPR
```

### 888. `/SAPAPO/CDPSC12`

```
T-Code: /SAPAPO/CDPSC12
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 889. `/SAPAPO/CDPSC13`

```
T-Code: /SAPAPO/CDPSC13
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 890. `/SAPAPO/CDPSC14`

```
T-Code: /SAPAPO/CDPSC14
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VMLTBPRF
```

### 891. `/SAPAPO/CDPSC2`

```
T-Code: /SAPAPO/CDPSC2
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_CHART, /SAPAPO/V_CHRDEC, /SAPAPO/V_CTX, /SAPAPO/V_GPHDEC, /SAPAPO/V_GPHOBJ, /SAPAPO/V_GPHREP, /SAPAPO/V_PLTPRF, /SAPAPO/V_PLTTLB
```

### 892. `/SAPAPO/CDPSC3`

```
T-Code: /SAPAPO/CDPSC3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_MAT, /SAPAPO/V_ORDER, /SAPAPO/V_RES, /SAPAPO/V_WRKFLD
```

### 893. `/SAPAPO/CDPSC4`

```
T-Code: /SAPAPO/CDPSC4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TIPRF
```

### 894. `/SAPAPO/CDPSC5`

```
T-Code: /SAPAPO/CDPSC5
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_OPT03, /SAPAPO/V_OPT2, /SAPAPO/V_OPTIPR
```

### 895. `/SAPAPO/CDPSC6`

```
T-Code: /SAPAPO/CDPSC6
└── C_APO_SETG
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_SETGRP → (empty — maintain in PFCG)
```

### 896. `/SAPAPO/CDPSC7`

```
T-Code: /SAPAPO/CDPSC7
└── C_APO_SETM
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_SETMAT → (empty — maintain in PFCG)
```

### 897. `/SAPAPO/CDPSC8`

```
T-Code: /SAPAPO/CDPSC8
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_PROPRF
```

### 898. `/SAPAPO/CDPSC9`

```
T-Code: /SAPAPO/CDPSC9
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 899. `/SAPAPO/CDPSS0`

```
T-Code: /SAPAPO/CDPSS0
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 900. `/SAPAPO/CDPSS0_HTML`

```
T-Code: /SAPAPO/CDPSS0_HTML
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 901. `/SAPAPO/CDPS_CHARMAT`

```
T-Code: /SAPAPO/CDPS_CHARMAT
└── C_APO_CHRM
    ├── ACTVT → 2, 3
    └── APO_CHRMAT → (empty — maintain in PFCG)
```

### 902. `/SAPAPO/CDPS_GRPRULE`

```
T-Code: /SAPAPO/CDPS_GRPRULE
└── C_APO_GRPR
    ├── ACTVT → 2, 3
    ├── APO_GRPRUL → (empty — maintain in PFCG)
    └── APO_LOC → (empty — maintain in PFCG)
```

### 903. `/SAPAPO/CDPS_MATGEN`

```
T-Code: /SAPAPO/CDPS_MATGEN
└── C_APO_MATR
    ├── ACTVT → 64
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_MATRUL → (empty — maintain in PFCG)
```

### 904. `/SAPAPO/CDPS_MATRULE`

```
T-Code: /SAPAPO/CDPS_MATRULE
└── C_APO_MATR
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_MATRUL → (empty — maintain in PFCG)
```

### 905. `/SAPAPO/CDPS_NOTMAT`

```
T-Code: /SAPAPO/CDPS_NOTMAT
└── C_APO_MATR
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_MATRUL → (empty — maintain in PFCG)
```

### 906. `/SAPAPO/CDPS_REPT`

```
T-Code: /SAPAPO/CDPS_REPT
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 907. `/SAPAPO/CDPS_SETUP`

```
T-Code: /SAPAPO/CDPS_SETUP
└── C_APO_SETM
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_SETMAT → (empty — maintain in PFCG)
```

### 908. `/SAPAPO/CDPS_SPRFL`

```
T-Code: /SAPAPO/CDPS_SPRFL
└── C_APO_SPRF
    └── ACTVT → 1, 2, 3, 6
```

### 909. `/SAPAPO/CFMINCR`

```
T-Code: /SAPAPO/CFMINCR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CIFCUSIN
```

### 910. `/SAPAPO/CL02CDP`

```
T-Code: /SAPAPO/CL02CDP
└── C_KLAH_BKP
    ├── ACTVT → 2
    └── BGRKP → (empty — maintain in PFCG)
```

### 911. `/SAPAPO/CLP01`

```
T-Code: /SAPAPO/CLP01
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 912. `/SAPAPO/CLP02`

```
T-Code: /SAPAPO/CLP02
└── S_TABU_DIS
    ├── ACTVT → 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 913. `/SAPAPO/CLPRPDEL`

```
T-Code: /SAPAPO/CLPRPDEL
├── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 914. `/SAPAPO/CLPSDP_USER`

```
T-Code: /SAPAPO/CLPSDP_USER
├── C_APO_CLPU
    ├── ACTVT → 1, 2, 3
    └── USER → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → &NC&
```

### 915. `/SAPAPO/CLP_SETTINGS`

```
T-Code: /SAPAPO/CLP_SETTINGS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 916. `/SAPAPO/CMDS_CHKFEA`

```
T-Code: /SAPAPO/CMDS_CHKFEA
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
```

### 917. `/SAPAPO/CMDS_CUST1`

```
T-Code: /SAPAPO/CMDS_CUST1
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 918. `/SAPAPO/CMDS_CUST11`

```
T-Code: /SAPAPO/CMDS_CUST11
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 919. `/SAPAPO/CMDS_CUST12`

```
T-Code: /SAPAPO/CMDS_CUST12
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 920. `/SAPAPO/CMDS_CUST13`

```
T-Code: /SAPAPO/CMDS_CUST13
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 921. `/SAPAPO/CMDS_CUST2`

```
T-Code: /SAPAPO/CMDS_CUST2
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 922. `/SAPAPO/CMDS_CUST6`

```
T-Code: /SAPAPO/CMDS_CUST6
├── S_PRO_AUTH
    ├── ACTVT → 2, 3
    └── PROJAUTH → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 923. `/SAPAPO/CMDS_CUST7`

```
T-Code: /SAPAPO/CMDS_CUST7
├── S_DOKU_AUT
    ├── DOKU_ACT → FINAL_VERS
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_TRANSLAT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── TLANGUAGE → (empty — maintain in PFCG)
    └── TRANOBJ → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 924. `/SAPAPO/CMDS_CUST8`

```
T-Code: /SAPAPO/CMDS_CUST8
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 925. `/SAPAPO/CMDS_DEFCNF`

```
T-Code: /SAPAPO/CMDS_DEFCNF
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 926. `/SAPAPO/CMDS_DEFCNTR`

```
T-Code: /SAPAPO/CMDS_DEFCNTR
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 927. `/SAPAPO/CMDS_DEL`

```
T-Code: /SAPAPO/CMDS_DEL
└── C_APO_SSA
    ├── ACTVT → 6
    ├── APO_CDSOBJ → S_HISTORY
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 928. `/SAPAPO/CMDS_SCO1`

```
T-Code: /SAPAPO/CMDS_SCO1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_KENN → A
    └── APO_MOD → 0
├── C_APO_PROD
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_SSA
    ├── ACTVT → 2, 3, 6
    ├── APO_CDSOBJ → S_DELSCHED, S_HISTORY, S_SAGR
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 929. `/SAPAPO/CMDS_SCO2`

```
T-Code: /SAPAPO/CMDS_SCO2
├── C_APO_MOD
    ├── ACTVT → 6
    ├── APO_KENN → A
    └── APO_MOD → 0
└── C_APO_SSA
    ├── ACTVT → 6
    ├── APO_CDSOBJ → S_SAGR
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 930. `/SAPAPO/CMDS_SCO4`

```
T-Code: /SAPAPO/CMDS_SCO4
├── C_APO_MOD
    ├── ACTVT → 6
    ├── APO_KENN → A
    └── APO_MOD → 0
└── C_APO_SSA
    ├── ACTVT → 6
    ├── APO_CDSOBJ → S_SAGR
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 931. `/SAPAPO/CMDS_STPD`

```
T-Code: /SAPAPO/CMDS_STPD
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 932. `/SAPAPO/CMDS_STPSR`

```
T-Code: /SAPAPO/CMDS_STPSR
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 933. `/SAPAPO/COMP_VARIANT`

```
T-Code: /SAPAPO/COMP_VARIANT
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 934. `/SAPAPO/CONFR_CFGREL`

```
T-Code: /SAPAPO/CONFR_CFGREL
└── C_TCLA_BKA
    └── KLART → 400
```

### 935. `/SAPAPO/CONSCHK`

```
T-Code: /SAPAPO/CONSCHK
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → VM
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 936. `/SAPAPO/CONSPRF`

```
T-Code: /SAPAPO/CONSPRF
└── C_APO_CPRF
    ├── ACTVT → 1, 2, 3, 32, 6
    └── APO_CONSPR → (empty — maintain in PFCG)
```

### 937. `/SAPAPO/COPT00`

```
T-Code: /SAPAPO/COPT00
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCCF_COMM_ENGINE
    └── RFC_TYPE → FUGR
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 938. `/SAPAPO/COPT00_OLD`

```
T-Code: /SAPAPO/COPT00_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 939. `/SAPAPO/COPT01`

```
T-Code: /SAPAPO/COPT01
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCCF_COMM_ENGINE
    └── RFC_TYPE → FUGR
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 940. `/SAPAPO/COPT01_OLD`

```
T-Code: /SAPAPO/COPT01_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 941. `/SAPAPO/COPT10`

```
T-Code: /SAPAPO/COPT10
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 942. `/SAPAPO/COPT10_OLD`

```
T-Code: /SAPAPO/COPT10_OLD
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 943. `/SAPAPO/COPT11`

```
T-Code: /SAPAPO/COPT11
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_ENGINE_START
    └── RFC_TYPE → FUNC
```

### 944. `/SAPAPO/COPY_RES_HU`

```
T-Code: /SAPAPO/COPY_RES_HU
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
└── C_APO_RES
    ├── ACTVT → 1, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 945. `/SAPAPO/CP1`

```
T-Code: /SAPAPO/CP1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 946. `/SAPAPO/CP3`

```
T-Code: /SAPAPO/CP3
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 947. `/SAPAPO/CPP`

```
T-Code: /SAPAPO/CPP
└── C_APO_CFPP
    ├── ACTVT → 2, 3, 6
    └── LOGSYS → (empty — maintain in PFCG)
```

### 948. `/SAPAPO/CPP1`

```
T-Code: /SAPAPO/CPP1
└── C_APO_CFPP
    ├── ACTVT → 2, 3, 6
    └── LOGSYS → (empty — maintain in PFCG)
```

### 949. `/SAPAPO/CPP2`

```
T-Code: /SAPAPO/CPP2
└── C_APO_CFPP
    ├── ACTVT → 3
    └── LOGSYS → (empty — maintain in PFCG)
```

### 950. `/SAPAPO/CPPA`

```
T-Code: /SAPAPO/CPPA
└── C_APO_CFPP
    ├── ACTVT → 3
    └── LOGSYS → (empty — maintain in PFCG)
```

### 951. `/SAPAPO/CPPR`

```
T-Code: /SAPAPO/CPPR
└── C_APO_CFPP
    ├── ACTVT → 2, 3, 6
    └── LOGSYS → (empty — maintain in PFCG)
```

### 952. `/SAPAPO/CQ`

```
T-Code: /SAPAPO/CQ
└── C_APO_CFQD
    ├── ACTVT → 3
    ├── APO_CFQNAM → ' '
    └── LOGSYS → ' '
```

### 953. `/SAPAPO/CT`

```
T-Code: /SAPAPO/CT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CMPRF, /SAPAPO/CPMTR, /SAPAPO/CTYPE, /SAPAPO/VCTATT, /SAPAPO/VCTCAP, /SAPAPO/VCTSTEP
```

### 954. `/SAPAPO/CT04CDP`

```
T-Code: /SAPAPO/CT04CDP
└── C_CABN
    └── ACTVT → 1, 2, 3
```

### 955. `/SAPAPO/CTM`

```
T-Code: /SAPAPO/CTM
└── C_APO_CTM
    ├── ACTVT → 1, 16, 2, 21, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 956. `/SAPAPO/CTM01`

```
T-Code: /SAPAPO/CTM01
└── C_APO_CTM
    ├── ACTVT → 1, 16, 2, 21, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 957. `/SAPAPO/CTM10`

```
T-Code: /SAPAPO/CTM10
└── C_APO_CTM
    ├── ACTVT → 16
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 958. `/SAPAPO/CTMB`

```
T-Code: /SAPAPO/CTMB
└── C_APO_CTM
    ├── ACTVT → 16
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 959. `/SAPAPO/CTMCUST`

```
T-Code: /SAPAPO/CTMCUST
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 960. `/SAPAPO/CTMEXPL`

```
T-Code: /SAPAPO/CTMEXPL
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 961. `/SAPAPO/CTMMSEL`

```
T-Code: /SAPAPO/CTMMSEL
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 962. `/SAPAPO/CTMORDSEL`

```
T-Code: /SAPAPO/CTMORDSEL
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 963. `/SAPAPO/CTMPARCOMP`

```
T-Code: /SAPAPO/CTMPARCOMP
└── C_APO_CTM
    ├── ACTVT → 3
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 964. `/SAPAPO/CTMSCPR`

```
T-Code: /SAPAPO/CTMSCPR
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 965. `/SAPAPO/CTMTP`

```
T-Code: /SAPAPO/CTMTP
└── C_APO_CTM
    ├── ACTVT → 21
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 966. `/SAPAPO/CTMVIEWER`

```
T-Code: /SAPAPO/CTMVIEWER
└── C_APO_CTM
    ├── ACTVT → 1, 2, 3, 6
    └── APO_CTMPRF → (empty — maintain in PFCG)
```

### 967. `/SAPAPO/CTM_TPP_COMP`

```
T-Code: /SAPAPO/CTM_TPP_COMP
└── C_APO_VERS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 968. `/SAPAPO/CUCDP_REFMAP`

```
T-Code: /SAPAPO/CUCDP_REFMAP
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 969. `/SAPAPO/CULL_RTO_DEL`

```
T-Code: /SAPAPO/CULL_RTO_DEL
└── C_APO_RTO
    ├── ACTVT → 6
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
```

### 970. `/SAPAPO/CURTO_EDIT`

```
T-Code: /SAPAPO/CURTO_EDIT
└── C_APO_RTO
    ├── ACTVT → 2, 3, 48
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
```

### 971. `/SAPAPO/CURTO_SIMU`

```
T-Code: /SAPAPO/CURTO_SIMU
└── C_APO_RTO
    ├── ACTVT → 3, 48
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
```

### 972. `/SAPAPO/CUSLNTRTO_CREATE_RTO_1`

```
T-Code: /SAPAPO/CUSLNTRTO_CREATE_RTO_1
├── C_APO_PSD
    └── ACTVT → 3
├── C_APO_RTO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
├── C_APO_SPRF
    └── ACTVT → 3
├── M_MATE_MAN
    └── ACTVT → 3
├── M_MATE_MAR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_STA
    ├── ACTVT → 3
    └── STATM → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 973. `/SAPAPO/CUTABLEINFO`

```
T-Code: /SAPAPO/CUTABLEINFO
└── S_TCODE
    └── TCD → /SAPAPO/CULINKINFO
```

### 974. `/SAPAPO/DBOR_REORG`

```
T-Code: /SAPAPO/DBOR_REORG
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 975. `/SAPAPO/DEPL_HAZ`

```
T-Code: /SAPAPO/DEPL_HAZ
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 976. `/SAPAPO/DISPLAY_CONF`

```
T-Code: /SAPAPO/DISPLAY_CONF
└── C_APO_SSA
    ├── ACTVT → 3
    ├── APO_CDSOBJ → S_HISTORY
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 977. `/SAPAPO/DMP1`

```
T-Code: /SAPAPO/DMP1
└── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
```

### 978. `/SAPAPO/DM_PEGCHECK`

```
T-Code: /SAPAPO/DM_PEGCHECK
└── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
```

### 979. `/SAPAPO/DPC3`

```
T-Code: /SAPAPO/DPC3
└── C_APO_PROD
    ├── ACTVT → 16
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 980. `/SAPAPO/DPL_OPTPR_CU`

```
T-Code: /SAPAPO/DPL_OPTPR_CU
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 981. `/SAPAPO/DRPAPPRSET`

```
T-Code: /SAPAPO/DRPAPPRSET
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 982. `/SAPAPO/DRPC_EP`

```
T-Code: /SAPAPO/DRPC_EP
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE, FINAL_VERS, RAW_VERS
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 983. `/SAPAPO/DRPEOALIM`

```
T-Code: /SAPAPO/DRPEOALIM
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE, FINAL_VERS, RAW_VERS
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 984. `/SAPAPO/DRP_APPRRLS`

```
T-Code: /SAPAPO/DRP_APPRRLS
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 985. `/SAPAPO/ECV`

```
T-Code: /SAPAPO/ECV
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_RTO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
├── C_CLAS_UTI
    └── ACTVT → 21
└── C_KLAH_BKP
    ├── ACTVT → 3, F4
    └── BGRKP → (empty — maintain in PFCG)
```

### 986. `/SAPAPO/EDQA`

```
T-Code: /SAPAPO/EDQA
└── C_APO_ATP
    ├── ACTVT → 2, 3
    └── APO_ATP_AR → Q
```

### 987. `/SAPAPO/EDQA_DEL`

```
T-Code: /SAPAPO/EDQA_DEL
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3
    └── APO_ATP_AR → (empty — maintain in PFCG)
```

### 988. `/SAPAPO/FATT`

```
T-Code: /SAPAPO/FATT
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → &NC&
```

### 989. `/SAPAPO/FCSTOUTL`

```
T-Code: /SAPAPO/FCSTOUTL
└── C_APO_FUN
    ├── APO_FUNC → C_FCSTSETT
    └── APO_PAREA → (empty — maintain in PFCG)
```

### 990. `/SAPAPO/FLXC_DEFINITION`

```
T-Code: /SAPAPO/FLXC_DEFINITION
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 991. `/SAPAPO/GCLEVL_MAINT`

```
T-Code: /SAPAPO/GCLEVL_MAINT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VLOC_GL
```

### 992. `/SAPAPO/GEN_CHK_PDS`

```
T-Code: /SAPAPO/GEN_CHK_PDS
├── C_APO_MALO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_RTO
    ├── ACTVT → 1
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
```

### 993. `/SAPAPO/GEN_PDS`

```
T-Code: /SAPAPO/GEN_PDS
├── C_APO_MALO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_RTO
    ├── ACTVT → 1
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 994. `/SAPAPO/GRPVAL_MAINT`

```
T-Code: /SAPAPO/GRPVAL_MAINT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/MATGPVAL
```

### 995. `/SAPAPO/HEUR1`

```
T-Code: /SAPAPO/HEUR1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 996. `/SAPAPO/HEUR2`

```
T-Code: /SAPAPO/HEUR2
├── C_APO_PROD
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 997. `/SAPAPO/INBND_STATUS`

```
T-Code: /SAPAPO/INBND_STATUS
└── S_DEVELOP
    ├── ACTVT → 3
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 998. `/SAPAPO/IPM01`

```
T-Code: /SAPAPO/IPM01
└── C_APO_DPPR
    ├── ACTVT → 2, 3
    └── APO_PROD → (empty — maintain in PFCG)
```

### 999. `/SAPAPO/KPIPRF_ASGN`

```
T-Code: /SAPAPO/KPIPRF_ASGN
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VKPF_RAP, /SAPAPO/VKPF_UAP
```

### 1000. `/SAPAPO/LOC3`

```
T-Code: /SAPAPO/LOC3
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6, F4
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1001. `/SAPAPO/LOCALI`

```
T-Code: /SAPAPO/LOCALI
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1002. `/SAPAPO/LOC_CD_READ`

```
T-Code: /SAPAPO/LOC_CD_READ
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1003. `/SAPAPO/LOC_DEL_GC`

```
T-Code: /SAPAPO/LOC_DEL_GC
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1004. `/SAPAPO/LOC_DEL_GCB`

```
T-Code: /SAPAPO/LOC_DEL_GCB
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1005. `/SAPAPO/LO_CDC_LOC`

```
T-Code: /SAPAPO/LO_CDC_LOC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_LOC
```

### 1006. `/SAPAPO/LTTYPE`

```
T-Code: /SAPAPO/LTTYPE
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6, F4
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1007. `/SAPAPO/LTTYPE_C`

```
T-Code: /SAPAPO/LTTYPE_C
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6, F4
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1008. `/SAPAPO/LTTYPE_D`

```
T-Code: /SAPAPO/LTTYPE_D
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6, F4
    └── APO_LOC → DUMMY
```

### 1009. `/SAPAPO/MAABCD`

```
T-Code: /SAPAPO/MAABCD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/MAABCD
```

### 1010. `/SAPAPO/MASSGEOCODEB`

```
T-Code: /SAPAPO/MASSGEOCODEB
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1011. `/SAPAPO/MAT1`

```
T-Code: /SAPAPO/MAT1
└── C_APO_PROD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1012. `/SAPAPO/MAT2`

```
T-Code: /SAPAPO/MAT2
└── C_APO_PROD
    ├── ACTVT → 2
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1013. `/SAPAPO/MAT3`

```
T-Code: /SAPAPO/MAT3
└── C_APO_PROD
    ├── ACTVT → 2
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1014. `/SAPAPO/MATLREF1`

```
T-Code: /SAPAPO/MATLREF1
├── C_APO_LOC
    ├── ACTVT → 2, F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 2
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → '*'
└── C_APO_VERS
    ├── ACTVT → 2
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1015. `/SAPAPO/MAT_CDC_PROD`

```
T-Code: /SAPAPO/MAT_CDC_PROD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_PPRFL, /SAPAPO/CD_PRDHD, /SAPAPO/CD_PRDLC
```

### 1016. `/SAPAPO/MAT_CI_GEN`

```
T-Code: /SAPAPO/MAT_CI_GEN
└── S_CTS_ADMI
    └── CTS_ADMFCT → EPS1, EPS2
```

### 1017. `/SAPAPO/MC01_BBD`

```
T-Code: /SAPAPO/MC01_BBD
└── C_TCLA_BKA
    └── KLART → 400
```

### 1018. `/SAPAPO/MC01_CLCH_RM`

```
T-Code: /SAPAPO/MC01_CLCH_RM
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
└── C_TCLA_BKA
    └── KLART → 400
```

### 1019. `/SAPAPO/MNTPROF01`

```
T-Code: /SAPAPO/MNTPROF01
└── C_APO_DPRF
    └── ACTVT → 23
```

### 1020. `/SAPAPO/MOIGRC`

```
T-Code: /SAPAPO/MOIGRC
├── S_DEVELOP
    ├── ACTVT → 1, 16, 2, 3, 40, 41, 42, 6, 7, 70, 90, 94, L0, MA
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1021. `/SAPAPO/MRPACUST01`

```
T-Code: /SAPAPO/MRPACUST01
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1022. `/SAPAPO/MSGTSTP_TR`

```
T-Code: /SAPAPO/MSGTSTP_TR
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1023. `/SAPAPO/MTI`

```
T-Code: /SAPAPO/MTI
└── C_APO_EXPR
    ├── ACTVT → 1, 2, 3
    ├── APO_EXPOBJ → (empty — maintain in PFCG)
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1024. `/SAPAPO/MTI2`

```
T-Code: /SAPAPO/MTI2
└── C_APO_EXPR
    ├── ACTVT → 3
    ├── APO_EXPOBJ → (empty — maintain in PFCG)
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1025. `/SAPAPO/MTRCOMB`

```
T-Code: /SAPAPO/MTRCOMB
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VCOMBATT, /SAPAPO/VCOMBCAP, /SAPAPO/VMTRCOMB
```

### 1026. `/SAPAPO/MVM`

```
T-Code: /SAPAPO/MVM
├── C_APO_MOD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1027. `/SAPAPO/MVSEL`

```
T-Code: /SAPAPO/MVSEL
├── C_APO_MALO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_PPL
    ├── ACTVT → 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
```

### 1028. `/SAPAPO/ND`

```
T-Code: /SAPAPO/ND
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → ND
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1029. `/SAPAPO/ODL`

```
T-Code: /SAPAPO/ODL
└── C_APO_ATP
    ├── ACTVT → 2, 3
    └── APO_ATP_AR → O
```

### 1030. `/SAPAPO/ODL_LOG`

```
T-Code: /SAPAPO/ODL_LOG
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → O
```

### 1031. `/SAPAPO/OM00`

```
T-Code: /SAPAPO/OM00
├── C_APO_LCAD
    ├── ACTVT → 16, 2, 3
    └── APO_LCTOOL → (empty — maintain in PFCG)
└── S_RZL_ADM
    └── ACTVT → 1, 3
```

### 1032. `/SAPAPO/OM01`

```
T-Code: /SAPAPO/OM01
├── S_ADMI_FCD
    └── S_ADMI_FCD → LC01
├── S_C_FUNCT
    ├── ACTVT → 16
    ├── CFUNCNAME → SYSTEM
    └── PROGRAM → /SAPAPO/OM_TRACEFILE_READ
├── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → *.lcapo_com_trace_*.*
    └── PROGRAM → /SAPAPO/OM_TRACEFILE_READ
├── S_GUI
    └── ACTVT → 61
├── S_LOG_COM
    ├── COMMAND → DBMGETF
    ├── HOST → *
    └── OPSYSTEM → ANYOS
├── S_RZL_ADM
    └── ACTVT → 1, 3
└── S_TRANSLAT
    ├── ACTVT → 2
    ├── TLANGUAGE → *
    └── TRANOBJ → LONG
```

### 1033. `/SAPAPO/OM02`

```
T-Code: /SAPAPO/OM02
├── S_ADMI_FCD
    └── S_ADMI_FCD → LC01
└── S_BTCH_ADM
    └── BTCADMIN → Y
```

### 1034. `/SAPAPO/OM03`

```
T-Code: /SAPAPO/OM03
└── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1035. `/SAPAPO/OM03A`

```
T-Code: /SAPAPO/OM03A
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → ''
└── S_TCODE
    └── TCD → /SAPAPO/OM03
```

### 1036. `/SAPAPO/OM04`

```
T-Code: /SAPAPO/OM04
└── S_GUI
    └── ACTVT → 61
```

### 1037. `/SAPAPO/OM13`

```
T-Code: /SAPAPO/OM13
├── S_ADMI_FCD
    └── S_ADMI_FCD → LC01
└── S_RZL_ADM
    └── ACTVT → 1, 3
```

### 1038. `/SAPAPO/OM14`

```
T-Code: /SAPAPO/OM14
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → ''
└── S_SPO_DEV
    └── SPODEVICE → (empty — maintain in PFCG)
```

### 1039. `/SAPAPO/OM15`

```
T-Code: /SAPAPO/OM15
└── S_GUI
    └── ACTVT → 61
```

### 1040. `/SAPAPO/OM16`

```
T-Code: /SAPAPO/OM16
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1041. `/SAPAPO/OM17`

```
T-Code: /SAPAPO/OM17
├── S_ADMI_FCD
    └── S_ADMI_FCD → LANG
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → (empty — maintain in PFCG)
    └── P_PROGNAM → /SAPAPO/OM_SYNC_LC_DB
```

### 1042. `/SAPAPO/OM18`

```
T-Code: /SAPAPO/OM18
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1043. `/SAPAPO/OM19`

```
T-Code: /SAPAPO/OM19
└── S_GUI
    └── ACTVT → 61
```

### 1044. `/SAPAPO/OM21`

```
T-Code: /SAPAPO/OM21
└── S_GUI
    └── ACTVT → 61
```

### 1045. `/SAPAPO/OM22`

```
T-Code: /SAPAPO/OM22
├── S_BTCH_ADM
    └── BTCADMIN → Y
└── S_PROGNAM
    ├── P_ACTION → (empty — maintain in PFCG)
    └── P_PROGNAM → /SAPAPO/OM_SET_OM_LOGGING
```

### 1046. `/SAPAPO/OM25`

```
T-Code: /SAPAPO/OM25
└── S_RZL_ADM
    └── ACTVT → 3
```

### 1047. `/SAPAPO/OM26`

```
T-Code: /SAPAPO/OM26
├── S_BTCH_ADM
    └── BTCADMIN → Y
└── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1048. `/SAPAPO/OMSL2`

```
T-Code: /SAPAPO/OMSL2
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TMCNV2
```

### 1049. `/SAPAPO/OMTS_BROWSE`

```
T-Code: /SAPAPO/OMTS_BROWSE
└── S_GUI
    └── ACTVT → 61
```

### 1050. `/SAPAPO/OMTS_DPLOG`

```
T-Code: /SAPAPO/OMTS_DPLOG
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → APO_TS
    └── ALG_SUBOBJ → TEST
└── S_GUI
    └── ACTVT → 61
```

### 1051. `/SAPAPO/OMTS_DPTF`

```
T-Code: /SAPAPO/OMTS_DPTF
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → APO_TS
    └── ALG_SUBOBJ → TEST
├── S_BTCH_ADM
    └── BTCADMIN → Y
└── S_GUI
    └── ACTVT → 61
```

### 1052. `/SAPAPO/OPT00`

```
T-Code: /SAPAPO/OPT00
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1053. `/SAPAPO/OPT01`

```
T-Code: /SAPAPO/OPT01
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1054. `/SAPAPO/OPT01_OLD`

```
T-Code: /SAPAPO/OPT01_OLD
└── S_TABU_DIS
    ├── ACTVT → 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1055. `/SAPAPO/OPT03`

```
T-Code: /SAPAPO/OPT03
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1056. `/SAPAPO/OPT03_OLD`

```
T-Code: /SAPAPO/OPT03_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1057. `/SAPAPO/OPT09`

```
T-Code: /SAPAPO/OPT09
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1058. `/SAPAPO/OPT09_OLD`

```
T-Code: /SAPAPO/OPT09_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1059. `/SAPAPO/OPT10`

```
T-Code: /SAPAPO/OPT10
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1060. `/SAPAPO/OPT11`

```
T-Code: /SAPAPO/OPT11
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1061. `/SAPAPO/OPT11_OLD`

```
T-Code: /SAPAPO/OPT11_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1062. `/SAPAPO/OPT12`

```
T-Code: /SAPAPO/OPT12
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1063. `/SAPAPO/OPT12_OLD`

```
T-Code: /SAPAPO/OPT12_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1064. `/SAPAPO/OPTDBG0`

```
T-Code: /SAPAPO/OPTDBG0
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1065. `/SAPAPO/OPTDBG1`

```
T-Code: /SAPAPO/OPTDBG1
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1066. `/SAPAPO/OPT_DPL`

```
T-Code: /SAPAPO/OPT_DPL
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1067. `/SAPAPO/OPT_EXP`

```
T-Code: /SAPAPO/OPT_EXP
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1068. `/SAPAPO/OPT_INST`

```
T-Code: /SAPAPO/OPT_INST
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1069. `/SAPAPO/OPT_INST_OLD`

```
T-Code: /SAPAPO/OPT_INST_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1070. `/SAPAPO/OPT_SEQ`

```
T-Code: /SAPAPO/OPT_SEQ
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1071. `/SAPAPO/OPT_STOP`

```
T-Code: /SAPAPO/OPT_STOP
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1072. `/SAPAPO/OPT_STOP_OLD`

```
T-Code: /SAPAPO/OPT_STOP_OLD
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1073. `/SAPAPO/OPT_VSR`

```
T-Code: /SAPAPO/OPT_VSR
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR
```

### 1074. `/SAPAPO/ORSP`

```
T-Code: /SAPAPO/ORSP
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1075. `/SAPAPO/PCMP`

```
T-Code: /SAPAPO/PCMP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_SETPCM
```

### 1076. `/SAPAPO/PCMT`

```
T-Code: /SAPAPO/PCMT
├── C_APO_PCM
    └── ACTVT → 1, 2, 3, 6
└── C_APO_PPL
    ├── ACTVT → 2
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
```

### 1077. `/SAPAPO/PCM_DELETE`

```
T-Code: /SAPAPO/PCM_DELETE
└── C_APO_PCM
    └── ACTVT → 1, 2, 3, 6
```

### 1078. `/SAPAPO/PDSLOG`

```
T-Code: /SAPAPO/PDSLOG
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1079. `/SAPAPO/PEG1`

```
T-Code: /SAPAPO/PEG1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1080. `/SAPAPO/PE_CFG`

```
T-Code: /SAPAPO/PE_CFG
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1081. `/SAPAPO/PE_CFG2`

```
T-Code: /SAPAPO/PE_CFG2
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1082. `/SAPAPO/PE_CFG3`

```
T-Code: /SAPAPO/PE_CFG3
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1083. `/SAPAPO/PE_CFG4`

```
T-Code: /SAPAPO/PE_CFG4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPESVUI2
```

### 1084. `/SAPAPO/PE_LOG_DISP`

```
T-Code: /SAPAPO/PE_LOG_DISP
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SAPAPO/PE
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1085. `/SAPAPO/PE_PERF_LOG`

```
T-Code: /SAPAPO/PE_PERF_LOG
├── S_ALV_LAYO
    └── ACTVT → 23
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SAPAPO/PE
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1086. `/SAPAPO/PE_SEL`

```
T-Code: /SAPAPO/PE_SEL
└── /SCMB/PESL
    ├── ACTVT → 34, 6
    └── USER → (empty — maintain in PFCG)
```

### 1087. `/SAPAPO/PE_SEL_SPP`

```
T-Code: /SAPAPO/PE_SEL_SPP
└── S_TCODE
    └── TCD → /SAPAPO/CFM_POWL
```

### 1088. `/SAPAPO/PE_SLG1`

```
T-Code: /SAPAPO/PE_SLG1
├── S_ALV_LAYO
    └── ACTVT → 23
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SAPAPO/PE
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1089. `/SAPAPO/PE_SRV_CFG`

```
T-Code: /SAPAPO/PE_SRV_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPEDM2PR, /SAPAPO/VPEDMPRM, /SAPAPO/VPESRVAP, /SAPAPO/VPESRVMD, /SAPAPO/VPESRVMT, /SAPAPO/VPE_APPL, /SAPAPO/VPE_DM, /SAPAPO/VPE_DMAP, /SAPAPO/VPE_METH, /SAPAPO/VPE_SRV
```

### 1090. `/SAPAPO/PFCS_SEM`

```
T-Code: /SAPAPO/PFCS_SEM
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1091. `/SAPAPO/PHP_MASSNOTE`

```
T-Code: /SAPAPO/PHP_MASSNOTE
└── S_TRANSLAT
    ├── ACTVT → 2, A8
    ├── TLANGUAGE → (empty — maintain in PFCG)
    └── TRANOBJ → (empty — maintain in PFCG)
```

### 1092. `/SAPAPO/PHP_OBJ_NAV`

```
T-Code: /SAPAPO/PHP_OBJ_NAV
└── S_TCODE
    └── TCD → /SAPAPO/PHP_MY_OBJ_N
```

### 1093. `/SAPAPO/PLANNER`

```
T-Code: /SAPAPO/PLANNER
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → SCC
```

### 1094. `/SAPAPO/PLN_GRP`

```
T-Code: /SAPAPO/PLN_GRP
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1095. `/SAPAPO/PMON`

```
T-Code: /SAPAPO/PMON
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1096. `/SAPAPO/PMONDEF`

```
T-Code: /SAPAPO/PMONDEF
└── C_APO_DEF
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_DEFN → (empty — maintain in PFCG)
    ├── APO_DEFT → (empty — maintain in PFCG)
    └── APO_PLNR → (empty — maintain in PFCG)
```

### 1097. `/SAPAPO/POV1`

```
T-Code: /SAPAPO/POV1
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1098. `/SAPAPO/POV2`

```
T-Code: /SAPAPO/POV2
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1099. `/SAPAPO/POV_ALERTS`

```
T-Code: /SAPAPO/POV_ALERTS
└── S_TCODE
    └── TCD → /SAPAPO/POV_ALERTS
```

### 1100. `/SAPAPO/POV_LAUNCH`

```
T-Code: /SAPAPO/POV_LAUNCH
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1101. `/SAPAPO/PPC1`

```
T-Code: /SAPAPO/PPC1
└── C_APO_PPC
    ├── ACTVT → 1, 2, 85
    ├── PPC_LOCNO → (empty — maintain in PFCG)
    └── PPC_RPTNAM → (empty — maintain in PFCG)
```

### 1102. `/SAPAPO/PPC2`

```
T-Code: /SAPAPO/PPC2
└── C_APO_PPC
    ├── ACTVT → 3
    ├── PPC_LOCNO → (empty — maintain in PFCG)
    └── PPC_RPTNAM → (empty — maintain in PFCG)
```

### 1103. `/SAPAPO/PPE`

```
T-Code: /SAPAPO/PPE
├── C_AENR_BGR
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── C_AENR_ERW
    ├── ACTVT → 3, F4
    ├── AEFUN → (empty — maintain in PFCG)
    ├── AENST → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── RLKEY → (empty — maintain in PFCG)
├── C_APO_DEF
    ├── ACTVT → 3
    ├── APO_DEFN → (empty — maintain in PFCG)
    ├── APO_DEFT → (empty — maintain in PFCG)
    └── APO_PLNR → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_MALO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → 3
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_RTO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
├── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
├── C_CABN
    └── ACTVT → 3, F4
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 3, F4
    └── BGRKP → (empty — maintain in PFCG)
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_GRID
    ├── ACTVT → 3
    ├── PPE_FNAME → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3, F4
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
└── C_TCLA_BKA
    └── KLART → 300
```

### 1104. `/SAPAPO/PPELUI`

```
T-Code: /SAPAPO/PPELUI
├── C_APO_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    └── APO_LOC → (empty — maintain in PFCG)
└── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
```

### 1105. `/SAPAPO/PPFSCO`

```
T-Code: /SAPAPO/PPFSCO
└── C_APO_SSA
    ├── ACTVT → 16
    ├── APO_CDSOBJ → S_PPFCONF
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1106. `/SAPAPO/PPL1`

```
T-Code: /SAPAPO/PPL1
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1107. `/SAPAPO/PPO_CP`

```
T-Code: /SAPAPO/PPO_CP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPPOCATP
```

### 1108. `/SAPAPO/PPO_DPEN`

```
T-Code: /SAPAPO/PPO_DPEN
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1109. `/SAPAPO/PPO_LOG`

```
T-Code: /SAPAPO/PPO_LOG
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → PPDS
    └── ALG_SUBOBJ → PPO
```

### 1110. `/SAPAPO/PPO_OPT`

```
T-Code: /SAPAPO/PPO_OPT
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_ALV_LAYR
    ├── ACTVT → 23
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → /SAPAPO/SAPLPPO_LOG
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → PPDS
    └── ALG_SUBOBJ → PPO
├── S_GUI
    └── ACTVT → 61
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SAPAPO/OPT_SVR_COM, /SAPAPO/PPO_OPTIMIZER_DOWNLOAD, /SAPAPO/PPO_OPTIMIZER_UPLOAD, /SAPAPO/PPO_OPT_MASTERDATA_ORD, /SAPAPO/PPO_PROCESS_ORDERS, RCC_COMM_ENGINE
    └── RFC_TYPE → FUGR, FUNC
```

### 1111. `/SAPAPO/PPO_PRFL`

```
T-Code: /SAPAPO/PPO_PRFL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/PPO_PRFL
```

### 1112. `/SAPAPO/PPO_TBP`

```
T-Code: /SAPAPO/PPO_TBP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/PPO_V_SH, /SAPAPO/PPO_V_TB, /SAPAPO/PPO_V_TU
```

### 1113. `/SAPAPO/PPT1`

```
T-Code: /SAPAPO/PPT1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1114. `/SAPAPO/PRJ1`

```
T-Code: /SAPAPO/PRJ1
├── C_APO_ATP
    ├── ACTVT → 2, 3
    └── APO_ATP_AR → 0, 3
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_VERS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
└── S_PROJECT
    ├── ACTVT → 3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
```

### 1115. `/SAPAPO/PROD_CD_READ`

```
T-Code: /SAPAPO/PROD_CD_READ
└── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1116. `/SAPAPO/PRT_LOCATION`

```
T-Code: /SAPAPO/PRT_LOCATION
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_LOCPRT
```

### 1117. `/SAPAPO/PRT_LOCPROD`

```
T-Code: /SAPAPO/PRT_LOCPROD
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1118. `/SAPAPO/PRT_PRODUCT`

```
T-Code: /SAPAPO/PRT_PRODUCT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_MATPRM, /SAPAPO/V_MATPRT
```

### 1119. `/SAPAPO/PRT_PURGROUP`

```
T-Code: /SAPAPO/PRT_PURGROUP
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1120. `/SAPAPO/PUSH`

```
T-Code: /SAPAPO/PUSH
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 1, 16, 2
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1121. `/SAPAPO/PWBSCH1`

```
T-Code: /SAPAPO/PWBSCH1
└── C_APO_EXPR
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_EXPOBJ → S_EXPR_CNF, S_EXPR_COS, S_EXPR_HIS, S_EXPR_MAS, S_EXPR_MES, S_EXPR_REL
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1122. `/SAPAPO/PWBSCH2`

```
T-Code: /SAPAPO/PWBSCH2
└── C_APO_EXPR
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_EXPOBJ → S_EXPR_CNF, S_EXPR_COS, S_EXPR_HIS, S_EXPR_MAS, S_EXPR_MES, S_EXPR_REL
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1123. `/SAPAPO/PWBSCH3`

```
T-Code: /SAPAPO/PWBSCH3
├── C_APO_EXPR
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_EXPOBJ → S_EXPR_CNF, S_EXPR_COS, S_EXPR_HIS, S_EXPR_MAS, S_EXPR_MES, S_EXPR_REL
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1124. `/SAPAPO/PWBSCH4`

```
T-Code: /SAPAPO/PWBSCH4
├── C_APO_EXPR
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_EXPOBJ → S_EXPR_CNF, S_EXPR_COS, S_EXPR_HIS, S_EXPR_MAS, S_EXPR_MES, S_EXPR_REL
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_MOD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1125. `/SAPAPO/PWBSRC1`

```
T-Code: /SAPAPO/PWBSRC1
└── C_APO_EXPR
    ├── ACTVT → 1, 2, 3
    ├── APO_EXPOBJ → (empty — maintain in PFCG)
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1126. `/SAPAPO/PWBSRC2`

```
T-Code: /SAPAPO/PWBSRC2
└── C_APO_EXPR
    ├── ACTVT → 6
    ├── APO_EXPOBJ → (empty — maintain in PFCG)
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PLANER → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1127. `/SAPAPO/PWB_CM1`

```
T-Code: /SAPAPO/PWB_CM1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → MCMD
```

### 1128. `/SAPAPO/QTA1`

```
T-Code: /SAPAPO/QTA1
├── C_APO_LOC
    ├── ACTVT → F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_PROD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1129. `/SAPAPO/QTA2`

```
T-Code: /SAPAPO/QTA2
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1130. `/SAPAPO/QUAN`

```
T-Code: /SAPAPO/QUAN
├── C_APO_PROD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VQUAN, /SAPAPO/VQUANH
```

### 1131. `/SAPAPO/RBA04`

```
T-Code: /SAPAPO/RBA04
└── C_APO_ATP
    ├── ACTVT → 3
    └── APO_ATP_AR → 9
```

### 1132. `/SAPAPO/RELHDELETE`

```
T-Code: /SAPAPO/RELHDELETE
└── C_APO_RELH
    └── ACTVT → 6
```

### 1133. `/SAPAPO/RELHMAPPING`

```
T-Code: /SAPAPO/RELHMAPPING
└── C_APO_RELH
    └── ACTVT → 6
```

### 1134. `/SAPAPO/RELHPER`

```
T-Code: /SAPAPO/RELHPER
└── C_APO_RELH
    └── ACTVT → 6
```

### 1135. `/SAPAPO/RELHSHOW`

```
T-Code: /SAPAPO/RELHSHOW
└── C_APO_RELH
    └── ACTVT → 1, 2, 3, 6
```

### 1136. `/SAPAPO/RELHTDLP_DEL`

```
T-Code: /SAPAPO/RELHTDLP_DEL
└── C_APO_RELH
    └── ACTVT → 6
```

### 1137. `/SAPAPO/REPDUE_STAT`

```
T-Code: /SAPAPO/REPDUE_STAT
└── C_APO_PROD
    ├── ACTVT → 2
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1138. `/SAPAPO/RES01`

```
T-Code: /SAPAPO/RES01
├── C_APO_RES
    ├── ACTVT → 1, 16, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → MD
    ├── APO_KENN → A, I
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1139. `/SAPAPO/RESC01`

```
T-Code: /SAPAPO/RESC01
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_RESVER
```

### 1140. `/SAPAPO/RESC02`

```
T-Code: /SAPAPO/RESC02
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_RESSEE, /SAPAPO/V_RESSEL
```

### 1141. `/SAPAPO/RESNET`

```
T-Code: /SAPAPO/RESNET
└── C_APO_RESN
    └── ACTVT → (empty — maintain in PFCG)
```

### 1142. `/SAPAPO/REST01`

```
T-Code: /SAPAPO/REST01
├── C_APO_RES
    ├── ACTVT → 16, 2
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1143. `/SAPAPO/REST02`

```
T-Code: /SAPAPO/REST02
└── C_APO_RES
    ├── ACTVT → 16
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 1144. `/SAPAPO/RET1`

```
T-Code: /SAPAPO/RET1
└── C_APO_RETS
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_RETAUT → (empty — maintain in PFCG)
    ├── APO_VERS → (empty — maintain in PFCG)
    └── PNAME → (empty — maintain in PFCG)
```

### 1145. `/SAPAPO/RET2`

```
T-Code: /SAPAPO/RET2
└── C_APO_RETS
    ├── ACTVT → 1, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_RETAUT → (empty — maintain in PFCG)
    ├── APO_VERS → (empty — maintain in PFCG)
    └── PNAME → (empty — maintain in PFCG)
```

### 1146. `/SAPAPO/RET_CHECK`

```
T-Code: /SAPAPO/RET_CHECK
└── C_APO_RELO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 1147. `/SAPAPO/RET_CLEAN_DB`

```
T-Code: /SAPAPO/RET_CLEAN_DB
└── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
```

### 1148. `/SAPAPO/RET_CU01`

```
T-Code: /SAPAPO/RET_CU01
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/RET_TCVC
```

### 1149. `/SAPAPO/RET_EVAL`

```
T-Code: /SAPAPO/RET_EVAL
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1150. `/SAPAPO/RET_REFRESH`

```
T-Code: /SAPAPO/RET_REFRESH
└── S_DEVELOP
    ├── ACTVT → 2, 3, 7
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1151. `/SAPAPO/RET_RP`

```
T-Code: /SAPAPO/RET_RP
├── C_APO_RETS
    ├── ACTVT → 1, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_RETAUT → (empty — maintain in PFCG)
    ├── APO_VERS → (empty — maintain in PFCG)
    └── PNAME → (empty — maintain in PFCG)
└── C_APO_RETU
    └── ACTVT → 1
```

### 1152. `/SAPAPO/RET_VIEW`

```
T-Code: /SAPAPO/RET_VIEW
└── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
```

### 1153. `/SAPAPO/RFCDEST`

```
T-Code: /SAPAPO/RFCDEST
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → PPDS
```

### 1154. `/SAPAPO/RFCLLC`

```
T-Code: /SAPAPO/RFCLLC
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → PPDS
```

### 1155. `/SAPAPO/RGPAT01`

```
T-Code: /SAPAPO/RGPAT01
└── C_APO_RELH
    └── ACTVT → 1, 2, 3, 6
```

### 1156. `/SAPAPO/RNWDEL`

```
T-Code: /SAPAPO/RNWDEL
├── C_APO_RESN
    └── ACTVT → 1
└── S_USER_GRP
    ├── ACTVT → (empty — maintain in PFCG)
    └── CLASS → (empty — maintain in PFCG)
```

### 1157. `/SAPAPO/ROUNDINGPROF`

```
T-Code: /SAPAPO/ROUNDINGPROF
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1158. `/SAPAPO/ROUNDPRF`

```
T-Code: /SAPAPO/ROUNDPRF
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → SCC
```

### 1159. `/SAPAPO/RPT`

```
T-Code: /SAPAPO/RPT
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1160. `/SAPAPO/RRP1`

```
T-Code: /SAPAPO/RRP1
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1161. `/SAPAPO/RRP2`

```
T-Code: /SAPAPO/RRP2
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1162. `/SAPAPO/RRP3`

```
T-Code: /SAPAPO/RRP3
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1163. `/SAPAPO/RRP4`

```
T-Code: /SAPAPO/RRP4
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1164. `/SAPAPO/RRP5`

```
T-Code: /SAPAPO/RRP5
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1165. `/SAPAPO/RRP7`

```
T-Code: /SAPAPO/RRP7
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1166. `/SAPAPO/RRP8`

```
T-Code: /SAPAPO/RRP8
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1167. `/SAPAPO/RRPC1`

```
T-Code: /SAPAPO/RRPC1
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPAPO/PP
```

### 1168. `/SAPAPO/RRPC2`

```
T-Code: /SAPAPO/RRPC2
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPAPO/PK
```

### 1169. `/SAPAPO/RRPCUST1`

```
T-Code: /SAPAPO/RRPCUST1
├── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → PPDS
└── S_TABU_NAM
    ├── ACTVT → 2
    └── TABLE → PPDS
```

### 1170. `/SAPAPO/RRPCUST2`

```
T-Code: /SAPAPO/RRPCUST2
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → PPDS
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → PPDS
```

### 1171. `/SAPAPO/RRPCUST3`

```
T-Code: /SAPAPO/RRPCUST3
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SAPAPO/RRPGRP, /SAPAPO/V_RRPMSG
```

### 1172. `/SAPAPO/RRPDEBUG`

```
T-Code: /SAPAPO/RRPDEBUG
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → (empty — maintain in PFCG)
```

### 1173. `/SAPAPO/RRPEVAL`

```
T-Code: /SAPAPO/RRPEVAL
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → APO, SF_LOG_OBJ
    └── ALG_SUBOBJ → PPDS, SF_LOG_SUB
```

### 1174. `/SAPAPO/RRPLOG1`

```
T-Code: /SAPAPO/RRPLOG1
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1175. `/SAPAPO/RRPLOG2`

```
T-Code: /SAPAPO/RRPLOG2
└── S_APPL_LOG
    ├── ACTVT → 6
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1176. `/SAPAPO/RRP_ATP2PPDS`

```
T-Code: /SAPAPO/RRP_ATP2PPDS
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1177. `/SAPAPO/RRP_CONF_QTY`

```
T-Code: /SAPAPO/RRP_CONF_QTY
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → PPDS
```

### 1178. `/SAPAPO/RRP_CUS_PROF`

```
T-Code: /SAPAPO/RRP_CUS_PROF
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPAPO/V_HEURPR
```

### 1179. `/SAPAPO/RRP_CVTYP`

```
T-Code: /SAPAPO/RRP_CVTYP
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → &NC&
```

### 1180. `/SAPAPO/RRP_NETCH`

```
T-Code: /SAPAPO/RRP_NETCH
├── C_APO_MALO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → 3
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1181. `/SAPAPO/RRP_PT_IR`

```
T-Code: /SAPAPO/RRP_PT_IR
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → APO
    └── ALG_SUBOBJ → PPDS
```

### 1182. `/SAPAPO/RRP_SET_RRPT`

```
T-Code: /SAPAPO/RRP_SET_RRPT
└── C_APO_PROD
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 1183. `/SAPAPO/RRP_SNP2PPDS`

```
T-Code: /SAPAPO/RRP_SNP2PPDS
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1184. `/SAPAPO/RSP_BINI`

```
T-Code: /SAPAPO/RSP_BINI
├── C_APO_BINI
    ├── ACTVT → 64, 66, D1
    └── APO_VERS → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1185. `/SAPAPO/RSP_CHPTRDEL`

```
T-Code: /SAPAPO/RSP_CHPTRDEL
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1186. `/SAPAPO/RSP_CLOSE`

```
T-Code: /SAPAPO/RSP_CLOSE
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1187. `/SAPAPO/RSP_CONSIS`

```
T-Code: /SAPAPO/RSP_CONSIS
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1188. `/SAPAPO/RSP_DBORSHOW`

```
T-Code: /SAPAPO/RSP_DBORSHOW
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1189. `/SAPAPO/RSP_DCR`

```
T-Code: /SAPAPO/RSP_DCR
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1190. `/SAPAPO/RSP_DICP`

```
T-Code: /SAPAPO/RSP_DICP
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1191. `/SAPAPO/RSP_DPND`

```
T-Code: /SAPAPO/RSP_DPND
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1192. `/SAPAPO/RSP_EXPLAIN`

```
T-Code: /SAPAPO/RSP_EXPLAIN
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1193. `/SAPAPO/RSP_G1`

```
T-Code: /SAPAPO/RSP_G1
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 1194. `/SAPAPO/RSP_G1CU`

```
T-Code: /SAPAPO/RSP_G1CU
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1195. `/SAPAPO/RSP_GC`

```
T-Code: /SAPAPO/RSP_GC
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → ATPC
```

### 1196. `/SAPAPO/RSP_INFO`

```
T-Code: /SAPAPO/RSP_INFO
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1197. `/SAPAPO/RSP_LC01`

```
T-Code: /SAPAPO/RSP_LC01
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1198. `/SAPAPO/RSP_LC03`

```
T-Code: /SAPAPO/RSP_LC03
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1199. `/SAPAPO/RSP_LC04`

```
T-Code: /SAPAPO/RSP_LC04
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1200. `/SAPAPO/RSP_MCSC`

```
T-Code: /SAPAPO/RSP_MCSC
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1201. `/SAPAPO/RSP_MON`

```
T-Code: /SAPAPO/RSP_MON
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1202. `/SAPAPO/RSP_MON_CLR`

```
T-Code: /SAPAPO/RSP_MON_CLR
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1203. `/SAPAPO/RSP_P1`

```
T-Code: /SAPAPO/RSP_P1
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1204. `/SAPAPO/RSP_P2`

```
T-Code: /SAPAPO/RSP_P2
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1205. `/SAPAPO/RSP_P3`

```
T-Code: /SAPAPO/RSP_P3
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1206. `/SAPAPO/RSP_PAPRO`

```
T-Code: /SAPAPO/RSP_PAPRO
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1207. `/SAPAPO/RSP_PLNVERS`

```
T-Code: /SAPAPO/RSP_PLNVERS
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1208. `/SAPAPO/RSP_PROV`

```
T-Code: /SAPAPO/RSP_PROV
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1209. `/SAPAPO/RSP_RD_SIMU`

```
T-Code: /SAPAPO/RSP_RD_SIMU
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1210. `/SAPAPO/RSP_REQ`

```
T-Code: /SAPAPO/RSP_REQ
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1211. `/SAPAPO/RSP_RSC`

```
T-Code: /SAPAPO/RSP_RSC
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1212. `/SAPAPO/RTO_ORD_COMP`

```
T-Code: /SAPAPO/RTO_ORD_COMP
└── C_APO_RTO
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PROD → (empty — maintain in PFCG)
    ├── APO_RTOUSE → (empty — maintain in PFCG)
    └── APO_SOS → (empty — maintain in PFCG)
```

### 1213. `/SAPAPO/SAPAPO`

```
T-Code: /SAPAPO/SAPAPO
└── C_APO_FUN
    ├── APO_FUNC → (empty — maintain in PFCG)
    └── APO_PAREA → (empty — maintain in PFCG)
```

### 1214. `/SAPAPO/SCC02`

```
T-Code: /SAPAPO/SCC02
├── C_APO_MOD
    ├── ACTVT → 3
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → SCC
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1215. `/SAPAPO/SCC07`

```
T-Code: /SAPAPO/SCC07
├── C_APO_MOD
    ├── ACTVT → 1, 2, 3, 6
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → SCC
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1216. `/SAPAPO/SCC_TB1`

```
T-Code: /SAPAPO/SCC_TB1
├── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1217. `/SAPAPO/SCC_TL2`

```
T-Code: /SAPAPO/SCC_TL2
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1218. `/SAPAPO/SCC_TL5`

```
T-Code: /SAPAPO/SCC_TL5
├── C_APO_LOC
    ├── ACTVT → 16, 2, 3, 6
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_MOD
    ├── ACTVT → 2
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 1219. `/SAPAPO/SCC_TQ1`

```
T-Code: /SAPAPO/SCC_TQ1
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1220. `/SAPAPO/SCC_TQ2`

```
T-Code: /SAPAPO/SCC_TQ2
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1221. `/SAPAPO/SCEVERSCOMP`

```
T-Code: /SAPAPO/SCEVERSCOMP
└── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → VM
    ├── APO_KENN → A, I
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1222. `/SAPAPO/SCE_QTA`

```
T-Code: /SAPAPO/SCE_QTA
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1223. `/SAPAPO/SCH_ACC_SEQ`

```
T-Code: /SAPAPO/SCH_ACC_SEQ
└── /SAPCND/CC
    ├── /SAPCND/AP → SCH
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → CA, DU, PA, TZ
    └── ACTVT → 2
```

### 1224. `/SAPAPO/SCH_COND_TYP`

```
T-Code: /SAPAPO/SCH_COND_TYP
└── /SAPCND/CC
    ├── /SAPCND/AP → SCH
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → CA, DU, PA, TZ
    └── ACTVT → 2
```

### 1225. `/SAPAPO/SCH_CT`

```
T-Code: /SAPAPO/SCH_CT
├── /SAPCND/CC
    ├── /SAPCND/AP → SCH
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → CA, DU, PA, TZ
    └── ACTVT → 2
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 1226. `/SAPAPO/SCH_DET_PROC`

```
T-Code: /SAPAPO/SCH_DET_PROC
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1227. `/SAPAPO/SCH_FC`

```
T-Code: /SAPAPO/SCH_FC
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 1228. `/SAPAPO/SCH_GCM`

```
T-Code: /SAPAPO/SCH_GCM
└── /SAPCND/CM
    ├── /SAPCND/AP → SCH
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1229. `/SAPAPO/SCMB_MP32`

```
T-Code: /SAPAPO/SCMB_MP32
└── C_APO_FUN
    ├── APO_FUNC → C_PROMO
    └── APO_PAREA → (empty — maintain in PFCG)
```

### 1230. `/SAPAPO/SCNCTRL`

```
T-Code: /SAPAPO/SCNCTRL
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1231. `/SAPAPO/SDPT`

```
T-Code: /SAPAPO/SDPT
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
├── C_APO_SSA
    ├── ACTVT → 16, 3
    ├── APO_CDSOBJ → S_HISTORY, S_SAGR, S_STACUR
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1232. `/SAPAPO/SDPTBFX`

```
T-Code: /SAPAPO/SDPTBFX
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1233. `/SAPAPO/SEQ1`

```
T-Code: /SAPAPO/SEQ1
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_APO_RETS
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_RETAUT → (empty — maintain in PFCG)
    ├── APO_VERS → (empty — maintain in PFCG)
    └── PNAME → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1234. `/SAPAPO/SEQ2`

```
T-Code: /SAPAPO/SEQ2
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1235. `/SAPAPO/SEQC1`

```
T-Code: /SAPAPO/SEQC1
├── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → (empty — maintain in PFCG)
```

### 1236. `/SAPAPO/SEQC10`

```
T-Code: /SAPAPO/SEQC10
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1237. `/SAPAPO/SEQC11`

```
T-Code: /SAPAPO/SEQC11
└── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
```

### 1238. `/SAPAPO/SEQC2`

```
T-Code: /SAPAPO/SEQC2
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1239. `/SAPAPO/SEQC3`

```
T-Code: /SAPAPO/SEQC3
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1240. `/SAPAPO/SEQC4`

```
T-Code: /SAPAPO/SEQC4
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1241. `/SAPAPO/SEQC5`

```
T-Code: /SAPAPO/SEQC5
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1242. `/SAPAPO/SEQC6`

```
T-Code: /SAPAPO/SEQC6
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1243. `/SAPAPO/SEQC7`

```
T-Code: /SAPAPO/SEQC7
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1244. `/SAPAPO/SEQC8`

```
T-Code: /SAPAPO/SEQC8
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → &NC&
```

### 1245. `/SAPAPO/SEQC9`

```
T-Code: /SAPAPO/SEQC9
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → SEQV
```

### 1246. `/SAPAPO/SHPEDI`

```
T-Code: /SAPAPO/SHPEDI
└── C_APO_TPVS
    ├── ACTVT → 16
    ├── APO_LOCFR → (empty — maintain in PFCG)
    ├── APO_LOCTO → (empty — maintain in PFCG)
    ├── APO_TPVS_A → H
    ├── APO_TPVS_P → (empty — maintain in PFCG)
    └── APO_TPVS_V → (empty — maintain in PFCG)
```

### 1247. `/SAPAPO/SNP2PPDS`

```
T-Code: /SAPAPO/SNP2PPDS
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1248. `/SAPAPO/SORSCRAPORD`

```
T-Code: /SAPAPO/SORSCRAPORD
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1249. `/SAPAPO/SPPDEPL`

```
T-Code: /SAPAPO/SPPDEPL
└── C_APO_UIF
    ├── /SCF/APPID → SPPDEPL
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1250. `/SAPAPO/SPPDMDH`

```
T-Code: /SAPAPO/SPPDMDH
└── C_APO_UIF
    ├── /SCF/APPID → SPPDMDH
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1251. `/SAPAPO/SPPDMRD`

```
T-Code: /SAPAPO/SPPDMRD
└── C_APO_UIF
    ├── /SCF/APPID → SPPDMRD
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1252. `/SAPAPO/SPPDRPAPPR`

```
T-Code: /SAPAPO/SPPDRPAPPR
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPAPPR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1253. `/SAPAPO/SPPDRPM`

```
T-Code: /SAPAPO/SPPDRPM
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPM
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1254. `/SAPAPO/SPPDRPMR`

```
T-Code: /SAPAPO/SPPDRPMR
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPMR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1255. `/SAPAPO/SPPDRPPR`

```
T-Code: /SAPAPO/SPPDRPPR
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPPR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1256. `/SAPAPO/SPPDRPSB`

```
T-Code: /SAPAPO/SPPDRPSB
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPSB
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1257. `/SAPAPO/SPPEOQPR`

```
T-Code: /SAPAPO/SPPEOQPR
└── C_APO_UIF
    ├── /SCF/APPID → SPPDRPPR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1258. `/SAPAPO/SPPFCST`

```
T-Code: /SAPAPO/SPPFCST
└── C_APO_UIF
    ├── /SCF/APPID → SPPFCST
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1259. `/SAPAPO/SPPFCSTAPPR`

```
T-Code: /SAPAPO/SPPFCSTAPPR
└── C_APO_UIF
    ├── /SCF/APPID → SPPFCSTAPPR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1260. `/SAPAPO/SPPFCSTPRF`

```
T-Code: /SAPAPO/SPPFCSTPRF
└── C_APO_UIF
    ├── /SCF/APPID → SPPFCSTPRF
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1261. `/SAPAPO/SPPFCSTTRACK`

```
T-Code: /SAPAPO/SPPFCSTTRACK
└── C_APO_UIF
    ├── /SCF/APPID → SPPFCSTTRACK
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1262. `/SAPAPO/SPPFIXREQ`

```
T-Code: /SAPAPO/SPPFIXREQ
└── C_APO_UIF
    ├── /SCF/APPID → SPPFIXREQ
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1263. `/SAPAPO/SPPINBD`

```
T-Code: /SAPAPO/SPPINBD
└── C_APO_UIF
    ├── /SCF/APPID → SPPINBD
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1264. `/SAPAPO/SPPINVP`

```
T-Code: /SAPAPO/SPPINVP
└── C_APO_UIF
    ├── /SCF/APPID → SPPINVP
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1265. `/SAPAPO/SPPINVPDEC`

```
T-Code: /SAPAPO/SPPINVPDEC
└── C_APO_UIF
    ├── /SCF/APPID → SPPINVPDEC
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1266. `/SAPAPO/SPPLONGFCST`

```
T-Code: /SAPAPO/SPPLONGFCST
└── C_APO_UIF
    ├── /SCF/APPID → SPPLONGFCST
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1267. `/SAPAPO/SPPMSP`

```
T-Code: /SAPAPO/SPPMSP
└── C_APO_UIF
    ├── /SCF/APPID → SPPSOR
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1268. `/SAPAPO/SPPNEWFCST`

```
T-Code: /SAPAPO/SPPNEWFCST
└── C_APO_UIF
    ├── /SCF/APPID → SPPNEWFCST
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1269. `/SAPAPO/SPPSNAPSHOT`

```
T-Code: /SAPAPO/SPPSNAPSHOT
└── C_APO_VERS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1270. `/SAPAPO/SPPSRVLOSS`

```
T-Code: /SAPAPO/SPPSRVLOSS
└── C_APO_UIF
    ├── /SCF/APPID → SPPFILLLOSS
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1271. `/SAPAPO/SPP_CNA_ACT`

```
T-Code: /SAPAPO/SPP_CNA_ACT
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1272. `/SAPAPO/SPP_NOTE_DEL`

```
T-Code: /SAPAPO/SPP_NOTE_DEL
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1273. `/SAPAPO/SPP_SCHD_EXP`

```
T-Code: /SAPAPO/SPP_SCHD_EXP
└── PLOG
    ├── INFOTYP → (empty — maintain in PFCG)
    ├── ISTAT → (empty — maintain in PFCG)
    ├── OTYPE → (empty — maintain in PFCG)
    ├── PLVAR → $PLVAR
    ├── PPFCODE → (empty — maintain in PFCG)
    └── SUBTYP → (empty — maintain in PFCG)
```

### 1274. `/SAPAPO/SPP_TG_ACT`

```
T-Code: /SAPAPO/SPP_TG_ACT
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1275. `/SAPAPO/SRVF_STATUS`

```
T-Code: /SAPAPO/SRVF_STATUS
├── /INCMD/AUT
    ├── /INCMD/ICC → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
```

### 1276. `/SAPAPO/SR_KOKR_MURX`

```
T-Code: /SAPAPO/SR_KOKR_MURX
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1277. `/SAPAPO/STEST`

```
T-Code: /SAPAPO/STEST
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1278. `/SAPAPO/STODEF_GEN_O`

```
T-Code: /SAPAPO/STODEF_GEN_O
└── C_APO_PSD
    └── ACTVT → 2, 3
```

### 1279. `/SAPAPO/STORDEF`

```
T-Code: /SAPAPO/STORDEF
└── C_APO_PSD
    └── ACTVT → 2, 3
```

### 1280. `/SAPAPO/STORDEF_GEN`

```
T-Code: /SAPAPO/STORDEF_GEN
└── C_APO_PSD
    └── ACTVT → 2, 3
```

### 1281. `/SAPAPO/STORDEF_OLD`

```
T-Code: /SAPAPO/STORDEF_OLD
└── C_APO_PSD
    └── ACTVT → 2, 3
```

### 1282. `/SAPAPO/STYPES_DP`

```
T-Code: /SAPAPO/STYPES_DP
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 1283. `/SAPAPO/SUPCAT`

```
T-Code: /SAPAPO/SUPCAT
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → CTM
```

### 1284. `/SAPAPO/SYPP_AGGREGATE_ORD`

```
T-Code: /SAPAPO/SYPP_AGGREGATE_ORD
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1285. `/SAPAPO/SYPP_AGGR_KEY_FIG`

```
T-Code: /SAPAPO/SYPP_AGGR_KEY_FIG
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1286. `/SAPAPO/SYPP_AGGR_KEY_FIG_INT`

```
T-Code: /SAPAPO/SYPP_AGGR_KEY_FIG_INT
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1287. `/SAPAPO/SYPP_COUNT_ALERTS`

```
T-Code: /SAPAPO/SYPP_COUNT_ALERTS
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1288. `/SAPAPO/SYPP_CREATE_BUCORD`

```
T-Code: /SAPAPO/SYPP_CREATE_BUCORD
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1289. `/SAPAPO/SYPP_CREATE_BUCSTR`

```
T-Code: /SAPAPO/SYPP_CREATE_BUCSTR
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1290. `/SAPAPO/SYPP_CREATE_FC`

```
T-Code: /SAPAPO/SYPP_CREATE_FC
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1291. `/SAPAPO/SYPP_RES_CAPA_GET`

```
T-Code: /SAPAPO/SYPP_RES_CAPA_GET
└── SYPP_PLAN
    ├── ACTVT → 16
    └── PLAN_VERS → 0
```

### 1292. `/SAPAPO/TCATTR`

```
T-Code: /SAPAPO/TCATTR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/TCATTR
```

### 1293. `/SAPAPO/TCLSYS`

```
T-Code: /SAPAPO/TCLSYS
└── C_TCLA_BKA
    └── KLART → 230, 300, 400
```

### 1294. `/SAPAPO/TDS`

```
T-Code: /SAPAPO/TDS
├── C_APO_MALO
    ├── ACTVT → 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PPFUNC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_PPL
    ├── ACTVT → 2, 3
    ├── APO_PL_PPS → (empty — maintain in PFCG)
    └── APO_PPFUNC → (empty — maintain in PFCG)
└── C_APO_VERS
    ├── ACTVT → 16, 2, 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → A
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1295. `/SAPAPO/TIERDEF`

```
T-Code: /SAPAPO/TIERDEF
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1296. `/SAPAPO/TL2`

```
T-Code: /SAPAPO/TL2
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1297. `/SAPAPO/TL3`

```
T-Code: /SAPAPO/TL3
└── C_APO_MOD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    └── APO_MOD → (empty — maintain in PFCG)
```

### 1298. `/SAPAPO/TMREF`

```
T-Code: /SAPAPO/TMREF
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → &NC&
```

### 1299. `/SAPAPO/TMS_CAL`

```
T-Code: /SAPAPO/TMS_CAL
└── C_APO_CAL
    └── ACTVT → 1, 2, 3, 6
```

### 1300. `/SAPAPO/TPK`

```
T-Code: /SAPAPO/TPK
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TPKOG, /SAPAPO/V_TPKOS, /SAPAPO/V_TPKTF, /SAPAPO/V_TPKTY, /SAPAPO/V_TPTRM
```

### 1301. `/SAPAPO/TPOP_ASSIGN`

```
T-Code: /SAPAPO/TPOP_ASSIGN
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → 4
```

### 1302. `/SAPAPO/TPOP_POREL`

```
T-Code: /SAPAPO/TPOP_POREL
└── C_APO_ATP
    ├── ACTVT → 16
    └── APO_ATP_AR → 4
```

### 1303. `/SAPAPO/TRCOUPTM`

```
T-Code: /SAPAPO/TRCOUPTM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/TRCOUPTM
```

### 1304. `/SAPAPO/TRIGGER`

```
T-Code: /SAPAPO/TRIGGER
└── C_APO_UIF
    ├── /SCF/APPID → TRIGGER
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1305. `/SAPAPO/TR_CDC_QUOTA`

```
T-Code: /SAPAPO/TR_CDC_QUOTA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_QUOTA
```

### 1306. `/SAPAPO/TR_CDC_TLANE`

```
T-Code: /SAPAPO/TR_CDC_TLANE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_TLANE
```

### 1307. `/SAPAPO/TR_IGS_BPSEL`

```
T-Code: /SAPAPO/TR_IGS_BPSEL
└── S_BTCH_JOB
    ├── JOBACTION → (empty — maintain in PFCG)
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1308. `/SAPAPO/TR_IGS_BUTRM`

```
T-Code: /SAPAPO/TR_IGS_BUTRM
└── S_BTCH_JOB
    ├── JOBACTION → (empty — maintain in PFCG)
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1309. `/SAPAPO/TR_IGS_RPSEL`

```
T-Code: /SAPAPO/TR_IGS_RPSEL
└── S_BTCH_JOB
    ├── JOBACTION → (empty — maintain in PFCG)
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 1310. `/SAPAPO/TTC1`

```
T-Code: /SAPAPO/TTC1
└── C_APO_RES
    ├── ACTVT → 16
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 1311. `/SAPAPO/TTV1`

```
T-Code: /SAPAPO/TTV1
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/TTVALID
```

### 1312. `/SAPAPO/TZONE01`

```
T-Code: /SAPAPO/TZONE01
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TZONE1
```

### 1313. `/SAPAPO/TZONE03`

```
T-Code: /SAPAPO/TZONE03
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TZONE1
```

### 1314. `/SAPAPO/USRPLN`

```
T-Code: /SAPAPO/USRPLN
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1315. `/SAPAPO/VERCOP`

```
T-Code: /SAPAPO/VERCOP
└── C_APO_VERS
    ├── ACTVT → 1, 2
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1316. `/SAPAPO/VERCOP_PAR`

```
T-Code: /SAPAPO/VERCOP_PAR
└── C_APO_VERS
    ├── ACTVT → 1, 2
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1317. `/SAPAPO/VERCOP_RESTR`

```
T-Code: /SAPAPO/VERCOP_RESTR
└── C_APO_VERS
    ├── ACTVT → 1, 2
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1318. `/SAPAPO/VERCREATE`

```
T-Code: /SAPAPO/VERCREATE
└── C_APO_VERS
    ├── ACTVT → 1
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

### 1319. `/SAPAPO/VERDELLD`

```
T-Code: /SAPAPO/VERDELLD
└── C_APO_VERS
    ├── ACTVT → 6
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
```

## `/SAPBOQ/` — SAP BOQ — Bill of Quantities
> T-codes: **5**


### 1320. `/SAPBOQ/AC0X`

```
T-Code: /SAPBOQ/AC0X
└── I_TCODE
    └── TCD → AC03
```

### 1321. `/SAPBOQ/SES`

```
T-Code: /SAPBOQ/SES
├── V_VBAK_AAT
    ├── ACTVT → (empty — maintain in PFCG)
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 1322. `/SAPBOQ/SES1`

```
T-Code: /SAPBOQ/SES1
├── V_VBAK_AAT
    ├── ACTVT → (empty — maintain in PFCG)
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 1323. `/SAPBOQ/SES2`

```
T-Code: /SAPBOQ/SES2
├── V_VBAK_AAT
    ├── ACTVT → (empty — maintain in PFCG)
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 1324. `/SAPBOQ/SES_NR`

```
T-Code: /SAPBOQ/SES_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPBOQ/01
```

## `/SAPCE/` — SAP Construction Engineering
> T-codes: **63**


### 1325. `/SAPCE/14000289`

```
T-Code: /SAPCE/14000289
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_PRNR
```

### 1326. `/SAPCE/14000292`

```
T-Code: /SAPCE/14000292
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_VTRG
```

### 1327. `/SAPCE/14000312`

```
T-Code: /SAPCE/14000312
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKROTX2_V
```

### 1328. `/SAPCE/14000317`

```
T-Code: /SAPCE/14000317
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_RE_V
```

### 1329. `/SAPCE/69000006`

```
T-Code: /SAPCE/69000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKROVPLIM
```

### 1330. `/SAPCE/69000007`

```
T-Code: /SAPCE/69000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKROVPBLA
```

### 1331. `/SAPCE/69000008`

```
T-Code: /SAPCE/69000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKRO_VTAX
```

### 1332. `/SAPCE/69000009`

```
T-Code: /SAPCE/69000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_UT_V
```

### 1333. `/SAPCE/69000023`

```
T-Code: /SAPCE/69000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_DT_V
```

### 1334. `/SAPCE/69000024`

```
T-Code: /SAPCE/69000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_DF_V
```

### 1335. `/SAPCE/69000025`

```
T-Code: /SAPCE/69000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_AJ_V
```

### 1336. `/SAPCE/69000029`

```
T-Code: /SAPCE/69000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKPL_TXGV
```

### 1337. `/SAPCE/69000031`

```
T-Code: /SAPCE/69000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKSI_TXRV
```

### 1338. `/SAPCE/69000048`

```
T-Code: /SAPCE/69000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKSK_SECV
```

### 1339. `/SAPCE/69000049`

```
T-Code: /SAPCE/69000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_BT_V, /SAPCE/FKUA_BU_V
```

### 1340. `/SAPCE/69000054`

```
T-Code: /SAPCE/69000054
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_RC_V
```

### 1341. `/SAPCE/70000178`

```
T-Code: /SAPCE/70000178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_VTRA
```

### 1342. `/SAPCE/70000298`

```
T-Code: /SAPCE/70000298
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/TXV
```

### 1343. `/SAPCE/80000002`

```
T-Code: /SAPCE/80000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FK_NDCR02
```

### 1344. `/SAPCE/80000003`

```
T-Code: /SAPCE/80000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FK_NDCRV1
```

### 1345. `/SAPCE/80000101`

```
T-Code: /SAPCE/80000101
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_CAPP
```

### 1346. `/SAPCE/80000102`

```
T-Code: /SAPCE/80000102
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_CABP
```

### 1347. `/SAPCE/80000104`

```
T-Code: /SAPCE/80000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_SDM
```

### 1348. `/SAPCE/80000105`

```
T-Code: /SAPCE/80000105
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_FIM
```

### 1349. `/SAPCE/80000106`

```
T-Code: /SAPCE/80000106
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBGTXCOL
```

### 1350. `/SAPCE/80000110`

```
T-Code: /SAPCE/80000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKGR_MYF
```

### 1351. `/SAPCE/80000111`

```
T-Code: /SAPCE/80000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKGR_KEY
```

### 1352. `/SAPCE/80000113`

```
T-Code: /SAPCE/80000113
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/TXV
```

### 1353. `/SAPCE/FKBG_001Z`

```
T-Code: /SAPCE/FKBG_001Z
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_001Z
```

### 1354. `/SAPCE/FKBG_FIM`

```
T-Code: /SAPCE/FKBG_FIM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_FIM
```

### 1355. `/SAPCE/FKBG_SDM`

```
T-Code: /SAPCE/FKBG_SDM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBG_SDM
```

### 1356. `/SAPCE/FKBG_TXCOL`

```
T-Code: /SAPCE/FKBG_TXCOL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKBGTXCOL
```

### 1357. `/SAPCE/FKGR_LEDGER`

```
T-Code: /SAPCE/FKGR_LEDGER
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → F4
    └── RELTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KKVK_BEG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1358. `/SAPCE/FKPL_BPLEDG`

```
T-Code: /SAPCE/FKPL_BPLEDG
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → F4
    └── RELTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KKKO_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KKVK_BEG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1359. `/SAPCE/FKPL_TRIBAL`

```
T-Code: /SAPCE/FKPL_TRIBAL
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → F4
    └── RELTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KKVK_BEG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1360. `/SAPCE/FKPL_VATOVDUE`

```
T-Code: /SAPCE/FKPL_VATOVDUE
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── F_KKKO_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
```

### 1361. `/SAPCE/FKPL_VATREG`

```
T-Code: /SAPCE/FKPL_VATREG
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── F_KKKO_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
```

### 1362. `/SAPCE/FKRO_CLRE`

```
T-Code: /SAPCE/FKRO_CLRE
└── F_KKVK_BUK
    ├── ACTVT → 1, 2, 3
    └── BUKRS → $BUKRS
```

### 1363. `/SAPCE/FKRO_SL`

```
T-Code: /SAPCE/FKRO_SL
├── F_KKKO_BUK
    ├── ACTVT → 2
    └── BUKRS → $BUKRS
└── S_PROGRAM
    ├── P_ACTION → SUBMIT
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1364. `/SAPCE/FKSI_CASHINV`

```
T-Code: /SAPCE/FKSI_CASHINV
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── F_KKKO_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
```

### 1365. `/SAPCE/FKSI_INVUPD`

```
T-Code: /SAPCE/FKSI_INVUPD
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SAPCE/FKSI
    └── ALG_SUBOBJ → INV_UPDT
```

### 1366. `/SAPCE/FKSI_NDCRN`

```
T-Code: /SAPCE/FKSI_NDCRN
├── F_KKKO_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1367. `/SAPCE/FKUA_NRCOR`

```
T-Code: /SAPCE/FKUA_NRCOR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPCE/TCN
```

### 1368. `/SAPCE/FKUA_NRTAX`

```
T-Code: /SAPCE/FKUA_NRTAX
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPCE/TVN
```

### 1369. `/SAPCE/FKUA_REC`

```
T-Code: /SAPCE/FKUA_REC
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── F_KKVK_BEG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1370. `/SAPCE/FKUA_TAX`

```
T-Code: /SAPCE/FKUA_TAX
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/FKUA_TAX
```

### 1371. `/SAPCE/FK_CZSPER`

```
T-Code: /SAPCE/FK_CZSPER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/CZS
```

### 1372. `/SAPCE/FK_KDFPER`

```
T-Code: /SAPCE/FK_KDFPER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/KDF
```

### 1373. `/SAPCE/FK_NDCRN_REV_TRANSF_DOC`

```
T-Code: /SAPCE/FK_NDCRN_REV_TRANSF_DOC
└── F_KKKO_BUK
    ├── ACTVT → 85
    └── BUKRS → $BUKRS
```

### 1374. `/SAPCE/FK_SKSPER`

```
T-Code: /SAPCE/FK_SKSPER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/SKS
```

### 1375. `/SAPCE/FK_SK_SIPOUC`

```
T-Code: /SAPCE/FK_SK_SIPOUC
├── F_KKVK_BEG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1376. `/SAPCE/IMG_DISPLAY`

```
T-Code: /SAPCE/IMG_DISPLAY
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE, FINAL_VERS, RAW_VERS
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_PROJECT
    ├── ACTVT → 23, 3, 71, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43
    └── TTYPE → (empty — maintain in PFCG)
```

### 1377. `/SAPCE/IUBENMASS`

```
T-Code: /SAPCE/IUBENMASS
├── /SAPCE/IU0
    └── /SAPCE/IU0 → *
└── /SAPCE/IU1
    └── /SAPCE/IU0 → *
```

### 1378. `/SAPCE/IUEBEN2`

```
T-Code: /SAPCE/IUEBEN2
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1379. `/SAPCE/IUEBEN3`

```
T-Code: /SAPCE/IUEBEN3
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1380. `/SAPCE/IUESUB`

```
T-Code: /SAPCE/IUESUB
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1381. `/SAPCE/IURU_OKOGUENH`

```
T-Code: /SAPCE/IURU_OKOGUENH
└── S_TCODE
    └── TCD → /SAPCE/IURU_OKOGUMIG
```

### 1382. `/SAPCE/IURU_OKOGUMIG`

```
T-Code: /SAPCE/IURU_OKOGUMIG
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SAPCE/IURU
    └── ALG_SUBOBJ → OKOGU
└── S_TCODE
    └── TCD → /SAPCE/IURU_OKOGUENH
```

### 1383. `/SAPCE/IUUA_MA02`

```
T-Code: /SAPCE/IUUA_MA02
├── F_KKVK_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
└── F_KKVK_VKT
    ├── ACTVT → F4
    └── VKTYP_KK → (empty — maintain in PFCG)
```

### 1384. `/SAPCE/IU_CZ_IMG`

```
T-Code: /SAPCE/IU_CZ_IMG
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE, FINAL_VERS, RAW_VERS
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_PROJECT
    ├── ACTVT → 23, 3, 71, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43
    └── TTYPE → (empty — maintain in PFCG)
```

### 1385. `/SAPCE/IU_DPRQ5`

```
T-Code: /SAPCE/IU_DPRQ5
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43
    └── TTYPE → (empty — maintain in PFCG)
```

### 1386. `/SAPCE/SPLEDGER`

```
T-Code: /SAPCE/SPLEDGER
└── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
```

### 1387. `/SAPCE/TXPER`

```
T-Code: /SAPCE/TXPER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCE/TXV
```

## `/SAPCEM/` — SAP Construction Engineering & Management
> T-codes: **40**


### 1388. `/SAPCEM/12000436`

```
T-Code: /SAPCEM/12000436
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/PIVARLV
```

### 1389. `/SAPCEM/12000437`

```
T-Code: /SAPCEM/12000437
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/VARPI
```

### 1390. `/SAPCEM/12000438`

```
T-Code: /SAPCEM/12000438
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/PIBLART
```

### 1391. `/SAPCEM/12000439`

```
T-Code: /SAPCEM/12000439
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/WF_EQUI
```

### 1392. `/SAPCEM/12000446`

```
T-Code: /SAPCEM/12000446
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → J_3GLASE_M
```

### 1393. `/SAPCEM/12000447`

```
T-Code: /SAPCEM/12000447
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/PSPBUKRS
```

### 1394. `/SAPCEM/12000560`

```
T-Code: /SAPCEM/12000560
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCEM/DISPOGRU
```

### 1395. `/SAPCEM/ABRLISTE`

```
T-Code: /SAPCEM/ABRLISTE
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1396. `/SAPCEM/BDNUM`

```
T-Code: /SAPCEM/BDNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPCEM/BD
```

### 1397. `/SAPCEM/BD_SHOW`

```
T-Code: /SAPCEM/BD_SHOW
├── C_KLAH_BSE
    ├── ACTVT → 16
    └── BGRSE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 3
    └── TCD → /SAPCEM/BD_SHOW
```

### 1398. `/SAPCEM/BEDARF01`

```
T-Code: /SAPCEM/BEDARF01
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── CEM_ACTVT
    └── ACTVT → F4
├── C_KLAH_BSE
    ├── ACTVT → 16
    └── BGRSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 1
    └── TCD → /SAPCEM/BEDARF01
```

### 1399. `/SAPCEM/BEDARF02`

```
T-Code: /SAPCEM/BEDARF02
├── C_KLAH_BSE
    ├── ACTVT → 16
    └── BGRSE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 2
    └── TCD → /SAPCEM/BEDARF02
```

### 1400. `/SAPCEM/BEDARF03`

```
T-Code: /SAPCEM/BEDARF03
├── C_KLAH_BSE
    ├── ACTVT → 16
    └── BGRSE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 3
    └── TCD → /SAPCEM/BEDARF03
```

### 1401. `/SAPCEM/BESTAUF`

```
T-Code: /SAPCEM/BESTAUF
└── J_3G_TCODE
    ├── ACTVT → 16
    └── TCD → /SAPCEM/BESTAUF
```

### 1402. `/SAPCEM/BESTKON`

```
T-Code: /SAPCEM/BESTKON
└── J_3G_TCODE
    ├── ACTVT → 16
    └── TCD → /SAPCEM/BESTKON
```

### 1403. `/SAPCEM/CI`

```
T-Code: /SAPCEM/CI
└── C_KLAH_BKP
    ├── ACTVT → (empty — maintain in PFCG)
    └── BGRKP → (empty — maintain in PFCG)
```

### 1404. `/SAPCEM/EINSAT`

```
T-Code: /SAPCEM/EINSAT
├── I_IWERK
    ├── IWERK → $IWERK
    └── TCD → /SAPCEM/EINSAT
└── I_SWERK
    ├── SWERK → $SWERK
    └── TCD → /SAPCEM/EINSAT
```

### 1405. `/SAPCEM/INVENTUR01`

```
T-Code: /SAPCEM/INVENTUR01
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 1
    └── TCD → /SAPCEM/INVENTUR01
```

### 1406. `/SAPCEM/INVENTUR02`

```
T-Code: /SAPCEM/INVENTUR02
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 2
    └── TCD → /SAPCEM/INVENTUR02
```

### 1407. `/SAPCEM/INVENTUR03`

```
T-Code: /SAPCEM/INVENTUR03
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 3
    └── TCD → /SAPCEM/INVENTUR03
```

### 1408. `/SAPCEM/INV_ABGLEICH`

```
T-Code: /SAPCEM/INV_ABGLEICH
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
└── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
```

### 1409. `/SAPCEM/INV_AIB`

```
T-Code: /SAPCEM/INV_AIB
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1410. `/SAPCEM/INV_AUSBUM`

```
T-Code: /SAPCEM/INV_AUSBUM
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1411. `/SAPCEM/INV_BEST`

```
T-Code: /SAPCEM/INV_BEST
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1412. `/SAPCEM/INV_CHECK`

```
T-Code: /SAPCEM/INV_CHECK
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1413. `/SAPCEM/INV_DIFFLIST`

```
T-Code: /SAPCEM/INV_DIFFLIST
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1414. `/SAPCEM/INV_MENGE`

```
T-Code: /SAPCEM/INV_MENGE
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1415. `/SAPCEM/INV_MOVE`

```
T-Code: /SAPCEM/INV_MOVE
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
└── J_3G_TCODE
    ├── ACTVT → 1
    └── TCD → /SAPCEM/INV_MOVE
```

### 1416. `/SAPCEM/INV_OHNEEQUI`

```
T-Code: /SAPCEM/INV_OHNEEQUI
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1417. `/SAPCEM/INV_RUECK`

```
T-Code: /SAPCEM/INV_RUECK
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1418. `/SAPCEM/MCI_TCO`

```
T-Code: /SAPCEM/MCI_TCO
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → F4
├── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
├── J_3G_TCODE
    ├── ACTVT → 71
    └── TCD → /SAPCEM/MCI_TCO
└── M_INFO_ANL
    ├── ACTVT → 71
    ├── MCAPP → 7
    └── MCINF → S492
```

### 1419. `/SAPCEM/MCT_1`

```
T-Code: /SAPCEM/MCT_1
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── J_3G_TCODE
    ├── ACTVT → 71
    └── TCD → /SAPCEM/MCT_1
└── M_INFO_ANL
    ├── ACTVT → 71
    ├── MCAPP → 1
    └── MCINF → S490
```

### 1420. `/SAPCEM/MCT_2`

```
T-Code: /SAPCEM/MCT_2
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → F4
├── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
├── J_3G_TCODE
    ├── ACTVT → 71
    └── TCD → /SAPCEM/MCT_2
└── M_INFO_ANL
    ├── ACTVT → 71
    ├── MCAPP → 1
    └── MCINF → S491
```

### 1421. `/SAPCEM/MZ01`

```
T-Code: /SAPCEM/MZ01
└── J_3G_TCODE
    ├── ACTVT → 1
    └── TCD → /SAPCEM/MZ01
```

### 1422. `/SAPCEM/MZ02`

```
T-Code: /SAPCEM/MZ02
└── J_3G_TCODE
    ├── ACTVT → 2
    └── TCD → /SAPCEM/MZ02
```

### 1423. `/SAPCEM/MZ03`

```
T-Code: /SAPCEM/MZ03
├── C_TCLA_BKA
    └── KLART → 600
└── J_3G_TCODE
    ├── ACTVT → 3
    └── TCD → /SAPCEM/MZ03
```

### 1424. `/SAPCEM/MZ04`

```
T-Code: /SAPCEM/MZ04
└── J_3G_TCODE
    ├── ACTVT → 6
    └── TCD → /SAPCEM/MZ04
```

### 1425. `/SAPCEM/PI_CSS`

```
T-Code: /SAPCEM/PI_CSS
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1426. `/SAPCEM/PRIR`

```
T-Code: /SAPCEM/PRIR
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── J_3GABRLST
    ├── BUKRS → $BUKRS
    ├── GSBER → $GSBER
    └── WERKS → $WERKS
└── S_WEBGUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1427. `/SAPCEM/SDL`

```
T-Code: /SAPCEM/SDL
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
```

## `/SAPCND/` — SAP Condition Contract Management
> T-codes: **58**


### 1428. `/SAPCND/43000002`

```
T-Code: /SAPCND/43000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_DEPEND, /SAPCND/V_DTEL, /SAPCND/V_RELAT, /SAPCND/V_SC_BAS, /SAPCND/V_SC_FLD, /SAPCND/V_SC_VAR, /SAPCND/V_USAGE, /SAPCND/V_USG_FL, /SAPCND/V_US_FLD
```

### 1429. `/SAPCND/43000003`

```
T-Code: /SAPCND/43000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_APPLIC
```

### 1430. `/SAPCND/43000004`

```
T-Code: /SAPCND/43000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_TASK
```

### 1431. `/SAPCND/43000005`

```
T-Code: /SAPCND/43000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CONTEX
```

### 1432. `/SAPCND/68000405`

```
T-Code: /SAPCND/68000405
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T685
```

### 1433. `/SAPCND/68000406`

```
T-Code: /SAPCND/68000406
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_R3COMP
```

### 1434. `/SAPCND/68000633`

```
T-Code: /SAPCND/68000633
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T682, /SAPCND/V1_T682I, /SAPCND/V1_T682Z
```

### 1435. `/SAPCND/68000744`

```
T-Code: /SAPCND/68000744
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 1436. `/SAPCND/68000747`

```
T-Code: /SAPCND/68000747
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CNT_GR
```

### 1437. `/SAPCND/68000927`

```
T-Code: /SAPCND/68000927
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 1438. `/SAPCND/83000118`

```
T-Code: /SAPCND/83000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CNT_GR
```

### 1439. `/SAPCND/98000005`

```
T-Code: /SAPCND/98000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_UERANG
```

### 1440. `/SAPCND/98000006`

```
T-Code: /SAPCND/98000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_UEAATT, /SAPCND/V_UEAIMP, /SAPCND/V_UELATT, /SAPCND/V_UELIMP, /SAPCND/V_UETYP
```

### 1441. `/SAPCND/AO01`

```
T-Code: /SAPCND/AO01
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A
```

### 1442. `/SAPCND/AO03`

```
T-Code: /SAPCND/AO03
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → A
```

### 1443. `/SAPCND/AO04`

```
T-Code: /SAPCND/AO04
└── C_APO_ATP
    ├── ACTVT → 2, 3, 4, 6
    └── APO_ATP_AR → A
```

### 1444. `/SAPCND/AO05`

```
T-Code: /SAPCND/AO05
└── C_APO_ATP
    ├── ACTVT → 3, 4
    └── APO_ATP_AR → A
```

### 1445. `/SAPCND/AO06`

```
T-Code: /SAPCND/AO06
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A
```

### 1446. `/SAPCND/AO07`

```
T-Code: /SAPCND/AO07
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A
```

### 1447. `/SAPCND/AO08`

```
T-Code: /SAPCND/AO08
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A
```

### 1448. `/SAPCND/AO11`

```
T-Code: /SAPCND/AO11
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → 9
```

### 1449. `/SAPCND/AO12`

```
T-Code: /SAPCND/AO12
└── C_APO_ATP
    ├── ACTVT → 2, 3, 4, 6
    └── APO_ATP_AR → 9
```

### 1450. `/SAPCND/AO13`

```
T-Code: /SAPCND/AO13
└── C_APO_ATP
    ├── ACTVT → 3, 4
    └── APO_ATP_AR → 9
```

### 1451. `/SAPCND/AO14`

```
T-Code: /SAPCND/AO14
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → 9
```

### 1452. `/SAPCND/ARC1`

```
T-Code: /SAPCND/ARC1
└── S_ARCHIVE
    ├── ACTVT → 1, 2, 3
    ├── APPLIC → AM, BC, BW, CA, CO, CR, EB, EC, FI, GL, HR, IS, MC, MM, OC, PM, PP, PS, QM, SD, TR, XF
    └── ARCH_OBJ → CND_RECORD
```

### 1453. `/SAPCND/ARC2`

```
T-Code: /SAPCND/ARC2
└── S_TCODE
    └── TCD → /SAPCND/ARC2
```

### 1454. `/SAPCND/ARC3`

```
T-Code: /SAPCND/ARC3
└── S_TCODE
    └── TCD → /SAPCND/ARC3
```

### 1455. `/SAPCND/ARCC`

```
T-Code: /SAPCND/ARCC
└── S_ARCHIVE
    ├── ACTVT → 1, 2, 3
    ├── APPLIC → AM, BC, BW, CA, CO, CR, EB, EC, FI, GL, HR, IS, MC, MM, OC, PM, PP, PS, QM, SD, TR, XF
    └── ARCH_OBJ → CND_RECORD
```

### 1456. `/SAPCND/AU01`

```
T-Code: /SAPCND/AU01
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1457. `/SAPCND/AU03`

```
T-Code: /SAPCND/AU03
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → T
```

### 1458. `/SAPCND/AU04`

```
T-Code: /SAPCND/AU04
└── C_APO_ATP
    ├── ACTVT → 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1459. `/SAPCND/AU05`

```
T-Code: /SAPCND/AU05
└── C_APO_ATP
    ├── ACTVT → 3, 4
    └── APO_ATP_AR → T
```

### 1460. `/SAPCND/AU06`

```
T-Code: /SAPCND/AU06
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1461. `/SAPCND/AU07`

```
T-Code: /SAPCND/AU07
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1462. `/SAPCND/AU08`

```
T-Code: /SAPCND/AU08
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1463. `/SAPCND/AU11`

```
T-Code: /SAPCND/AU11
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → T
```

### 1464. `/SAPCND/AU12`

```
T-Code: /SAPCND/AU12
└── C_APO_ATP
    ├── ACTVT → 2, 3, 4, 6
    └── APO_ATP_AR → T
```

### 1465. `/SAPCND/AU13`

```
T-Code: /SAPCND/AU13
└── C_APO_ATP
    ├── ACTVT → 3, 4
    └── APO_ATP_AR → T
```

### 1466. `/SAPCND/AU14`

```
T-Code: /SAPCND/AU14
└── C_APO_ATP
    ├── ACTVT → 1, 3, 4
    └── APO_ATP_AR → T
```

### 1467. `/SAPCND/CONTEXT_GCM`

```
T-Code: /SAPCND/CONTEXT_GCM
└── /SAPCND/CM
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
```

### 1468. `/SAPCND/CTCT`

```
T-Code: /SAPCND/CTCT
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 1469. `/SAPCND/CT_ACC_SEQ`

```
T-Code: /SAPCND/CT_ACC_SEQ
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T682, /SAPCND/V1_T682I, /SAPCND/V1_T682Z
```

### 1470. `/SAPCND/CT_CNTXT_GR`

```
T-Code: /SAPCND/CT_CNTXT_GR
└── /SAPCND/CC
    ├── /SAPCND/AP → *
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → *
    └── ACTVT → *
```

### 1471. `/SAPCND/CT_COND_TYPE`

```
T-Code: /SAPCND/CT_COND_TYPE
└── /SAPCND/CC
    ├── /SAPCND/AP → *
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → *
    └── ACTVT → *
```

### 1472. `/SAPCND/CT_DET_PROC`

```
T-Code: /SAPCND/CT_DET_PROC
└── S_PROJECT
    ├── ACTVT → 2, 23, 3, 6, 71, 76, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
```

### 1473. `/SAPCND/CT_GCM_GRP`

```
T-Code: /SAPCND/CT_GCM_GRP
└── S_PROJECT
    ├── ACTVT → 2, 23, 3, 6, 71, 76, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
```

### 1474. `/SAPCND/MASS_GEN`

```
T-Code: /SAPCND/MASS_GEN
└── /SAPCND/CO
    └── ACTVT → 64, 65
```

### 1475. `/SAPCND/UEASS`

```
T-Code: /SAPCND/UEASS
└── /SAPCND/CC
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1476. `/SAPCND/UELIB`

```
T-Code: /SAPCND/UELIB
└── /SAPCND/CC
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1477. `/SAPCND/UERNG`

```
T-Code: /SAPCND/UERNG
└── /SAPCND/CC
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1478. `/SAPCND/UE_DEV`

```
T-Code: /SAPCND/UE_DEV
└── S_GUI
    └── ACTVT → 61
```

### 1479. `/SAPCND/VK01`

```
T-Code: /SAPCND/VK01
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A, T
```

### 1480. `/SAPCND/VK03`

```
T-Code: /SAPCND/VK03
└── V_KONG_VWE
    ├── ACTVT → 1
    ├── KAPPL → (empty — maintain in PFCG)
    └── KVEWE → (empty — maintain in PFCG)
```

### 1481. `/SAPCND/VK04`

```
T-Code: /SAPCND/VK04
└── V_KONG_VWE
    ├── ACTVT → 2, 3
    ├── KAPPL → (empty — maintain in PFCG)
    └── KVEWE → (empty — maintain in PFCG)
```

### 1482. `/SAPCND/VK05`

```
T-Code: /SAPCND/VK05
├── C_APO_ATP
    ├── ACTVT → 3, 4
    └── APO_ATP_AR → A
└── V_KONG_VWE
    ├── ACTVT → 3
    ├── KAPPL → RB
    └── KVEWE → O
```

### 1483. `/SAPCND/VKVE`

```
T-Code: /SAPCND/VKVE
├── V_KONH_VKO
    ├── ACTVT → 2, 3
    ├── APO_CLASS → (empty — maintain in PFCG)
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── KAPPL → (empty — maintain in PFCG)
    ├── KVEWE → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    ├── PROJ_CONF → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── TLANGUAGE → (empty — maintain in PFCG)
    ├── TRANOBJ → (empty — maintain in PFCG)
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
└── V_KONH_VKS
    ├── ACTVT → 2, 3
    ├── APO_CLASS → (empty — maintain in PFCG)
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── KAPPL → (empty — maintain in PFCG)
    ├── KSCHL → (empty — maintain in PFCG)
    ├── KVEWE → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    ├── PROJ_CONF → (empty — maintain in PFCG)
    ├── TLANGUAGE → (empty — maintain in PFCG)
    └── TRANOBJ → (empty — maintain in PFCG)
```

### 1484. `/SAPCND/VKVI`

```
T-Code: /SAPCND/VKVI
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A, T
```

### 1485. `/SAPCND/VOFM`

```
T-Code: /SAPCND/VOFM
└── C_APO_ATP
    ├── ACTVT → 1, 2, 3, 4, 6
    └── APO_ATP_AR → A, T
```

## `/SAPDII/` — SAP Digital Integration Infrastructure
> T-codes: **19**


### 1486. `/SAPDII/71000002`

```
T-Code: /SAPDII/71000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWB_AC
```

### 1487. `/SAPDII/71000003`

```
T-Code: /SAPDII/71000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWB_ACT
```

### 1488. `/SAPDII/71000004`

```
T-Code: /SAPDII/71000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWBDS
```

### 1489. `/SAPDII/71000005`

```
T-Code: /SAPDII/71000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWB_DST
```

### 1490. `/SAPDII/71000006`

```
T-Code: /SAPDII/71000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWB_ACC
```

### 1491. `/SAPDII/71000007`

```
T-Code: /SAPDII/71000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CWTYSTAT
```

### 1492. `/SAPDII/71000008`

```
T-Code: /SAPDII/71000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CWTYWBST
```

### 1493. `/SAPDII/71000009`

```
T-Code: /SAPDII/71000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CAPPID
```

### 1494. `/SAPDII/71000010`

```
T-Code: /SAPDII/71000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CAPPROP
```

### 1495. `/SAPDII/71000011`

```
T-Code: /SAPDII/71000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDWB_ACM
```

### 1496. `/SAPDII/71000012`

```
T-Code: /SAPDII/71000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CAPPSET
```

### 1497. `/SAPDII/71000013`

```
T-Code: /SAPDII/71000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CEXINTID
```

### 1498. `/SAPDII/71000014`

```
T-Code: /SAPDII/71000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CEXTINT
```

### 1499. `/SAPDII/71000015`

```
T-Code: /SAPDII/71000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDOCPROF
```

### 1500. `/SAPDII/71000016`

```
T-Code: /SAPDII/71000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CDOCPROT
```

### 1501. `/SAPDII/71000017`

```
T-Code: /SAPDII/71000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CSYSRFCM
```

### 1502. `/SAPDII/71000018`

```
T-Code: /SAPDII/71000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CPRSYSM
```

### 1503. `/SAPDII/71000019`

```
T-Code: /SAPDII/71000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/VUSPRMAP
```

### 1504. `/SAPDII/71000020`

```
T-Code: /SAPDII/71000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPDII/CIMGMANT
```

## `/SAPDMC/` — SAP Digital Manufacturing Cloud
> T-codes: **1**


### 1505. `/SAPDMC/LSMW`

```
T-Code: /SAPDMC/LSMW
└── B_LSMW
    ├── ACTVT → 3
    └── TCD → LSMW
```

## `/SAPF15/` — SAP Financial Closing (F15)
> T-codes: **13**


### 1506. `/SAPF15/BEL_FREI_DUE`

```
T-Code: /SAPF15/BEL_FREI_DUE
├── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 31
├── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
├── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SAPF15/BELEG_FREIGABE_DUE
└── S_PROGRAM
    ├── P_ACTION → SUBMIT
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1507. `/SAPF15/DT_LOESCHANF`

```
T-Code: /SAPF15/DT_LOESCHANF
├── /SAPF15/BU
    ├── ACTVT → 31
    └── BUKRS → $BUKRS
├── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 31
└── F_BKPF_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1508. `/SAPF15/F15H_LISTE_E`

```
T-Code: /SAPF15/F15H_LISTE_E
└── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 1509. `/SAPF15/FMITBACT`

```
T-Code: /SAPF15/FMITBACT
├── S_TABU_DIS
    ├── ACTVT → 3
    └── DICBERCLS → /F15
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPF15/FMITBACT
```

### 1510. `/SAPF15/FREIGABE`

```
T-Code: /SAPF15/FREIGABE
└── /SAPF15/BU
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1511. `/SAPF15/FREIG_A`

```
T-Code: /SAPF15/FREIG_A
└── /SAPF15/BU
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1512. `/SAPF15/KZ_BELEGE_LI`

```
T-Code: /SAPF15/KZ_BELEGE_LI
├── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 3
└── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
```

### 1513. `/SAPF15/KZ_FORD`

```
T-Code: /SAPF15/KZ_FORD
├── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 3
└── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
```

### 1514. `/SAPF15/KZ_FORD_EINS`

```
T-Code: /SAPF15/KZ_FORD_EINS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → /F15
```

### 1515. `/SAPF15/SM_STORNO`

```
T-Code: /SAPF15/SM_STORNO
├── /SAPF15/BU
    ├── ACTVT → 36
    └── BUKRS → $BUKRS
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → /SAPF15/
```

### 1516. `/SAPF15/TELKOVSL`

```
T-Code: /SAPF15/TELKOVSL
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → /F15
```

### 1517. `/SAPF15/ZUEV101_ABGL`

```
T-Code: /SAPF15/ZUEV101_ABGL
├── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → /SAPF15/
```

### 1518. `/SAPF15/ZUEV101_LIST`

```
T-Code: /SAPF15/ZUEV101_LIST
├── /SAPF15/BW
    ├── /SAPF15/BW → (empty — maintain in PFCG)
    └── ACTVT → 3
├── F_BKPF_BUK
    ├── ACTVT → 3
    └── BUKRS → $BUKRS
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → /SAPF15/
```

## `/SAPMP/` — SAP Master Data Management (MDM)
> T-codes: **20**


### 1519. `/SAPMP/12001171`

```
T-Code: /SAPMP/12001171
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/RC_T001RV, /SAPMP/RC_T001TV, /SAPMP/RC_T001_V
```

### 1520. `/SAPMP/12001172`

```
T-Code: /SAPMP/12001172
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/RC_T002_V, /SAPMP/RC_T003_V
```

### 1521. `/SAPMP/12001221`

```
T-Code: /SAPMP/12001221
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/LBA_CUST
```

### 1522. `/SAPMP/12001267`

```
T-Code: /SAPMP/12001267
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/SD_LGFD, /SAPMP/SD_LGFK, /SAPMP/SD_LGPR, /SAPMP/SD_LGST_V
```

### 1523. `/SAPMP/12001273`

```
T-Code: /SAPMP/12001273
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/CHARACT_V
```

### 1524. `/SAPMP/15000029`

```
T-Code: /SAPMP/15000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/CMTSBV
```

### 1525. `/SAPMP/97000004`

```
T-Code: /SAPMP/97000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/V_TATRKZ
```

### 1526. `/SAPMP/97000045`

```
T-Code: /SAPMP/97000045
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/PP_FHM_WC
```

### 1527. `/SAPMP/CHARACT`

```
T-Code: /SAPMP/CHARACT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/CHARACT_V
```

### 1528. `/SAPMP/CMTSBV_STATUS`

```
T-Code: /SAPMP/CMTSBV_STATUS
├── C_AFKO_ATY
    ├── ACTVT → 2, 3
    └── AUTYP → 10
├── C_AFKO_AWA
    ├── ACTVT → 2, 3
    ├── AUFART → (empty — maintain in PFCG)
    ├── AUTYP → 10
    └── WERKS → $WERKS
└── C_AFKO_AWK
    ├── AUFART → (empty — maintain in PFCG)
    └── WERKS → $WERKS
```

### 1529. `/SAPMP/COCA`

```
T-Code: /SAPMP/COCA
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 6, 78, 90
    └── TTYPE → CLCP, CUST, DTRA, MOVE, PATC, PIEC, TASK, TRAN
```

### 1530. `/SAPMP/CUST_LGPR`

```
T-Code: /SAPMP/CUST_LGPR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/SD_LGPR
```

### 1531. `/SAPMP/OATRKZ_PLA_OC`

```
T-Code: /SAPMP/OATRKZ_PLA_OC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/V_ATRKZ_P
```

### 1532. `/SAPMP/OTATRKZ_OB_OC`

```
T-Code: /SAPMP/OTATRKZ_OB_OC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/V_TATRKZ
```

### 1533. `/SAPMP/PP_FHM_WC`

```
T-Code: /SAPMP/PP_FHM_WC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPMP/PP_FHM_WC
```

### 1534. `/SAPMP/RTSQMT`

```
T-Code: /SAPMP/RTSQMT
└── /SAPMP/RTS
    └── WERKS → $WERKS
```

### 1535. `/SAPMP/SEROU`

```
T-Code: /SAPMP/SEROU
└── B_ALE_MAST
    └── EDI_MES → /SAPMP/ROUTING
```

### 1536. `/SAPMP/STOCK`

```
T-Code: /SAPMP/STOCK
├── M_BEST_BSA
    ├── ACTVT → 2, 8, 9
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 2, 8, 9
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 2, 8, 9
    └── EKORG → $EKORG
└── M_BEST_WRK
    ├── ACTVT → 2, 8, 9
    └── WERKS → $WERKS
```

### 1537. `/SAPMP/WEHB`

```
T-Code: /SAPMP/WEHB
├── M_BEST_EKG
    ├── ACTVT → (empty — maintain in PFCG)
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → (empty — maintain in PFCG)
    └── EKORG → $EKORG
└── M_BEST_WRK
    ├── ACTVT → (empty — maintain in PFCG)
    └── WERKS → $WERKS
```

### 1538. `/SAPMP/WEHF`

```
T-Code: /SAPMP/WEHF
├── C_AFKO_ATY
    ├── ACTVT → 2
    └── AUTYP → 10
├── C_AFKO_AWA
    ├── ACTVT → 2
    ├── AUFART → (empty — maintain in PFCG)
    ├── AUTYP → 10
    └── WERKS → $WERKS
└── C_AFKO_AWK
    ├── AUFART → (empty — maintain in PFCG)
    └── WERKS → $WERKS
```

## `/SAPNEA/` — SAP Near East & Africa Localization
> T-codes: **8**


### 1539. `/SAPNEA/01001906`

```
T-Code: /SAPNEA/01001906
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPNEA/JSC01
```

### 1540. `/SAPNEA/01001951`

```
T-Code: /SAPNEA/01001951
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPNEA/J_SC_ACT
```

### 1541. `/SAPNEA/06000189`

```
T-Code: /SAPNEA/06000189
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T001F
```

### 1542. `/SAPNEA/06000204`

```
T-Code: /SAPNEA/06000204
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TAKOF
```

### 1543. `/SAPNEA/06000205`

```
T-Code: /SAPNEA/06000205
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_THKON
```

### 1544. `/SAPNEA/06000380`

```
T-Code: /SAPNEA/06000380
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → VV_T685A_VA
```

### 1545. `/SAPNEA/60000001`

```
T-Code: /SAPNEA/60000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPNEA/J_SC_PSY
```

### 1546. `/SAPNEA/FORMSW`

```
T-Code: /SAPNEA/FORMSW
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPNEA/T001F_SW
```

## `/SAPPCE/` — SAP PCE — Process Control Engineering
> T-codes: **66**


### 1547. `/SAPPCE/01001764`

```
T-Code: /SAPPCE/01001764
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/JVA_T8JZ
```

### 1548. `/SAPPCE/06000011`

```
T-Code: /SAPPCE/06000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/VJVAPROF
```

### 1549. `/SAPPCE/06000013`

```
T-Code: /SAPPCE/06000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/VJVAROLE
```

### 1550. `/SAPPCE/12001093`

```
T-Code: /SAPPCE/12001093
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC02
```

### 1551. `/SAPPCE/12001095`

```
T-Code: /SAPPCE/12001095
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC03
```

### 1552. `/SAPPCE/12001096`

```
T-Code: /SAPPCE/12001096
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC05
```

### 1553. `/SAPPCE/12001097`

```
T-Code: /SAPPCE/12001097
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC07
```

### 1554. `/SAPPCE/12001098`

```
T-Code: /SAPPCE/12001098
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC09
```

### 1555. `/SAPPCE/12001099`

```
T-Code: /SAPPCE/12001099
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC12
```

### 1556. `/SAPPCE/12001101`

```
T-Code: /SAPPCE/12001101
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TPNV01, /SAPPCE/V_TPNV02, /SAPPCE/V_TPNV14, /SAPPCE/V_TPNV21
```

### 1557. `/SAPPCE/12001109`

```
T-Code: /SAPPCE/12001109
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TPNV03
```

### 1558. `/SAPPCE/12001112`

```
T-Code: /SAPPCE/12001112
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/PNV01
```

### 1559. `/SAPPCE/12001115`

```
T-Code: /SAPPCE/12001115
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC13
```

### 1560. `/SAPPCE/12001116`

```
T-Code: /SAPPCE/12001116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TPNV04
```

### 1561. `/SAPPCE/12001118`

```
T-Code: /SAPPCE/12001118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TPNV05
```

### 1562. `/SAPPCE/12001120`

```
T-Code: /SAPPCE/12001120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TPNV06
```

### 1563. `/SAPPCE/12001152`

```
T-Code: /SAPPCE/12001152
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TORG01
```

### 1564. `/SAPPCE/12001162`

```
T-Code: /SAPPCE/12001162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TORG02
```

### 1565. `/SAPPCE/24000002`

```
T-Code: /SAPPCE/24000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TPNV11
```

### 1566. `/SAPPCE/24000003`

```
T-Code: /SAPPCE/24000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TPNV13
```

### 1567. `/SAPPCE/24000004`

```
T-Code: /SAPPCE/24000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/PNVIND
```

### 1568. `/SAPPCE/24000006`

```
T-Code: /SAPPCE/24000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TPNV08
```

### 1569. `/SAPPCE/24000010`

```
T-Code: /SAPPCE/24000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TPNV15
```

### 1570. `/SAPPCE/24000012`

```
T-Code: /SAPPCE/24000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/DPC01
```

### 1571. `/SAPPCE/50000132`

```
T-Code: /SAPPCE/50000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_PODPC
```

### 1572. `/SAPPCE/50000138`

```
T-Code: /SAPPCE/50000138
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/DPC_V_CT
```

### 1573. `/SAPPCE/50000139`

```
T-Code: /SAPPCE/50000139
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/DPC04
```

### 1574. `/SAPPCE/50000140`

```
T-Code: /SAPPCE/50000140
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_SGL2RA
```

### 1575. `/SAPPCE/50000222`

```
T-Code: /SAPPCE/50000222
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPCSO
```

### 1576. `/SAPPCE/50000223`

```
T-Code: /SAPPCE/50000223
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPCIN
```

### 1577. `/SAPPCE/50000225`

```
T-Code: /SAPPCE/50000225
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC15
```

### 1578. `/SAPPCE/50000226`

```
T-Code: /SAPPCE/50000226
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC14
```

### 1579. `/SAPPCE/50000227`

```
T-Code: /SAPPCE/50000227
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC16
```

### 1580. `/SAPPCE/50000228`

```
T-Code: /SAPPCE/50000228
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC17
```

### 1581. `/SAPPCE/50000229`

```
T-Code: /SAPPCE/50000229
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC18
```

### 1582. `/SAPPCE/50000230`

```
T-Code: /SAPPCE/50000230
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC30
```

### 1583. `/SAPPCE/50000234`

```
T-Code: /SAPPCE/50000234
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC7O
```

### 1584. `/SAPPCE/50000244`

```
T-Code: /SAPPCE/50000244
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC32
```

### 1585. `/SAPPCE/50000258`

```
T-Code: /SAPPCE/50000258
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPCSO
```

### 1586. `/SAPPCE/50000259`

```
T-Code: /SAPPCE/50000259
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPCIN
```

### 1587. `/SAPPCE/50000260`

```
T-Code: /SAPPCE/50000260
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPCIN
```

### 1588. `/SAPPCE/69000002`

```
T-Code: /SAPPCE/69000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/V_TDPC3O
```

### 1589. `/SAPPCE/69000174`

```
T-Code: /SAPPCE/69000174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TDPC70
```

### 1590. `/SAPPCE/69000175`

```
T-Code: /SAPPCE/69000175
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/TDPC71
```

### 1591. `/SAPPCE/DPCD01`

```
T-Code: /SAPPCE/DPCD01
├── C_PRPS_ART
    ├── PS_ACTVT → 26
    └── PS_PRART → (empty — maintain in PFCG)
├── C_PRPS_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → 26
├── C_PRPS_KST
    ├── PS_ACTVT → 26
    ├── PS_FKOKR → (empty — maintain in PFCG)
    └── PS_FKSTL → (empty — maintain in PFCG)
├── C_PRPS_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 26
├── C_PRPS_VNR
    ├── PS_ACTVT → 26
    └── PS_VERNR → (empty — maintain in PFCG)
└── J_DI_DPC
    ├── /SAPPCE/TP → (empty — maintain in PFCG)
    ├── ACTVT → 3
    ├── BUKRS → $BUKRS
    └── KOART → $KOART
```

### 1592. `/SAPPCE/DPCK01`

```
T-Code: /SAPPCE/DPCK01
├── C_PRPS_ART
    ├── PS_ACTVT → 24
    └── PS_PRART → (empty — maintain in PFCG)
├── C_PRPS_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → 24
├── C_PRPS_KST
    ├── PS_ACTVT → 24
    ├── PS_FKOKR → (empty — maintain in PFCG)
    └── PS_FKSTL → (empty — maintain in PFCG)
├── C_PRPS_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 24
├── C_PRPS_VNR
    ├── PS_ACTVT → 24
    └── PS_VERNR → (empty — maintain in PFCG)
└── J_DI_DPC
    ├── /SAPPCE/TP → (empty — maintain in PFCG)
    ├── ACTVT → 3
    ├── BUKRS → $BUKRS
    └── KOART → $KOART
```

### 1593. `/SAPPCE/DPCNKR`

```
T-Code: /SAPPCE/DPCNKR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPPCE/DP
```

### 1594. `/SAPPCE/DPC_LIST_C`

```
T-Code: /SAPPCE/DPC_LIST_C
├── C_AFKO_ACT
    └── PS_ACTVT → 24, 26
├── C_PROJ_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → 24, 26
├── C_PROJ_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 24
├── C_PROJ_VNR
    ├── PS_ACTVT → 24
    └── PS_VERNR → (empty — maintain in PFCG)
├── C_PRPS_ART
    ├── PS_ACTVT → 24
    └── PS_PRART → (empty — maintain in PFCG)
├── C_PRPS_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → 24
├── C_PRPS_KST
    ├── PS_ACTVT → 24
    ├── PS_FKOKR → (empty — maintain in PFCG)
    └── PS_FKSTL → (empty — maintain in PFCG)
├── C_PRPS_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 24
├── C_PRPS_VNR
    ├── PS_ACTVT → 24
    └── PS_VERNR → (empty — maintain in PFCG)
└── J_DI_DPC
    ├── /SAPPCE/TP → (empty — maintain in PFCG)
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    └── KOART → $KOART
```

### 1595. `/SAPPCE/DPC_LIST_D`

```
T-Code: /SAPPCE/DPC_LIST_D
├── C_PROJ_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → (empty — maintain in PFCG)
├── C_PROJ_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → (empty — maintain in PFCG)
├── C_PROJ_VNR
    ├── PS_ACTVT → 26
    └── PS_VERNR → (empty — maintain in PFCG)
├── C_PRPS_ART
    ├── PS_ACTVT → 26
    └── PS_PRART → (empty — maintain in PFCG)
├── C_PRPS_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → (empty — maintain in PFCG)
├── C_PRPS_KST
    ├── PS_ACTVT → 26
    ├── PS_FKOKR → (empty — maintain in PFCG)
    └── PS_FKSTL → (empty — maintain in PFCG)
├── C_PRPS_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 26
├── C_PRPS_VNR
    ├── PS_ACTVT → 26
    └── PS_VERNR → (empty — maintain in PFCG)
└── J_DI_DPC
    ├── /SAPPCE/TP → (empty — maintain in PFCG)
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    └── KOART → $KOART
```

### 1596. `/SAPPCE/JVA_ECP_ACT`

```
T-Code: /SAPPCE/JVA_ECP_ACT
└── J_JVA_PRC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    └── JV_PROCA → (empty — maintain in PFCG)
```

### 1597. `/SAPPCE/JVA_PART`

```
T-Code: /SAPPCE/JVA_PART
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPCE/JVA_8JOV
```

### 1598. `/SAPPCE/JVA_VENCH`

```
T-Code: /SAPPCE/JVA_VENCH
├── J_JVA_JOA
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    └── JV_JOA_SUB → (empty — maintain in PFCG)
└── J_JVA_VNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    ├── JV_VCLASS → (empty — maintain in PFCG)
    └── JV_VTYPE → (empty — maintain in PFCG)
```

### 1599. `/SAPPCE/JVA_VENCR`

```
T-Code: /SAPPCE/JVA_VENCR
├── J_JVA_JOA
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    └── JV_JOA_SUB → (empty — maintain in PFCG)
└── J_JVA_VNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    ├── JV_VCLASS → (empty — maintain in PFCG)
    └── JV_VTYPE → (empty — maintain in PFCG)
```

### 1600. `/SAPPCE/JVA_VENDIS`

```
T-Code: /SAPPCE/JVA_VENDIS
├── J_JVA_JOA
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    └── JV_JOA_SUB → (empty — maintain in PFCG)
└── J_JVA_VNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── JV_ACLASS → (empty — maintain in PFCG)
    ├── JV_VCLASS → (empty — maintain in PFCG)
    └── JV_VTYPE → (empty — maintain in PFCG)
```

### 1601. `/SAPPCE/OIC`

```
T-Code: /SAPPCE/OIC
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
├── F_BKPF_GSB
    ├── ACTVT → (empty — maintain in PFCG)
    └── GSBER → $GSBER
├── F_BKPF_KOA
    ├── ACTVT → (empty — maintain in PFCG)
    └── KOART → $KOART
├── F_IT_ALV
    └── ACTVT → 3
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
└── F_LFA1_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1602. `/SAPPCE/OIGL`

```
T-Code: /SAPPCE/OIGL
├── F_BKPF_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
├── F_BKPF_KOA
    ├── ACTVT → (empty — maintain in PFCG)
    └── KOART → $KOART
├── F_IT_ALV
    └── ACTVT → (empty — maintain in PFCG)
└── F_SKA1_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1603. `/SAPPCE/OIV`

```
T-Code: /SAPPCE/OIV
├── F_BKPF_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
├── F_BKPF_KOA
    ├── ACTVT → (empty — maintain in PFCG)
    └── KOART → $KOART
├── F_IT_ALV
    └── ACTVT → (empty — maintain in PFCG)
└── F_LFA1_BUK
    ├── ACTVT → (empty — maintain in PFCG)
    └── BUKRS → $BUKRS
```

### 1604. `/SAPPCE/ORG01`

```
T-Code: /SAPPCE/ORG01
├── PLOG
    ├── INFOTYP → 1000, 1001
    ├── ISTAT → (empty — maintain in PFCG)
    ├── OTYPE → C, O, P, S, T, US
    ├── PLVAR → $PLVAR
    ├── PPFCODE → (empty — maintain in PFCG)
    └── SUBTYP → (empty — maintain in PFCG)
├── P_ORGIN
    ├── AUTHC → (empty — maintain in PFCG)
    ├── INFTY → 0, 1, 2, 3
    ├── PERSA → (empty — maintain in PFCG)
    ├── PERSG → (empty — maintain in PFCG)
    ├── PERSK → (empty — maintain in PFCG)
    ├── SUBTY → (empty — maintain in PFCG)
    └── VDSK1 → (empty — maintain in PFCG)
└── P_TCODE
    └── TCD → PPOME
```

### 1605. `/SAPPCE/ORG02`

```
T-Code: /SAPPCE/ORG02
├── PLOG
    ├── INFOTYP → 1000, 1001
    ├── ISTAT → (empty — maintain in PFCG)
    ├── OTYPE → C, O, P, S, T, US
    ├── PLVAR → $PLVAR
    ├── PPFCODE → (empty — maintain in PFCG)
    └── SUBTYP → (empty — maintain in PFCG)
├── P_ORGIN
    ├── AUTHC → (empty — maintain in PFCG)
    ├── INFTY → 0, 1, 2, 3
    ├── PERSA → (empty — maintain in PFCG)
    ├── PERSG → (empty — maintain in PFCG)
    ├── PERSK → (empty — maintain in PFCG)
    ├── SUBTY → (empty — maintain in PFCG)
    └── VDSK1 → (empty — maintain in PFCG)
└── P_TCODE
    └── TCD → PPOME
```

### 1606. `/SAPPCE/ORG03`

```
T-Code: /SAPPCE/ORG03
├── PLOG
    ├── INFOTYP → 1000, 1001
    ├── ISTAT → (empty — maintain in PFCG)
    ├── OTYPE → C, O, P, S, T, US
    ├── PLVAR → $PLVAR
    ├── PPFCODE → (empty — maintain in PFCG)
    └── SUBTYP → (empty — maintain in PFCG)
├── P_ORGIN
    ├── AUTHC → (empty — maintain in PFCG)
    ├── INFTY → 0, 1, 2, 3
    ├── PERSA → (empty — maintain in PFCG)
    ├── PERSG → (empty — maintain in PFCG)
    ├── PERSK → (empty — maintain in PFCG)
    ├── SUBTY → (empty — maintain in PFCG)
    └── VDSK1 → (empty — maintain in PFCG)
└── P_TCODE
    └── TCD → PPOME
```

### 1607. `/SAPPCE/PNV01`

```
T-Code: /SAPPCE/PNV01
└── J_DI_PNV01
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── PRCTR → $PRCTR
    └── VERNR → (empty — maintain in PFCG)
```

### 1608. `/SAPPCE/PNV03`

```
T-Code: /SAPPCE/PNV03
└── J_DI_PNV01
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── PRCTR → $PRCTR
    └── VERNR → (empty — maintain in PFCG)
```

### 1609. `/SAPPCE/PNV04`

```
T-Code: /SAPPCE/PNV04
└── J_DI_PNV01
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── PRCTR → $PRCTR
    └── VERNR → (empty — maintain in PFCG)
```

### 1610. `/SAPPCE/PNV05`

```
T-Code: /SAPPCE/PNV05
└── J_DI_PNV01
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── PRCTR → $PRCTR
    └── VERNR → (empty — maintain in PFCG)
```

### 1611. `/SAPPCE/PNV07`

```
T-Code: /SAPPCE/PNV07
└── J_DI_PNV01
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BUKRS → $BUKRS
    ├── KOKRS → $KOKRS
    ├── PRCTR → $PRCTR
    └── VERNR → (empty — maintain in PFCG)
```

### 1612. `/SAPPCE/PVNNUM`

```
T-Code: /SAPPCE/PVNNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPPCE/PN
```

## `/SAPPO/` — SAP Portfolio & Project Management (PPM)
> T-codes: **41**


### 1613. `/SAPPO/16000009`

```
T-Code: /SAPPO/16000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VNF_PWBD, /SAPPO/VNF_US, /SAPPO/VNF_WL
```

### 1614. `/SAPPO/21000024`

```
T-Code: /SAPPO/21000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/V_HINT
```

### 1615. `/SAPPO/21000026`

```
T-Code: /SAPPO/21000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/CO_POPUP
```

### 1616. `/SAPPO/21000027`

```
T-Code: /SAPPO/21000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/V_BOBJ_BP
```

### 1617. `/SAPPO/21000079`

```
T-Code: /SAPPO/21000079
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VRESITIME
```

### 1618. `/SAPPO/26000050`

```
T-Code: /SAPPO/26000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VC_CMPNT
```

### 1619. `/SAPPO/26000051`

```
T-Code: /SAPPO/26000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VCMP_ASG
```

### 1620. `/SAPPO/26000058`

```
T-Code: /SAPPO/26000058
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VWL
```

### 1621. `/SAPPO/72000176`

```
T-Code: /SAPPO/72000176
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VSCRN_HDR
```

### 1622. `/SAPPO/72000177`

```
T-Code: /SAPPO/72000177
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VC_BPROC
```

### 1623. `/SAPPO/72000178`

```
T-Code: /SAPPO/72000178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VA_BPROC
```

### 1624. `/SAPPO/72000179`

```
T-Code: /SAPPO/72000179
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VP_BPROC
```

### 1625. `/SAPPO/72000180`

```
T-Code: /SAPPO/72000180
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VREACT_DL
```

### 1626. `/SAPPO/72000181`

```
T-Code: /SAPPO/72000181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VWLRANGE
```

### 1627. `/SAPPO/72000182`

```
T-Code: /SAPPO/72000182
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VWLUNIT
```

### 1628. `/SAPPO/72000183`

```
T-Code: /SAPPO/72000183
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → PPO1
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VOBJECT
```

### 1629. `/SAPPO/72000184`

```
T-Code: /SAPPO/72000184
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VOBJ_ASG
```

### 1630. `/SAPPO/72000185`

```
T-Code: /SAPPO/72000185
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VOBJ_BOR
```

### 1631. `/SAPPO/72000186`

```
T-Code: /SAPPO/72000186
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/V_F_ASG
```

### 1632. `/SAPPO/72000187`

```
T-Code: /SAPPO/72000187
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VSCRN_ASG
```

### 1633. `/SAPPO/72000188`

```
T-Code: /SAPPO/72000188
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VFA_FLDS
```

### 1634. `/SAPPO/72000189`

```
T-Code: /SAPPO/72000189
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VVALID_OT
```

### 1635. `/SAPPO/72000190`

```
T-Code: /SAPPO/72000190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VSRV_GRP
```

### 1636. `/SAPPO/72000195`

```
T-Code: /SAPPO/72000195
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/V_USER
```

### 1637. `/SAPPO/72000244`

```
T-Code: /SAPPO/72000244
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VOBJREDIR
```

### 1638. `/SAPPO/72000315`

```
T-Code: /SAPPO/72000315
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPO/VC_RTRY_G
```

### 1639. `/SAPPO/API_PROXY_EXEC_FUNC`

```
T-Code: /SAPPO/API_PROXY_EXEC_FUNC
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 1640. `/SAPPO/ARCHIVING_HDS`

```
T-Code: /SAPPO/ARCHIVING_HDS
└── HDS_TREE
    ├── ACTVT → 1, 2, 3, 6, 7
    ├── HDS_APPLID → AR_ENGINE
    └── HDS_TREEID → /SAPPO/PPO
```

### 1641. `/SAPPO/ARCH_JOBS`

```
T-Code: /SAPPO/ARCH_JOBS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBANK_PP_DISTR
```

### 1642. `/SAPPO/CLOSE_ORDERS`

```
T-Code: /SAPPO/CLOSE_ORDERS
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6, A8
```

### 1643. `/SAPPO/DELETE_ORDERS`

```
T-Code: /SAPPO/DELETE_ORDERS
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6, A8
```

### 1644. `/SAPPO/LOGSYS`

```
T-Code: /SAPPO/LOGSYS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1645. `/SAPPO/LOGSYS_AS`

```
T-Code: /SAPPO/LOGSYS_AS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1646. `/SAPPO/NF_ORDER_ED`

```
T-Code: /SAPPO/NF_ORDER_ED
├── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── S_ALV_LAYO
    └── ACTVT → 23
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1647. `/SAPPO/PPO2`

```
T-Code: /SAPPO/PPO2
├── /SAPPO/FLT
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    ├── /SAPPO/FLT → (empty — maintain in PFCG)
    └── ACTVT → 2
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6, A8
```

### 1648. `/SAPPO/PPO3`

```
T-Code: /SAPPO/PPO3
├── /SAPPO/FLT
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    ├── /SAPPO/FLT → (empty — maintain in PFCG)
    └── ACTVT → 2
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 1649. `/SAPPO/RESUBMIT`

```
T-Code: /SAPPO/RESUBMIT
└── /SAPPO/ORD
    ├── /SAPPO/BPR → (empty — maintain in PFCG)
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2, A8
```

### 1650. `/SAPPO/SYS_VAL_DISP`

```
T-Code: /SAPPO/SYS_VAL_DISP
└── S_PROGRAM
    ├── P_ACTION → SUBMIT
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1651. `/SAPPO/USER_REMOTE`

```
T-Code: /SAPPO/USER_REMOTE
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1652. `/SAPPO/WL_CHANGE`

```
T-Code: /SAPPO/WL_CHANGE
└── /SAPPO/WLA
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1653. `/SAPPO/WL_DISPLAY`

```
T-Code: /SAPPO/WL_DISPLAY
└── /SAPPO/WLA
    ├── /SAPPO/CMP → (empty — maintain in PFCG)
    └── ACTVT → 3
```

## `/SAPPSPRO/` — SAP Project System Professional
> T-codes: **13**


### 1654. `/SAPPSPRO/98000102`

```
T-Code: /SAPPSPRO/98000102
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPSPRO/T169Y
```

### 1655. `/SAPPSPRO/98000133`

```
T-Code: /SAPPSPRO/98000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPPSPRO/SRVCLC
```

### 1656. `/SAPPSPRO/GET_SRM_LOGSYS_BANF`

```
T-Code: /SAPPSPRO/GET_SRM_LOGSYS_BANF
└── S_RFC_ADM
    ├── ACTVT → 3
    ├── ICF_VALUE → (empty — maintain in PFCG)
    ├── RFCDEST → (empty — maintain in PFCG)
    └── RFCTYPE → (empty — maintain in PFCG)
```

### 1657. `/SAPPSPRO/GM_CP_INTF_UPD_CHK`

```
T-Code: /SAPPSPRO/GM_CP_INTF_UPD_CHK
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1658. `/SAPPSPRO/MPN_IN_RFC`

```
T-Code: /SAPPSPRO/MPN_IN_RFC
└── M_MATE_MAN
    └── ACTVT → 1, 2, 3, 6
```

### 1659. `/SAPPSPRO/PD_GM_EMF_CREATE`

```
T-Code: /SAPPSPRO/PD_GM_EMF_CREATE
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1660. `/SAPPSPRO/PD_GM_EMF_HEAD_FILL`

```
T-Code: /SAPPSPRO/PD_GM_EMF_HEAD_FILL
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1661. `/SAPPSPRO/PD_GM_EMF_ITEM_FILL`

```
T-Code: /SAPPSPRO/PD_GM_EMF_ITEM_FILL
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1662. `/SAPPSPRO/PD_GM_EMF_MAINTAIN`

```
T-Code: /SAPPSPRO/PD_GM_EMF_MAINTAIN
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3, 6
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1663. `/SAPPSPRO/PD_GM_FMR2_READ_KBLK`

```
T-Code: /SAPPSPRO/PD_GM_FMR2_READ_KBLK
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1664. `/SAPPSPRO/PD_GM_FMR2_READ_KBLP`

```
T-Code: /SAPPSPRO/PD_GM_FMR2_READ_KBLP
└── F_FUNDSRES
    ├── BUKRS → $BUKRS
    ├── FMRE_AUTH → 1, 2, 3
    ├── FMRE_BLART → (empty — maintain in PFCG)
    └── FMRE_BLTYP → (empty — maintain in PFCG)
```

### 1665. `/SAPPSPRO/SRM_RM_CALL_API`

```
T-Code: /SAPPSPRO/SRM_RM_CALL_API
└── S_SRMGS_PR
    ├── ACTVT → 1, 2, 6
    ├── DOCUMENTID → (empty — maintain in PFCG)
    ├── PROPGROUP → (empty — maintain in PFCG)
    ├── PROPNAME → (empty — maintain in PFCG)
    ├── SPS_ID → (empty — maintain in PFCG)
    └── SRM_MODEL → (empty — maintain in PFCG)
```

### 1666. `/SAPPSPRO/START_WITH_POID`

```
T-Code: /SAPPSPRO/START_WITH_POID
└── S_SRMGS_DC
    ├── ACTVT → 6
    ├── DOCUMENTID → (empty — maintain in PFCG)
    ├── SPS_ID → (empty — maintain in PFCG)
    └── SRM_MODEL → (empty — maintain in PFCG)
```

## `/SAPSLL/` — SAP Global Trade Services (GTS)
> T-codes: **114**


### 1667. `/SAPSLL/36000162`

```
T-Code: /SAPSLL/36000162
└── S_DEVELOP
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DEVCLASS → ' '
    ├── OBJNAME → /SAPSLL/PI_MM0C_TRANSFER
    ├── OBJTYPE → ENBC
    └── P_GROUP → ' '
```

### 1668. `/SAPSLL/38000050`

```
T-Code: /SAPSLL/38000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T6GWLS
```

### 1669. `/SAPSLL/38000051`

```
T-Code: /SAPSLL/38000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_WLSBP
```

### 1670. `/SAPSLL/40000805`

```
T-Code: /SAPSLL/40000805
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TECOOP
```

### 1671. `/SAPSLL/52000313`

```
T-Code: /SAPSLL/52000313
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDNOS
```

### 1672. `/SAPSLL/52000314`

```
T-Code: /SAPSLL/52000314
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUNS01
```

### 1673. `/SAPSLL/52000315`

```
T-Code: /SAPSLL/52000315
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUNS03
```

### 1674. `/SAPSLL/52000640`

```
T-Code: /SAPSLL/52000640
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ITMSCR, /SAPSLL/V_ITMSLS, /SAPSLL/V_ITMSQU, /SAPSLL/V_ITMSSA
```

### 1675. `/SAPSLL/52000641`

```
T-Code: /SAPSLL/52000641
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ITMDLV, /SAPSLL/V_ITMIBD
```

### 1676. `/SAPSLL/52000642`

```
T-Code: /SAPSLL/52000642
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TCCACT, /SAPSLL/V_TCCDLV, /SAPSLL/V_TCCIBD, /SAPSLL/V_TCCPCR, /SAPSLL/V_TCCPRQ, /SAPSLL/V_TCCPSA, /SAPSLL/V_TCCPUR, /SAPSLL/V_TCCSCR, /SAPSLL/V_TCCSLS, /SAPSLL/V_TCCSQU, /SAPSLL/V_TCCSSA, /SAPSLL/V_TCCTCR
```

### 1677. `/SAPSLL/52000643`

```
T-Code: /SAPSLL/52000643
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TPAFCT
```

### 1678. `/SAPSLL/52000644`

```
T-Code: /SAPSLL/52000644
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TPAGR, /SAPSLL/V_TPAGRA
```

### 1679. `/SAPSLL/52000646`

```
T-Code: /SAPSLL/52000646
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T6GDF
```

### 1680. `/SAPSLL/52000647`

```
T-Code: /SAPSLL/52000647
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ALGRG1
```

### 1681. `/SAPSLL/52000648`

```
T-Code: /SAPSLL/52000648
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T6GEMB
```

### 1682. `/SAPSLL/52000649`

```
T-Code: /SAPSLL/52000649
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T6GLDT
```

### 1683. `/SAPSLL/52000650`

```
T-Code: /SAPSLL/52000650
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T606
```

### 1684. `/SAPSLL/52000651`

```
T-Code: /SAPSLL/52000651
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TSFDLV, /SAPSLL/V_TSFIBD, /SAPSLL/V_TSFSCR, /SAPSLL/V_TSFSLS, /SAPSLL/V_TSFSQU, /SAPSLL/V_TSFSSA, /SAPSLL/V_TSFTCR
```

### 1685. `/SAPSLL/52000652`

```
T-Code: /SAPSLL/52000652
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOSCA
```

### 1686. `/SAPSLL/52000653`

```
T-Code: /SAPSLL/52000653
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TLEPFM
```

### 1687. `/SAPSLL/52000663`

```
T-Code: /SAPSLL/52000663
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_DNOSCT
```

### 1688. `/SAPSLL/52000664`

```
T-Code: /SAPSLL/52000664
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDNOS
```

### 1689. `/SAPSLL/52000665`

```
T-Code: /SAPSLL/52000665
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDNOS
```

### 1690. `/SAPSLL/52000666`

```
T-Code: /SAPSLL/52000666
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOSCA
```

### 1691. `/SAPSLL/52000667`

```
T-Code: /SAPSLL/52000667
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUNS01
```

### 1692. `/SAPSLL/52000668`

```
T-Code: /SAPSLL/52000668
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUNS03
```

### 1693. `/SAPSLL/52000758`

```
T-Code: /SAPSLL/52000758
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ALRLCC
```

### 1694. `/SAPSLL/52000759`

```
T-Code: /SAPSLL/52000759
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ALRWLS
```

### 1695. `/SAPSLL/52000847`

```
T-Code: /SAPSLL/52000847
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ALREMB
```

### 1696. `/SAPSLL/52000848`

```
T-Code: /SAPSLL/52000848
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T6GEMB
```

### 1697. `/SAPSLL/52000849`

```
T-Code: /SAPSLL/52000849
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TISDS2
```

### 1698. `/SAPSLL/52000944`

```
T-Code: /SAPSLL/52000944
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUNS04
```

### 1699. `/SAPSLL/52001044`

```
T-Code: /SAPSLL/52001044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_ITMPCR, /SAPSLL/V_ITMPRQ, /SAPSLL/V_ITMPSA, /SAPSLL/V_ITMPUR
```

### 1700. `/SAPSLL/52001056`

```
T-Code: /SAPSLL/52001056
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TUOMC, /SAPSLL/V_TUOMM, /SAPSLL/V_TUOMS
```

### 1701. `/SAPSLL/52001083`

```
T-Code: /SAPSLL/52001083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDPDD
```

### 1702. `/SAPSLL/52001250`

```
T-Code: /SAPSLL/52001250
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T610VA
```

### 1703. `/SAPSLL/52001251`

```
T-Code: /SAPSLL/52001251
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T610VA
```

### 1704. `/SAPSLL/52001765`

```
T-Code: /SAPSLL/52001765
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDNOSU
```

### 1705. `/SAPSLL/52002143`

```
T-Code: /SAPSLL/52002143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOS01
```

### 1706. `/SAPSLL/52002144`

```
T-Code: /SAPSLL/52002144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSC01
```

### 1707. `/SAPSLL/52002145`

```
T-Code: /SAPSLL/52002145
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOS04
```

### 1708. `/SAPSLL/52002146`

```
T-Code: /SAPSLL/52002146
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSC04
```

### 1709. `/SAPSLL/52002147`

```
T-Code: /SAPSLL/52002147
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOS02
```

### 1710. `/SAPSLL/52002148`

```
T-Code: /SAPSLL/52002148
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSC02
```

### 1711. `/SAPSLL/52002149`

```
T-Code: /SAPSLL/52002149
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSC03
```

### 1712. `/SAPSLL/52002150`

```
T-Code: /SAPSLL/52002150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NOS03
```

### 1713. `/SAPSLL/52002167`

```
T-Code: /SAPSLL/52002167
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_T340D
```

### 1714. `/SAPSLL/52002178`

```
T-Code: /SAPSLL/52002178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSCA01
```

### 1715. `/SAPSLL/52002179`

```
T-Code: /SAPSLL/52002179
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSCA02
```

### 1716. `/SAPSLL/52002180`

```
T-Code: /SAPSLL/52002180
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSCA04
```

### 1717. `/SAPSLL/52002181`

```
T-Code: /SAPSLL/52002181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_NSCA03
```

### 1718. `/SAPSLL/52002954`

```
T-Code: /SAPSLL/52002954
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TDCBT
```

### 1719. `/SAPSLL/52002956`

```
T-Code: /SAPSLL/52002956
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TCBTP
```

### 1720. `/SAPSLL/91000019`

```
T-Code: /SAPSLL/91000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TLER3, /SAPSLL/V_TLER3B, /SAPSLL/V_TLER3C
```

### 1721. `/SAPSLL/91000020`

```
T-Code: /SAPSLL/91000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TIVDR3
```

### 1722. `/SAPSLL/91000021`

```
T-Code: /SAPSLL/91000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TSKCR3
```

### 1723. `/SAPSLL/91000023`

```
T-Code: /SAPSLL/91000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TECPCI
```

### 1724. `/SAPSLL/91000024`

```
T-Code: /SAPSLL/91000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TECOR3
```

### 1725. `/SAPSLL/91000080`

```
T-Code: /SAPSLL/91000080
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_BTCAR3, /SAPSLL/V_BTCMR3, /SAPSLL/V_BTCUR3, /SAPSLL/V_BTGAR3, /SAPSLL/V_BTGMR3, /SAPSLL/V_BTGUR3, /SAPSLL/V_BTPAR3, /SAPSLL/V_BTPMR3, /SAPSLL/V_BTPUR3, /SAPSLL/V_TBTCR3, /SAPSLL/V_TBTGR3, /SAPSLL/V_TBTPR3
```

### 1726. `/SAPSLL/API_COMCO_CLS_DISTR`

```
T-Code: /SAPSLL/API_COMCO_CLS_DISTR
└── ITM_CLS_NS
    ├── /SAPSLL/NS → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1727. `/SAPSLL/API_COMCO_MD_DISTR`

```
T-Code: /SAPSLL/API_COMCO_MD_DISTR
└── ITM_CLS_NS
    ├── /SAPSLL/NS → (empty — maintain in PFCG)
    └── ACTVT → 2
```

### 1728. `/SAPSLL/BANK_DIST_R3`

```
T-Code: /SAPSLL/BANK_DIST_R3
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1729. `/SAPSLL/BL_DOC_MM_R3`

```
T-Code: /SAPSLL/BL_DOC_MM_R3
├── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3
├── F_LFA1_GRP
    ├── ACTVT → 3
    └── KTOKK → (empty — maintain in PFCG)
├── M_ANFR_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANFR_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_ANFR_EKO
    ├── ACTVT → (empty — maintain in PFCG)
    └── EKORG → $EKORG
├── M_ANFR_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_ANGB_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANGB_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_ANGB_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_ANGB_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_BEST_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_BEST_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_MATE_WRK
    ├── ACTVT → 3, F4
    └── WERKS → $WERKS
├── M_QTN_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_QTN_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_QTN_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_QTN_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_RAHM_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_RAHM_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_RAHM_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_RAHM_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_RFQ_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_RFQ_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_RFQ_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_RFQ_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
└── W_BETR_USR
    ├── ACTVT → F4
    └── WERKS → $WERKS
```

### 1730. `/SAPSLL/BL_DOC_RS_R3`

```
T-Code: /SAPSLL/BL_DOC_RS_R3
├── V_EMBK_GEG
    ├── ACTVT → 36
    ├── GEART → COMA
    └── GEGRU → SL
└── V_VBAK_AAT
    ├── ACTVT → 3, F4
    └── AUART → (empty — maintain in PFCG)
```

### 1731. `/SAPSLL/BL_DOC_SD_R3`

```
T-Code: /SAPSLL/BL_DOC_SD_R3
├── B_BUP_DCPD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── V_LIKP_VST
    ├── ACTVT → 3, F4
    └── VSTEL → $VSTEL
├── V_VBAK_AAT
    ├── ACTVT → 3, F4
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 1732. `/SAPSLL/BOMMAT_DIRR3`

```
T-Code: /SAPSLL/BOMMAT_DIRR3
└── W_BETR_USR
    ├── ACTVT → F4
    └── WERKS → $WERKS
```

### 1733. `/SAPSLL/CCOLCLIC`

```
T-Code: /SAPSLL/CCOLCLIC
└── S_PROGRAM
    ├── P_ACTION → (empty — maintain in PFCG)
    └── P_GROUP → ITM_CCO
```

### 1734. `/SAPSLL/CD_SD0A_SYNCH_R3`

```
T-Code: /SAPSLL/CD_SD0A_SYNCH_R3
├── B_BUP_DCPD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── GTS_AUART
    ├── ACTVT → 2
    └── AUART → (empty — maintain in PFCG)
```

### 1735. `/SAPSLL/CLSNR_01`

```
T-Code: /SAPSLL/CLSNR_01
└── ITM_CLS_NS
    ├── /SAPSLL/NS → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
```

### 1736. `/SAPSLL/CLSNR_03`

```
T-Code: /SAPSLL/CLSNR_03
└── ITM_CLS_NS
    ├── /SAPSLL/NS → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
```

### 1737. `/SAPSLL/CPMAS_DIRR3`

```
T-Code: /SAPSLL/CPMAS_DIRR3
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → 3, F4
├── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3, F4
└── F_LFA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKK → (empty — maintain in PFCG)
```

### 1738. `/SAPSLL/CREMAS_DIRR3`

```
T-Code: /SAPSLL/CREMAS_DIRR3
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3, F4
└── F_LFA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKK → (empty — maintain in PFCG)
```

### 1739. `/SAPSLL/CRM_RECHECK`

```
T-Code: /SAPSLL/CRM_RECHECK
└── CRM_SAO
    └── ACTVT → 45
```

### 1740. `/SAPSLL/CUDC_STAT_R3`

```
T-Code: /SAPSLL/CUDC_STAT_R3
├── V_VBRK_FKA
    ├── ACTVT → 3
    └── FKART → (empty — maintain in PFCG)
└── V_VBRK_VKO
    ├── ACTVT → 3
    └── VKORG → $VKORG
```

### 1741. `/SAPSLL/CUS_EXPM_R3`

```
T-Code: /SAPSLL/CUS_EXPM_R3
├── V_VBRK_FKA
    ├── ACTVT → 3, F4
    └── FKART → (empty — maintain in PFCG)
└── V_VBRK_VKO
    ├── ACTVT → 3
    └── VKORG → $VKORG
```

### 1742. `/SAPSLL/CUS_INV_R3`

```
T-Code: /SAPSLL/CUS_INV_R3
├── V_VBRK_FKA
    ├── ACTVT → 1, 19
    └── FKART → (empty — maintain in PFCG)
└── V_VBRK_VKO
    ├── ACTVT → 3
    └── VKORG → $VKORG
```

### 1743. `/SAPSLL/CUS_OFF_CO`

```
T-Code: /SAPSLL/CUS_OFF_CO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_COPRCO
```

### 1744. `/SAPSLL/CUS_OFF_RO`

```
T-Code: /SAPSLL/CUS_OFF_RO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_COPRRO
```

### 1745. `/SAPSLL/DEBMAS_DIRR3`

```
T-Code: /SAPSLL/DEBMAS_DIRR3
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3, F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BUK
    ├── ACTVT → 3, F4
    └── BUKRS → $BUKRS
├── F_KNA1_GEN
    └── ACTVT → 3, F4
└── F_KNA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKD → (empty — maintain in PFCG)
```

### 1746. `/SAPSLL/DS_D_MM0A_R3`

```
T-Code: /SAPSLL/DS_D_MM0A_R3
├── M_ANFR_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANFR_EKG
    ├── ACTVT → (empty — maintain in PFCG)
    └── EKGRP → $EKGRP
├── M_ANFR_EKO
    ├── ACTVT → (empty — maintain in PFCG)
    └── EKORG → $EKORG
├── M_ANGB_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANGB_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_ANGB_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_BEST_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_QTN_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_QTN_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_QTN_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_RAHM_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_RAHM_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_RAHM_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_RFQ_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_RFQ_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
└── M_RFQ_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
```

### 1747. `/SAPSLL/DS_D_MM0C_R3`

```
T-Code: /SAPSLL/DS_D_MM0C_R3
├── M_MATE_MAR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── M_MSEG_BWE
    ├── ACTVT → 3
    └── BWART → (empty — maintain in PFCG)
```

### 1748. `/SAPSLL/DS_D_SD0A_R3`

```
T-Code: /SAPSLL/DS_D_SD0A_R3
├── V_VBAK_AAT
    ├── ACTVT → 3, F4
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → 3, F4
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 1749. `/SAPSLL/DS_D_SD0C_R3`

```
T-Code: /SAPSLL/DS_D_SD0C_R3
└── V_VBRK_FKA
    ├── ACTVT → 3, F4
    └── FKART → (empty — maintain in PFCG)
```

### 1750. `/SAPSLL/DS_I_SD0C_R3`

```
T-Code: /SAPSLL/DS_I_SD0C_R3
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1751. `/SAPSLL/DS_P_MM0A_R3`

```
T-Code: /SAPSLL/DS_P_MM0A_R3
├── M_BANF_BSA
    ├── ACTVT → 3, F4
    └── BSART → (empty — maintain in PFCG)
├── M_BANF_EKG
    ├── ACTVT → 3, F4
    └── EKGRP → $EKGRP
├── M_BANF_EKO
    ├── ACTVT → 6, F4
    └── EKORG → $EKORG
└── M_BANF_WRK
    ├── ACTVT → 3, F4
    └── WERKS → $WERKS
```

### 1752. `/SAPSLL/KMATBOM_DIR3`

```
T-Code: /SAPSLL/KMATBOM_DIR3
├── C_STUE_BER
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    ├── STLAN → (empty — maintain in PFCG)
    └── STLTY → (empty — maintain in PFCG)
└── C_STUE_WRK
    ├── ACTVT → 3
    └── CSWRK → $WERKS
```

### 1753. `/SAPSLL/KMATWLR3_03`

```
T-Code: /SAPSLL/KMATWLR3_03
├── C_STUE_BER
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    ├── STLAN → (empty — maintain in PFCG)
    └── STLTY → (empty — maintain in PFCG)
└── C_STUE_WRK
    ├── ACTVT → 3
    └── CSWRK → $WERKS
```

### 1754. `/SAPSLL/KMATWLR3_DEL`

```
T-Code: /SAPSLL/KMATWLR3_DEL
├── C_STUE_BER
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    ├── STLAN → (empty — maintain in PFCG)
    └── STLTY → (empty — maintain in PFCG)
└── C_STUE_WRK
    ├── ACTVT → 3
    └── CSWRK → $WERKS
```

### 1755. `/SAPSLL/LCLIC03`

```
T-Code: /SAPSLL/LCLIC03
└── ITM_LMGM
    ├── /SAPSLL/LR → (empty — maintain in PFCG)
    ├── /SAPSLL/LT → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 1756. `/SAPSLL/MATMAS_DIRR3`

```
T-Code: /SAPSLL/MATMAS_DIRR3
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
```

### 1757. `/SAPSLL/PERTMVAR_R3`

```
T-Code: /SAPSLL/PERTMVAR_R3
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1758. `/SAPSLL/PRCMAT_DIRR3`

```
T-Code: /SAPSLL/PRCMAT_DIRR3
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
```

### 1759. `/SAPSLL/PRECIP_R3`

```
T-Code: /SAPSLL/PRECIP_R3
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1760. `/SAPSLL/PRPARV_DIRR3`

```
T-Code: /SAPSLL/PRPARV_DIRR3
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → F4
├── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
├── M_EINF_EKO
    ├── ACTVT → 3, F4
    └── EKORG → $EKORG
├── M_EINF_WRK
    ├── ACTVT → 3, F4
    └── WERKS → $WERKS
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
```

### 1761. `/SAPSLL/PSD_INIT_R3`

```
T-Code: /SAPSLL/PSD_INIT_R3
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── M_MSEG_WMB
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 1762. `/SAPSLL/SAKE_DLV_MON`

```
T-Code: /SAPSLL/SAKE_DLV_MON
├── M_BEST_EKO
    ├── ACTVT → 3
    └── EKORG → $EKORG
├── M_MSEG_WWA
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── M_MSEG_WWE
    ├── ACTVT → 3
    └── WERKS → $WERKS
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 1763. `/SAPSLL/SAKE_MTH_R3`

```
T-Code: /SAPSLL/SAKE_MTH_R3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TSKCR3
```

### 1764. `/SAPSLL/SAKE_XMON_R3`

```
T-Code: /SAPSLL/SAKE_XMON_R3
└── V_LIKP_VST
    ├── ACTVT → (empty — maintain in PFCG)
    └── VSTEL → $VSTEL
```

### 1765. `/SAPSLL/SD0C_RIMAR3`

```
T-Code: /SAPSLL/SD0C_RIMAR3
├── V_VBRK_FKA
    ├── ACTVT → 3
    └── FKART → (empty — maintain in PFCG)
└── V_VBRK_VKO
    ├── ACTVT → 3
    └── VKORG → $VKORG
```

### 1766. `/SAPSLL/SNUM_LICNO`

```
T-Code: /SAPSLL/SNUM_LICNO
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → SLL_LICNO
```

### 1767. `/SAPSLL/T000_R3`

```
T-Code: /SAPSLL/T000_R3
├── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → T000
```

### 1768. `/SAPSLL/T000_SPI`

```
T-Code: /SAPSLL/T000_SPI
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1769. `/SAPSLL/TBDLS_R3`

```
T-Code: /SAPSLL/TBDLS_R3
├── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBDLS
```

### 1770. `/SAPSLL/TBDLS_SPI`

```
T-Code: /SAPSLL/TBDLS_SPI
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1771. `/SAPSLL/TECOR3`

```
T-Code: /SAPSLL/TECOR3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TECOR3
```

### 1772. `/SAPSLL/TITMCAT_CRM`

```
T-Code: /SAPSLL/TITMCAT_CRM
└── S_PROJECT
    ├── ACTVT → 2, 23, 3, 6, 71, 76, 78, A3
    ├── APPL_COMP → (empty — maintain in PFCG)
    ├── PROJECT_ID → (empty — maintain in PFCG)
    └── PROJ_CONF → (empty — maintain in PFCG)
```

### 1773. `/SAPSLL/TIVDR3`

```
T-Code: /SAPSLL/TIVDR3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TIVDR3
```

### 1774. `/SAPSLL/TLER3`

```
T-Code: /SAPSLL/TLER3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TLER3
```

### 1775. `/SAPSLL/TLER3B`

```
T-Code: /SAPSLL/TLER3B
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TLER3B
```

### 1776. `/SAPSLL/TLER3_B_R3`

```
T-Code: /SAPSLL/TLER3_B_R3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TLER3, /SAPSLL/V_TLER3B, /SAPSLL/V_TLER3C
```

### 1777. `/SAPSLL/TLESPIB_CRM`

```
T-Code: /SAPSLL/TLESPIB_CRM
├── S_CTS_ADMI
    └── CTS_ADMFCT → EPS1, EPS2, IMPA, IMPS, IMPT, INBX, INIT, PROJ, QTEA, SYSC, TABL, TADD, TADM, TDEL, TQAS
├── S_DEVELOP
    ├── ACTVT → 1, 2, 3, 6
    ├── DEVCLASS → /SAPSLL/IF_CRM, /SAPSLL/IF_SRM
    ├── OBJNAME → /SAPSLL/*
    ├── OBJTYPE → BUS6801
    └── P_GROUP → /SAPSLL/
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SLL
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 3, 6
    └── TTYPE → CUST, DTRA
```

### 1778. `/SAPSLL/TLSDST`

```
T-Code: /SAPSLL/TLSDST
└── S_DEVELOP
    ├── ACTVT → 16
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 1779. `/SAPSLL/WLSBP_SLG1`

```
T-Code: /SAPSLL/WLSBP_SLG1
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SAPSLL/WLS
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 1780. `/SAPSLL/WLS_POSTPRO`

```
T-Code: /SAPSLL/WLS_POSTPRO
└── ITM_LGREG
    ├── /SAPSLL/LR → (empty — maintain in PFCG)
    └── ACTVT → 16
```

## `/SAPSMOSS/` — SAP Service & Asset Intelligence
> T-codes: **19**


### 1781. `/SAPSMOSS/IQS1`

```
T-Code: /SAPSMOSS/IQS1
├── B_NOTIF
    ├── NOTIF_ACT → (empty — maintain in PFCG)
    ├── PARVW → (empty — maintain in PFCG)
    └── QMART → (empty — maintain in PFCG)
├── Q_GP_CODE
    ├── QCODEGRP → $$OSS*
    └── QKATART → 2, D
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/IQS1
    └── WERKS → $WERKS
└── Q_VORG_MEL
    ├── BETRVORG → (empty — maintain in PFCG)
    └── QMART → (empty — maintain in PFCG)
```

### 1782. `/SAPSMOSS/IQS2`

```
T-Code: /SAPSMOSS/IQS2
├── B_NOT_TASK
    ├── NOT_TSK_AC → 11, 12, 21, 22
    ├── QCODEGRP → (empty — maintain in PFCG)
    └── QKATART → (empty — maintain in PFCG)
├── Q_GP_CODE
    ├── QCODEGRP → $$OSS*
    └── QKATART → 2, D
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/IQS2
    └── WERKS → $WERKS
└── Q_VORG_MEL
    ├── BETRVORG → (empty — maintain in PFCG)
    └── QMART → (empty — maintain in PFCG)
```

### 1783. `/SAPSMOSS/IQS3`

```
T-Code: /SAPSMOSS/IQS3
├── B_NOT_TASK
    ├── NOT_TSK_AC → 11, 12
    ├── QCODEGRP → (empty — maintain in PFCG)
    └── QKATART → (empty — maintain in PFCG)
├── Q_GP_CODE
    ├── QCODEGRP → $$OSS*
    └── QKATART → 2, D
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/IQS3
    └── WERKS → $WERKS
└── Q_VORG_MEL
    ├── BETRVORG → (empty — maintain in PFCG)
    └── QMART → (empty — maintain in PFCG)
```

### 1784. `/SAPSMOSS/O01`

```
T-Code: /SAPSMOSS/O01
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/ADDID
```

### 1785. `/SAPSMOSS/O02`

```
T-Code: /SAPSMOSS/O02
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/ADDRL
```

### 1786. `/SAPSMOSS/O03`

```
T-Code: /SAPSMOSS/O03
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/DBSY
```

### 1787. `/SAPSMOSS/O04`

```
T-Code: /SAPSMOSS/O04
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/FRNT
```

### 1788. `/SAPSMOSS/O05`

```
T-Code: /SAPSMOSS/O05
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/INSTN
```

### 1789. `/SAPSMOSS/O06`

```
T-Code: /SAPSMOSS/O06
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/OSYS
```

### 1790. `/SAPSMOSS/O07`

```
T-Code: /SAPSMOSS/O07
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/REL
```

### 1791. `/SAPSMOSS/O08`

```
T-Code: /SAPSMOSS/O08
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/SYST
```

### 1792. `/SAPSMOSS/O09`

```
T-Code: /SAPSMOSS/O09
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSMOSS/TURL
```

### 1793. `/SAPSMOSS/QM10`

```
T-Code: /SAPSMOSS/QM10
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM10
    └── WERKS → $WERKS
└── Q_TCODE
    └── TCD → /SAPSMOSS/QM10
```

### 1794. `/SAPSMOSS/QM11`

```
T-Code: /SAPSMOSS/QM11
├── C_TCLA_BKA
    └── KLART → 15
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM11
    └── WERKS → $WERKS
└── Q_TCODE
    └── TCD → /SAPSMOSS/QM11
```

### 1795. `/SAPSMOSS/QM12`

```
T-Code: /SAPSMOSS/QM12
├── C_TCLA_BKA
    └── KLART → 15
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM12
    └── WERKS → $WERKS
└── Q_TCODE
    └── TCD → /SAPSMOSS/QM12
```

### 1796. `/SAPSMOSS/QM13`

```
T-Code: /SAPSMOSS/QM13
├── C_TCLA_BKA
    └── KLART → 15
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM13
    └── WERKS → $WERKS
└── Q_TCODE
    └── TCD → /SAPSMOSS/QM13
```

### 1797. `/SAPSMOSS/QM19`

```
T-Code: /SAPSMOSS/QM19
└── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM19
    └── WERKS → $WERKS
```

### 1798. `/SAPSMOSS/QM50`

```
T-Code: /SAPSMOSS/QM50
├── C_TCLA_BKA
    └── KLART → 15
├── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → /SAPSMOSS/QM50
    └── WERKS → $WERKS
└── Q_TCODE
    └── TCD → /SAPSMOSS/QM50
```

### 1799. `/SAPSMOSS/U01`

```
T-Code: /SAPSMOSS/U01
├── Q_GP_CODE
    ├── QCODEGRP → (empty — maintain in PFCG)
    └── QKATART → (empty — maintain in PFCG)
└── Q_QMEL
    ├── QMART → (empty — maintain in PFCG)
    ├── TCD → (empty — maintain in PFCG)
    └── WERKS → $WERKS
```

## `/SAPTRX/` — SAP Event Management — Tracking
> T-codes: **35**


### 1800. `/SAPTRX/47000048`

```
T-Code: /SAPTRX/47000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/VEXT_AOT, /SAPTRX/VEXT_EE, /SAPTRX/VEXT_ETY, /SAPTRX/VEXT_EVT, /SAPTRX/VEXT_FD, /SAPTRX/VEXT_FLD, /SAPTRX/VEXT_MTO, /SAPTRX/VEXT_PAR
```

### 1801. `/SAPTRX/52000019`

```
T-Code: /SAPTRX/52000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBDLS
```

### 1802. `/SAPTRX/52000020`

```
T-Code: /SAPTRX/52000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_CTTSRV
```

### 1803. `/SAPTRX/52000021`

```
T-Code: /SAPTRX/52000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_AOATAB, /SAPTRX/V_ATOTYP
```

### 1804. `/SAPTRX/52000024`

```
T-Code: /SAPTRX/52000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_AOTCTT, /SAPTRX/V_EVTCTT, /SAPTRX/V_MTOTYP
```

### 1805. `/SAPTRX/52000025`

```
T-Code: /SAPTRX/52000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/VFUNC_A, /SAPTRX/VFUNC_B, /SAPTRX/VFUNC_D, /SAPTRX/VFUNC_E, /SAPTRX/VFUNC_F, /SAPTRX/VFUNC_G, /SAPTRX/VFUNC_H, /SAPTRX/VFUNC_L
```

### 1806. `/SAPTRX/68000157`

```
T-Code: /SAPTRX/68000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBDLS
```

### 1807. `/SAPTRX/68000158`

```
T-Code: /SAPTRX/68000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_TRXSRV
```

### 1808. `/SAPTRX/68000159`

```
T-Code: /SAPTRX/68000159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_AOATAB, /SAPTRX/V_ATOTYP
```

### 1809. `/SAPTRX/68000160`

```
T-Code: /SAPTRX/68000160
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_AOATAB, /SAPTRX/V_ATOTYP
```

### 1810. `/SAPTRX/68000161`

```
T-Code: /SAPTRX/68000161
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/V_AOTYPE, /SAPTRX/V_EVTYPE, /SAPTRX/V_MTOTYP
```

### 1811. `/SAPTRX/68000162`

```
T-Code: /SAPTRX/68000162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/VFUNC_A, /SAPTRX/VFUNC_B, /SAPTRX/VFUNC_D, /SAPTRX/VFUNC_E, /SAPTRX/VFUNC_F, /SAPTRX/VFUNC_G, /SAPTRX/VFUNC_H, /SAPTRX/VFUNC_I, /SAPTRX/VFUNC_J, /SAPTRX/VFUNC_L, /SAPTRX/VFUNC_Y, /SAPTRX/V_ASFCEV, /SAPTRX/V_ASFCPC, /SAPTRX/V_ASFCPI
```

### 1812. `/SAPTRX/68000163`

```
T-Code: /SAPTRX/68000163
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPTRX/VCONDIDL, /SAPTRX/VCONDIDN
```

### 1813. `/SAPTRX/ASAPLOG`

```
T-Code: /SAPTRX/ASAPLOG
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1814. `/SAPTRX/ASC0AO`

```
T-Code: /SAPTRX/ASC0AO
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1815. `/SAPTRX/ASC0AO_CTT`

```
T-Code: /SAPTRX/ASC0AO_CTT
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1816. `/SAPTRX/ASC0AP`

```
T-Code: /SAPTRX/ASC0AP
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1817. `/SAPTRX/ASC0SCU`

```
T-Code: /SAPTRX/ASC0SCU
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1818. `/SAPTRX/ASC0SD1`

```
T-Code: /SAPTRX/ASC0SD1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1819. `/SAPTRX/ASC0SD2`

```
T-Code: /SAPTRX/ASC0SD2
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1820. `/SAPTRX/ASC0TC`

```
T-Code: /SAPTRX/ASC0TC
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1821. `/SAPTRX/ASC0TE`

```
T-Code: /SAPTRX/ASC0TE
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1822. `/SAPTRX/ASC0TF`

```
T-Code: /SAPTRX/ASC0TF
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1823. `/SAPTRX/ASC0TF_CTT`

```
T-Code: /SAPTRX/ASC0TF_CTT
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1824. `/SAPTRX/ASC0TO`

```
T-Code: /SAPTRX/ASC0TO
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1825. `/SAPTRX/ASC0TS`

```
T-Code: /SAPTRX/ASC0TS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1826. `/SAPTRX/ASC0TS_CTT`

```
T-Code: /SAPTRX/ASC0TS_CTT
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1827. `/SAPTRX/DISP_LINK`

```
T-Code: /SAPTRX/DISP_LINK
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1828. `/SAPTRX/TRACKING_MON`

```
T-Code: /SAPTRX/TRACKING_MON
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1829. `/SAPTRX/TSC0GBPT`

```
T-Code: /SAPTRX/TSC0GBPT
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1830. `/SAPTRX/TSC0SCR`

```
T-Code: /SAPTRX/TSC0SCR
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1831. `/SAPTRX/TSC0SCU`

```
T-Code: /SAPTRX/TSC0SCU
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1832. `/SAPTRX/TSC0SO`

```
T-Code: /SAPTRX/TSC0SO
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1833. `/SAPTRX/TSC0SO1`

```
T-Code: /SAPTRX/TSC0SO1
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

### 1834. `/SAPTRX/TSC0SO2`

```
T-Code: /SAPTRX/TSC0SO2
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → TRXT
```

## `/SCA/` — SAP Supply Chain Analytics
> T-codes: **49**


### 1835. `/SCA/09000283`

```
T-Code: /SCA/09000283
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/SCNCTRLV
```

### 1836. `/SCA/15000030`

```
T-Code: /SCA/15000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCSCR
```

### 1837. `/SCA/15000031`

```
T-Code: /SCA/15000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCP4A
```

### 1838. `/SCA/15000032`

```
T-Code: /SCA/15000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCD4A
```

### 1839. `/SCA/15000033`

```
T-Code: /SCA/15000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCPAD
```

### 1840. `/SCA/15000034`

```
T-Code: /SCA/15000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCPAT
```

### 1841. `/SCA/15000035`

```
T-Code: /SCA/15000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCERT
```

### 1842. `/SCA/15000043`

```
T-Code: /SCA/15000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCSCC
```

### 1843. `/SCA/60000192`

```
T-Code: /SCA/60000192
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCTIM
```

### 1844. `/SCA/60000193`

```
T-Code: /SCA/60000193
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCSAC
```

### 1845. `/SCA/60000194`

```
T-Code: /SCA/60000194
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCOTT
```

### 1846. `/SCA/60000195`

```
T-Code: /SCA/60000195
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCOTD
```

### 1847. `/SCA/60000196`

```
T-Code: /SCA/60000196
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCAQT
```

### 1848. `/SCA/60000197`

```
T-Code: /SCA/60000197
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_SPLRCGLO
```

### 1849. `/SCA/60000198`

```
T-Code: /SCA/60000198
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCDVC
```

### 1850. `/SCA/60000582`

```
T-Code: /SCA/60000582
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCPTM
```

### 1851. `/SCA/60000689`

```
T-Code: /SCA/60000689
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCTPR
```

### 1852. `/SCA/60000715`

```
T-Code: /SCA/60000715
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCSAT
```

### 1853. `/SCA/83000007`

```
T-Code: /SCA/83000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCA/TS_EX_SUPP
```

### 1854. `/SCA/86000085`

```
T-Code: /SCA/86000085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCA/VTIMEPRF, /SCA/VTSDAREA, /SCA/VTSDAREASC, /SCA/VTSDTYPE, /SCA/VTSDTYPECH, /SCA/VTSDTYPECHE, /SCA/VTSDTYPEDE, /SCA/VTSDTYPEDEF, /SCA/VTSDTYPEDR, /SCA/VTSDTYPEKEY, /SCA/VTSTYPE
```

### 1855. `/SCA/94000265`

```
T-Code: /SCA/94000265
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCA/TLB01
```

### 1856. `/SCA/94000302`

```
T-Code: /SCA/94000302
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CDS_OGDL
```

### 1857. `/SCA/94000303`

```
T-Code: /SCA/94000303
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CDS_PLOC
```

### 1858. `/SCA/94000304`

```
T-Code: /SCA/94000304
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CDS_PPRD
```

### 1859. `/SCA/94000305`

```
T-Code: /SCA/94000305
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CDS_PCAT
```

### 1860. `/SCA/94000310`

```
T-Code: /SCA/94000310
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/MOIGR_V
```

### 1861. `/SCA/94000313`

```
T-Code: /SCA/94000313
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/MOI_V
```

### 1862. `/SCA/94000323`

```
T-Code: /SCA/94000323
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_PALIAS
```

### 1863. `/SCA/BOL_INV_READ`

```
T-Code: /SCA/BOL_INV_READ
├── C_LIME_DOC
    └── LIME_PRCTP → SCA
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6
└── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6, I06
```

### 1864. `/SCA/CONFCONTROL`

```
T-Code: /SCA/CONFCONTROL
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/CONFCONTROL
```

### 1865. `/SCA/CREATEINV`

```
T-Code: /SCA/CREATEINV
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → (empty — maintain in PFCG)
├── B_BUP_PCPT
    └── ACTVT → 3
├── C_LIME_DOC
    └── LIME_PRCTP → SCA
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6
└── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6, I06
```

### 1866. `/SCA/DLVVAL`

```
T-Code: /SCA/DLVVAL
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/DLVVAL
```

### 1867. `/SCA/DM_DELETEINV`

```
T-Code: /SCA/DM_DELETEINV
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → (empty — maintain in PFCG)
├── B_BUP_PCPT
    └── ACTVT → 3
├── C_LIME_DOC
    └── LIME_PRCTP → SCA
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6
└── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → 6, I06
```

### 1868. `/SCA/DM_PRPP`

```
T-Code: /SCA/DM_PRPP
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
```

### 1869. `/SCA/DUELISTAV`

```
T-Code: /SCA/DUELISTAV
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
```

### 1870. `/SCA/PJSTKAV`

```
T-Code: /SCA/PJSTKAV
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/PRJSTKAV
```

### 1871. `/SCA/PRJSTKPRFLV`

```
T-Code: /SCA/PRJSTKPRFLV
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
```

### 1872. `/SCA/RR_QTYSPLIT`

```
T-Code: /SCA/RR_QTYSPLIT
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/RR_QTYSPLIT
```

### 1873. `/SCA/SCHEDCFG`

```
T-Code: /SCA/SCHEDCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/SCHED_PR
```

### 1874. `/SCA/SPLRCGLO`

```
T-Code: /SCA/SPLRCGLO
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/V_SPLRCGLO
```

### 1875. `/SCA/SPLRCGLO2`

```
T-Code: /SCA/SPLRCGLO2
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/V_SPLRCGL2
```

### 1876. `/SCA/SPLRCPAD`

```
T-Code: /SCA/SPLRCPAD
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
```

### 1877. `/SCA/SPLRCSAT`

```
T-Code: /SCA/SPLRCSAT
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/SPLRCSAT
```

### 1878. `/SCA/SPLRCSDT`

```
T-Code: /SCA/SPLRCSDT
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/SPLRCSDT
```

### 1879. `/SCA/SPLRCTWT`

```
T-Code: /SCA/SPLRCTWT
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/SPLRCTWT
```

### 1880. `/SCA/SPLRTW_ONTI`

```
T-Code: /SCA/SPLRTW_ONTI
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/SPLRTW_ONTI
```

### 1881. `/SCA/SPPSDPR_STATUS`

```
T-Code: /SCA/SPPSDPR_STATUS
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 1882. `/SCA/TERMSV`

```
T-Code: /SCA/TERMSV
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → ADMC
```

### 1883. `/SCA/TSDMCFG`

```
T-Code: /SCA/TSDMCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → TSDC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCA/VTIMEPRF, /SCA/VTSDAREA, /SCA/VTSDAREASC, /SCA/VTSDTYPE, /SCA/VTSDTYPECH, /SCA/VTSDTYPECHE, /SCA/VTSDTYPEDE, /SCA/VTSDTYPEDEF, /SCA/VTSDTYPEDR, /SCA/VTSDTYPEKEY, /SCA/VTSTYPE
```

## `/SCDL/` — SAP Delivery (SCM)
> T-codes: **39**


### 1884. `/SCDL/23000001`

```
T-Code: /SCDL/23000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TRD_CATTRV, /SCDL/TRD_CPROFV
```

### 1885. `/SCDL/23000002`

```
T-Code: /SCDL/23000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDTTYPE
```

### 1886. `/SCDL/23000003`

```
T-Code: /SCDL/23000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDT_CATTRV, /SCDL/TDT_CPROFV
```

### 1887. `/SCDL/23000005`

```
T-Code: /SCDL/23000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TPC_CATTRV, /SCDL/TPC_CPROFV
```

### 1888. `/SCDL/23000025`

```
T-Code: /SCDL/23000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/VDF_CNFRQ
```

### 1889. `/SCDL/60000104`

```
T-Code: /SCDL/60000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TSTM_PROFV, /SCDL/TSTM_PROTV
```

### 1890. `/SCDL/60000108`

```
T-Code: /SCDL/60000108
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDEFSETGRV, /SCDL/TDEFSETV, /SCDL/TFIELDSV, /SCDL/TFLDGROUPV, /SCDL/TSPROF
```

### 1891. `/SCDL/60000113`

```
T-Code: /SCDL/60000113
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TKEYSV, /SCDL/TLOGFIELDV, /SCDL/TSTRUCTURE
```

### 1892. `/SCDL/60000114`

```
T-Code: /SCDL/60000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/FIELDLISLV, /SCDL/TDUMGROUPV, /SCDL/TGROUPSETV, /SCDL/TPROFILE, /SCDL/TSETTINGSV
```

### 1893. `/SCDL/60000267`

```
T-Code: /SCDL/60000267
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TICCPROF, /SCDL/TICCSETV
```

### 1894. `/SCDL/60000297`

```
T-Code: /SCDL/60000297
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/V_TDTXTTYP
```

### 1895. `/SCDL/60000463`

```
T-Code: /SCDL/60000463
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/V_TDDETER, /SCDL/V_TDPFITEM, /SCDL/V_TDPROF, /SCDL/V_TDSEQU
```

### 1896. `/SCDL/60000585`

```
T-Code: /SCDL/60000585
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/V_TDTXTITM
```

### 1897. `/SCDL/60000606`

```
T-Code: /SCDL/60000606
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDLVPRIO_V
```

### 1898. `/SCDL/60000607`

```
T-Code: /SCDL/60000607
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TREFCAT
```

### 1899. `/SCDL/60000617`

```
T-Code: /SCDL/60000617
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TPRTROLE
```

### 1900. `/SCDL/60000620`

```
T-Code: /SCDL/60000620
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TEXCODE_V
```

### 1901. `/SCDL/60000638`

```
T-Code: /SCDL/60000638
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETTYPE_INB
```

### 1902. `/SCDL/60000639`

```
T-Code: /SCDL/60000639
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETITP_INB
```

### 1903. `/SCDL/60000644`

```
T-Code: /SCDL/60000644
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETTYPE_OUT
```

### 1904. `/SCDL/60000645`

```
T-Code: /SCDL/60000645
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETITP_OUT
```

### 1905. `/SCDL/60000662`

```
T-Code: /SCDL/60000662
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TALITYPE_INB
```

### 1906. `/SCDL/60000663`

```
T-Code: /SCDL/60000663
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TALITYPE_OUT
```

### 1907. `/SCDL/60000670`

```
T-Code: /SCDL/60000670
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TSRVLVL
```

### 1908. `/SCDL/60000672`

```
T-Code: /SCDL/60000672
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TPR_CATTRV, /SCDL/TPR_CPROFV
```

### 1909. `/SCDL/69000011`

```
T-Code: /SCDL/69000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TALITYPE_P
```

### 1910. `/SCDL/ACTION_CONF`

```
T-Code: /SCDL/ACTION_CONF
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 1911. `/SCDL/ACTION_DEF`

```
T-Code: /SCDL/ACTION_DEF
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 1912. `/SCDL/ACTION_WIZARD`

```
T-Code: /SCDL/ACTION_WIZARD
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 1913. `/SCDL/ALIAS`

```
T-Code: /SCDL/ALIAS
├── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TKEYSV, /SCDL/TLOGFIELDV, /SCDL/TSTRUCTURE
└── S_TRANSPRT
    ├── ACTVT → 2
    └── TTYPE → (empty — maintain in PFCG)
```

### 1914. `/SCDL/BO_ARCHIVE`

```
T-Code: /SCDL/BO_ARCHIVE
└── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 24
```

### 1915. `/SCDL/BO_ARCHIVE_STD`

```
T-Code: /SCDL/BO_ARCHIVE_STD
└── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 24
```

### 1916. `/SCDL/BO_TEST`

```
T-Code: /SCDL/BO_TEST
└── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 43, 6
```

### 1917. `/SCDL/DL_NURA`

```
T-Code: /SCDL/DL_NURA
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCDL/DBAL
```

### 1918. `/SCDL/IMG`

```
T-Code: /SCDL/IMG
└── S_TABU_CLI
    └── CLIIDMAINT → ' '
```

### 1919. `/SCDL/NURA_EGR`

```
T-Code: /SCDL/NURA_EGR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCDL/EGR
```

### 1920. `/SCDL/NURA_FD`

```
T-Code: /SCDL/NURA_FD
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCDL/FD
```

### 1921. `/SCDL/NURA_PRD`

```
T-Code: /SCDL/NURA_PRD
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCDL/PRD
```

### 1922. `/SCDL/NURA_PWR`

```
T-Code: /SCDL/NURA_PWR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCDL/PRD
```

## `/SCF/` — SAP Supply Chain Foundation
> T-codes: **56**


### 1923. `/SCF/ADVTLBRCFG`

```
T-Code: /SCF/ADVTLBRCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/XT, /SCF/XTBPROP, /SCF/XTBTN, /SCF/XTFLD, /SCF/XTFPROP
```

### 1924. `/SCF/ALDATARETRSEQ`

```
T-Code: /SCF/ALDATARETRSEQ
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CC_ALSELPTH
```

### 1925. `/SCF/ALERTACTIVE`

```
T-Code: /SCF/ALERTACTIVE
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → (empty — maintain in PFCG)
```

### 1926. `/SCF/ALERTCATCFG`

```
T-Code: /SCF/ALERTCATCFG
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/ALCATATYPAS, /SCF/ALCATDESCRV, /SCF/ALPRMLYT
```

### 1927. `/SCF/ALPIVOTS`

```
T-Code: /SCF/ALPIVOTS
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1928. `/SCF/ALPRIORITY`

```
T-Code: /SCF/ALPRIORITY
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1929. `/SCF/ANOTURLCFG`

```
T-Code: /SCF/ANOTURLCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → (empty — maintain in PFCG)
```

### 1930. `/SCF/APPCLSS`

```
T-Code: /SCF/APPCLSS
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CLSAPP
```

### 1931. `/SCF/APPDATAASS`

```
T-Code: /SCF/APPDATAASS
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDATAASS
```

### 1932. `/SCF/APPDATACLSS`

```
T-Code: /SCF/APPDATACLSS
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CLSAPPDATA
```

### 1933. `/SCF/APPDATADEF`

```
T-Code: /SCF/APPDATADEF
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDATADEF
```

### 1934. `/SCF/APPMODES`

```
T-Code: /SCF/APPMODES
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/FRMWKMODES
```

### 1935. `/SCF/APPVIEWCFG`

```
T-Code: /SCF/APPVIEWCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPVIEW
```

### 1936. `/SCF/BINDANDASS`

```
T-Code: /SCF/BINDANDASS
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/ASPECTASS, /SCF/CONTBNDG, /SCF/NAMEBNDG
```

### 1937. `/SCF/CHARTCFG`

```
T-Code: /SCF/CHARTCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CATMAP, /SCF/CHART, /SCF/SERIESMAP
```

### 1938. `/SCF/CHARTPROP`

```
T-Code: /SCF/CHARTPROP
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CHARTPROP
```

### 1939. `/SCF/CMPTTYPE`

```
T-Code: /SCF/CMPTTYPE
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CMPTTYP
```

### 1940. `/SCF/FRMCFG`

```
T-Code: /SCF/FRMCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/FIELDPRM, /SCF/FORMBOX, /SCF/FRMFIELDS, /SCF/FRMLYT, /SCF/FRMPROP
```

### 1941. `/SCF/FROBJCFG`

```
T-Code: /SCF/FROBJCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/FROBJCNTX, /SCF/FROBJDEF, /SCF/FROBJHIER
```

### 1942. `/SCF/ICHDMAXTSDMTSTP`

```
T-Code: /SCF/ICHDMAXTSDMTSTP
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1943. `/SCF/ICHDMCFG`

```
T-Code: /SCF/ICHDMCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CBAGGLEVDEF, /SCF/CBSERVICES, /SCF/CB_DEF, /SCF/VCBEAGGCPRM, /SCF/VCBEAGGKPRM, /SCF/VCBEAGGLEV, /SCF/VCBEBASSERV, /SCF/VCBEKPRMSER, /SCF/VCBE_ACTINP, /SCF/VCBE_ACTION, /SCF/VCBE_DEP
```

### 1944. `/SCF/ICHDMMODELCFG`

```
T-Code: /SCF/ICHDMMODELCFG
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1945. `/SCF/ICHDMVIEWCFG`

```
T-Code: /SCF/ICHDMVIEWCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/CBVCPROP, /SCF/CBVLYT, /SCF/CBV_DEF, /SCF/VCBVECMPLX, /SCF/VCBVEH, /SCF/VCBVESTATES, /SCF/VCBVEV, /SCF/VCBVE_COMP
```

### 1946. `/SCF/LINKSET`

```
T-Code: /SCF/LINKSET
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/LINKSETV
```

### 1947. `/SCF/LINKSETASS`

```
T-Code: /SCF/LINKSETASS
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → ADMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/LINKSETASCV, /SCF/LINKSETASNV
```

### 1948. `/SCF/NAVCFG`

```
T-Code: /SCF/NAVCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/NAV, /SCF/NAVCLSS
```

### 1949. `/SCF/PMD_LOC`

```
T-Code: /SCF/PMD_LOC
├── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_DEVELOP
    ├── ACTVT → 1, 16, 2, 3, 40, 41, 42, 6, 7, 70, 90, 94, L0, MA
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
├── S_DOKU_AUT
    ├── DOKU_ACT → ACTIVATE
    ├── DOKU_DEVCL → (empty — maintain in PFCG)
    └── DOKU_MODE → (empty — maintain in PFCG)
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
└── S_TRANSLAT
    ├── ACTVT → 2
    ├── TLANGUAGE → (empty — maintain in PFCG)
    └── TRANOBJ → (empty — maintain in PFCG)
```

### 1950. `/SCF/PMD_MAT`

```
T-Code: /SCF/PMD_MAT
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1951. `/SCF/PRMEVALMETH`

```
T-Code: /SCF/PRMEVALMETH
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 23, 3, 43, 5, 50, 6, 60, 65, 75, 78, 90
    └── TTYPE → (empty — maintain in PFCG)
```

### 1952. `/SCF/RFC`

```
T-Code: /SCF/RFC
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/RFC
```

### 1953. `/SCF/RFCROLES`

```
T-Code: /SCF/RFCROLES
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 1954. `/SCF/SCREENCFG`

```
T-Code: /SCF/SCREENCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/EVCMPT, /SCF/SCRDEF, /SCF/SCRLYT
```

### 1955. `/SCF/SELMODE_PD`

```
T-Code: /SCF/SELMODE_PD
├── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/SELMODE_PD
```

### 1956. `/SCF/SELOBJ_CFG`

```
T-Code: /SCF/SELOBJ_CFG
├── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPAPO/SCNCTRLV, /SCF/SELMODE, /SCF/SELMODE_DEF, /SCF/SELMODE_PD, /SCF/SELOBJ, /SCF/SELOBJ_KF, /SCF/SELOBJ_KF_D
```

### 1957. `/SCF/SNC_VIS_DATA`

```
T-Code: /SCF/SNC_VIS_DATA
├── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6
    └── APO_LOC → '
└── C_APO_PROD
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_LOC → '
    └── APO_PROD → '
```

### 1958. `/SCF/SPECCLSS`

```
T-Code: /SCF/SPECCLSS
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/SPECCLASS
```

### 1959. `/SCF/SPPSDPRX`

```
T-Code: /SCF/SPPSDPRX
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── C_APO_UIF
    ├── /SCF/APPID → /SCF/SPPSDPRX
    ├── /SCF/SCRID → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 1960. `/SCF/TABCFG`

```
T-Code: /SCF/TABCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/TAB
```

### 1961. `/SCF/TBVCFG`

```
T-Code: /SCF/TBVCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/FIELDPRM, /SCF/TBVSTCOLLYT, /SCF/TBVSTD, /SCF/TBVSTDCOL
```

### 1962. `/SCF/TBVTSCFG`

```
T-Code: /SCF/TBVTSCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/TBVTS, /SCF/TBVTSCOL, /SCF/TBVTSROW, /SCF/TBVTSROWLYT
```

### 1963. `/SCF/TLBRCFG`

```
T-Code: /SCF/TLBRCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/EVCMPT, /SCF/SCRLYT, /SCF/TLBR, /SCF/TLBRPROP
```

### 1964. `/SCF/TLBR_FILTER_SET`

```
T-Code: /SCF/TLBR_FILTER_SET
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/TLBR_FILTER
```

### 1965. `/SCF/TXTAREACFG`

```
T-Code: /SCF/TXTAREACFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/TAFIELDS, /SCF/TAREAPROP, /SCF/TEXTAREA
```

### 1966. `/SCF/UFRMCFG`

```
T-Code: /SCF/UFRMCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/UFRMFIELDS, /SCF/UFRMLYT, /SCF/UFRMPROP
```

### 1967. `/SCF/UNAVCFG`

```
T-Code: /SCF/UNAVCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/UNAV
```

### 1968. `/SCF/USCREENSCFG`

```
T-Code: /SCF/USCREENSCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/USCRDEF, /SCF/USCRLYT
```

### 1969. `/SCF/USELBOXCFG`

```
T-Code: /SCF/USELBOXCFG
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/USELBOXLYT, /SCF/USELBOXPRM
```

### 1970. `/SCF/UTBVSTDCFG`

```
T-Code: /SCF/UTBVSTDCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/UTBVSTCOLYT, /SCF/UTBVSTD, /SCF/UTBVSTDCOL
```

### 1971. `/SCF/UTBVTSCFG`

```
T-Code: /SCF/UTBVTSCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/UTBVTS, /SCF/UTBVTSCOL, /SCF/UTBVTSROW, /SCF/UTBVTSROWLT
```

### 1972. `/SCF/UTLBRCFG`

```
T-Code: /SCF/UTLBRCFG
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCF/APPDEF, /SCF/APPVIEW, /SCF/UTLBR, /SCF/UTLBRPROP
```

### 1973. `/SCF/VISCTRLPROFASSN`

```
T-Code: /SCF/VISCTRLPROFASSN
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1974. `/SCF/VISCTRLPROF_CRE`

```
T-Code: /SCF/VISCTRLPROF_CRE
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1975. `/SCF/VISCTRLPROF_DEL`

```
T-Code: /SCF/VISCTRLPROF_DEL
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1976. `/SCF/VISCTRLPROF_DIS`

```
T-Code: /SCF/VISCTRLPROF_DIS
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1977. `/SCF/VISCTRLPROF_MDY`

```
T-Code: /SCF/VISCTRLPROF_MDY
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

### 1978. `/SCF/VPROF_SHM_RESET`

```
T-Code: /SCF/VPROF_SHM_RESET
└── S_DATASET
    ├── ACTVT → 33, 34, 6, A6, A7
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
```

## `/SCMB/` — SAP SCM Basis
> T-codes: **197**


### 1979. `/SCMB/05000001`

```
T-Code: /SCMB/05000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSGLOBAL
```

### 1980. `/SCMB/05000002`

```
T-Code: /SCMB/05000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSLEVEL
```

### 1981. `/SCMB/05000003`

```
T-Code: /SCMB/05000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSELEM
```

### 1982. `/SCMB/05000004`

```
T-Code: /SCMB/05000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VPSLVSET, /SCWM/VPSLVSETAS, /SCWM/VPSLVSETES
```

### 1983. `/SCMB/05000005`

```
T-Code: /SCMB/05000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSWSTYP
```

### 1984. `/SCMB/05000006`

```
T-Code: /SCMB/05000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VPSLETPMTY
```

### 1985. `/SCMB/05000007`

```
T-Code: /SCMB/05000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VPSWSPMTY
```

### 1986. `/SCMB/05000014`

```
T-Code: /SCMB/05000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_MAT_ORNT
```

### 1987. `/SCMB/05000018`

```
T-Code: /SCMB/05000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_OPT
```

### 1988. `/SCMB/05000019`

```
T-Code: /SCMB/05000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334QSD
```

### 1989. `/SCMB/05000023`

```
T-Code: /SCMB/05000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_OPTRP, /SCMB/V_PB_OPTRU
```

### 1990. `/SCMB/05000034`

```
T-Code: /SCMB/05000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_FTPT_UOM
```

### 1991. `/SCMB/15000003`

```
T-Code: /SCMB/15000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/MD_LOCK
```

### 1992. `/SCMB/15000023`

```
T-Code: /SCMB/15000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSDISTRIB
```

### 1993. `/SCMB/15000024`

```
T-Code: /SCMB/15000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VPSLVSET_A
```

### 1994. `/SCMB/15000042`

```
T-Code: /SCMB/15000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPESMPPR
```

### 1995. `/SCMB/15000105`

```
T-Code: /SCMB/15000105
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDLINASS, /SCMB/TDLORASS, /SCMB/TDLTSASS, /SCMB/VTDLCPI, /SCMB/VTDLCPO, /SCMB/VTDLCPTS
```

### 1996. `/SCMB/17000018`

```
T-Code: /SCMB/17000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VLOC_GL
```

### 1997. `/SCMB/17000020`

```
T-Code: /SCMB/17000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_DDD_STRA
```

### 1998. `/SCMB/17000021`

```
T-Code: /SCMB/17000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_GIS_ZONE
```

### 1999. `/SCMB/17000022`

```
T-Code: /SCMB/17000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_GIS_ZN_A
```

### 2000. `/SCMB/17000023`

```
T-Code: /SCMB/17000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/DDD_BUFC
```

### 2001. `/SCMB/17000024`

```
T-Code: /SCMB/17000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRAT
```

### 2002. `/SCMB/17000025`

```
T-Code: /SCMB/17000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPOOL
```

### 2003. `/SCMB/17000026`

```
T-Code: /SCMB/17000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATM
```

### 2004. `/SCMB/17000027`

```
T-Code: /SCMB/17000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/PARDEF
```

### 2005. `/SCMB/17000028`

```
T-Code: /SCMB/17000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPAR
```

### 2006. `/SCMB/17000029`

```
T-Code: /SCMB/17000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATMP
```

### 2007. `/SCMB/17000043`

```
T-Code: /SCMB/17000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ALPRIOCFGV
```

### 2008. `/SCMB/17000044`

```
T-Code: /SCMB/17000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_PRC_SRV
```

### 2009. `/SCMB/17000046`

```
T-Code: /SCMB/17000046
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_SLC
```

### 2010. `/SCMB/17000047`

```
T-Code: /SCMB/17000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_T016
```

### 2011. `/SCMB/17000048`

```
T-Code: /SCMB/17000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_T052
```

### 2012. `/SCMB/17000049`

```
T-Code: /SCMB/17000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_T077D
```

### 2013. `/SCMB/17000050`

```
T-Code: /SCMB/17000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_T077K
```

### 2014. `/SCMB/17000051`

```
T-Code: /SCMB/17000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_TPAR
```

### 2015. `/SCMB/17000052`

```
T-Code: /SCMB/17000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_TPRIO
```

### 2016. `/SCMB/17000059`

```
T-Code: /SCMB/17000059
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_VSB2TSL
```

### 2017. `/SCMB/36000001`

```
T-Code: /SCMB/36000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOC_F4F_CV
```

### 2018. `/SCMB/47000033`

```
T-Code: /SCMB/47000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_DF_DCTP
```

### 2019. `/SCMB/47000034`

```
T-Code: /SCMB/47000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → MDOBLACLA, MDOBLAOBJ, MVOBLPROP, MVOBLREL, MVOBLRO, MVOBLROL
```

### 2020. `/SCMB/47000035`

```
T-Code: /SCMB/47000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_DF_CHAIN, /SCMB/V_DF_REL
```

### 2021. `/SCMB/48000013`

```
T-Code: /SCMB/48000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTPMTYP
```

### 2022. `/SCMB/48000015`

```
T-Code: /SCMB/48000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPACKGR
```

### 2023. `/SCMB/48000016`

```
T-Code: /SCMB/48000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TAPM
```

### 2024. `/SCMB/48000017`

```
T-Code: /SCMB/48000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TIDART
```

### 2025. `/SCMB/48000018`

```
T-Code: /SCMB/48000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/THUTYP
```

### 2026. `/SCMB/48000029`

```
T-Code: /SCMB/48000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSGROUP
```

### 2027. `/SCMB/60000626`

```
T-Code: /SCMB/60000626
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPP_DF, /SCMB/SPP_WF
```

### 2028. `/SCMB/68000006`

```
T-Code: /SCMB/68000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VPESTPAP, /SAPAPO/VPE_STAP
```

### 2029. `/SCMB/68000017`

```
T-Code: /SCMB/68000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TBUSSYS
```

### 2030. `/SCMB/72000005`

```
T-Code: /SCMB/72000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 2031. `/SCMB/72000007`

```
T-Code: /SCMB/72000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/VTG_ATR, /SCMB/VTG_ENT, /SCMB/VTG_NID, /SCMB/VTG_SEL
```

### 2032. `/SCMB/72000008`

```
T-Code: /SCMB/72000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/VTG_GEN, /SCMB/VTG_GRIE, /SCMB/VTG_GRP, /SCMB/VTG_GRST, /SCMB/VTG_USR, /SCMB/VT_GRDEF
```

### 2033. `/SCMB/72000009`

```
T-Code: /SCMB/72000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDL_BC_V, /SCMB/TDL_BO_V, /SCMB/TDL_CO_V, /SCMB/TDL_DA_V, /SCMB/TDL_DR_V, /SCMB/TDL_FA_V
```

### 2034. `/SCMB/72000010`

```
T-Code: /SCMB/72000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDL_BC2V, /SCMB/TDL_BO_V, /SCMB/TDL_CO_V, /SCMB/TDL_DA_V, /SCMB/TDL_DI_V, /SCMB/TDL_DP_V, /SCMB/TDL_DT_V, /SCMB/TDL_PB_V
```

### 2035. `/SCMB/72000012`

```
T-Code: /SCMB/72000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/THUTYP
```

### 2036. `/SCMB/72000013`

```
T-Code: /SCMB/72000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TIDART
```

### 2037. `/SCMB/72000014`

```
T-Code: /SCMB/72000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDL_BC_V, /SCMB/TDL_BO_V, /SCMB/TDL_CO_V, /SCMB/TDL_DA_V, /SCMB/TDL_DR_V, /SCMB/TDL_FA_V
```

### 2038. `/SCMB/72000072`

```
T-Code: /SCMB/72000072
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDL_TSSV, /SCMB/TDL_TS_V, /SCMB/V_SEMI, /SCMB/V_SEMKEY, /SCMB/V_SEMTSTP
```

### 2039. `/SCMB/73000006`

```
T-Code: /SCMB/73000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_SPLRCGL2
```

### 2040. `/SCMB/73000009`

```
T-Code: /SCMB/73000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_BPMAPGEN
```

### 2041. `/SCMB/73000010`

```
T-Code: /SCMB/73000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_BPMAPDIF
```

### 2042. `/SCMB/73000012`

```
T-Code: /SCMB/73000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_BPTMTXT
```

### 2043. `/SCMB/78000001`

```
T-Code: /SCMB/78000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_TINC
```

### 2044. `/SCMB/78000002`

```
T-Code: /SCMB/78000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/FRREC
```

### 2045. `/SCMB/78000003`

```
T-Code: /SCMB/78000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/BUYTYPE
```

### 2046. `/SCMB/78000004`

```
T-Code: /SCMB/78000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TPRRGRTY
```

### 2047. `/SCMB/78000005`

```
T-Code: /SCMB/78000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/THR_BSKEY, /SCMB/VHR_WGETYP
```

### 2048. `/SCMB/78000008`

```
T-Code: /SCMB/78000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/RG_BASIC_V
```

### 2049. `/SCMB/78000009`

```
T-Code: /SCMB/78000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SCRCBO
```

### 2050. `/SCMB/78000010`

```
T-Code: /SCMB/78000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SCRCAF
```

### 2051. `/SCMB/78000011`

```
T-Code: /SCMB/78000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SCRCAM
```

### 2052. `/SCMB/78000012`

```
T-Code: /SCMB/78000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_GTS_MOT
```

### 2053. `/SCMB/78000029`

```
T-Code: /SCMB/78000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_RGREQTY
```

### 2054. `/SCMB/78000030`

```
T-Code: /SCMB/78000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_GAF, /SCTM/V_GAR2, /SCTM/V_GAV
```

### 2055. `/SCMB/78000031`

```
T-Code: /SCMB/78000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCF/CBAGGLEVDEF, /SCF/CBSERVICES, /SCF/CB_DEF, /SCF/VCBEAGGCPRM, /SCF/VCBEAGGKPRM, /SCF/VCBEAGGLEV, /SCF/VCBEBASSERV, /SCF/VCBEKPRMSER, /SCF/VCBE_ACTINP, /SCF/VCBE_ACTION, /SCF/VCBE_DEP
```

### 2056. `/SCMB/78000038`

```
T-Code: /SCMB/78000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SPLRCSDT
```

### 2057. `/SCMB/78000042`

```
T-Code: /SCMB/78000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SCRCEQ
```

### 2058. `/SCMB/78000043`

```
T-Code: /SCMB/78000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SCRCLOG
```

### 2059. `/SCMB/78000049`

```
T-Code: /SCMB/78000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PDA
```

### 2060. `/SCMB/78000050`

```
T-Code: /SCMB/78000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/PDA_CONFIG
```

### 2061. `/SCMB/78000055`

```
T-Code: /SCMB/78000055
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCF/CBSERVICES, /SCF/VCBAGGKPRM, /SCF/VCBAGGLEV, /SCF/VCBKPRMSERV, /SCF/VCB_DEF, /SCF/VCB_DEP, /SCF/VCCBAGGKPRM, /SCF/VCCBKPRMSER, /SCF/VCCB_DEP
```

### 2062. `/SCMB/78000075`

```
T-Code: /SCMB/78000075
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SE_DEMPLCD
```

### 2063. `/SCMB/78000076`

```
T-Code: /SCMB/78000076
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/SE_PRDGRCD
```

### 2064. `/SCMB/83000009`

```
T-Code: /SCMB/83000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_PROF
```

### 2065. `/SCMB/83000011`

```
T-Code: /SCMB/83000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCBEGRU_V
```

### 2066. `/SCMB/83000012`

```
T-Code: /SCMB/83000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCPARAL_V
```

### 2067. `/SCMB/83000013`

```
T-Code: /SCMB/83000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCEAPP_V
```

### 2068. `/SCMB/83000014`

```
T-Code: /SCMB/83000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCEARV_V
```

### 2069. `/SCMB/83000015`

```
T-Code: /SCMB/83000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCNXTCH_V
```

### 2070. `/SCMB/83000016`

```
T-Code: /SCMB/83000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOCECLSP_V, /SCMB/LOCECLSS_V
```

### 2071. `/SCMB/83000020`

```
T-Code: /SCMB/83000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/LOC_L_R
```

### 2072. `/SCMB/86000086`

```
T-Code: /SCMB/86000086
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/VOCIND, /SCMB/VOCINDDEF, /SCMB/VOCOMP, /SCMB/VOCOMPGEN, /SCMB/VOCPRM, /SCMB/VODAR, /SCMB/VODARSC, /SCMB/VODARTAB, /SCMB/VODTPACC, /SCMB/VODTPACCSQ, /SCMB/VODTPDEF, /SCMB/VODTPEXT, /SCMB/VODTPGEN, /SCMB/VODTPIND, /SCMB/VOTPDEF
```

### 2073. `/SCMB/86000087`

```
T-Code: /SCMB/86000087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ORITMTP, /SCMB/ORPRTROLE, /SCMB/ORSDLTP, /SCMB/ORTSTTP
```

### 2074. `/SCMB/86000089`

```
T-Code: /SCMB/86000089
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → RSSCM_BWAPO_V, RSSCM_BWCLNT
```

### 2075. `/SCMB/86000090`

```
T-Code: /SCMB/86000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/PARAM
```

### 2076. `/SCMB/86000101`

```
T-Code: /SCMB/86000101
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ANOTAPPL
```

### 2077. `/SCMB/86000107`

```
T-Code: /SCMB/86000107
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ANOTMCH
```

### 2078. `/SCMB/86000125`

```
T-Code: /SCMB/86000125
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_SCACTI, /SCMB/V_SCDATE, /SCMB/V_SCDURA, /SCMB/V_SCLASS
```

### 2079. `/SCMB/86000126`

```
T-Code: /SCMB/86000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCDURG, /SCMB/TSCEXPN, /SCMB/TSCIMAP, /SCMB/TSCLOCA, /SCMB/TSCMAST, /SCMB/TSCMETG, /SCMB/TSCSCHD, /SCMB/TSCSCHE, /SCMB/TSCTSTR, /SCMB/TSCTZON, /SCMB/V_SCSCHM
```

### 2080. `/SCMB/86000127`

```
T-Code: /SCMB/86000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCDMAP
```

### 2081. `/SCMB/86000128`

```
T-Code: /SCMB/86000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TIMECUUT
```

### 2082. `/SCMB/86000129`

```
T-Code: /SCMB/86000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCUNDA, /SCMB/V_SCUNAS, V_TIMECUUT
```

### 2083. `/SCMB/86000132`

```
T-Code: /SCMB/86000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_SCPRAL
```

### 2084. `/SCMB/86000134`

```
T-Code: /SCMB/86000134
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCMASTC
```

### 2085. `/SCMB/86000135`

```
T-Code: /SCMB/86000135
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCMASTD
```

### 2086. `/SCMB/86000136`

```
T-Code: /SCMB/86000136
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSCMASTZ
```

### 2087. `/SCMB/AID`

```
T-Code: /SCMB/AID
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMB/AID
```

### 2088. `/SCMB/ALENCFG`

```
T-Code: /SCMB/ALENCFG
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
```

### 2089. `/SCMB/ALPRIOCFG`

```
T-Code: /SCMB/ALPRIOCFG
├── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → AMON
└── S_TCODE
    └── TCD → '/SCMB/ALPRIOCFG'
```

### 2090. `/SCMB/ANOTAPPL`

```
T-Code: /SCMB/ANOTAPPL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ANOTAPPL
```

### 2091. `/SCMB/ANOTMCH`

```
T-Code: /SCMB/ANOTMCH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ANOTMCH
```

### 2092. `/SCMB/ANOTMP`

```
T-Code: /SCMB/ANOTMP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ANOTMP, /SCMB/VANOTMPP
```

### 2093. `/SCMB/ANOTPULL`

```
T-Code: /SCMB/ANOTPULL
└── C_APO_AMON
    └── ACTVT → 16, 3
```

### 2094. `/SCMB/APO_LOC3_CALL`

```
T-Code: /SCMB/APO_LOC3_CALL
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2095. `/SCMB/APO_MAT1_CALL`

```
T-Code: /SCMB/APO_MAT1_CALL
└── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 2096. `/SCMB/BOL_INVMAP_TR`

```
T-Code: /SCMB/BOL_INVMAP_TR
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2097. `/SCMB/CNACUST`

```
T-Code: /SCMB/CNACUST
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/CNA_APP, /SCMB/CNA_APPOBJ, /SCMB/CNA_APPSOB
```

### 2098. `/SCMB/CNACUSTOBJ`

```
T-Code: /SCMB/CNACUSTOBJ
└── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → &NC&
```

### 2099. `/SCMB/DDD_B_DELETION`

```
T-Code: /SCMB/DDD_B_DELETION
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 2100. `/SCMB/DDD_B_PRECALC`

```
T-Code: /SCMB/DDD_B_PRECALC
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 2101. `/SCMB/DDD_CLEANUP`

```
T-Code: /SCMB/DDD_CLEANUP
└── /SCTM/RG
    └── ACTVT → 2, 3, 6
```

### 2102. `/SCMB/DDD_DELETION`

```
T-Code: /SCMB/DDD_DELETION
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMB/MD
    └── ALG_SUBOBJ → DDD
```

### 2103. `/SCMB/DDD_LOG_DELETE`

```
T-Code: /SCMB/DDD_LOG_DELETE
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SCMB/MD
    └── ALG_SUBOBJ → DDD
```

### 2104. `/SCMB/DDD_LOG_SHOW`

```
T-Code: /SCMB/DDD_LOG_SHOW
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMB/MD
    └── ALG_SUBOBJ → DDD
```

### 2105. `/SCMB/DDD_MAINTAIN`

```
T-Code: /SCMB/DDD_MAINTAIN
├── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SCMB/MD
    └── ALG_SUBOBJ → DDD
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 2106. `/SCMB/DDD_PRECALC`

```
T-Code: /SCMB/DDD_PRECALC
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMB/MD
    └── ALG_SUBOBJ → DDD
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 2107. `/SCMB/DF_CHAIN`

```
T-Code: /SCMB/DF_CHAIN
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2108. `/SCMB/DF_CUST`

```
T-Code: /SCMB/DF_CUST
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2109. `/SCMB/DLV_PTGR`

```
T-Code: /SCMB/DLV_PTGR
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/DM_PTGR
```

### 2110. `/SCMB/GEOMAPCUST`

```
T-Code: /SCMB/GEOMAPCUST
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/GEOMAPCUST
```

### 2111. `/SCMB/LD_BUF_NEGATIV`

```
T-Code: /SCMB/LD_BUF_NEGATIV
└── /SCTM/RG
    └── ACTVT → 1, 2, 3, 6
```

### 2112. `/SCMB/LD_TEST`

```
T-Code: /SCMB/LD_TEST
└── /SCTM/RG
    └── ACTVT → 3
```

### 2113. `/SCMB/LOC_CUST`

```
T-Code: /SCMB/LOC_CUST
└── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
```

### 2114. `/SCMB/LOC_DEL`

```
T-Code: /SCMB/LOC_DEL
└── C_APO_LOC
    ├── ACTVT → 16, 32, 6
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2115. `/SCMB/LOC_DISPL_LOG`

```
T-Code: /SCMB/LOC_DISPL_LOG
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2116. `/SCMB/LOC_PRE_EOP`

```
T-Code: /SCMB/LOC_PRE_EOP
├── /SCMB/LOCB
    ├── ACTVT → 3, 5, 8
    └── BRGRU → (empty — maintain in PFCG)
└── C_APO_LOC
    ├── ACTVT → 2, 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2117. `/SCMB/LOC_UNBLOCK_MD`

```
T-Code: /SCMB/LOC_UNBLOCK_MD
├── /SCMB/LOCB
    ├── ACTVT → 3, 5
    └── BRGRU → (empty — maintain in PFCG)
└── C_APO_LOC
    ├── ACTVT → 16, 2, 32
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2118. `/SCMB/MAT_ORIENT`

```
T-Code: /SCMB/MAT_ORIENT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_MAT_ORNT
```

### 2119. `/SCMB/ODM_BASE_CFG`

```
T-Code: /SCMB/ODM_BASE_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/ORITMTP, /SCMB/ORPRTROLE, /SCMB/ORSDLTP, /SCMB/ORTSTTP
```

### 2120. `/SCMB/ODM_META_CFG`

```
T-Code: /SCMB/ODM_META_CFG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/VOCIND, /SCMB/VOCINDDEF, /SCMB/VOCOMP, /SCMB/VOCOMPGEN, /SCMB/VOCPRM, /SCMB/VODAR, /SCMB/VODARSC, /SCMB/VODARTAB, /SCMB/VODTPACC, /SCMB/VODTPACCSQ, /SCMB/VODTPDEF, /SCMB/VODTPEXT, /SCMB/VODTPGEN, /SCMB/VODTPIND, /SCMB/VOTPDEF
```

### 2121. `/SCMB/PB_FOOTPRT`

```
T-Code: /SCMB/PB_FOOTPRT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_FTPT
```

### 2122. `/SCMB/PB_NLQ_DET`

```
T-Code: /SCMB/PB_NLQ_DET
└── P_PKGBUILD
    └── ACTVT → 3
```

### 2123. `/SCMB/PB_OPTRP`

```
T-Code: /SCMB/PB_OPTRP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_OPTRP, /SCMB/V_PB_OPTRU
```

### 2124. `/SCMB/PB_OPT_PRF`

```
T-Code: /SCMB/PB_OPT_PRF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_OPT
```

### 2125. `/SCMB/PB_PKG_ASS`

```
T-Code: /SCMB/PB_PKG_ASS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_BP_PTA, /SCMB/V_PPTA, /SCMB/V_PTA
```

### 2126. `/SCMB/PB_PRD_PKG_ASS`

```
T-Code: /SCMB/PB_PRD_PKG_ASS
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PTYPE_AS
```

### 2127. `/SCMB/PB_PROFILE`

```
T-Code: /SCMB/PB_PROFILE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_PROFX
```

### 2128. `/SCMB/PB_PTA_DEL`

```
T-Code: /SCMB/PB_PTA_DEL
└── P_PKGBUILD
    └── ACTVT → 3, 6
```

### 2129. `/SCMB/PFM2_DELETE`

```
T-Code: /SCMB/PFM2_DELETE
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
└── S_TCODE
    └── TCD → PFM
```

### 2130. `/SCMB/PLN_GRP`

```
T-Code: /SCMB/PLN_GRP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/PLNRGRP
```

### 2131. `/SCMB/PRMCFG`

```
T-Code: /SCMB/PRMCFG
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → TSDC
```

### 2132. `/SCMB/PRR1`

```
T-Code: /SCMB/PRR1
├── B_BUPA_ATT
    ├── ACTVT → 1
    ├── AUTHTYP → (empty — maintain in PFCG)
    ├── AUVAL1 → (empty — maintain in PFCG)
    └── AUVAL2 → (empty — maintain in PFCG)
├── B_BUPA_FDG
    ├── ACTVT → 2
    └── FLDGR → (empty — maintain in PFCG)
├── B_BUPA_GRP
    ├── ACTVT → 1
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 1
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 1, 2, 3, 6
    └── RELTYP → LM0001, LM0002
├── B_CCARD
    └── ACTVT → 1, 2, 3
└── B_USERSTAT
    ├── ACTVT → 1, 6
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → BUS
    └── STSMA → (empty — maintain in PFCG)
```

### 2133. `/SCMB/PRR2`

```
T-Code: /SCMB/PRR2
├── B_BUPA_ATT
    ├── ACTVT → 2
    ├── AUTHTYP → (empty — maintain in PFCG)
    ├── AUVAL1 → (empty — maintain in PFCG)
    └── AUVAL2 → (empty — maintain in PFCG)
├── B_BUPA_FDG
    ├── ACTVT → 2
    └── FLDGR → (empty — maintain in PFCG)
└── B_BUPA_RLT
    ├── ACTVT → 2
    └── RLTYP → (empty — maintain in PFCG)
```

### 2134. `/SCMB/PRR3`

```
T-Code: /SCMB/PRR3
├── B_BUPA_ATT
    ├── ACTVT → 3
    ├── AUTHTYP → (empty — maintain in PFCG)
    ├── AUVAL1 → (empty — maintain in PFCG)
    └── AUVAL2 → (empty — maintain in PFCG)
├── B_BUPA_FDG
    ├── ACTVT → 3
    └── FLDGR → 2985, 2986, 2987, 2988
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
```

### 2135. `/SCMB/PRT_PARTNER`

```
T-Code: /SCMB/PRT_PARTNER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/VPRTPARTNER
```

### 2136. `/SCMB/PRT_PAYTERMS`

```
T-Code: /SCMB/PRT_PAYTERMS
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2137. `/SCMB/PRT_PURDOC`

```
T-Code: /SCMB/PRT_PURDOC
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2138. `/SCMB/REASON`

```
T-Code: /SCMB/REASON
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/REASON
```

### 2139. `/SCMB/SCHED_CAL`

```
T-Code: /SCMB/SCHED_CAL
└── C_SCMBS_CA
    └── ACTVT → 1, 2, 3, 6
```

### 2140. `/SCMB/SCHED_DEL`

```
T-Code: /SCMB/SCHED_DEL
└── C_SCMBS_DD
    └── ACTVT → 41
```

### 2141. `/SCMB/SCR_EQ_STATUS`

```
T-Code: /SCMB/SCR_EQ_STATUS
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 2142. `/SCMB/SCUHIERMAIN`

```
T-Code: /SCMB/SCUHIERMAIN
└── C_MD_HIER
    └── ACTVT → 2, 3
```

### 2143. `/SCMB/SCUMAIN`

```
T-Code: /SCMB/SCUMAIN
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
```

### 2144. `/SCMB/SC_CUST_ALIAS`

```
T-Code: /SCMB/SC_CUST_ALIAS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2145. `/SCMB/SC_CUST_BLOCKS`

```
T-Code: /SCMB/SC_CUST_BLOCKS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2146. `/SCMB/SC_CUST_DMAP`

```
T-Code: /SCMB/SC_CUST_DMAP
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2147. `/SCMB/SC_CUST_SCHEMA`

```
T-Code: /SCMB/SC_CUST_SCHEMA
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2148. `/SCMB/SC_CUST_TUNIT`

```
T-Code: /SCMB/SC_CUST_TUNIT
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2149. `/SCMB/SC_CUST_UNITAS`

```
T-Code: /SCMB/SC_CUST_UNITAS
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2150. `/SCMB/SC_MAST_CAL`

```
T-Code: /SCMB/SC_MAST_CAL
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2151. `/SCMB/SC_MAST_DURA`

```
T-Code: /SCMB/SC_MAST_DURA
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2152. `/SCMB/SC_MAST_TZON`

```
T-Code: /SCMB/SC_MAST_TZON
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCMS
```

### 2153. `/SCMB/SELCFG`

```
T-Code: /SCMB/SELCFG
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2154. `/SCMB/SPLREP_DISPLOG`

```
T-Code: /SCMB/SPLREP_DISPLOG
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 4, 60, 61
```

### 2155. `/SCMB/SP_NR`

```
T-Code: /SCMB/SP_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SAPAPO/SP
```

### 2156. `/SCMB/TDL_BO_COPY`

```
T-Code: /SCMB/TDL_BO_COPY
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TDLINASS, /SCMB/TDLORASS, /SCMB/TDLTSASS, /SCMB/VTDLCPI, /SCMB/VTDLCPO, /SCMB/VTDLCPTS
```

### 2157. `/SCMB/TEST_PB`

```
T-Code: /SCMB/TEST_PB
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── P_PKGBUILD
    └── ACTVT → 3
```

### 2158. `/SCMB/TEST_UPB`

```
T-Code: /SCMB/TEST_UPB
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── P_PKGBUILD
    └── ACTVT → 3
```

### 2159. `/SCMB/TG_CFG`

```
T-Code: /SCMB/TG_CFG
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2160. `/SCMB/TG_ID`

```
T-Code: /SCMB/TG_ID
└── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2161. `/SCMB/TMORG02`

```
T-Code: /SCMB/TMORG02
└── S_TABU_NAM
    ├── ACTVT → 2
    └── TABLE → T001
```

### 2162. `/SCMB/TMSRESCPT`

```
T-Code: /SCMB/TMSRESCPT
└── C_APO_RES
    ├── ACTVT → 1, 16
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 2163. `/SCMB/TMSRESLBR`

```
T-Code: /SCMB/TMSRESLBR
├── B_BUPA_RLT
    ├── ACTVT → 1, 2, 3
    └── RLTYP → (empty — maintain in PFCG)
└── C_APO_RES
    ├── ACTVT → 1, 16
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 2164. `/SCMB/TRIG_ACTIVATE`

```
T-Code: /SCMB/TRIG_ACTIVATE
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 2165. `/SCMB/TSMAPPING`

```
T-Code: /SCMB/TSMAPPING
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_MSGSEM, /SCMB/V_MSGTSTP, /SCMB/V_SEMKEY, /SCMB/V_SEMTSTP
```

### 2166. `/SCMB/TSRESET`

```
T-Code: /SCMB/TSRESET
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2167. `/SCMB/UPB_PROFILE`

```
T-Code: /SCMB/UPB_PROFILE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_PROFX
```

### 2168. `/SCMB/VALCHK`

```
T-Code: /SCMB/VALCHK
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/BOL_VALCHK
```

### 2169. `/SCMB/VALCHKATTR`

```
T-Code: /SCMB/VALCHKATTR
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/BOL_CHKCMP
```

### 2170. `/SCMB/VALCHKATTRC`

```
T-Code: /SCMB/VALCHKATTRC
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2171. `/SCMB/VALCHKC`

```
T-Code: /SCMB/VALCHKC
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2172. `/SCMB/VALGRP`

```
T-Code: /SCMB/VALGRP
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/BOL_VALGRP
```

### 2173. `/SCMB/VALPRO`

```
T-Code: /SCMB/VALPRO
├── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMB/BOL_VALPRO
```

### 2174. `/SCMB/VALPROC`

```
T-Code: /SCMB/VALPROC
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → BDMC
```

### 2175. `/SCMB/VC_MAT_PKG_RP`

```
T-Code: /SCMB/VC_MAT_PKG_RP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PKG_R, /SCMB/V_PKG_RC, /SCMB/V_PKG_RP
```

## `/SCMTMS/` — SAP Transportation Management (TM)
> T-codes: **436**


### 2176. `/SCMTMS/07000001`

```
T-Code: /SCMTMS/07000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_RPCURR
```

### 2177. `/SCMTMS/07000002`

```
T-Code: /SCMTMS/07000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_STGTY
```

### 2178. `/SCMTMS/07000003`

```
T-Code: /SCMTMS/07000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_MVMTY
```

### 2179. `/SCMTMS/07000004`

```
T-Code: /SCMTMS/07000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_MVMSTG
```

### 2180. `/SCMTMS/07000005`

```
T-Code: /SCMTMS/07000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_STGMOT
```

### 2181. `/SCMTMS/07000006`

```
T-Code: /SCMTMS/07000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CM_RSN, /SCMTMS/V_CM_T
```

### 2182. `/SCMTMS/07000031`

```
T-Code: /SCMTMS/07000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SRDP_T, /SCMTMS/V_SRDR_T
```

### 2183. `/SCMTMS/07000035`

```
T-Code: /SCMTMS/07000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQITY
```

### 2184. `/SCMTMS/07000036`

```
T-Code: /SCMTMS/07000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQIT3
```

### 2185. `/SCMTMS/07000037`

```
T-Code: /SCMTMS/07000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQIT4
```

### 2186. `/SCMTMS/07000038`

```
T-Code: /SCMTMS/07000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_BCV_AP, /SCMTMS/V_TORCAT, /SCMTMS/V_TRQCAT
```

### 2187. `/SCMTMS/07000039`

```
T-Code: /SCMTMS/07000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CAPR
```

### 2188. `/SCMTMS/07000040`

```
T-Code: /SCMTMS/07000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FACOND, /SCMTMS/V_FAIASN, /SCMTMS/V_FATY
```

### 2189. `/SCMTMS/07000041`

```
T-Code: /SCMTMS/07000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ATIASG, /SCMTMS/V_FWATY, /SCMTMS/V_FWCOND
```

### 2190. `/SCMTMS/07000042`

```
T-Code: /SCMTMS/07000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SCHTY
```

### 2191. `/SCMTMS/07000044`

```
T-Code: /SCMTMS/07000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SISORG
```

### 2192. `/SCMTMS/07000045`

```
T-Code: /SCMTMS/07000045
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SICORG
```

### 2193. `/SCMTMS/07000046`

```
T-Code: /SCMTMS/07000046
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITS, /SCMTMS/V_TORTYS
```

### 2194. `/SCMTMS/07000047`

```
T-Code: /SCMTMS/07000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TSR
```

### 2195. `/SCMTMS/07000049`

```
T-Code: /SCMTMS/07000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_VENT
```

### 2196. `/SCMTMS/07000050`

```
T-Code: /SCMTMS/07000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TTAMT
```

### 2197. `/SCMTMS/07000054`

```
T-Code: /SCMTMS/07000054
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SEG_ID, /SCMTMS/V_SICORG, /SCMTMS/V_SISORG
```

### 2198. `/SCMTMS/07000058`

```
T-Code: /SCMTMS/07000058
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_INCSTY
```

### 2199. `/SCMTMS/07000064`

```
T-Code: /SCMTMS/07000064
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CNLRC
```

### 2200. `/SCMTMS/07000074`

```
T-Code: /SCMTMS/07000074
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORBW
```

### 2201. `/SCMTMS/20000002`

```
T-Code: /SCMTMS/20000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DCASS, /SCMTMS/V_DCFLT, /SCMTMS/V_DCFLTP, /SCMTMS/V_DCPRF
```

### 2202. `/SCMTMS/20000004`

```
T-Code: /SCMTMS/20000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_AFCCTR
```

### 2203. `/SCMTMS/20000012`

```
T-Code: /SCMTMS/20000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RATETY
```

### 2204. `/SCMTMS/20000015`

```
T-Code: /SCMTMS/20000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FRTERM
```

### 2205. `/SCMTMS/20000016`

```
T-Code: /SCMTMS/20000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FRTCLA
```

### 2206. `/SCMTMS/20000018`

```
T-Code: /SCMTMS/20000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PAYDEF
```

### 2207. `/SCMTMS/20000019`

```
T-Code: /SCMTMS/20000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SHPCPE
```

### 2208. `/SCMTMS/20000021`

```
T-Code: /SCMTMS/20000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DLVCPE
```

### 2209. `/SCMTMS/20000022`

```
T-Code: /SCMTMS/20000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TTSG
```

### 2210. `/SCMTMS/20000030`

```
T-Code: /SCMTMS/20000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CONTI, /SCMTMS/V_CONTIC
```

### 2211. `/SCMTMS/20000031`

```
T-Code: /SCMTMS/20000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORITY, /SCMTMS/V_TORSTA
```

### 2212. `/SCMTMS/20000035`

```
T-Code: /SCMTMS/20000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_OP_ST, /SCMTMS/V_ST_CND
```

### 2213. `/SCMTMS/20000038`

```
T-Code: /SCMTMS/20000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RFQTST, /SCMTMS/V_RFQTYP
```

### 2214. `/SCMTMS/20000039`

```
T-Code: /SCMTMS/20000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FASETP
```

### 2215. `/SCMTMS/20000040`

```
T-Code: /SCMTMS/20000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PDT
```

### 2216. `/SCMTMS/20000041`

```
T-Code: /SCMTMS/20000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_UNLOC
```

### 2217. `/SCMTMS/20000042`

```
T-Code: /SCMTMS/20000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PA_ROL
```

### 2218. `/SCMTMS/20000043`

```
T-Code: /SCMTMS/20000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EV4RC, /SCMTMS/V_EVRC, /SCMTMS/V_RC_EV
```

### 2219. `/SCMTMS/20000044`

```
T-Code: /SCMTMS/20000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EVTASG, /SCMTMS/V_EVTPRF
```

### 2220. `/SCMTMS/20000045`

```
T-Code: /SCMTMS/20000045
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DG_RQT
```

### 2221. `/SCMTMS/20000046`

```
T-Code: /SCMTMS/20000046
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DISPRS, /SCMTMS/V_DISPTY
```

### 2222. `/SCMTMS/20000047`

```
T-Code: /SCMTMS/20000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_WFL_LV
```

### 2223. `/SCMTMS/20000048`

```
T-Code: /SCMTMS/20000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RFQRSN
```

### 2224. `/SCMTMS/20000049`

```
T-Code: /SCMTMS/20000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FWRTYP
```

### 2225. `/SCMTMS/20000050`

```
T-Code: /SCMTMS/20000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DISFCP, /SCMTMS/V_FCPDID
```

### 2226. `/SCMTMS/20000051`

```
T-Code: /SCMTMS/20000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_INDSID
```

### 2227. `/SCMTMS/20000052`

```
T-Code: /SCMTMS/20000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_INDITO, /SCMTMS/V_TOL_G, /SCMTMS/V_TOL_GC, /SCMTMS/V_TOL_GT
```

### 2228. `/SCMTMS/20000054`

```
T-Code: /SCMTMS/20000054
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBASE, /SCMTMS/V_RCBASE
```

### 2229. `/SCMTMS/20000055`

```
T-Code: /SCMTMS/20000055
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PACMT1, /SCMTMS/V_PACMT2
```

### 2230. `/SCMTMS/20000061`

```
T-Code: /SCMTMS/20000061
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TDLTY
```

### 2231. `/SCMTMS/20000062`

```
T-Code: /SCMTMS/20000062
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_MAPSYS
```

### 2232. `/SCMTMS/20000067`

```
T-Code: /SCMTMS/20000067
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PSREASC
```

### 2233. `/SCMTMS/20000072`

```
T-Code: /SCMTMS/20000072
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCET_P
```

### 2234. `/SCMTMS/20000074`

```
T-Code: /SCMTMS/20000074
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_EQINTRES
```

### 2235. `/SCMTMS/20000075`

```
T-Code: /SCMTMS/20000075
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_FLINTRES
```

### 2236. `/SCMTMS/20000084`

```
T-Code: /SCMTMS/20000084
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CTRY_T
```

### 2237. `/SCMTMS/20000090`

```
T-Code: /SCMTMS/20000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CHRGT, /SCMTMS/V_CHSCAT, /SCMTMS/V_CH_CAT, /SCMTMS/V_CTRY
```

### 2238. `/SCMTMS/20000098`

```
T-Code: /SCMTMS/20000098
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FCI_T
```

### 2239. `/SCMTMS/24000009`

```
T-Code: /SCMTMS/24000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GIDTC
```

### 2240. `/SCMTMS/24000011`

```
T-Code: /SCMTMS/24000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_BLRC
```

### 2241. `/SCMTMS/24000013`

```
T-Code: /SCMTMS/24000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_BTDIT
```

### 2242. `/SCMTMS/24000023`

```
T-Code: /SCMTMS/24000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_BTDTY
```

### 2243. `/SCMTMS/24000030`

```
T-Code: /SCMTMS/24000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_PTRO
```

### 2244. `/SCMTMS/24000090`

```
T-Code: /SCMTMS/24000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FASET
```

### 2245. `/SCMTMS/24000094`

```
T-Code: /SCMTMS/24000094
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FATY
```

### 2246. `/SCMTMS/24000095`

```
T-Code: /SCMTMS/24000095
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FADE
```

### 2247. `/SCMTMS/24000102`

```
T-Code: /SCMTMS/24000102
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SCBASE
```

### 2248. `/SCMTMS/24000104`

```
T-Code: /SCMTMS/24000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RBCM, /SCMTMS/V_RES_BS, /SCMTMS/V_RES_HR
```

### 2249. `/SCMTMS/24000105`

```
T-Code: /SCMTMS/24000105
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBASE, /SCMTMS/V_RCBASE
```

### 2250. `/SCMTMS/24000107`

```
T-Code: /SCMTMS/24000107
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_LBRAVSD
```

### 2251. `/SCMTMS/24000108`

```
T-Code: /SCMTMS/24000108
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_LBRAVS
```

### 2252. `/SCMTMS/24000110`

```
T-Code: /SCMTMS/24000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCMAM, /SCMTMS/V_TCMAMC, /SCMTMS/V_TCMAP
```

### 2253. `/SCMTMS/24000111`

```
T-Code: /SCMTMS/24000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CLBSRO
```

### 2254. `/SCMTMS/24000112`

```
T-Code: /SCMTMS/24000112
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCMNRI
```

### 2255. `/SCMTMS/24000113`

```
T-Code: /SCMTMS/24000113
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCCAT
```

### 2256. `/SCMTMS/24000114`

```
T-Code: /SCMTMS/24000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCCLS
```

### 2257. `/SCMTMS/24000115`

```
T-Code: /SCMTMS/24000115
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FTTCET, /SCMTMS/V_INTCET, /SCMTMS/V_SRVTYP, /SCMTMS/V_TCET_D, /SCMTMS/V_TCET_L
```

### 2258. `/SCMTMS/24000116`

```
T-Code: /SCMTMS/24000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RDRULE, /SCMTMS/V_RNDPR1, /SCMTMS/V_RNDPR2, /SCMTMS/V_RNDPRF
```

### 2259. `/SCMTMS/24000126`

```
T-Code: /SCMTMS/24000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CALMCL
```

### 2260. `/SCMTMS/24000133`

```
T-Code: /SCMTMS/24000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_TM_MTR
```

### 2261. `/SCMTMS/24000144`

```
T-Code: /SCMTMS/24000144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_RESTUDF
```

### 2262. `/SCMTMS/24000169`

```
T-Code: /SCMTMS/24000169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CTX, /SCMTMS/V_CTXCL
```

### 2263. `/SCMTMS/24000181`

```
T-Code: /SCMTMS/24000181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_LOC
```

### 2264. `/SCMTMS/24000182`

```
T-Code: /SCMTMS/24000182
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_TLANE
```

### 2265. `/SCMTMS/26000006`

```
T-Code: /SCMTMS/26000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_TYAFLD, /SCMB/C_TYASH
```

### 2266. `/SCMTMS/26000011`

```
T-Code: /SCMTMS/26000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_SCRM
```

### 2267. `/SCMTMS/26000012`

```
T-Code: /SCMTMS/26000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_SCRE
```

### 2268. `/SCMTMS/26000013`

```
T-Code: /SCMTMS/26000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GMCCME, /SCMTMS/V_GMCCMH, /SCMTMS/V_GMCICO, /SCMTMS/V_GMCLHD, /SCMTMS/V_GMCRAT, /SCMTMS/V_GMCRHD, /SCMTMS/V_GMCRLA, /SCMTMS/V_GMCRLC, /SCMTMS/V_GMCRLH, /SCMTMS/V_GMCRSC
```

### 2269. `/SCMTMS/26000014`

```
T-Code: /SCMTMS/26000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EDICP
```

### 2270. `/SCMTMS/26000020`

```
T-Code: /SCMTMS/26000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_UNDOST
```

### 2271. `/SCMTMS/26000022`

```
T-Code: /SCMTMS/26000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PLAPCO, /SCMTMS/V_PLAPP, /SCMTMS/V_PLAPPB, /SCMTMS/V_PLB, /SCMTMS/V_PLCO, /SCMTMS/V_PLCOC, /SCMTMS/V_PLCOCB
```

### 2272. `/SCMTMS/26000023`

```
T-Code: /SCMTMS/26000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_BLKINH, /SCMTMS/V_BLKPRF
```

### 2273. `/SCMTMS/26000025`

```
T-Code: /SCMTMS/26000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_AWCONR
```

### 2274. `/SCMTMS/26000029`

```
T-Code: /SCMTMS/26000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/C_CD_RES
```

### 2275. `/SCMTMS/26000031`

```
T-Code: /SCMTMS/26000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/VV_AGR_ORD_PARTY, /SCMTMS/VV_FWA_ORG, /SCMTMS/V_AGRCOM, /SCMTMS/V_AGRPAR, /SCMTMS/V_AGRPRE, /SCMTMS/V_AGRTL, /SCMTMS/V_FWAGR, /SCMTMS/V_FWAITM
```

### 2276. `/SCMTMS/26000032`

```
T-Code: /SCMTMS/26000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/VV_AGR_CARRIER, /SCMTMS/VV_FA_ORG, /SCMTMS/V_AGRCOM, /SCMTMS/V_AGRPAR, /SCMTMS/V_AGRPRE, /SCMTMS/V_AGRTL, /SCMTMS/V_FAGR, /SCMTMS/V_FAITM
```

### 2277. `/SCMTMS/26000033`

```
T-Code: /SCMTMS/26000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/VV_AGR_CARRIER, /SCMTMS/VV_IA_ORG, /SCMTMS/V_AGRCOM, /SCMTMS/V_AGRPAR, /SCMTMS/V_AGRPRE, /SCMTMS/V_AGRTL, /SCMTMS/V_IAGR, /SCMTMS/V_IAITM
```

### 2278. `/SCMTMS/26000034`

```
T-Code: /SCMTMS/26000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CICMRS, /SCMTMS/V_CICM_T
```

### 2279. `/SCMTMS/26000036`

```
T-Code: /SCMTMS/26000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_UINAV
```

### 2280. `/SCMTMS/50000008`

```
T-Code: /SCMTMS/50000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TBUSSYS
```

### 2281. `/SCMTMS/50000011`

```
T-Code: /SCMTMS/50000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RES_UG
```

### 2282. `/SCMTMS/50000021`

```
T-Code: /SCMTMS/50000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EVMETH, /SCMTMS/V_EV_REL, /SCMTMS/V_EV_TY, /SCMTMS/V_TRAC
```

### 2283. `/SCMTMS/50000022`

```
T-Code: /SCMTMS/50000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQTY1
```

### 2284. `/SCMTMS/50000023`

```
T-Code: /SCMTMS/50000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQTY2
```

### 2285. `/SCMTMS/50000024`

```
T-Code: /SCMTMS/50000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQTY3
```

### 2286. `/SCMTMS/50000025`

```
T-Code: /SCMTMS/50000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQTY4
```

### 2287. `/SCMTMS/50000047`

```
T-Code: /SCMTMS/50000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_SFIR_T
```

### 2288. `/SCMTMS/50000048`

```
T-Code: /SCMTMS/50000048
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SFIR_T
```

### 2289. `/SCMTMS/50000051`

```
T-Code: /SCMTMS/50000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CFIR_T
```

### 2290. `/SCMTMS/50000053`

```
T-Code: /SCMTMS/50000053
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DG_ACT
```

### 2291. `/SCMTMS/50000058`

```
T-Code: /SCMTMS/50000058
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CONDTY
```

### 2292. `/SCMTMS/50000059`

```
T-Code: /SCMTMS/50000059
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITA, /SCMTMS/V_TORTYA
```

### 2293. `/SCMTMS/50000060`

```
T-Code: /SCMTMS/50000060
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITB, /SCMTMS/V_TORTYB
```

### 2294. `/SCMTMS/50000061`

```
T-Code: /SCMTMS/50000061
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITF, /SCMTMS/V_TORTYF
```

### 2295. `/SCMTMS/50000066`

```
T-Code: /SCMTMS/50000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_TENVIS
```

### 2296. `/SCMTMS/50000067`

```
T-Code: /SCMTMS/50000067
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DG_PRF
```

### 2297. `/SCMTMS/50000069`

```
T-Code: /SCMTMS/50000069
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FCP
```

### 2298. `/SCMTMS/50000070`

```
T-Code: /SCMTMS/50000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DIMWP
```

### 2299. `/SCMTMS/50000076`

```
T-Code: /SCMTMS/50000076
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SFIR_T
```

### 2300. `/SCMTMS/50000083`

```
T-Code: /SCMTMS/50000083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_TENDCP
```

### 2301. `/SCMTMS/50000094`

```
T-Code: /SCMTMS/50000094
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TVTR
```

### 2302. `/SCMTMS/50000095`

```
T-Code: /SCMTMS/50000095
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_REJRN, /SCMTMS/C_TENDCP, /SCMTMS/C_TENDDE, /SCMTMS/C_TENDGC, /SCMTMS/C_TENDNM, /SCMTMS/C_TENDPS, /SCMTMS/C_TENVIS
```

### 2303. `/SCMTMS/50000130`

```
T-Code: /SCMTMS/50000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SHFOTY
```

### 2304. `/SCMTMS/50000134`

```
T-Code: /SCMTMS/50000134
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCM_MD
```

### 2305. `/SCMTMS/50000137`

```
T-Code: /SCMTMS/50000137
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_SCAC
```

### 2306. `/SCMTMS/50000142`

```
T-Code: /SCMTMS/50000142
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ROUTY
```

### 2307. `/SCMTMS/50000143`

```
T-Code: /SCMTMS/50000143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DSCPRA, /SCMTMS/V_DSCPRF, /SCMTMS/V_DSCTY
```

### 2308. `/SCMTMS/50000144`

```
T-Code: /SCMTMS/50000144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_INS, /SCMTMS/V_INSASN, /SCMTMS/V_INSSET
```

### 2309. `/SCMTMS/50000146`

```
T-Code: /SCMTMS/50000146
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PA_SCH, /SCMTMS/V_PR_SCH
```

### 2310. `/SCMTMS/50000148`

```
T-Code: /SCMTMS/50000148
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GT_ST, /SCMTMS/V_ST_ASS, /SCMTMS/V_ST_CUM
```

### 2311. `/SCMTMS/50000150`

```
T-Code: /SCMTMS/50000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_AICOND, /SCMTMS/V_AISASG, /SCMTMS/V_AITY
```

### 2312. `/SCMTMS/50000151`

```
T-Code: /SCMTMS/50000151
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_STCTY
```

### 2313. `/SCMTMS/50000152`

```
T-Code: /SCMTMS/50000152
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_COBAS
```

### 2314. `/SCMTMS/50000153`

```
T-Code: /SCMTMS/50000153
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORATT
```

### 2315. `/SCMTMS/50000154`

```
T-Code: /SCMTMS/50000154
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ACTASS, /SCMTMS/V_CCPRO, /SCMTMS/V_GT_ACT
```

### 2316. `/SCMTMS/50000155`

```
T-Code: /SCMTMS/50000155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_IATAC
```

### 2317. `/SCMTMS/50000156`

```
T-Code: /SCMTMS/50000156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_AIRLC
```

### 2318. `/SCMTMS/50000157`

```
T-Code: /SCMTMS/50000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_AIRCR
```

### 2319. `/SCMTMS/50000158`

```
T-Code: /SCMTMS/50000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_SPHC
```

### 2320. `/SCMTMS/50000159`

```
T-Code: /SCMTMS/50000159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_IATAR
```

### 2321. `/SCMTMS/50000160`

```
T-Code: /SCMTMS/50000160
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_IATAT
```

### 2322. `/SCMTMS/50000161`

```
T-Code: /SCMTMS/50000161
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_IATA05
```

### 2323. `/SCMTMS/50000164`

```
T-Code: /SCMTMS/50000164
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCDPRO
```

### 2324. `/SCMTMS/50000165`

```
T-Code: /SCMTMS/50000165
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCDCLS
```

### 2325. `/SCMTMS/50000169`

```
T-Code: /SCMTMS/50000169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_NSS
```

### 2326. `/SCMTMS/50000170`

```
T-Code: /SCMTMS/50000170
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_OFST
```

### 2327. `/SCMTMS/50000171`

```
T-Code: /SCMTMS/50000171
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_IACOND, /SCMTMS/V_IATIAS, /SCMTMS/V_INTY
```

### 2328. `/SCMTMS/50000174`

```
T-Code: /SCMTMS/50000174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/DIST_EMISS
```

### 2329. `/SCMTMS/50000176`

```
T-Code: /SCMTMS/50000176
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CCODE, /SCMTMS/V_CTYPE
```

### 2330. `/SCMTMS/50000177`

```
T-Code: /SCMTMS/50000177
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → VBIV_MAP_SERVERC
```

### 2331. `/SCMTMS/50000178`

```
T-Code: /SCMTMS/50000178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → VBIV_APP, VBIV_APP_MS_ASGN, VBIV_CXT_MS_ASGN, VBIV_GC_SERV, VBIV_MAP_PRODUCT, VBIV_MAP_SERVER, VBIV_MAP_STACK, VBIV_MSTACK_ASGN, VBIV_SERV_URL
```

### 2332. `/SCMTMS/50000187`

```
T-Code: /SCMTMS/50000187
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_SHTYP, /SCMTMS/V_MODSH
```

### 2333. `/SCMTMS/50000188`

```
T-Code: /SCMTMS/50000188
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_IATAC
```

### 2334. `/SCMTMS/50000189`

```
T-Code: /SCMTMS/50000189
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ULDRTY
```

### 2335. `/SCMTMS/50000190`

```
T-Code: /SCMTMS/50000190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ULDRT
```

### 2336. `/SCMTMS/50000194`

```
T-Code: /SCMTMS/50000194
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_HIERAR, /SCMTMS/V_OHSWIT, /SCMTMS/V_OHTYP
```

### 2337. `/SCMTMS/50000203`

```
T-Code: /SCMTMS/50000203
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SLC_S, /SCMTMS/V_SLC_SM
```

### 2338. `/SCMTMS/50000204`

```
T-Code: /SCMTMS/50000204
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TALTY
```

### 2339. `/SCMTMS/50000211`

```
T-Code: /SCMTMS/50000211
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_NOTES
```

### 2340. `/SCMTMS/50000212`

```
T-Code: /SCMTMS/50000212
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_IATA_C
```

### 2341. `/SCMTMS/50000216`

```
T-Code: /SCMTMS/50000216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITU, /SCMTMS/V_TORTYU
```

### 2342. `/SCMTMS/50000218`

```
T-Code: /SCMTMS/50000218
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DSUTY
```

### 2343. `/SCMTMS/50000222`

```
T-Code: /SCMTMS/50000222
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FAIASN
```

### 2344. `/SCMTMS/50000223`

```
T-Code: /SCMTMS/50000223
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FAISAS
```

### 2345. `/SCMTMS/50000224`

```
T-Code: /SCMTMS/50000224
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FAICON, /SCMTMS/V_FAITY
```

### 2346. `/SCMTMS/50000225`

```
T-Code: /SCMTMS/50000225
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SEC_MF
```

### 2347. `/SCMTMS/50000228`

```
T-Code: /SCMTMS/50000228
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_WBNTY
```

### 2348. `/SCMTMS/50000229`

```
T-Code: /SCMTMS/50000229
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_STGPR, /SCMTMS/V_STRSTG
```

### 2349. `/SCMTMS/50000231`

```
T-Code: /SCMTMS/50000231
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESCLS
```

### 2350. `/SCMTMS/50000232`

```
T-Code: /SCMTMS/50000232
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_IAICON, /SCMTMS/V_IAITY
```

### 2351. `/SCMTMS/50000233`

```
T-Code: /SCMTMS/50000233
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/C_BPTMTXT
```

### 2352. `/SCMTMS/50000238`

```
T-Code: /SCMTMS/50000238
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RJRC
```

### 2353. `/SCMTMS/50000243`

```
T-Code: /SCMTMS/50000243
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_HCPRO, /SCMTMS/V_HCPRS, /SCMTMS/V_MTRHC
```

### 2354. `/SCMTMS/50000244`

```
T-Code: /SCMTMS/50000244
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_INC_PR
```

### 2355. `/SCMTMS/50000245`

```
T-Code: /SCMTMS/50000245
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SFCMRS, /SCMTMS/V_SFCM_T
```

### 2356. `/SCMTMS/52000460`

```
T-Code: /SCMTMS/52000460
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TINC
```

### 2357. `/SCMTMS/52000474`

```
T-Code: /SCMTMS/52000474
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TVTY_TM
```

### 2358. `/SCMTMS/52000507`

```
T-Code: /SCMTMS/52000507
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TTGR
```

### 2359. `/SCMTMS/52000538`

```
T-Code: /SCMTMS/52000538
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRIG
```

### 2360. `/SCMTMS/52000553`

```
T-Code: /SCMTMS/52000553
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TPAR
```

### 2361. `/SCMTMS/52000554`

```
T-Code: /SCMTMS/52000554
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TPAUM
```

### 2362. `/SCMTMS/52000570`

```
T-Code: /SCMTMS/52000570
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PA_TTY
```

### 2363. `/SCMTMS/52000635`

```
T-Code: /SCMTMS/52000635
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LOINT
```

### 2364. `/SCMTMS/52000685`

```
T-Code: /SCMTMS/52000685
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SFIR_I
```

### 2365. `/SCMTMS/52000686`

```
T-Code: /SCMTMS/52000686
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CH_TAX, /SCMTMS/V_CH_TYP, /SCMTMS/V_CS_TAX, /SCMTMS/V_TMCTRY
```

### 2366. `/SCMTMS/52000688`

```
T-Code: /SCMTMS/52000688
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SRVGRP
```

### 2367. `/SCMTMS/52000691`

```
T-Code: /SCMTMS/52000691
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCCAT, /SCMTMS/V_TCETYP, /SCMTMS/V_TCET_D, /SCMTMS/V_TCSCAT
```

### 2368. `/SCMTMS/52000987`

```
T-Code: /SCMTMS/52000987
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TVLS_KEY
```

### 2369. `/SCMTMS/52000988`

```
T-Code: /SCMTMS/52000988
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TTSG
```

### 2370. `/SCMTMS/52001143`

```
T-Code: /SCMTMS/52001143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CHTX_F, /SCMTMS/V_CTRY
```

### 2371. `/SCMTMS/52001144`

```
T-Code: /SCMTMS/52001144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CFSD_T
```

### 2372. `/SCMTMS/52001153`

```
T-Code: /SCMTMS/52001153
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCM_V_SLSASSGN
```

### 2373. `/SCMTMS/52001154`

```
T-Code: /SCMTMS/52001154
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCM_V_PURASSGN
```

### 2374. `/SCMTMS/52001155`

```
T-Code: /SCMTMS/52001155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCM_V_PURASSGN
```

### 2375. `/SCMTMS/52001156`

```
T-Code: /SCMTMS/52001156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCM_V_TC_INTR_CE
```

### 2376. `/SCMTMS/52001157`

```
T-Code: /SCMTMS/52001157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CONCHT
```

### 2377. `/SCMTMS/52001158`

```
T-Code: /SCMTMS/52001158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TCDCHT
```

### 2378. `/SCMTMS/52001174`

```
T-Code: /SCMTMS/52001174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCM_V_TCTX_MAP, VCTRY_CODES
```

### 2379. `/SCMTMS/52001216`

```
T-Code: /SCMTMS/52001216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_OTTAMT
```

### 2380. `/SCMTMS/52001217`

```
T-Code: /SCMTMS/52001217
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DTTAMT
```

### 2381. `/SCMTMS/52001223`

```
T-Code: /SCMTMS/52001223
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_FUBDEF
```

### 2382. `/SCMTMS/52001538`

```
T-Code: /SCMTMS/52001538
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LCUOMC
```

### 2383. `/SCMTMS/52001657`

```
T-Code: /SCMTMS/52001657
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_KBLK, /SCMTMS/V_KBLYT
```

### 2384. `/SCMTMS/52001701`

```
T-Code: /SCMTMS/52001701
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CCPLNT
```

### 2385. `/SCMTMS/52001702`

```
T-Code: /SCMTMS/52001702
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CTPVCC
```

### 2386. `/SCMTMS/52001962`

```
T-Code: /SCMTMS/52001962
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_CAVSRT, /SCMTMS/D_COSTRO, /SCMTMS/D_MPSETT, /SCMTMS/D_OPTSET, /SCMTMS/D_TCSTRT, /SCMTMS/D_VSSSET, /SCMTMS/V_CAVSRU, /SCMTMS/V_CDCSC, /SCMTMS/V_CDCSR, /SCMTMS/V_COSTFU, /SCMTMS/V_COSTMT, /SCMTMS/V_PLNPRF, /SCMTMS/V_VSSFLD
```

### 2387. `/SCMTMS/52001982`

```
T-Code: /SCMTMS/52001982
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LOC_CA
```

### 2388. `/SCMTMS/52002012`

```
T-Code: /SCMTMS/52002012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_LDDP
```

### 2389. `/SCMTMS/52002013`

```
T-Code: /SCMTMS/52002013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_VEHLP
```

### 2390. `/SCMTMS/52002014`

```
T-Code: /SCMTMS/52002014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_AEQUP, /SCMB/V_AEQUPAE
```

### 2391. `/SCMTMS/52002015`

```
T-Code: /SCMTMS/52002015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_LLP
```

### 2392. `/SCMTMS/52002023`

```
T-Code: /SCMTMS/52002023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORTYC
```

### 2393. `/SCMTMS/52002028`

```
T-Code: /SCMTMS/52002028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITC, /SCMTMS/V_TORTYC
```

### 2394. `/SCMTMS/52002515`

```
T-Code: /SCMTMS/52002515
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LPPP
```

### 2395. `/SCMTMS/52002516`

```
T-Code: /SCMTMS/52002516
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_LPPPA
```

### 2396. `/SCMTMS/52002530`

```
T-Code: /SCMTMS/52002530
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LPPL
```

### 2397. `/SCMTMS/52002531`

```
T-Code: /SCMTMS/52002531
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_LPPLF
```

### 2398. `/SCMTMS/52002774`

```
T-Code: /SCMTMS/52002774
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GRPRLC
```

### 2399. `/SCMTMS/52002879`

```
T-Code: /SCMTMS/52002879
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PLN_UI
```

### 2400. `/SCMTMS/52002895`

```
T-Code: /SCMTMS/52002895
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_FUBRT
```

### 2401. `/SCMTMS/52002896`

```
T-Code: /SCMTMS/52002896
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_FUBRPQ
```

### 2402. `/SCMTMS/52002910`

```
T-Code: /SCMTMS/52002910
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_GRPROT
```

### 2403. `/SCMTMS/52002911`

```
T-Code: /SCMTMS/52002911
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_GRPRUL
```

### 2404. `/SCMTMS/52002913`

```
T-Code: /SCMTMS/52002913
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_PL, /SCMTMS/D_PLB, /SCMTMS/D_PLC, /SCMTMS/D_PLCC, /SCMTMS/D_PLCCCV, /SCMTMS/D_PLD, /SCMTMS/D_PLSWIT, /SCMTMS/D_PLV, /SCMTMS/D_PL_ADW
```

### 2405. `/SCMTMS/52003009`

```
T-Code: /SCMTMS/52003009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ASN_DT
```

### 2406. `/SCMTMS/52003029`

```
T-Code: /SCMTMS/52003029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_FUBRPQ, /SCMTMS/D_FUBRT
```

### 2407. `/SCMTMS/52003092`

```
T-Code: /SCMTMS/52003092
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCLOC
```

### 2408. `/SCMTMS/52003184`

```
T-Code: /SCMTMS/52003184
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCBP2
```

### 2409. `/SCMTMS/52003190`

```
T-Code: /SCMTMS/52003190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ACPACT, /SCMTMS/V_ACPROT
```

### 2410. `/SCMTMS/52003198`

```
T-Code: /SCMTMS/52003198
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCCRT
```

### 2411. `/SCMTMS/52003199`

```
T-Code: /SCMTMS/52003199
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCLO2
```

### 2412. `/SCMTMS/52003374`

```
T-Code: /SCMTMS/52003374
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCFAG
```

### 2413. `/SCMTMS/52003375`

```
T-Code: /SCMTMS/52003375
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CBCSRV
```

### 2414. `/SCMTMS/52003408`

```
T-Code: /SCMTMS/52003408
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TOWNBS
```

### 2415. `/SCMTMS/52003443`

```
T-Code: /SCMTMS/52003443
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DRFLDS, /SCMTMS/V_DRPROF
```

### 2416. `/SCMTMS/52003738`

```
T-Code: /SCMTMS/52003738
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GRPRLR
```

### 2417. `/SCMTMS/52003794`

```
T-Code: /SCMTMS/52003794
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SWITCH
```

### 2418. `/SCMTMS/52003859`

```
T-Code: /SCMTMS/52003859
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERH_BS
```

### 2419. `/SCMTMS/52003860`

```
T-Code: /SCMTMS/52003860
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERHPCT
```

### 2420. `/SCMTMS/52003861`

```
T-Code: /SCMTMS/52003861
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERHRSN
```

### 2421. `/SCMTMS/52003877`

```
T-Code: /SCMTMS/52003877
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_DRVTYP
```

### 2422. `/SCMTMS/52004155`

```
T-Code: /SCMTMS/52004155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_NOS
```

### 2423. `/SCMTMS/52004156`

```
T-Code: /SCMTMS/52004156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_NSC
```

### 2424. `/SCMTMS/52004158`

```
T-Code: /SCMTMS/52004158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_NSCA
```

### 2425. `/SCMTMS/52004159`

```
T-Code: /SCMTMS/52004159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TUNS
```

### 2426. `/SCMTMS/52004183`

```
T-Code: /SCMTMS/52004183
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TDRDET
```

### 2427. `/SCMTMS/52004330`

```
T-Code: /SCMTMS/52004330
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LI_EWM
```

### 2428. `/SCMTMS/52004331`

```
T-Code: /SCMTMS/52004331
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LPFEWM
```

### 2429. `/SCMTMS/52004332`

```
T-Code: /SCMTMS/52004332
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TDREWM
```

### 2430. `/SCMTMS/62000004`

```
T-Code: /SCMTMS/62000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DAD
```

### 2431. `/SCMTMS/62000005`

```
T-Code: /SCMTMS/62000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_CN_DAT
```

### 2432. `/SCMTMS/62000006`

```
T-Code: /SCMTMS/62000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_CN_DAL
```

### 2433. `/SCMTMS/62000021`

```
T-Code: /SCMTMS/62000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPAR
```

### 2434. `/SCMTMS/62000044`

```
T-Code: /SCMTMS/62000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CFUNC
```

### 2435. `/SCMTMS/72000003`

```
T-Code: /SCMTMS/72000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RE_A
```

### 2436. `/SCMTMS/72000005`

```
T-Code: /SCMTMS/72000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CALRUL, /SCMTMS/V_RATE, /SCMTMS/V_RTSCAL, /SCMTMS/V_RTVAL, /SCMTMS/V_RT_ORG, /SCMTMS/V_VALCRL
```

### 2437. `/SCMTMS/72000006`

```
T-Code: /SCMTMS/72000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SCAITM, /SCMTMS/V_SCALE
```

### 2438. `/SCMTMS/72000014`

```
T-Code: /SCMTMS/72000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FWATY
```

### 2439. `/SCMTMS/72000015`

```
T-Code: /SCMTMS/72000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQAT1
```

### 2440. `/SCMTMS/72000016`

```
T-Code: /SCMTMS/72000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQAT2
```

### 2441. `/SCMTMS/72000026`

```
T-Code: /SCMTMS/72000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/GEOMAPCUST
```

### 2442. `/SCMTMS/72000027`

```
T-Code: /SCMTMS/72000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PRTTF, /SCMTMS/V_PRTTTY
```

### 2443. `/SCMTMS/72000028`

```
T-Code: /SCMTMS/72000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_MSG_SE
```

### 2444. `/SCMTMS/72000031`

```
T-Code: /SCMTMS/72000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_CSGEN, /SCMTMS/C_CSKEY, /SCMTMS/C_CSKEYV
```

### 2445. `/SCMTMS/72000034`

```
T-Code: /SCMTMS/72000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_PLN_AC, /SCMTMS/V_PLN_AA, /SCMTMS/V_PLN_BD, /SCMTMS/V_PLN_CB, /SCMTMS/V_PLN_CM, /SCMTMS/V_PLN_CS
```

### 2446. `/SCMTMS/72000050`

```
T-Code: /SCMTMS/72000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SRVCE, /SCMTMS/V_TCCS, /SCMTMS/V_TCCSIT, /SCMTMS/V_TCS_PR, /SCMTMS/V_TCS_TL, /SCMTMS/V_TS_CRL
```

### 2447. `/SCMTMS/76000006`

```
T-Code: /SCMTMS/76000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PATTRN
```

### 2448. `/SCMTMS/76000007`

```
T-Code: /SCMTMS/76000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESTPL
```

### 2449. `/SCMTMS/76000008`

```
T-Code: /SCMTMS/76000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESAID
```

### 2450. `/SCMTMS/76000009`

```
T-Code: /SCMTMS/76000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESQTP, /SCMB/V_RESQU
```

### 2451. `/SCMTMS/76000010`

```
T-Code: /SCMTMS/76000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESGRA, /SCMB/V_RESGTP
```

### 2452. `/SCMTMS/76000011`

```
T-Code: /SCMTMS/76000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESAEQ
```

### 2453. `/SCMTMS/76000012`

```
T-Code: /SCMTMS/76000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESAGCRS
```

### 2454. `/SCMTMS/76000020`

```
T-Code: /SCMTMS/76000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/TALVIEW
```

### 2455. `/SCMTMS/76000022`

```
T-Code: /SCMTMS/76000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/CDL_AGN, /SCMB/CDL_ID, /SCMB/CDL_TYPE, /SCMB/V_CDLMAP
```

### 2456. `/SCMTMS/76000023`

```
T-Code: /SCMTMS/76000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_EQU_CMBV, /SCMB/V_EQU_CODE, /SCMB/V_EQU_CODH, /SCMB/V_EQU_CODV, /SCMB/V_EQU_MOT, /SCMB/V_EQU_TYPE, /SCMB/V_EQU_TYPH, /SCMB/V_EQU_TYPV
```

### 2457. `/SCMTMS/76000024`

```
T-Code: /SCMTMS/76000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PDA
```

### 2458. `/SCMTMS/76000025`

```
T-Code: /SCMTMS/76000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/PDA_CONFIG
```

### 2459. `/SCMTMS/76000035`

```
T-Code: /SCMTMS/76000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRAT
```

### 2460. `/SCMTMS/76000036`

```
T-Code: /SCMTMS/76000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPOOL
```

### 2461. `/SCMTMS/76000037`

```
T-Code: /SCMTMS/76000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATM
```

### 2462. `/SCMTMS/76000038`

```
T-Code: /SCMTMS/76000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/PARDEF
```

### 2463. `/SCMTMS/76000039`

```
T-Code: /SCMTMS/76000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATMP
```

### 2464. `/SCMTMS/76000056`

```
T-Code: /SCMTMS/76000056
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/HIERPRIO
```

### 2465. `/SCMTMS/76000066`

```
T-Code: /SCMTMS/76000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_COMMON
```

### 2466. `/SCMTMS/96000004`

```
T-Code: /SCMTMS/96000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EWMPR
```

### 2467. `/SCMTMS/96000005`

```
T-Code: /SCMTMS/96000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LPV_CH, /SCMTMS/V_LPV_CM, /SCMTMS/V_LPV_CO, /SCMTMS/V_LPV_DS, /SCMTMS/V_LPV_KB, /SCMTMS/V_LPV_KS, /SCMTMS/V_LPV_LT, /SCMTMS/V_LPV_MD, /SCMTMS/V_LPV_SM, /SCMTMS/V_LPV_WC, /SCMTMS/V_LPV_WG
```

### 2468. `/SCMTMS/96000006`

```
T-Code: /SCMTMS/96000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FAFLAT, /SCMTMS/V_FLAPRF
```

### 2469. `/SCMTMS/96000007`

```
T-Code: /SCMTMS/96000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GNT_PL
```

### 2470. `/SCMTMS/99000001`

```
T-Code: /SCMTMS/99000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RFQ, /SCMTMS/V_RFQCOM, /SCMTMS/V_RFQITM
```

### 2471. `/SCMTMS/99000005`

```
T-Code: /SCMTMS/99000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EXLFLD
```

### 2472. `/SCMTMS/99000007`

```
T-Code: /SCMTMS/99000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_EXLFLD
```

### 2473. `/SCMTMS/99000012`

```
T-Code: /SCMTMS/99000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GNT_AC, /SCMTMS/V_GNT_FL, /SCMTMS/V_GNT_HL, /SCMTMS/V_GNT_HY, /SCMTMS/V_GNT_LA, /SCMTMS/V_GNT_LD, /SCMTMS/V_GNT_LH, /SCMTMS/V_GNT_LS, /SCMTMS/V_GNT_LV, /SCMTMS/V_GNT_NA, /SCMTMS/V_GNT_NH, /SCMTMS/V_GNT_OR, /SCMTMS/V_GNT_US, /SCMTMS/V_GNT_VH, /SCMTMS/V_GNT_VI, /SCMTMS/V_GNT_WA
```

### 2474. `/SCMTMS/99000013`

```
T-Code: /SCMTMS/99000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GNT_FD, /SCMTMS/V_GNT_FE, /SCMTMS/V_GNT_FL, /SCMTMS/V_GNT_FS, /SCMTMS/V_GNT_HY, /SCMTMS/V_GNT_LA, /SCMTMS/V_GNT_LB, /SCMTMS/V_GNT_LC, /SCMTMS/V_GNT_LD, /SCMTMS/V_GNT_LE, /SCMTMS/V_GNT_LL, /SCMTMS/V_GNT_LR, /SCMTMS/V_GNT_LS, /SCMTMS/V_GNT_LT
```

### 2475. `/SCMTMS/99000014`

```
T-Code: /SCMTMS/99000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GNT_AC, /SCMTMS/V_GNT_AS, /SCMTMS/V_GNT_AT, /SCMTMS/V_GNT_NA, /SCMTMS/V_GNT_ND, /SCMTMS/V_GNT_NH, /SCMTMS/V_GNT_OR, /SCMTMS/V_GNT_OS, /SCMTMS/V_GNT_OT, /SCMTMS/V_GNT_PA, /SCMTMS/V_GNT_PL, /SCMTMS/V_GNT_SC, /SCMTMS/V_GNT_US, /SCMTMS/V_GNT_WA, /SCMTMS/V_GNT_WE
```

### 2476. `/SCMTMS/99000020`

```
T-Code: /SCMTMS/99000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_RESCOND
```

### 2477. `/SCMTMS/99000021`

```
T-Code: /SCMTMS/99000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_SPINSTR
```

### 2478. `/SCMTMS/99000FFF`

```
T-Code: /SCMTMS/99000FFF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FAGMAP, /SCMTMS/V_FAMPRF
```

### 2479. `/SCMTMS/APPLOG`

```
T-Code: /SCMTMS/APPLOG
└── S_APPL_LOG
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ALG_OBJECT → /SCMB/PATTERN_UI, /SCMB/RG, /SCMB/TDL, /SCMB/TRIGGER, /SCMTMS/TMS, BAPI, CIFSCM, SCMBALERT, SCMB_BAPI
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 2480. `/SCMTMS/AUDIT`

```
T-Code: /SCMTMS/AUDIT
└── T_ADMIN
    └── ACTVT → 71
```

### 2481. `/SCMTMS/BACKGRD_PLAN`

```
T-Code: /SCMTMS/BACKGRD_PLAN
├── /SCTM/RG
    └── ACTVT → 3
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 33
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMPL
```

### 2482. `/SCMTMS/BACKGRD_TSPS`

```
T-Code: /SCMTMS/BACKGRD_TSPS
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMPL
```

### 2483. `/SCMTMS/BCT`

```
T-Code: /SCMTMS/BCT
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMBF
```

### 2484. `/SCMTMS/BONAV`

```
T-Code: /SCMTMS/BONAV
└── S_DEVELOP
    ├── ACTVT → 3
    ├── DEVCLASS → (empty — maintain in PFCG)
    ├── OBJNAME → (empty — maintain in PFCG)
    ├── OBJTYPE → (empty — maintain in PFCG)
    └── P_GROUP → (empty — maintain in PFCG)
```

### 2485. `/SCMTMS/BWIS`

```
T-Code: /SCMTMS/BWIS
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS
```

### 2486. `/SCMTMS/BWIU_BSH`

```
T-Code: /SCMTMS/BWIU_BSH
└── T_BW_INIT
    └── /SCMTMS/BW → BSH, BWIS
```

### 2487. `/SCMTMS/BWIU_CFIR`

```
T-Code: /SCMTMS/BWIU_CFIR
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, FWSD
```

### 2488. `/SCMTMS/BWIU_FAG`

```
T-Code: /SCMTMS/BWIU_FAG
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, FA
```

### 2489. `/SCMTMS/BWIU_SFIR`

```
T-Code: /SCMTMS/BWIU_SFIR
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, FSD
```

### 2490. `/SCMTMS/BWIU_TAL`

```
T-Code: /SCMTMS/BWIU_TAL
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, TAL
```

### 2491. `/SCMTMS/BWIU_TOR`

```
T-Code: /SCMTMS/BWIU_TOR
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, TO
```

### 2492. `/SCMTMS/BWIU_TRQ`

```
T-Code: /SCMTMS/BWIU_TRQ
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── T_BW_INIT
    └── /SCMTMS/BW → BWIS, TR
```

### 2493. `/SCMTMS/CALCNLQ`

```
T-Code: /SCMTMS/CALCNLQ
├── T_TOR_EXE
    ├── ACTVT → 2, 3, 6, FA
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3, 6, FA
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2494. `/SCMTMS/CALC_PROF`

```
T-Code: /SCMTMS/CALC_PROF
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMCF
```

### 2495. `/SCMTMS/CFIR_CREATE`

```
T-Code: /SCMTMS/CFIR_CREATE
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMCF
```

### 2496. `/SCMTMS/CFIR_TRANSFR`

```
T-Code: /SCMTMS/CFIR_TRANSFR
├── B_BUPA_RLT
    ├── ACTVT → 3, F4
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMCF
```

### 2497. `/SCMTMS/CSAPPL`

```
T-Code: /SCMTMS/CSAPPL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_CSAPPL
```

### 2498. `/SCMTMS/CSCUST`

```
T-Code: /SCMTMS/CSCUST
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_CSGEN, /SCMTMS/C_CSKEY, /SCMTMS/C_CSKEYV
```

### 2499. `/SCMTMS/CUST_CFIR_NR`

```
T-Code: /SCMTMS/CUST_CFIR_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/CF
```

### 2500. `/SCMTMS/CUST_FWAQ_NR`

```
T-Code: /SCMTMS/CUST_FWAQ_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/QT
```

### 2501. `/SCMTMS/CUST_OTA_NR`

```
T-Code: /SCMTMS/CUST_OTA_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/OT
```

### 2502. `/SCMTMS/CUST_TCMFANR`

```
T-Code: /SCMTMS/CUST_TCMFANR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/FA
```

### 2503. `/SCMTMS/CUST_TCMRTNR`

```
T-Code: /SCMTMS/CUST_TCMRTNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/ID
```

### 2504. `/SCMTMS/CUST_TCMSCNR`

```
T-Code: /SCMTMS/CUST_TCMSCNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/SC
```

### 2505. `/SCMTMS/CUST_TCMTANR`

```
T-Code: /SCMTMS/CUST_TCMTANR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/T1
```

### 2506. `/SCMTMS/CUST_TCMTCNR`

```
T-Code: /SCMTMS/CUST_TCMTCNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/CS
```

### 2507. `/SCMTMS/CUST_TRQ_NR`

```
T-Code: /SCMTMS/CUST_TRQ_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/TQ
```

### 2508. `/SCMTMS/CUST_WBN_SID`

```
T-Code: /SCMTMS/CUST_WBN_SID
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/WS
```

### 2509. `/SCMTMS/C_BOR`

```
T-Code: /SCMTMS/C_BOR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITB, /SCMTMS/V_TORTYB
```

### 2510. `/SCMTMS/C_CON`

```
T-Code: /SCMTMS/C_CON
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITC, /SCMTMS/V_TORTYC
```

### 2511. `/SCMTMS/C_COND`

```
T-Code: /SCMTMS/C_COND
├── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_CONDTY
└── T_COND
    ├── ACTVT → 3
    ├── TM_COND_ID → (empty — maintain in PFCG)
    └── TM_COND_TY → (empty — maintain in PFCG)
```

### 2512. `/SCMTMS/C_ERH`

```
T-Code: /SCMTMS/C_ERH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERH_BS
```

### 2513. `/SCMTMS/C_ERH_PCT`

```
T-Code: /SCMTMS/C_ERH_PCT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERHPCT
```

### 2514. `/SCMTMS/C_ERH_RSN`

```
T-Code: /SCMTMS/C_ERH_RSN
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_ERHRSN
```

### 2515. `/SCMTMS/C_FOR`

```
T-Code: /SCMTMS/C_FOR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITA, /SCMTMS/V_TORTYA
```

### 2516. `/SCMTMS/C_FU`

```
T-Code: /SCMTMS/C_FU
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITF, /SCMTMS/V_TORTYF
```

### 2517. `/SCMTMS/C_FUBRDEF`

```
T-Code: /SCMTMS/C_FUBRDEF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_FUBDEF
```

### 2518. `/SCMTMS/C_FWO`

```
T-Code: /SCMTMS/C_FWO
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMTMS/C_TRQTY, /SCMTMS/C_TRQTYT, /SCMTMS/V_TRQTY3
```

### 2519. `/SCMTMS/C_FWQ`

```
T-Code: /SCMTMS/C_FWQ
├── S_TABU_DIS
    ├── ACTVT → (empty — maintain in PFCG)
    └── DICBERCLS → TMFW
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMTMS/C_TRQTY, /SCMTMS/C_TRQTYT, /SCMTMS/V_TRQTY4
```

### 2520. `/SCMTMS/C_ITY`

```
T-Code: /SCMTMS/C_ITY
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORITY, /SCMTMS/V_TORSTA
```

### 2521. `/SCMTMS/C_NLQ_ALQ`

```
T-Code: /SCMTMS/C_NLQ_ALQ
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LCUOMC
```

### 2522. `/SCMTMS/C_OH`

```
T-Code: /SCMTMS/C_OH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_HIERAR, /SCMTMS/V_OHSWIT, /SCMTMS/V_OHTYP
```

### 2523. `/SCMTMS/C_PL`

```
T-Code: /SCMTMS/C_PL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PLAPCO, /SCMTMS/V_PLAPP, /SCMTMS/V_PLAPPB, /SCMTMS/V_PLB, /SCMTMS/V_PLCO, /SCMTMS/V_PLCOC, /SCMTMS/V_PLCOCB
```

### 2524. `/SCMTMS/C_SOR`

```
T-Code: /SCMTMS/C_SOR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITS, /SCMTMS/V_TORTYS
```

### 2525. `/SCMTMS/C_SWITCH`

```
T-Code: /SCMTMS/C_SWITCH
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_SWITCH
```

### 2526. `/SCMTMS/C_TORITY`

```
T-Code: /SCMTMS/C_TORITY
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TORITY
```

### 2527. `/SCMTMS/C_TOR_HV`

```
T-Code: /SCMTMS/C_TOR_HV
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_HIERAR, /SCMTMS/V_OHSWIT, /SCMTMS/V_OHTYP
```

### 2528. `/SCMTMS/C_TRIGGER`

```
T-Code: /SCMTMS/C_TRIGGER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRIG
```

### 2529. `/SCMTMS/C_TUN`

```
T-Code: /SCMTMS/C_TUN
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TOREVT, /SCMTMS/V_TORITU, /SCMTMS/V_TORTYU
```

### 2530. `/SCMTMS/DCPRF`

```
T-Code: /SCMTMS/DCPRF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_DCASS, /SCMTMS/V_DCFLT, /SCMTMS/V_DCFLTP, /SCMTMS/V_DCPRF
```

### 2531. `/SCMTMS/DDM`

```
T-Code: /SCMTMS/DDM
└── T_ADMIN
    └── ACTVT → 71
```

### 2532. `/SCMTMS/DLV_BATCH`

```
T-Code: /SCMTMS/DLV_BATCH
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 16, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2533. `/SCMTMS/DLV_CREATE`

```
T-Code: /SCMTMS/DLV_CREATE
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO, TU
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO, TU
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2534. `/SCMTMS/DSO_BATCH`

```
T-Code: /SCMTMS/DSO_BATCH
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2535. `/SCMTMS/DS_TRACE`

```
T-Code: /SCMTMS/DS_TRACE
└── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SCMTMS/DS_TRACE_VIEWER
```

### 2536. `/SCMTMS/EFS`

```
T-Code: /SCMTMS/EFS
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMTMS/V_EXTFS
```

### 2537. `/SCMTMS/EXP`

```
T-Code: /SCMTMS/EXP
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/PLN
    └── ALG_SUBOBJ → TC
├── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMPL
└── T_EXP_USR
    ├── ACTVT → 3
    └── USER → (empty — maintain in PFCG)
```

### 2538. `/SCMTMS/GET_BP_DESCR`

```
T-Code: /SCMTMS/GET_BP_DESCR
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → 0
```

### 2539. `/SCMTMS/HIERPRIO`

```
T-Code: /SCMTMS/HIERPRIO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/HIERPRIO
```

### 2540. `/SCMTMS/INCLOC_MAP`

```
T-Code: /SCMTMS/INCLOC_MAP
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_INCLOC
```

### 2541. `/SCMTMS/JOB_SCHED`

```
T-Code: /SCMTMS/JOB_SCHED
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, VARIANT
    └── P_GROUP → (empty — maintain in PFCG)
```

### 2542. `/SCMTMS/LBN_BPID_UPD`

```
T-Code: /SCMTMS/LBN_BPID_UPD
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMBF
```

### 2543. `/SCMTMS/LBN_CARRLIST`

```
T-Code: /SCMTMS/LBN_CARRLIST
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMBF
```

### 2544. `/SCMTMS/LI_DLV_SPLIT_UPD_RFC`

```
T-Code: /SCMTMS/LI_DLV_SPLIT_UPD_RFC
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO, TU
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → FU, TO, TU
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2545. `/SCMTMS/LOC3`

```
T-Code: /SCMTMS/LOC3
├── C_APO_DEF
    ├── ACTVT → 3
    ├── APO_DEFN → (empty — maintain in PFCG)
    ├── APO_DEFT → (empty — maintain in PFCG)
    └── APO_PLNR → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, F4
    └── APO_LOC → (empty — maintain in PFCG)
└── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
```

### 2546. `/SCMTMS/LOC_TRANS`

```
T-Code: /SCMTMS/LOC_TRANS
├── /SCTM/RG
    └── ACTVT → 3
├── C_APO_LOC
    ├── ACTVT → 1, 2, 3
    └── APO_LOC → (empty — maintain in PFCG)
└── T_TRANSLOC
    └── ACTVT → 3
```

### 2547. `/SCMTMS/MASS_UPD`

```
T-Code: /SCMTMS/MASS_UPD
└── T_RATE
    ├── /SCMTMS/RT → (empty — maintain in PFCG)
    ├── /SCMTMS/TC → (empty — maintain in PFCG)
    ├── /SCMTMS/TE → (empty — maintain in PFCG)
    ├── ACTVT → 1, 2, 3
    ├── TM_XPORG → $XPORG
    └── TM_XSORG → $XSORG
```

### 2548. `/SCMTMS/MSG_STORE`

```
T-Code: /SCMTMS/MSG_STORE
└── T_ADMIN
    └── ACTVT → 36
```

### 2549. `/SCMTMS/M_ASRDOOR`

```
T-Code: /SCMTMS/M_ASRDOOR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/D_SRDOOR
```

### 2550. `/SCMTMS/ORG_INT`

```
T-Code: /SCMTMS/ORG_INT
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMBF
```

### 2551. `/SCMTMS/PLN_CRT_PU`

```
T-Code: /SCMTMS/PLN_CRT_PU
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/TMS
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SCMTMS/PLN_CRT_PU_BGD
```

### 2552. `/SCMTMS/PLN_LOADCONS`

```
T-Code: /SCMTMS/PLN_LOADCONS
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/TMS
    └── ALG_SUBOBJ → PLN
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_PROGNAM → /SCMTMS/PLN_LOAD_CONS_BGD
```

### 2553. `/SCMTMS/PLN_LOADPLAN`

```
T-Code: /SCMTMS/PLN_LOADPLAN
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2554. `/SCMTMS/PLN_PB`

```
T-Code: /SCMTMS/PLN_PB
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2555. `/SCMTMS/PLN_PB_FU`

```
T-Code: /SCMTMS/PLN_PB_FU
├── T_TOR_EXE
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XEGRP → $XEGRP
    └── TM_XEORG → $XEORG
└── T_TOR_PUR
    ├── ACTVT → 2, 3
    ├── BO_SERVICE → (empty — maintain in PFCG)
    ├── TM_MOT → (empty — maintain in PFCG)
    ├── TM_OC → (empty — maintain in PFCG)
    ├── TM_OT → (empty — maintain in PFCG)
    ├── TM_RP → (empty — maintain in PFCG)
    ├── TM_XPGRP → $XPGRP
    └── TM_XPORG → $XPORG
```

### 2556. `/SCMTMS/PPRF`

```
T-Code: /SCMTMS/PPRF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_PARPRF
```

### 2557. `/SCMTMS/PROC_TRIGGER`

```
T-Code: /SCMTMS/PROC_TRIGGER
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMBF
```

### 2558. `/SCMTMS/PT`

```
T-Code: /SCMTMS/PT
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2559. `/SCMTMS/PTI`

```
T-Code: /SCMTMS/PTI
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2560. `/SCMTMS/QUERY_ENH`

```
T-Code: /SCMTMS/QUERY_ENH
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCMTMS/V_QENH1, /SCMTMS/V_QENH2, /SCMTMS/V_QENH3, /SCMTMS/V_QENH4, /SCMTMS/V_QENH5, /SCMTMS/V_QENH6
```

### 2561. `/SCMTMS/RATE_EXCEL07`

```
T-Code: /SCMTMS/RATE_EXCEL07
└── T_RATE
    ├── /SCMTMS/RT → (empty — maintain in PFCG)
    ├── /SCMTMS/TC → (empty — maintain in PFCG)
    ├── /SCMTMS/TE → (empty — maintain in PFCG)
    ├── ACTVT → 1, 2, 3
    ├── TM_XPORG → $XPORG
    └── TM_XSORG → $XSORG
```

### 2562. `/SCMTMS/RES01`

```
T-Code: /SCMTMS/RES01
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
```

### 2563. `/SCMTMS/SCH_CRT_DEP`

```
T-Code: /SCMTMS/SCH_CRT_DEP
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/TMS
    └── ALG_SUBOBJ → MD
```

### 2564. `/SCMTMS/SCH_SNUM`

```
T-Code: /SCMTMS/SCH_SNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/SL
```

### 2565. `/SCMTMS/SCH_UPLOAD`

```
T-Code: /SCMTMS/SCH_UPLOAD
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/TMS
    └── ALG_SUBOBJ → MD
```

### 2566. `/SCMTMS/SFIR_CREATE`

```
T-Code: /SCMTMS/SFIR_CREATE
├── B_BUPA_RLT
    ├── ACTVT → 3, F4
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3, F4
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMSF
```

### 2567. `/SCMTMS/SFIR_TRANSFR`

```
T-Code: /SCMTMS/SFIR_TRANSFR
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMSF
```

### 2568. `/SCMTMS/STAD_CUST`

```
T-Code: /SCMTMS/STAD_CUST
└── T_ADMIN
    └── ACTVT → 36
```

### 2569. `/SCMTMS/SUPP_NR`

```
T-Code: /SCMTMS/SUPP_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/ST
```

### 2570. `/SCMTMS/TACT_UPLOAD`

```
T-Code: /SCMTMS/TACT_UPLOAD
└── T_RATE
    ├── /SCMTMS/RT → (empty — maintain in PFCG)
    ├── /SCMTMS/TC → (empty — maintain in PFCG)
    ├── /SCMTMS/TE → (empty — maintain in PFCG)
    ├── ACTVT → 1, 2, 3
    ├── TM_XPORG → $XPORG
    └── TM_XSORG → $XSORG
```

### 2571. `/SCMTMS/TAL_IMPORT`

```
T-Code: /SCMTMS/TAL_IMPORT
└── T_TAL
    ├── ACTVT → 1, 2, 3, F4
    └── TM_TALTYPE → (empty — maintain in PFCG)
```

### 2572. `/SCMTMS/TAL_NR`

```
T-Code: /SCMTMS/TAL_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/TA
```

### 2573. `/SCMTMS/TCC_LOGON`

```
T-Code: /SCMTMS/TCC_LOGON
└── T_RATE
    ├── /SCMTMS/RT → (empty — maintain in PFCG)
    ├── /SCMTMS/TC → (empty — maintain in PFCG)
    ├── /SCMTMS/TE → (empty — maintain in PFCG)
    ├── ACTVT → 3
    ├── TM_XPORG → $XPORG
    └── TM_XSORG → $XSORG
```

### 2574. `/SCMTMS/TCM_FASET`

```
T-Code: /SCMTMS/TCM_FASET
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_FASET
```

### 2575. `/SCMTMS/TCM_RULES`

```
T-Code: /SCMTMS/TCM_RULES
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_RUL099, /SCMTMS/V_RUL101, /SCMTMS/V_RUL113, /SCMTMS/V_RUL153, /SCMTMS/V_RUL157, /SCMTMS/V_RUL184, /SCMTMS/V_RUL185
```

### 2576. `/SCMTMS/TDL_NR`

```
T-Code: /SCMTMS/TDL_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/TD
```

### 2577. `/SCMTMS/TENDBATCH`

```
T-Code: /SCMTMS/TENDBATCH
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2578. `/SCMTMS/TENDCOMMPROF`

```
T-Code: /SCMTMS/TENDCOMMPROF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_TENDCP
```

### 2579. `/SCMTMS/TENDCONT`

```
T-Code: /SCMTMS/TENDCONT
├── /SCTM/RG
    └── ACTVT → 3
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2580. `/SCMTMS/TENDCUST`

```
T-Code: /SCMTMS/TENDCUST
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_REJRN, /SCMTMS/C_TENDCP, /SCMTMS/C_TENDDE, /SCMTMS/C_TENDGC, /SCMTMS/C_TENDNM, /SCMTMS/C_TENDPS, /SCMTMS/C_TENVIS
```

### 2581. `/SCMTMS/TENDMAIL`

```
T-Code: /SCMTMS/TENDMAIL
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2582. `/SCMTMS/TENDMAILIN`

```
T-Code: /SCMTMS/TENDMAILIN
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2583. `/SCMTMS/TENDMAILOUT`

```
T-Code: /SCMTMS/TENDMAILOUT
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2584. `/SCMTMS/TENDREJCODE`

```
T-Code: /SCMTMS/TENDREJCODE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_REJRN
```

### 2585. `/SCMTMS/TENDREQNR`

```
T-Code: /SCMTMS/TENDREQNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/R1
```

### 2586. `/SCMTMS/TENDVISIPROF`

```
T-Code: /SCMTMS/TENDVISIPROF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/C_TENVIS
```

### 2587. `/SCMTMS/TEST_PLN_NPD`

```
T-Code: /SCMTMS/TEST_PLN_NPD
└── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
```

### 2588. `/SCMTMS/TL5`

```
T-Code: /SCMTMS/TL5
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2589. `/SCMTMS/TLC_DELETION`

```
T-Code: /SCMTMS/TLC_DELETION
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 2590. `/SCMTMS/TLOC_CHAIN`

```
T-Code: /SCMTMS/TLOC_CHAIN
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22, 33
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 2, 3, 32
    └── APO_LOC → (empty — maintain in PFCG)
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 32
```

### 2591. `/SCMTMS/TOR_BO_PROC`

```
T-Code: /SCMTMS/TOR_BO_PROC
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2592. `/SCMTMS/TOR_CALC`

```
T-Code: /SCMTMS/TOR_CALC
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2593. `/SCMTMS/TOR_FO_PROC`

```
T-Code: /SCMTMS/TOR_FO_PROC
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFO
```

### 2594. `/SCMTMS/TOR_NR`

```
T-Code: /SCMTMS/TOR_NR
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/TO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 2595. `/SCMTMS/TOR_PROC_CHACO_STRAT`

```
T-Code: /SCMTMS/TOR_PROC_CHACO_STRAT
└── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SCMTMS/TOR_PROC_CHACO_STRAT
    └── RFC_TYPE → FUNC
```

### 2596. `/SCMTMS/TRQ_CALC`

```
T-Code: /SCMTMS/TRQ_CALC
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFW
```

### 2597. `/SCMTMS/TRQ_CONF`

```
T-Code: /SCMTMS/TRQ_CONF
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFW
```

### 2598. `/SCMTMS/TRQ_PREPPLN`

```
T-Code: /SCMTMS/TRQ_PREPPLN
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFW
```

### 2599. `/SCMTMS/TRQ_PREP_PLN`

```
T-Code: /SCMTMS/TRQ_PREP_PLN
└── S_PROGRAM
    ├── P_ACTION → BTCSUBMIT, SUBMIT, VARIANT
    └── P_GROUP → TMFW
```

### 2600. `/SCMTMS/TSPP`

```
T-Code: /SCMTMS/TSPP
├── /SCTM/RG
    └── ACTVT → 3
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
```

### 2601. `/SCMTMS/UI_PLN_LOG`

```
T-Code: /SCMTMS/UI_PLN_LOG
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCMTMS/PLN
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 2602. `/SCMTMS/VC_EXP`

```
T-Code: /SCMTMS/VC_EXP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/PLNEXP, /SCMTMS/PLNEXPAP, /SCMTMS/PLNEXPT
```

### 2603. `/SCMTMS/VC_GMC`

```
T-Code: /SCMTMS/VC_GMC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GMCCME, /SCMTMS/V_GMCCMH, /SCMTMS/V_GMCICO, /SCMTMS/V_GMCLHD, /SCMTMS/V_GMCRAT, /SCMTMS/V_GMCRHD, /SCMTMS/V_GMCRLA, /SCMTMS/V_GMCRLC, /SCMTMS/V_GMCRLH, /SCMTMS/V_GMCRSC
```

### 2604. `/SCMTMS/VC_GNT`

```
T-Code: /SCMTMS/VC_GNT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_GNT_AC, /SCMTMS/V_GNT_AS, /SCMTMS/V_GNT_AT, /SCMTMS/V_GNT_FD, /SCMTMS/V_GNT_FE, /SCMTMS/V_GNT_FL, /SCMTMS/V_GNT_FS, /SCMTMS/V_GNT_HL, /SCMTMS/V_GNT_HY, /SCMTMS/V_GNT_LA, /SCMTMS/V_GNT_LB, /SCMTMS/V_GNT_LC, /SCMTMS/V_GNT_LD, /SCMTMS/V_GNT_LE, /SCMTMS/V_GNT_LH, /SCMTMS/V_GNT_LL, /SCMTMS/V_GNT_LS, /SCMTMS/V_GNT_LV, /SCMTMS/V_GNT_NA, /SCMTMS/V_GNT_ND, /SCMTMS/V_GNT_NH, /SCMTMS/V_GNT_OR, /SCMTMS/V_GNT_OS, /SCMTMS/V_GNT_OT, /SCMTMS/V_GNT_PA, /SCMTMS/V_GNT_PL, /SCMTMS/V_GNT_SC, /SCMTMS/V_GNT_US, /SCMTMS/V_GNT_VH, /SCMTMS/V_GNT_VI, /SCMTMS/V_GNT_WA, /SCMTMS/V_GNT_WE
```

### 2605. `/SCMTMS/VC_LPV`

```
T-Code: /SCMTMS/VC_LPV
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_LPV_CH, /SCMTMS/V_LPV_CM, /SCMTMS/V_LPV_CO, /SCMTMS/V_LPV_DS, /SCMTMS/V_LPV_KB, /SCMTMS/V_LPV_KS, /SCMTMS/V_LPV_LT, /SCMTMS/V_LPV_MD, /SCMTMS/V_LPV_SM, /SCMTMS/V_LPV_WC, /SCMTMS/V_LPV_WG
```

### 2606. `/SCMTMS/VOY_SNUM`

```
T-Code: /SCMTMS/VOY_SNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCMTMS/VO
```

### 2607. `/SCMTMS/V_STGTY`

```
T-Code: /SCMTMS/V_STGTY
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_STGTY
```

### 2608. `/SCMTMS/V_TRQIT3`

```
T-Code: /SCMTMS/V_TRQIT3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQIT3
```

### 2609. `/SCMTMS/V_TRQTY3`

```
T-Code: /SCMTMS/V_TRQTY3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TRQTY3
```

### 2610. `/SCMTMS/V_TSR`

```
T-Code: /SCMTMS/V_TSR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMTMS/V_TSR
```

### 2611. `/SCMTMS/ZONE`

```
T-Code: /SCMTMS/ZONE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 1, 2, 3, 6
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22, 33
├── C_APO_LOC
    ├── ACTVT → 1, 2, 3, 32, 6
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 32, 6
```

## `/SCTM/` — SAP Contract & Time Management
> T-codes: **60**


### 2612. `/SCTM/09000400`

```
T-Code: /SCTM/09000400
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/STRATM
```

### 2613. `/SCTM/09000403`

```
T-Code: /SCTM/09000403
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/PARDEF
```

### 2614. `/SCTM/60000026`

```
T-Code: /SCTM/60000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_GAF, /SCTM/V_GAR2, /SCTM/V_GAV
```

### 2615. `/SCTM/60000028`

```
T-Code: /SCTM/60000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_SHPTYP
```

### 2616. `/SCTM/60000034`

```
T-Code: /SCTM/60000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_SHPTYPDT
```

### 2617. `/SCTM/60000035`

```
T-Code: /SCTM/60000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_FRDTYP
```

### 2618. `/SCTM/60000039`

```
T-Code: /SCTM/60000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_FRDTYPDT
```

### 2619. `/SCTM/60000040`

```
T-Code: /SCTM/60000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TTPLST
```

### 2620. `/SCTM/60000041`

```
T-Code: /SCTM/60000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TTPLSTDT
```

### 2621. `/SCTM/60000042`

```
T-Code: /SCTM/60000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TPS_TPL
```

### 2622. `/SCTM/60000103`

```
T-Code: /SCTM/60000103
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TMFG
```

### 2623. `/SCTM/60000218`

```
T-Code: /SCTM/60000218
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_SHPPPF
```

### 2624. `/SCTM/60000219`

```
T-Code: /SCTM/60000219
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_FRDPPF
```

### 2625. `/SCTM/60000220`

```
T-Code: /SCTM/60000220
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBDLS
```

### 2626. `/SCTM/93000008`

```
T-Code: /SCTM/93000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TTPLOCB
```

### 2627. `/SCTM/93000009`

```
T-Code: /SCTM/93000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TCONVDIR
```

### 2628. `/SCTM/93000010`

```
T-Code: /SCTM/93000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TFTPDIR
```

### 2629. `/SCTM/93000011`

```
T-Code: /SCTM/93000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TMAPCL
```

### 2630. `/SCTM/93000012`

```
T-Code: /SCTM/93000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TSOURCAT
```

### 2631. `/SCTM/APPLOG`

```
T-Code: /SCTM/APPLOG
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 2632. `/SCTM/BOLFORMDET`

```
T-Code: /SCTM/BOLFORMDET
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_BOLFRMDS, /SCTM/V_BOLFRMDT
```

### 2633. `/SCTM/CHECK_RG`

```
T-Code: /SCTM/CHECK_RG
└── /SCTM/RG
    └── ACTVT → 1, 2, 3
```

### 2634. `/SCTM/CUST_NR_BL`

```
T-Code: /SCTM/CUST_NR_BL
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCTM/TMBL
```

### 2635. `/SCTM/CUST_NR_FRD`

```
T-Code: /SCTM/CUST_NR_FRD
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCTM/TMFR
```

### 2636. `/SCTM/CUST_NR_SHP`

```
T-Code: /SCTM/CUST_NR_SHP
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCTM/TMSH
```

### 2637. `/SCTM/CUST_PPF_COND`

```
T-Code: /SCTM/CUST_PPF_COND
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 2638. `/SCTM/CUST_PPF_PROF`

```
T-Code: /SCTM/CUST_PPF_PROF
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 2639. `/SCTM/DEPCAL`

```
T-Code: /SCTM/DEPCAL
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22
└── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6
    └── APO_LOC → (empty — maintain in PFCG)
```

### 2640. `/SCTM/FRD`

```
T-Code: /SCTM/FRD
├── /SCTM/AUTH
    ├── /SCTM/CAT → FRD
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 1, 16, 2, 3, 4, 6
└── /SCTM/STAT
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SACT → TM01, TM04, TM05, TM06, TM07, TM08, TM09, TM13, TM15, TM16, TM18, TM19, TM21, TM22, TM23, TM43, TM44, TM45, TM46, TM93, TM94, TM95, TM96, TM97, TM98, TM99
    └── /SCTM/SHPT → (empty — maintain in PFCG)
```

### 2641. `/SCTM/FRDFORMDET`

```
T-Code: /SCTM/FRDFORMDET
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_FRDFRMDS, /SCTM/V_FRDFRMDT
```

### 2642. `/SCTM/FRD_MON`

```
T-Code: /SCTM/FRD_MON
└── /SCTM/AUTH
    ├── /SCTM/CAT → FRD
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 3, 4
```

### 2643. `/SCTM/INCON_REP`

```
T-Code: /SCTM/INCON_REP
└── /SCTM/AUTH
    ├── /SCTM/CAT → FRD, FSH, PSH
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 2644. `/SCTM/LEG`

```
T-Code: /SCTM/LEG
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22, 33
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 32
```

### 2645. `/SCTM/OCB10`

```
T-Code: /SCTM/OCB10
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TTPLOCB
```

### 2646. `/SCTM/OCB11`

```
T-Code: /SCTM/OCB11
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TCONVDIR
```

### 2647. `/SCTM/OCB12`

```
T-Code: /SCTM/OCB12
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TFTPDIR
```

### 2648. `/SCTM/OCB13`

```
T-Code: /SCTM/OCB13
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TMAPCL
```

### 2649. `/SCTM/OCB14`

```
T-Code: /SCTM/OCB14
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TSOURCAT
```

### 2650. `/SCTM/OCB5`

```
T-Code: /SCTM/OCB5
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TSHPIDEN
```

### 2651. `/SCTM/OCB6`

```
T-Code: /SCTM/OCB6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TBPIDENT
```

### 2652. `/SCTM/OCB7`

```
T-Code: /SCTM/OCB7
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_TUNLOCOD
```

### 2653. `/SCTM/PC`

```
T-Code: /SCTM/PC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_PRC_SRV, /SCTM/V_VC_METH, /SCTM/V_VC_METHP, /SCTM/V_VC_PARAM, /SCTM/V_VC_STRHD, /SCTM/V_VC_STRM, /SCTM/V_VC_STRMP
```

### 2654. `/SCTM/PC00`

```
T-Code: /SCTM/PC00
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_PRC_SRV
```

### 2655. `/SCTM/PC01`

```
T-Code: /SCTM/PC01
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRAT
```

### 2656. `/SCTM/PC02`

```
T-Code: /SCTM/PC02
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATM
```

### 2657. `/SCTM/PC03`

```
T-Code: /SCTM/PC03
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_STRATMP
```

### 2658. `/SCTM/PC04`

```
T-Code: /SCTM/PC04
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPOOL
```

### 2659. `/SCTM/PC05`

```
T-Code: /SCTM/PC05
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/METHPAR
```

### 2660. `/SCTM/PC06`

```
T-Code: /SCTM/PC06
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/PARDEF
```

### 2661. `/SCTM/PRDETFOM`

```
T-Code: /SCTM/PRDETFOM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCTM/V_PRDETFOM, /SCTM/V_PRDSFOM
```

### 2662. `/SCTM/RGINT`

```
T-Code: /SCTM/RGINT
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 3
└── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 33
```

### 2663. `/SCTM/RG_MD_DISTRI`

```
T-Code: /SCTM/RG_MD_DISTRI
└── /SCTM/RG
    └── ACTVT → 59
```

### 2664. `/SCTM/ROUTE`

```
T-Code: /SCTM/ROUTE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/CD
    └── ACTVT → 1, 2, 6
├── /SCTM/RG
    └── ACTVT → 1, 2, 3, 59, 6
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22, 33
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3, 32, 6
    └── APO_LOC → (empty — maintain in PFCG)
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 32
```

### 2665. `/SCTM/ROUTES_DELETE`

```
T-Code: /SCTM/ROUTES_DELETE
└── /SCTM/RG
    └── ACTVT → 1, 2, 3, 59, 6
```

### 2666. `/SCTM/SHP`

```
T-Code: /SCTM/SHP
├── /SCTM/AUTH
    ├── /SCTM/CAT → FSH, PSH
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 1, 16, 2, 3, 4, 6
└── /SCTM/STAT
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SACT → TM01, TM04, TM05, TM06, TM07, TM08, TM09, TM11, TM12, TM14, TM15, TM41, TM42, TM43, TM44, TM45, TM46, TM94, TM95, TM96, TM97, TM98
    └── /SCTM/SHPT → (empty — maintain in PFCG)
```

### 2667. `/SCTM/SHP_MON`

```
T-Code: /SCTM/SHP_MON
└── /SCTM/AUTH
    ├── /SCTM/CAT → FSH, PSH
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 3, 4
```

### 2668. `/SCTM/TSPP`

```
T-Code: /SCTM/TSPP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 1, 2, 3, 59, 6
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → (empty — maintain in PFCG)
```

### 2669. `/SCTM/ZONE`

```
T-Code: /SCTM/ZONE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 1, 2, 3, 59, 6
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 22, 33
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 32
```

### 2670. `/SCTM/ZONES_DELETE`

```
T-Code: /SCTM/ZONES_DELETE
└── /SCTM/RG
    └── ACTVT → 1, 2, 3, 59, 6
```

### 2671. `/SCTM/ZONE_LOCATIONS`

```
T-Code: /SCTM/ZONE_LOCATIONS
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 3
└── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 33
```

## `/SCWM/` — SAP Extended Warehouse Management (EWM)
> T-codes: **1107**


### 2672. `/SCWM/09000017`

```
T-Code: /SCWM/09000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PIVCUST_ML
```

### 2673. `/SCWM/09000018`

```
T-Code: /SCWM/09000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PI_ARE
```

### 2674. `/SCWM/09000019`

```
T-Code: /SCWM/09000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TOL_DIFF
```

### 2675. `/SCWM/09000020`

```
T-Code: /SCWM/09000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_FLD_MAP, /SCWM/V_HOTSPOT
```

### 2676. `/SCWM/09000028`

```
T-Code: /SCWM/09000028
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TROUTLS
```

### 2677. `/SCWM/09000029`

```
T-Code: /SCWM/09000029
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREASA
```

### 2678. `/SCWM/09000050`

```
T-Code: /SCWM/09000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_YM_3010
```

### 2679. `/SCWM/09000051`

```
T-Code: /SCWM/09000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_YM_302
```

### 2680. `/SCWM/09000052`

```
T-Code: /SCWM/09000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SR_ACTIV
```

### 2681. `/SCWM/09000053`

```
T-Code: /SCWM/09000053
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_YM_ACTIV
```

### 2682. `/SCWM/09000054`

```
T-Code: /SCWM/09000054
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_YM_CHKPN
```

### 2683. `/SCWM/09000079`

```
T-Code: /SCWM/09000079
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QPRPQTYR
```

### 2684. `/SCWM/09000080`

```
T-Code: /SCWM/09000080
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QPRPVALR
```

### 2685. `/SCWM/09000081`

```
T-Code: /SCWM/09000081
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQRGRP
```

### 2686. `/SCWM/09000082`

```
T-Code: /SCWM/09000082
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDEFGRP
```

### 2687. `/SCWM/09000083`

```
T-Code: /SCWM/09000083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDFCT
```

### 2688. `/SCWM/09000084`

```
T-Code: /SCWM/09000084
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDEFECT, /SCWM/TQDEFGRP, /SCWM/TQEFFORT, /SCWM/V_TQDFCT, /SCWM/V_TQDGEF
```

### 2689. `/SCWM/09000085`

```
T-Code: /SCWM/09000085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQEFFORT
```

### 2690. `/SCWM/09000086`

```
T-Code: /SCWM/09000086
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDGEF
```

### 2691. `/SCWM/09000087`

```
T-Code: /SCWM/09000087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQFU, /SCWM/TQFUCODE, /SCWM/V_QBIDFUP, QIE_DCOD_BUND
```

### 2692. `/SCWM/09000088`

```
T-Code: /SCWM/09000088
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQSTCHG
```

### 2693. `/SCWM/09000090`

```
T-Code: /SCWM/09000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQRGRP
```

### 2694. `/SCWM/09000091`

```
T-Code: /SCWM/09000091
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDGEF
```

### 2695. `/SCWM/09000110`

```
T-Code: /SCWM/09000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DGTS
```

### 2696. `/SCWM/09000111`

```
T-Code: /SCWM/09000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GTS_DTYP
```

### 2697. `/SCWM/09000112`

```
T-Code: /SCWM/09000112
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TBUSSYS
```

### 2698. `/SCWM/09000113`

```
T-Code: /SCWM/09000113
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSWHS
```

### 2699. `/SCWM/09000114`

```
T-Code: /SCWM/09000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TVASWHS
```

### 2700. `/SCWM/09000119`

```
T-Code: /SCWM/09000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QIOT, /SCWM/V_QIOTPRP
```

### 2701. `/SCWM/09000120`

```
T-Code: /SCWM/09000120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSERIALWM
```

### 2702. `/SCWM/09000150`

```
T-Code: /SCWM/09000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/ITEMTYPE_W
```

### 2703. `/SCWM/09000173`

```
T-Code: /SCWM/09000173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQGRP
```

### 2704. `/SCWM/09000181`

```
T-Code: /SCWM/09000181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSERIAL
```

### 2705. `/SCWM/09000191`

```
T-Code: /SCWM/09000191
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWAVE_CAPA
```

### 2706. `/SCWM/09000210`

```
T-Code: /SCWM/09000210
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SR_IDT
```

### 2707. `/SCWM/09000211`

```
T-Code: /SCWM/09000211
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_FORMDET, /SCWM/V_SFORMDET
```

### 2708. `/SCWM/09000226`

```
T-Code: /SCWM/09000226
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_LP_PAT
```

### 2709. `/SCWM/09000232`

```
T-Code: /SCWM/09000232
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/ITEMTYPE_O
```

### 2710. `/SCWM/09000271`

```
T-Code: /SCWM/09000271
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSACTD
```

### 2711. `/SCWM/09000272`

```
T-Code: /SCWM/09000272
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSACTF
```

### 2712. `/SCWM/09000273`

```
T-Code: /SCWM/09000273
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCP
```

### 2713. `/SCWM/09000274`

```
T-Code: /SCWM/09000274
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCPTY
```

### 2714. `/SCWM/09000275`

```
T-Code: /SCWM/09000275
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCS
```

### 2715. `/SCWM/09000276`

```
T-Code: /SCWM/09000276
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCSGR
```

### 2716. `/SCWM/09000277`

```
T-Code: /SCWM/09000277
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSGRTY
```

### 2717. `/SCWM/09000278`

```
T-Code: /SCWM/09000278
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSPLC
```

### 2718. `/SCWM/09000279`

```
T-Code: /SCWM/09000279
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSSEGA
```

### 2719. `/SCWM/09000285`

```
T-Code: /SCWM/09000285
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSR_GM
```

### 2720. `/SCWM/09000286`

```
T-Code: /SCWM/09000286
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPSLL/V_TECPCI
```

### 2721. `/SCWM/09000293`

```
T-Code: /SCWM/09000293
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PICUST_PRI
```

### 2722. `/SCWM/09000294`

```
T-Code: /SCWM/09000294
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PICUST_REA
```

### 2723. `/SCWM/09000295`

```
T-Code: /SCWM/09000295
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PICUST_DTY
```

### 2724. `/SCWM/09000305`

```
T-Code: /SCWM/09000305
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_FCOD_C, /SCWM/V_FCOD_P, /SCWM/V_FCOD_TXT, /SCWM/V_PARAM_C, /SCWM/V_PRES_C, /SCWM/V_PRSN_P, /SCWM/V_STAT_C, /SCWM/V_STEP_C, /SCWM/V_STEP_F, /SCWM/V_STEP_S, /SCWM/V_TRNS_C, /SCWM/V_TRNS_N, /SCWM/V_TRNS_TXT, /SCWM/V_VALID_P, /SCWM/V_VLID_C
```

### 2725. `/SCWM/09000407`

```
T-Code: /SCWM/09000407
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340VAS
```

### 2726. `/SCWM/09000408`

```
T-Code: /SCWM/09000408
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPACKSTDTA
```

### 2727. `/SCWM/09000410`

```
T-Code: /SCWM/09000410
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QIOTWM_2
```

### 2728. `/SCWM/09000411`

```
T-Code: /SCWM/09000411
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_NUM_PAT
```

### 2729. `/SCWM/09000414`

```
T-Code: /SCWM/09000414
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LGN_PRES
```

### 2730. `/SCWM/09000415`

```
T-Code: /SCWM/09000415
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TANA
```

### 2731. `/SCWM/09000416`

```
T-Code: /SCWM/09000416
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TDEMFAC
```

### 2732. `/SCWM/09000452`

```
T-Code: /SCWM/09000452
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCPDELAY
```

### 2733. `/SCWM/09000453`

```
T-Code: /SCWM/09000453
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPTWYDELAY
```

### 2734. `/SCWM/09000454`

```
T-Code: /SCWM/09000454
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_SER
```

### 2735. `/SCWM/09000468`

```
T-Code: /SCWM/09000468
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VSTN_FKEYS
```

### 2736. `/SCWM/09000479`

```
T-Code: /SCWM/09000479
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVROUTE
```

### 2737. `/SCWM/09000497`

```
T-Code: /SCWM/09000497
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SR_GEN
```

### 2738. `/SCWM/09000512`

```
T-Code: /SCWM/09000512
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMOVEDUR
```

### 2739. `/SCWM/09000553`

```
T-Code: /SCWM/09000553
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSWHS
```

### 2740. `/SCWM/09000554`

```
T-Code: /SCWM/09000554
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340PS
```

### 2741. `/SCWM/09000556`

```
T-Code: /SCWM/09000556
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TVASDUR
```

### 2742. `/SCWM/09000557`

```
T-Code: /SCWM/09000557
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TVASFIXDUR
```

### 2743. `/SCWM/09000558`

```
T-Code: /SCWM/09000558
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TVASECHD
```

### 2744. `/SCWM/09000559`

```
T-Code: /SCWM/09000559
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TVASECAC
```

### 2745. `/SCWM/09000633`

```
T-Code: /SCWM/09000633
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SR_REST
```

### 2746. `/SCWM/09000680`

```
T-Code: /SCWM/09000680
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T333
```

### 2747. `/SCWM/09000681`

```
T-Code: /SCWM/09000681
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_CP
```

### 2748. `/SCWM/09000682`

```
T-Code: /SCWM/09000682
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TOL_POST
```

### 2749. `/SCWM/09000683`

```
T-Code: /SCWM/09000683
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TOL_RECO
```

### 2750. `/SCWM/09000705`

```
T-Code: /SCWM/09000705
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334RA
```

### 2751. `/SCWM/0DSTGRP`

```
T-Code: /SCWM/0DSTGRP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/DSGR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 2752. `/SCWM/0SSCC`

```
T-Code: /SCWM/0SSCC
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/SSCC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 2753. `/SCWM/17000001`

```
T-Code: /SCWM/17000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_BMRTY
```

### 2754. `/SCWM/17000003`

```
T-Code: /SCWM/17000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WBBMS
```

### 2755. `/SCWM/17000004`

```
T-Code: /SCWM/17000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MSTY
```

### 2756. `/SCWM/17000005`

```
T-Code: /SCWM/17000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_BMTY
```

### 2757. `/SCWM/17000006`

```
T-Code: /SCWM/17000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WBSNAP
```

### 2758. `/SCWM/17000009`

```
T-Code: /SCWM/17000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/MSTY_MAP
```

### 2759. `/SCWM/17000025`

```
T-Code: /SCWM/17000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 2760. `/SCWM/17000032`

```
T-Code: /SCWM/17000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWDOCMAP
```

### 2761. `/SCWM/17000033`

```
T-Code: /SCWM/17000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWITMMAP
```

### 2762. `/SCWM/22000002`

```
T-Code: /SCWM/22000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/CD_PRDHD, /SAPAPO/CD_PRDLW
```

### 2763. `/SCWM/22000005`

```
T-Code: /SCWM/22000005
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T303S
```

### 2764. `/SCWM/22000009`

```
T-Code: /SCWM/22000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRANSPL_I
```

### 2765. `/SCWM/22000011`

```
T-Code: /SCWM/22000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRANSREL_I
```

### 2766. `/SCWM/22000012`

```
T-Code: /SCWM/22000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRANSPL_O
```

### 2767. `/SCWM/22000013`

```
T-Code: /SCWM/22000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRANSREL_O
```

### 2768. `/SCWM/22000015`

```
T-Code: /SCWM/22000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TTMINTDEF
```

### 2769. `/SCWM/22000023`

```
T-Code: /SCWM/22000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CHM_CAT, /SCWM/V_CHM_GRP, /SCWM/V_CHM_TYP
```

### 2770. `/SCWM/22000024`

```
T-Code: /SCWM/22000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CHM_IMC, /SCWM/V_CHM_IMCD, /SCWM/V_CHM_IMS, /SCWM/V_CHM_IMSD
```

### 2771. `/SCWM/22000029`

```
T-Code: /SCWM/22000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TCHMDEST
```

### 2772. `/SCWM/22000031`

```
T-Code: /SCWM/22000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CHM_VW, /SCWM/V_CHM_VWFD, /SCWM/V_CHM_VWMF, /SCWM/V_CHM_VWMS
```

### 2773. `/SCWM/22000043`

```
T-Code: /SCWM/22000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LICENSE
```

### 2774. `/SCWM/22000065`

```
T-Code: /SCWM/22000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VERPSKIPRQ
```

### 2775. `/SCWM/22000066`

```
T-Code: /SCWM/22000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VBATCHORGA
```

### 2776. `/SCWM/22000077`

```
T-Code: /SCWM/22000077
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LA_ACTTY
```

### 2777. `/SCWM/22000078`

```
T-Code: /SCWM/22000078
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_EV_TYPE
```

### 2778. `/SCWM/22000079`

```
T-Code: /SCWM/22000079
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LA_GROUP
```

### 2779. `/SCWM/22000081`

```
T-Code: /SCWM/22000081
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPRRTYPE
```

### 2780. `/SCWM/22000082`

```
T-Code: /SCWM/22000082
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPRRREPG
```

### 2781. `/SCWM/22000083`

```
T-Code: /SCWM/22000083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPRRWRKC
```

### 2782. `/SCWM/22000085`

```
T-Code: /SCWM/22000085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PCTLTTY
```

### 2783. `/SCWM/22000092`

```
T-Code: /SCWM/22000092
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TACTTY
```

### 2784. `/SCWM/22000107`

```
T-Code: /SCWM/22000107
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_PC, /SCWM/V_COM_SCCE, /SCWM/V_COM_SCDE
```

### 2785. `/SCWM/22000108`

```
T-Code: /SCWM/22000108
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_APP
```

### 2786. `/SCWM/22000109`

```
T-Code: /SCWM/22000109
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_BC
```

### 2787. `/SCWM/22000110`

```
T-Code: /SCWM/22000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_SF
```

### 2788. `/SCWM/22000111`

```
T-Code: /SCWM/22000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_ST
```

### 2789. `/SCWM/22000112`

```
T-Code: /SCWM/22000112
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_COM_SC
```

### 2790. `/SCWM/22000114`

```
T-Code: /SCWM/22000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWHCAPDEF
```

### 2791. `/SCWM/22000115`

```
T-Code: /SCWM/22000115
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSHIFTTOL
```

### 2792. `/SCWM/22000123`

```
T-Code: /SCWM/22000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_QMI
```

### 2793. `/SCWM/22000124`

```
T-Code: /SCWM/22000124
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_RESVER
```

### 2794. `/SCWM/22000133`

```
T-Code: /SCWM/22000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_PB_PROF
```

### 2795. `/SCWM/22000135`

```
T-Code: /SCWM/22000135
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PCKHUDT, /SCWM/V_PCKHUDTS
```

### 2796. `/SCWM/22000137`

```
T-Code: /SCWM/22000137
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DSVC_ASS
```

### 2797. `/SCWM/22000138`

```
T-Code: /SCWM/22000138
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WHCAPDEF
```

### 2798. `/SCWM/22000139`

```
T-Code: /SCWM/22000139
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SHIFTTOL
```

### 2799. `/SCWM/22000144`

```
T-Code: /SCWM/22000144
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VABINCD
```

### 2800. `/SCWM/22000145`

```
T-Code: /SCWM/22000145
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_OC
```

### 2801. `/SCWM/22000146`

```
T-Code: /SCWM/22000146
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GBT_REG
```

### 2802. `/SCWM/22000149`

```
T-Code: /SCWM/22000149
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340SYGM
```

### 2803. `/SCWM/22000150`

```
T-Code: /SCWM/22000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VFORMFLD
```

### 2804. `/SCWM/22000151`

```
T-Code: /SCWM/22000151
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DSSC
```

### 2805. `/SCWM/22000152`

```
T-Code: /SCWM/22000152
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GBT_LMAP
```

### 2806. `/SCWM/22000153`

```
T-Code: /SCWM/22000153
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_OD
```

### 2807. `/SCWM/22000154`

```
T-Code: /SCWM/22000154
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331SSC
```

### 2808. `/SCWM/22000154_CE`

```
T-Code: /SCWM/22000154_CE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T305Q, /SCWM/T305R, /SCWM/T331B, /SCWM/T334Q, /SCWM/T334QSD, /SCWM/T334R, /SCWM/T334RSD, /SCWM/T337B, /SCWM/V_PIAREA, /SCWM/V_PIPROC, /SCWM/V_T3010, /SCWM/V_T334QS, /SCWM/V_T334RS, /SCWM/V_TAAREA, /SCWM/V_TAAREAD, /SCWM/V_TAAREAS, /SCWM/V_TMAPSTLO, /SCWM/V_TWCRSSEQ
```

### 2809. `/SCWM/22000156`

```
T-Code: /SCWM/22000156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VFORMFLDSC
```

### 2810. `/SCWM/22000157`

```
T-Code: /SCWM/22000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PI_AREA, /SCWM/V_PI_PROC
```

### 2811. `/SCWM/22000158`

```
T-Code: /SCWM/22000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T300MSSC
```

### 2812. `/SCWM/22000159`

```
T-Code: /SCWM/22000159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/V_BSGDEF
```

### 2813. `/SCWM/22000160`

```
T-Code: /SCWM/22000160
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3010SSC
```

### 2814. `/SCWM/22000161`

```
T-Code: /SCWM/22000161
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3010
```

### 2815. `/SCWM/22000162`

```
T-Code: /SCWM/22000162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREA
```

### 2816. `/SCWM/22000163`

```
T-Code: /SCWM/22000163
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAD
```

### 2817. `/SCWM/22000164`

```
T-Code: /SCWM/22000164
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAS
```

### 2818. `/SCWM/22000165`

```
T-Code: /SCWM/22000165
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PI_AREA, /SCWM/V_PI_PROC
```

### 2819. `/SCWM/22000166`

```
T-Code: /SCWM/22000166
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334QS
```

### 2820. `/SCWM/22000167`

```
T-Code: /SCWM/22000167
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334Q
```

### 2821. `/SCWM/22000168`

```
T-Code: /SCWM/22000168
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334RSD, /SCWM/V_T334RS
```

### 2822. `/SCWM/22000169`

```
T-Code: /SCWM/22000169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334R
```

### 2823. `/SCWM/22000170`

```
T-Code: /SCWM/22000170
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSSEQ
```

### 2824. `/SCWM/22000172`

```
T-Code: /SCWM/22000172
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PIAREA, /SCWM/V_PIPROC
```

### 2825. `/SCWM/22000173`

```
T-Code: /SCWM/22000173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PIAREA, /SCWM/V_PIPROC
```

### 2826. `/SCWM/22000174`

```
T-Code: /SCWM/22000174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3000
```

### 2827. `/SCWM/22000175`

```
T-Code: /SCWM/22000175
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DYBI_PAT
```

### 2828. `/SCWM/22000187`

```
T-Code: /SCWM/22000187
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTMAPPLANT
```

### 2829. `/SCWM/22000188`

```
T-Code: /SCWM/22000188
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDECENTRAL
```

### 2830. `/SCWM/22000201`

```
T-Code: /SCWM/22000201
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDEFVAL
```

### 2831. `/SCWM/22000202`

```
T-Code: /SCWM/22000202
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQDEFVALWH
```

### 2832. `/SCWM/22000216`

```
T-Code: /SCWM/22000216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MEPSA
```

### 2833. `/SCWM/22000219`

```
T-Code: /SCWM/22000219
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDLDETTYPI
```

### 2834. `/SCWM/22000223`

```
T-Code: /SCWM/22000223
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SR_CD
```

### 2835. `/SCWM/22000226`

```
T-Code: /SCWM/22000226
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340WSIM
```

### 2836. `/SCWM/22000236`

```
T-Code: /SCWM/22000236
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DHUR
```

### 2837. `/SCWM/22000237`

```
T-Code: /SCWM/22000237
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TM_CTRL
```

### 2838. `/SCWM/22000243`

```
T-Code: /SCWM/22000243
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDLVPACI_I, /SCWM/VDLVPACMAT, /SCWM/VDLVPACR_I
```

### 2839. `/SCWM/22000244`

```
T-Code: /SCWM/22000244
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_DOCM
```

### 2840. `/SCWM/22000245`

```
T-Code: /SCWM/22000245
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VPASRCAT
```

### 2841. `/SCWM/22000246`

```
T-Code: /SCWM/22000246
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VAPASCD
```

### 2842. `/SCWM/22000260`

```
T-Code: /SCWM/22000260
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TDGRP_ST
```

### 2843. `/SCWM/22000262`

```
T-Code: /SCWM/22000262
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_WPTD
```

### 2844. `/SCWM/22000263`

```
T-Code: /SCWM/22000263
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_WPTA
```

### 2845. `/SCWM/22000269`

```
T-Code: /SCWM/22000269
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WI_CTRL
```

### 2846. `/SCWM/22000270`

```
T-Code: /SCWM/22000270
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WI_LGSYS
```

### 2847. `/SCWM/22000271`

```
T-Code: /SCWM/22000271
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WI_LG_WH
```

### 2848. `/SCWM/22000272`

```
T-Code: /SCWM/22000272
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WI_LGRFC
```

### 2849. `/SCWM/22000275`

```
T-Code: /SCWM/22000275
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DCBC
```

### 2850. `/SCWM/22000276`

```
T-Code: /SCWM/22000276
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DSGM
```

### 2851. `/SCWM/22000278`

```
T-Code: /SCWM/22000278
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333SSC
```

### 2852. `/SCWM/22000281`

```
T-Code: /SCWM/22000281
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMESST
```

### 2853. `/SCWM/22000282`

```
T-Code: /SCWM/22000282
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WI_WHNUM
```

### 2854. `/SCWM/22000283`

```
T-Code: /SCWM/22000283
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SDESTDEF
```

### 2855. `/SCWM/22000293`

```
T-Code: /SCWM/22000293
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STOCKDET
```

### 2856. `/SCWM/22000294`

```
T-Code: /SCWM/22000294
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MG_STCKD
```

### 2857. `/SCWM/22000295`

```
T-Code: /SCWM/22000295
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MG_STDAS
```

### 2858. `/SCWM/22000296`

```
T-Code: /SCWM/22000296
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWPTDSSC
```

### 2859. `/SCWM/22000297`

```
T-Code: /SCWM/22000297
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333CSSC
```

### 2860. `/SCWM/22000298`

```
T-Code: /SCWM/22000298
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTIMREAGRP, /SCWM/VTREAMTSSC, /SCWM/VTREARQSSC, /SCWM/V_TREASON
```

### 2861. `/SCWM/22000308`

```
T-Code: /SCWM/22000308
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346, /SCWM/V_T346A, /SCWM/V_T346L, /SCWM/V_T346L_PI
```

### 2862. `/SCWM/22000309`

```
T-Code: /SCWM/22000309
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSORT
```

### 2863. `/SCWM/22000310`

```
T-Code: /SCWM/22000310
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DA
```

### 2864. `/SCWM/22000311`

```
T-Code: /SCWM/22000311
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MODE, /SCWM/V_MODEDET2
```

### 2865. `/SCWM/22000312`

```
T-Code: /SCWM/22000312
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRSORTD
```

### 2866. `/SCWM/22000313`

```
T-Code: /SCWM/22000313
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSORT
```

### 2867. `/SCWM/22000317`

```
T-Code: /SCWM/22000317
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TOL_DIFF
```

### 2868. `/SCWM/22000318`

```
T-Code: /SCWM/22000318
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTSTG_DOCM
```

### 2869. `/SCWM/22000319`

```
T-Code: /SCWM/22000319
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTSTG_WPTD
```

### 2870. `/SCWM/22000320`

```
T-Code: /SCWM/22000320
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VSTG_WPTA
```

### 2871. `/SCWM/22000330`

```
T-Code: /SCWM/22000330
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMAPSTLO
```

### 2872. `/SCWM/22000331`

```
T-Code: /SCWM/22000331
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TAVLGROUP
```

### 2873. `/SCWM/22000332`

```
T-Code: /SCWM/22000332
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DPBP
```

### 2874. `/SCWM/22000333`

```
T-Code: /SCWM/22000333
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SETDSH
```

### 2875. `/SCWM/22000336`

```
T-Code: /SCWM/22000336
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VSOAPDEF
```

### 2876. `/SCWM/23000012`

```
T-Code: /SCWM/23000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMQDEF
```

### 2877. `/SCWM/23000013`

```
T-Code: /SCWM/23000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMQCGROUP, /SCWM/VMQCGRPCNT
```

### 2878. `/SCWM/23000018`

```
T-Code: /SCWM/23000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDSREFCAT
```

### 2879. `/SCWM/23000032`

```
T-Code: /SCWM/23000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LDPACTIV
```

### 2880. `/SCWM/23000037`

```
T-Code: /SCWM/23000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDLVPACITM, /SCWM/VDLVPACMAT, /SCWM/VDLVPACREL
```

### 2881. `/SCWM/23000058`

```
T-Code: /SCWM/23000058
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TUPROF
```

### 2882. `/SCWM/23000059`

```
T-Code: /SCWM/23000059
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TUPROFAC
```

### 2883. `/SCWM/23000060`

```
T-Code: /SCWM/23000060
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMS_SCCTX, /SCWM/VMS_SCGRP, /SCWM/VMS_SCGRPA, /SCWM/VMS_SCGRPD, /SCWM/VMS_SCMCTX
```

### 2884. `/SCWM/30000005`

```
T-Code: /SCWM/30000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340PSA
```

### 2885. `/SCWM/30000008`

```
T-Code: /SCWM/30000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TGWLCAT
```

### 2886. `/SCWM/30000009`

```
T-Code: /SCWM/30000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GWLOBJ
```

### 2887. `/SCWM/30000012`

```
T-Code: /SCWM/30000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/TMODE
```

### 2888. `/SCWM/30000014`

```
T-Code: /SCWM/30000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMEDI_AP
```

### 2889. `/SCWM/30000015`

```
T-Code: /SCWM/30000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMEDI_AM
```

### 2890. `/SCWM/30000016`

```
T-Code: /SCWM/30000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_IP
```

### 2891. `/SCWM/30000017`

```
T-Code: /SCWM/30000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VTRTYPWM
```

### 2892. `/SCWM/30000018`

```
T-Code: /SCWM/30000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVPPGIE
```

### 2893. `/SCWM/30000019`

```
T-Code: /SCWM/30000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVPPGIC
```

### 2894. `/SCWM/30000035`

```
T-Code: /SCWM/30000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DCD
```

### 2895. `/SCWM/30000037`

```
T-Code: /SCWM/30000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREC
```

### 2896. `/SCWM/30000038`

```
T-Code: /SCWM/30000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRECGRP
```

### 2897. `/SCWM/30000042`

```
T-Code: /SCWM/30000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T347
```

### 2898. `/SCWM/30000044`

```
T-Code: /SCWM/30000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333MEDI
```

### 2899. `/SCWM/30000047`

```
T-Code: /SCWM/30000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_PA
```

### 2900. `/SCWM/41000004`

```
T-Code: /SCWM/41000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TUOM_QCLA
```

### 2901. `/SCWM/41000014`

```
T-Code: /SCWM/41000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_BPMAPGEN
```

### 2902. `/SCWM/41000015`

```
T-Code: /SCWM/41000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCWTOLGR
```

### 2903. `/SCWM/41000017`

```
T-Code: /SCWM/41000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCWPROC
```

### 2904. `/SCWM/41000019`

```
T-Code: /SCWM/41000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_BPMAPDIF
```

### 2905. `/SCWM/41000021`

```
T-Code: /SCWM/41000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCWPROCWM
```

### 2906. `/SCWM/41000022`

```
T-Code: /SCWM/41000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SHPMNTNO
```

### 2907. `/SCWM/41000024`

```
T-Code: /SCWM/41000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCWTOLGRWM
```

### 2908. `/SCWM/41000025`

```
T-Code: /SCWM/41000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEGF_CHT
```

### 2909. `/SCWM/41000026`

```
T-Code: /SCWM/41000026
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEGF_ATH
```

### 2910. `/SCWM/41000034`

```
T-Code: /SCWM/41000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPAPO/VTRTYPWM
```

### 2911. `/SCWM/41000035`

```
T-Code: /SCWM/41000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEGF_FNC, /SCWM/VEGF_FINP
```

### 2912. `/SCWM/41000036`

```
T-Code: /SCWM/41000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VEGF_OBJ, /SCWM/VEGF_OCHT, /SCWM/VEGF_OFINP, /SCWM/VEGF_OFNC, /SCWM/VEGF_OINP
```

### 2913. `/SCWM/41000037`

```
T-Code: /SCWM/41000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEGF_CPT, /SCWM/VEGF_CINP, /SCWM/VEGF_COBJ, /SCWM/VEGF_COCHT, /SCWM/VEGF_COFIN, /SCWM/VEGF_COFNC, /SCWM/VEGF_COINP
```

### 2914. `/SCWM/41000040`

```
T-Code: /SCWM/41000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333B_ST
```

### 2915. `/SCWM/41000041`

```
T-Code: /SCWM/41000041
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TACC_CAT, /SCWM/V_ACC_LGN
```

### 2916. `/SCWM/41000050`

```
T-Code: /SCWM/41000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DTPRC, /SCWM/V_SDTPRC
```

### 2917. `/SCWM/41000051`

```
T-Code: /SCWM/41000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CNT_GR
```

### 2918. `/SCWM/41000053`

```
T-Code: /SCWM/41000053
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBWDLVPROC
```

### 2919. `/SCWM/41000056`

```
T-Code: /SCWM/41000056
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPH_CPROF
```

### 2920. `/SCWM/41000057`

```
T-Code: /SCWM/41000057
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPI_CPROF
```

### 2921. `/SCWM/41000060`

```
T-Code: /SCWM/41000060
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SAVCPROF, /SCWM/V_TAVCPROF
```

### 2922. `/SCWM/41000063`

```
T-Code: /SCWM/41000063
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_I
```

### 2923. `/SCWM/41000064`

```
T-Code: /SCWM/41000064
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_I
```

### 2924. `/SCWM/41000065`

```
T-Code: /SCWM/41000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_O
```

### 2925. `/SCWM/41000066`

```
T-Code: /SCWM/41000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_O
```

### 2926. `/SCWM/41000067`

```
T-Code: /SCWM/41000067
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_T
```

### 2927. `/SCWM/41000068`

```
T-Code: /SCWM/41000068
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_T
```

### 2928. `/SCWM/41000069`

```
T-Code: /SCWM/41000069
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_S
```

### 2929. `/SCWM/41000070`

```
T-Code: /SCWM/41000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_S
```

### 2930. `/SCWM/41000071`

```
T-Code: /SCWM/41000071
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVBATCH_I
```

### 2931. `/SCWM/41000072`

```
T-Code: /SCWM/41000072
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVBATCH_O
```

### 2932. `/SCWM/41000076`

```
T-Code: /SCWM/41000076
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/GEOMAPCUST
```

### 2933. `/SCWM/41000077`

```
T-Code: /SCWM/41000077
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCTRLPROCS
```

### 2934. `/SCWM/41000078`

```
T-Code: /SCWM/41000078
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → VBCVMAP_PRODUCT, VBCV_APP2, VBCV_APP_MS_ASGN, VBCV_GC_SERV, VBCV_MAP_SERVER, VBCV_MAP_STACK, VBCV_MSTACK_ASGN, VBCV_SERV_URL
```

### 2935. `/SCWM/41000079`

```
T-Code: /SCWM/41000079
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → VBCV_MAP_SERVERC
```

### 2936. `/SCWM/41000080`

```
T-Code: /SCWM/41000080
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVRGSCTL, /SCWM/V_TDLVRGSD
```

### 2937. `/SCWM/41000081`

```
T-Code: /SCWM/41000081
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPROUTE
```

### 2938. `/SCWM/41000082`

```
T-Code: /SCWM/41000082
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SDLVAVC, /SCWM/V_TDLVAVC
```

### 2939. `/SCWM/41000085`

```
T-Code: /SCWM/41000085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MIGSTYPR
```

### 2940. `/SCWM/41000087`

```
T-Code: /SCWM/41000087
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVBATCH
```

### 2941. `/SCWM/41000088`

```
T-Code: /SCWM/41000088
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVBATCH_ST
```

### 2942. `/SCWM/41000089`

```
T-Code: /SCWM/41000089
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TITYPE_BT
```

### 2943. `/SCWM/41000090`

```
T-Code: /SCWM/41000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBATCHUPD
```

### 2944. `/SCWM/41000097`

```
T-Code: /SCWM/41000097
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MIGPATOA
```

### 2945. `/SCWM/41000100`

```
T-Code: /SCWM/41000100
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MIGTWHPT
```

### 2946. `/SCWM/41000102`

```
T-Code: /SCWM/41000102
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MIGSUTHT
```

### 2947. `/SCWM/41000117`

```
T-Code: /SCWM/41000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_LM, /SCWM/V_TLMPROCS, /SCWM/V_TLMSTEPS
```

### 2948. `/SCWM/41000118`

```
T-Code: /SCWM/41000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_EPC
```

### 2949. `/SCWM/41000119`

```
T-Code: /SCWM/41000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_EPC_PMT
```

### 2950. `/SCWM/41000120`

```
T-Code: /SCWM/41000120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VASTYKIT
```

### 2951. `/SCWM/41000121`

```
T-Code: /SCWM/41000121
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VASTYREV
```

### 2952. `/SCWM/41000122`

```
T-Code: /SCWM/41000122
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEPC_TYPE
```

### 2953. `/SCWM/41000123`

```
T-Code: /SCWM/41000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/RFID_RFC
```

### 2954. `/SCWM/41000131`

```
T-Code: /SCWM/41000131
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VASTYINT
```

### 2955. `/SCWM/41000132`

```
T-Code: /SCWM/41000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVPAR
```

### 2956. `/SCWM/41000133`

```
T-Code: /SCWM/41000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333_BB
```

### 2957. `/SCWM/41000134`

```
T-Code: /SCWM/41000134
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_LM, /SCWM/V_TLMPROCS, /SCWM/V_TLMSTEPS
```

### 2958. `/SCWM/41000135`

```
T-Code: /SCWM/41000135
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_LM, /SCWM/V_TLMPROCS, /SCWM/V_TLMSTEPS
```

### 2959. `/SCWM/41000136`

```
T-Code: /SCWM/41000136
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPROCS
```

### 2960. `/SCWM/41000139`

```
T-Code: /SCWM/41000139
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBASIC_MSV
```

### 2961. `/SCWM/41000140`

```
T-Code: /SCWM/41000140
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TGROUP_MSV
```

### 2962. `/SCWM/41000146`

```
T-Code: /SCWM/41000146
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LM_T302, /SCWM/V_LM_T331, /SCWM/V_LM_WORKC
```

### 2963. `/SCWM/41000155`

```
T-Code: /SCWM/41000155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346MFS
```

### 2964. `/SCWM/41000156`

```
T-Code: /SCWM/41000156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TRTYPMFS
```

### 2965. `/SCWM/41000157`

```
T-Code: /SCWM/41000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCCH
```

### 2966. `/SCWM/41000158`

```
T-Code: /SCWM/41000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSCCH_A
```

### 2967. `/SCWM/41000159`

```
T-Code: /SCWM/41000159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCCHR
```

### 2968. `/SCWM/41000160`

```
T-Code: /SCWM/41000160
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSEXCMPI
```

### 2969. `/SCWM/41000161`

```
T-Code: /SCWM/41000161
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFEXCMC
```

### 2970. `/SCWM/41000162`

```
T-Code: /SCWM/41000162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSERRMPI
```

### 2971. `/SCWM/43000002`

```
T-Code: /SCWM/43000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSLD_E
```

### 2972. `/SCWM/43000003`

```
T-Code: /SCWM/43000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSLD_P
```

### 2973. `/SCWM/43000005`

```
T-Code: /SCWM/43000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSROUTE
```

### 2974. `/SCWM/43000014`

```
T-Code: /SCWM/43000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCAPALGO, /SCWM/TCAPALGSQ, /SCWM/V_CAP_ALPR, /SCWM/V_CAP_ASQ, /SCWM/V_CAP_PRF
```

### 2975. `/SCWM/43000015`

```
T-Code: /SCWM/43000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TCAPPP
```

### 2976. `/SCWM/43000017`

```
T-Code: /SCWM/43000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CAPASQ
```

### 2977. `/SCWM/43000018`

```
T-Code: /SCWM/43000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCAPPPD
```

### 2978. `/SCWM/43000019`

```
T-Code: /SCWM/43000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMIGMAPSTY
```

### 2979. `/SCWM/43000020`

```
T-Code: /SCWM/43000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMIGSEP
```

### 2980. `/SCWM/43000023`

```
T-Code: /SCWM/43000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_CAP
```

### 2981. `/SCWM/43000024`

```
T-Code: /SCWM/43000024
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SDESTMIG
```

### 2982. `/SCWM/43000031`

```
T-Code: /SCWM/43000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SDESTQM
```

### 2983. `/SCWM/43000050`

```
T-Code: /SCWM/43000050
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_SDEST
```

### 2984. `/SCWM/47000001`

```
T-Code: /SCWM/47000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPSORG_V
```

### 2985. `/SCWM/47000002`

```
T-Code: /SCWM/47000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VGR_UICTRL
```

### 2986. `/SCWM/47000005`

```
T-Code: /SCWM/47000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_E
```

### 2987. `/SCWM/47000006`

```
T-Code: /SCWM/47000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_E
```

### 2988. `/SCWM/47000008`

```
T-Code: /SCWM/47000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TALITYPE_EGR
```

### 2989. `/SCWM/47000012`

```
T-Code: /SCWM/47000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETTYPE_EGR
```

### 2990. `/SCWM/47000013`

```
T-Code: /SCWM/47000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCDL/TDETITP_EGR
```

### 2991. `/SCWM/47000031`

```
T-Code: /SCWM/47000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBIF_YRD_N
```

### 2992. `/SCWM/47000032`

```
T-Code: /SCWM/47000032
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBIF_YRD_D
```

### 2993. `/SCWM/47000033`

```
T-Code: /SCWM/47000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBIF_Y_A_V
```

### 2994. `/SCWM/47000035`

```
T-Code: /SCWM/47000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBIF_YRD
```

### 2995. `/SCWM/47000036`

```
T-Code: /SCWM/47000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSCERMPI
```

### 2996. `/SCWM/47000068`

```
T-Code: /SCWM/47000068
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCPFU
```

### 2997. `/SCWM/47000118`

```
T-Code: /SCWM/47000118
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DIFF_REA
```

### 2998. `/SCWM/47000126`

```
T-Code: /SCWM/47000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMS_TLR
```

### 2999. `/SCWM/47000127`

```
T-Code: /SCWM/47000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMS_CLC
```

### 3000. `/SCWM/47000128`

```
T-Code: /SCWM/47000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PPR_TIME
```

### 3001. `/SCWM/47000129`

```
T-Code: /SCWM/47000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSPLCIF
```

### 3002. `/SCWM/47000132`

```
T-Code: /SCWM/47000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TOWNBS
```

### 3003. `/SCWM/52001314`

```
T-Code: /SCWM/52001314
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QIOTWM_2, /SCWM/V_QIOTWM_N
```

### 3004. `/SCWM/52001796`

```
T-Code: /SCWM/52001796
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_THUNR_S4
```

### 3005. `/SCWM/52001868`

```
T-Code: /SCWM/52001868
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_THSERPRO
```

### 3006. `/SCWM/52001959`

```
T-Code: /SCWM/52001959
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TGTL
```

### 3007. `/SCWM/52002151`

```
T-Code: /SCWM/52002151
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QNFU_S4, /SCWM/V_TQFU_S4
```

### 3008. `/SCWM/52002702`

```
T-Code: /SCWM/52002702
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QFUCO_S4, /SCWM/V_TQFU_S4
```

### 3009. `/SCWM/52002703`

```
T-Code: /SCWM/52002703
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TQFU_S4
```

### 3010. `/SCWM/52003080`

```
T-Code: /SCWM/52003080
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_LAGP
```

### 3011. `/SCWM/52003081`

```
T-Code: /SCWM/52003081
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_MATLW
```

### 3012. `/SCWM/52003082`

```
T-Code: /SCWM/52003082
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_RSRC
```

### 3013. `/SCWM/52003083`

```
T-Code: /SCWM/52003083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_SYSDI
```

### 3014. `/SCWM/52003084`

```
T-Code: /SCWM/52003084
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_T300M
```

### 3015. `/SCWM/52003085`

```
T-Code: /SCWM/52003085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_T300S
```

### 3016. `/SCWM/52003086`

```
T-Code: /SCWM/52003086
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_TMAPS
```

### 3017. `/SCWM/52003087`

```
T-Code: /SCWM/52003087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCBC_TWMEQ
```

### 3018. `/SCWM/59000002`

```
T-Code: /SCWM/59000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTIMCGRBEW
```

### 3019. `/SCWM/59000009`

```
T-Code: /SCWM/59000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/EDI_MONI
```

### 3020. `/SCWM/59000014`

```
T-Code: /SCWM/59000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MPHU_DET, /SCWM/V_MPHU_DTS
```

### 3021. `/SCWM/59000017`

```
T-Code: /SCWM/59000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PTRO
```

### 3022. `/SCWM/59000018`

```
T-Code: /SCWM/59000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_NSS
```

### 3023. `/SCWM/59000019`

```
T-Code: /SCWM/59000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_SCRM
```

### 3024. `/SCWM/59000020`

```
T-Code: /SCWM/59000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/V_ACS_SCRE
```

### 3025. `/SCWM/59000027`

```
T-Code: /SCWM/59000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPRF_COMB, /SCWM/TWPRF_COMB, /SCWM/V_COMB_DTS
```

### 3026. `/SCWM/59000028`

```
T-Code: /SCWM/59000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STEP_PBV, /SCWM/V_STEP_SVP, /SCWM/V_TRNS_PBV
```

### 3027. `/SCWM/60000015`

```
T-Code: /SCWM/60000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333U
```

### 3028. `/SCWM/60000032`

```
T-Code: /SCWM/60000032
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREPA
```

### 3029. `/SCWM/60000033`

```
T-Code: /SCWM/60000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREPLTM
```

### 3030. `/SCWM/60000043`

```
T-Code: /SCWM/60000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMATPP
```

### 3031. `/SCWM/60000047`

```
T-Code: /SCWM/60000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3000
```

### 3032. `/SCWM/60000049`

```
T-Code: /SCWM/60000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3010
```

### 3033. `/SCWM/60000050`

```
T-Code: /SCWM/60000050
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T302
```

### 3034. `/SCWM/60000051`

```
T-Code: /SCWM/60000051
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T301G
```

### 3035. `/SCWM/60000052`

```
T-Code: /SCWM/60000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T301M
```

### 3036. `/SCWM/60000054`

```
T-Code: /SCWM/60000054
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334RRD, /SCWM/V_T334RR
```

### 3037. `/SCWM/60000056`

```
T-Code: /SCWM/60000056
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334RSD, /SCWM/V_T334RS
```

### 3038. `/SCWM/60000057`

```
T-Code: /SCWM/60000057
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334RA
```

### 3039. `/SCWM/60000060`

```
T-Code: /SCWM/60000060
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCATG
```

### 3040. `/SCWM/60000061`

```
T-Code: /SCWM/60000061
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPROCTYG
```

### 3041. `/SCWM/60000062`

```
T-Code: /SCWM/60000062
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STCKDTGR
```

### 3042. `/SCWM/60000063`

```
T-Code: /SCWM/60000063
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STCKDT
```

### 3043. `/SCWM/60000064`

```
T-Code: /SCWM/60000064
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TROUTL
```

### 3044. `/SCWM/60000065`

```
T-Code: /SCWM/60000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPRCES, /SCWM/TROUTP, /SCWM/TWHSPROCS, /SCWM/V_TPRCESA, /SCWM/V_TPROCS
```

### 3045. `/SCWM/60000066`

```
T-Code: /SCWM/60000066
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T333
```

### 3046. `/SCWM/60000067`

```
T-Code: /SCWM/60000067
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREPA
```

### 3047. `/SCWM/60000068`

```
T-Code: /SCWM/60000068
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREPLTM
```

### 3048. `/SCWM/60000069`

```
T-Code: /SCWM/60000069
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333U
```

### 3049. `/SCWM/60000070`

```
T-Code: /SCWM/60000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_2S
```

### 3050. `/SCWM/60000076`

```
T-Code: /SCWM/60000076
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWRKTYP
```

### 3051. `/SCWM/60000077`

```
T-Code: /SCWM/60000077
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WORKST
```

### 3052. `/SCWM/60000078`

```
T-Code: /SCWM/60000078
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAS
```

### 3053. `/SCWM/60000087`

```
T-Code: /SCWM/60000087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TBUSSYS
```

### 3054. `/SCWM/60000088`

```
T-Code: /SCWM/60000088
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPDLVTYP
```

### 3055. `/SCWM/60000089`

```
T-Code: /SCWM/60000089
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPITMTYP
```

### 3056. `/SCWM/60000090`

```
T-Code: /SCWM/60000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCTRLMSG
```

### 3057. `/SCWM/60000091`

```
T-Code: /SCWM/60000091
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPWHNUM
```

### 3058. `/SCWM/60000116`

```
T-Code: /SCWM/60000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSTAREA_V
```

### 3059. `/SCWM/60000117`

```
T-Code: /SCWM/60000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T303
```

### 3060. `/SCWM/60000118`

```
T-Code: /SCWM/60000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T343G
```

### 3061. `/SCWM/60000119`

```
T-Code: /SCWM/60000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T343XYZ
```

### 3062. `/SCWM/60000120`

```
T-Code: /SCWM/60000120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TACTTY
```

### 3063. `/SCWM/60000121`

```
T-Code: /SCWM/60000121
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREA
```

### 3064. `/SCWM/60000122`

```
T-Code: /SCWM/60000122
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAD
```

### 3065. `/SCWM/60000123`

```
T-Code: /SCWM/60000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAS
```

### 3066. `/SCWM/60000124`

```
T-Code: /SCWM/60000124
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPTYPDET
```

### 3067. `/SCWM/60000128`

```
T-Code: /SCWM/60000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRFILTER
```

### 3068. `/SCWM/60000129`

```
T-Code: /SCWM/60000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRLIMIT
```

### 3069. `/SCWM/60000130`

```
T-Code: /SCWM/60000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRPP
```

### 3070. `/SCWM/60000132`

```
T-Code: /SCWM/60000132
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCR
```

### 3071. `/SCWM/60000133`

```
T-Code: /SCWM/60000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSSEQ
```

### 3072. `/SCWM/60000134`

```
T-Code: /SCWM/60000134
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRFILTER, /SCWM/TWCRLIMIT, /SCWM/V_TWCRFILT, /SCWM/V_TWCRPACK, /SCWM/V_TWCRPP, /SCWM/V_TWCRSORT, /SCWM/V_TWCRSRTO, /SCWM/V_TWCR_SUB
```

### 3073. `/SCWM/60000137`

```
T-Code: /SCWM/60000137
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TBUSID, /SCWM/TEXECSTEP, /SCWM/TIPRCODE, /SCWM/V_ESTEPDEF, /SCWM/V_EXCIBUSC, /SCWM/V_TEXCODEI, /SCWM/V_TIPRDEF, /SCWM/V_TSTMIDDF
```

### 3074. `/SCWM/60000138`

```
T-Code: /SCWM/60000138
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TEXCEP, /SCWM/TEXCEPWF, /SCWM/V_TEXCPROF, /SCWM/V_TEXEADJ, /SCWM/V_TEXEALT, /SCWM/V_TEXEH, /SCWM/V_TEXEIPR, /SCWM/V_TEXESTM, /SCWM/V_TEXEWF, /SCWM/V_WFRECEIV
```

### 3075. `/SCWM/60000155`

```
T-Code: /SCWM/60000155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T313Z
```

### 3076. `/SCWM/60000158`

```
T-Code: /SCWM/60000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMTR_TR_GR
```

### 3077. `/SCWM/60000164`

```
T-Code: /SCWM/60000164
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_BIN_AT
```

### 3078. `/SCWM/60000166`

```
T-Code: /SCWM/60000166
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_BIN_AT_P, /SCWM/V_HU_G_P, /SCWM/V_RSRC_T
```

### 3079. `/SCWM/60000168`

```
T-Code: /SCWM/60000168
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CATEGORY, /SCWM/V_NODE, /SCWM/V_NODE_PRF, /SCWM/V_OBCLS
```

### 3080. `/SCWM/60000169`

```
T-Code: /SCWM/60000169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MONITOR, /SCWM/V_TREE
```

### 3081. `/SCWM/60000170`

```
T-Code: /SCWM/60000170
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_METHOD, /SCWM/V_OBCLS_MT
```

### 3082. `/SCWM/60000171`

```
T-Code: /SCWM/60000171
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TWHMATGR
```

### 3083. `/SCWM/60000172`

```
T-Code: /SCWM/60000172
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/THNDLCD
```

### 3084. `/SCWM/60000173`

```
T-Code: /SCWM/60000173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TWHSTC
```

### 3085. `/SCWM/60000174`

```
T-Code: /SCWM/60000174
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPROCPRFL, /SCWM/TPROCPRFLD
```

### 3086. `/SCWM/60000175`

```
T-Code: /SCWM/60000175
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340WVP
```

### 3087. `/SCWM/60000176`

```
T-Code: /SCWM/60000176
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WAVETYPE
```

### 3088. `/SCWM/60000177`

```
T-Code: /SCWM/60000177
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WAVECAT
```

### 3089. `/SCWM/60000182`

```
T-Code: /SCWM/60000182
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 3090. `/SCWM/60000190`

```
T-Code: /SCWM/60000190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3001_CU
```

### 3091. `/SCWM/60000191`

```
T-Code: /SCWM/60000191
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T300_MD
```

### 3092. `/SCWM/60000199`

```
T-Code: /SCWM/60000199
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T305Q
```

### 3093. `/SCWM/60000201`

```
T-Code: /SCWM/60000201
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T304
```

### 3094. `/SCWM/60000202`

```
T-Code: /SCWM/60000202
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TSECT_SQ
```

### 3095. `/SCWM/60000204`

```
T-Code: /SCWM/60000204
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334QSD
```

### 3096. `/SCWM/60000206`

```
T-Code: /SCWM/60000206
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334QS
```

### 3097. `/SCWM/60000208`

```
T-Code: /SCWM/60000208
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DS
```

### 3098. `/SCWM/60000209`

```
T-Code: /SCWM/60000209
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331SL
```

### 3099. `/SCWM/60000210`

```
T-Code: /SCWM/60000210
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T303CON
```

### 3100. `/SCWM/60000224`

```
T-Code: /SCWM/60000224
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/THUTYP
```

### 3101. `/SCWM/60000225`

```
T-Code: /SCWM/60000225
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T307
```

### 3102. `/SCWM/60000226`

```
T-Code: /SCWM/60000226
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334E
```

### 3103. `/SCWM/60000228`

```
T-Code: /SCWM/60000228
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/THUTYPG
```

### 3104. `/SCWM/60000229`

```
T-Code: /SCWM/60000229
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTPMTYP
```

### 3105. `/SCWM/60000230`

```
T-Code: /SCWM/60000230
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPACKGR
```

### 3106. `/SCWM/60000231`

```
T-Code: /SCWM/60000231
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDEMTYP
```

### 3107. `/SCWM/60000232`

```
T-Code: /SCWM/60000232
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDEMIND
```

### 3108. `/SCWM/60000234`

```
T-Code: /SCWM/60000234
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDEMINDD
```

### 3109. `/SCWM/60000235`

```
T-Code: /SCWM/60000235
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDIMIND
```

### 3110. `/SCWM/60000236`

```
T-Code: /SCWM/60000236
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDIMINDD
```

### 3111. `/SCWM/60000252`

```
T-Code: /SCWM/60000252
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCTCOND
```

### 3112. `/SCWM/60000253`

```
T-Code: /SCWM/60000253
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 3113. `/SCWM/60000279`

```
T-Code: /SCWM/60000279
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMATPP
```

### 3114. `/SCWM/60000280`

```
T-Code: /SCWM/60000280
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/BINMAT
```

### 3115. `/SCWM/60000281`

```
T-Code: /SCWM/60000281
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334Q
```

### 3116. `/SCWM/60000282`

```
T-Code: /SCWM/60000282
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334QA
```

### 3117. `/SCWM/60000283`

```
T-Code: /SCWM/60000283
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331_J
```

### 3118. `/SCWM/60000284`

```
T-Code: /SCWM/60000284
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TBINTYP
```

### 3119. `/SCWM/60000285`

```
T-Code: /SCWM/60000285
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T337A
```

### 3120. `/SCWM/60000286`

```
T-Code: /SCWM/60000286
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T337Z
```

### 3121. `/SCWM/60000287`

```
T-Code: /SCWM/60000287
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T331B
```

### 3122. `/SCWM/60000288`

```
T-Code: /SCWM/60000288
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T338
```

### 3123. `/SCWM/60000289`

```
T-Code: /SCWM/60000289
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T337B
```

### 3124. `/SCWM/60000290`

```
T-Code: /SCWM/60000290
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T331C
```

### 3125. `/SCWM/60000291`

```
T-Code: /SCWM/60000291
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T337C
```

### 3126. `/SCWM/60000293`

```
T-Code: /SCWM/60000293
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MAXSP_PA
```

### 3127. `/SCWM/60000294`

```
T-Code: /SCWM/60000294
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAAREAJ
```

### 3128. `/SCWM/60000308`

```
T-Code: /SCWM/60000308
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPDTTYPE
```

### 3129. `/SCWM/60000309`

```
T-Code: /SCWM/60000309
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPPTYRLE
```

### 3130. `/SCWM/60000316`

```
T-Code: /SCWM/60000316
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPMTYPWM
```

### 3131. `/SCWM/60000322`

```
T-Code: /SCWM/60000322
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331SC1
```

### 3132. `/SCWM/60000324`

```
T-Code: /SCWM/60000324
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331SC2
```

### 3133. `/SCWM/60000325`

```
T-Code: /SCWM/60000325
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TSPSTDET
```

### 3134. `/SCWM/60000326`

```
T-Code: /SCWM/60000326
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDOOR_V
```

### 3135. `/SCWM/60000327`

```
T-Code: /SCWM/60000327
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDOOR_L_GR
```

### 3136. `/SCWM/60000329`

```
T-Code: /SCWM/60000329
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDOOR_STA
```

### 3137. `/SCWM/60000331`

```
T-Code: /SCWM/60000331
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VBWUPD
```

### 3138. `/SCWM/60000332`

```
T-Code: /SCWM/60000332
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VBWACT
```

### 3139. `/SCWM/60000341`

```
T-Code: /SCWM/60000341
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWMEQUEUE
```

### 3140. `/SCWM/60000344`

```
T-Code: /SCWM/60000344
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DR
```

### 3141. `/SCWM/60000345`

```
T-Code: /SCWM/60000345
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333R
```

### 3142. `/SCWM/60000346`

```
T-Code: /SCWM/60000346
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TFBIN_UNAS
```

### 3143. `/SCWM/60000347`

```
T-Code: /SCWM/60000347
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMAPSTLO
```

### 3144. `/SCWM/60000370`

```
T-Code: /SCWM/60000370
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDSTGRP
```

### 3145. `/SCWM/60000375`

```
T-Code: /SCWM/60000375
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_PD
```

### 3146. `/SCWM/60000376`

```
T-Code: /SCWM/60000376
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333_PD
```

### 3147. `/SCWM/60000380`

```
T-Code: /SCWM/60000380
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333RPL
```

### 3148. `/SCWM/60000381`

```
T-Code: /SCWM/60000381
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T313C, /SCWM/V_T313D, /SCWM/V_T313G, /SCWM/V_T313M
```

### 3149. `/SCWM/60000383`

```
T-Code: /SCWM/60000383
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPTDETIND
```

### 3150. `/SCWM/60000393`

```
T-Code: /SCWM/60000393
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TEXCOIE
```

### 3151. `/SCWM/60000399`

```
T-Code: /SCWM/60000399
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PACKSTDT
```

### 3152. `/SCWM/60000409`

```
T-Code: /SCWM/60000409
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPFIELD
```

### 3153. `/SCWM/60000411`

```
T-Code: /SCWM/60000411
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VPRF, /SCWM/V_VPRF_CNT
```

### 3154. `/SCWM/60000412`

```
T-Code: /SCWM/60000412
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VPRF_DET, /SCWM/V_VPRF_DTS
```

### 3155. `/SCWM/60000413`

```
T-Code: /SCWM/60000413
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T327A, /SCWM/V_T327B, /SCWM/V_T327V, /SCWM/V_T340DE
```

### 3156. `/SCWM/60000422`

```
T-Code: /SCWM/60000422
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPMTYPPR
```

### 3157. `/SCWM/60000423`

```
T-Code: /SCWM/60000423
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T313W
```

### 3158. `/SCWM/60000430`

```
T-Code: /SCWM/60000430
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333_A
```

### 3159. `/SCWM/60000431`

```
T-Code: /SCWM/60000431
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T329A, /SCWM/V_T329L, /SCWM/V_T329P, /SCWM/V_T340D_DP
```

### 3160. `/SCWM/60000480`

```
T-Code: /SCWM/60000480
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CNT_GR
```

### 3161. `/SCWM/60000501`

```
T-Code: /SCWM/60000501
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_ESI_TDL
```

### 3162. `/SCWM/60000502`

```
T-Code: /SCWM/60000502
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_ESI_SWIT
```

### 3163. `/SCWM/60000504`

```
T-Code: /SCWM/60000504
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_ESI_MAIN
```

### 3164. `/SCWM/60000517`

```
T-Code: /SCWM/60000517
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRDETOUT, /SCWM/V_SPRDETOU
```

### 3165. `/SCWM/60000521`

```
T-Code: /SCWM/60000521
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRDETIN, /SCWM/V_SPRDETIN
```

### 3166. `/SCWM/60000527`

```
T-Code: /SCWM/60000527
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_ACT_TID
```

### 3167. `/SCWM/60000530`

```
T-Code: /SCWM/60000530
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T685
```

### 3168. `/SCWM/60000531`

```
T-Code: /SCWM/60000531
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T682, /SAPCND/V1_T682I, /SAPCND/V1_T682Z
```

### 3169. `/SCWM/60000538`

```
T-Code: /SCWM/60000538
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_ACT_TTY
```

### 3170. `/SCWM/60000540`

```
T-Code: /SCWM/60000540
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_MTR_EXEC
```

### 3171. `/SCWM/60000542`

```
T-Code: /SCWM/60000542
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDRDETGR
```

### 3172. `/SCWM/60000549`

```
T-Code: /SCWM/60000549
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WV_CTPRC
```

### 3173. `/SCWM/60000572`

```
T-Code: /SCWM/60000572
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TREASON
```

### 3174. `/SCWM/60000574`

```
T-Code: /SCWM/60000574
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_BINTPSQ2
```

### 3175. `/SCWM/60000575`

```
T-Code: /SCWM/60000575
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TQUANCLA
```

### 3176. `/SCWM/60000576`

```
T-Code: /SCWM/60000576
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDSTGRPNO
```

### 3177. `/SCWM/60000599`

```
T-Code: /SCWM/60000599
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TCAT
```

### 3178. `/SCWM/60000604`

```
T-Code: /SCWM/60000604
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DQ
```

### 3179. `/SCWM/60000605`

```
T-Code: /SCWM/60000605
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3180. `/SCWM/60000608`

```
T-Code: /SCWM/60000608
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T309
```

### 3181. `/SCWM/60000615`

```
T-Code: /SCWM/60000615
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T333WAVE
```

### 3182. `/SCWM/60000630`

```
T-Code: /SCWM/60000630
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PI_V_LGNUM
```

### 3183. `/SCWM/60000631`

```
T-Code: /SCWM/60000631
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PI_CC_PERI
```

### 3184. `/SCWM/60000635`

```
T-Code: /SCWM/60000635
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPRF_ACT
```

### 3185. `/SCWM/60000640`

```
T-Code: /SCWM/60000640
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T305R
```

### 3186. `/SCWM/60000651`

```
T-Code: /SCWM/60000651
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDETTYPE_TP
```

### 3187. `/SCWM/60000652`

```
T-Code: /SCWM/60000652
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDETITP_ST
```

### 3188. `/SCWM/60000653`

```
T-Code: /SCWM/60000653
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDETTYPE_ST
```

### 3189. `/SCWM/60000654`

```
T-Code: /SCWM/60000654
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDETITP_TP
```

### 3190. `/SCWM/60000660`

```
T-Code: /SCWM/60000660
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TSATIND
```

### 3191. `/SCWM/60000664`

```
T-Code: /SCWM/60000664
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TALITYPE_TP
```

### 3192. `/SCWM/60000665`

```
T-Code: /SCWM/60000665
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TALITYPE_ST
```

### 3193. `/SCWM/60000666`

```
T-Code: /SCWM/60000666
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/THUNR
```

### 3194. `/SCWM/60000668`

```
T-Code: /SCWM/60000668
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TACTTY_IND
```

### 3195. `/SCWM/60000674`

```
T-Code: /SCWM/60000674
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TAPOSYS
```

### 3196. `/SCWM/60000692`

```
T-Code: /SCWM/60000692
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_QIE_DMOD_CRIT, V_QIE_DMOD_CRITP
```

### 3197. `/SCWM/60000693`

```
T-Code: /SCWM/60000693
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → QIE_DMOD_RULE, V_QIE_INSP_STAG
```

### 3198. `/SCWM/69000005`

```
T-Code: /SCWM/69000005
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_SL
```

### 3199. `/SCWM/69000006`

```
T-Code: /SCWM/69000006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_P
```

### 3200. `/SCWM/69000007`

```
T-Code: /SCWM/69000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VITYPE_P
```

### 3201. `/SCWM/69000008`

```
T-Code: /SCWM/69000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMAPMFGTYP
```

### 3202. `/SCWM/69000009`

```
T-Code: /SCWM/69000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMAPMFGITM
```

### 3203. `/SCWM/69000010`

```
T-Code: /SCWM/69000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDLVBATCH_P
```

### 3204. `/SCWM/69000021`

```
T-Code: /SCWM/69000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340_MFG
```

### 3205. `/SCWM/69000028`

```
T-Code: /SCWM/69000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSAISL
```

### 3206. `/SCWM/69000029`

```
T-Code: /SCWM/69000029
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T307_BD
```

### 3207. `/SCWM/69000030`

```
T-Code: /SCWM/69000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSCPGR
```

### 3208. `/SCWM/69000033`

```
T-Code: /SCWM/69000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T303M
```

### 3209. `/SCWM/73000001`

```
T-Code: /SCWM/73000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_VASBDET
```

### 3210. `/SCWM/73000010`

```
T-Code: /SCWM/73000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMAPPDT
```

### 3211. `/SCWM/82000001`

```
T-Code: /SCWM/82000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRFILTER
```

### 3212. `/SCWM/82000002`

```
T-Code: /SCWM/82000002
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRFILTER
```

### 3213. `/SCWM/82000003`

```
T-Code: /SCWM/82000003
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRLIMIT
```

### 3214. `/SCWM/82000004`

```
T-Code: /SCWM/82000004
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSSEQ
```

### 3215. `/SCWM/82000007`

```
T-Code: /SCWM/82000007
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TIDART
```

### 3216. `/SCWM/82000009`

```
T-Code: /SCWM/82000009
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334R
```

### 3217. `/SCWM/82000010`

```
T-Code: /SCWM/82000010
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334R
```

### 3218. `/SCWM/82000011`

```
T-Code: /SCWM/82000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334RSD
```

### 3219. `/SCWM/82000012`

```
T-Code: /SCWM/82000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334RS
```

### 3220. `/SCWM/82000013`

```
T-Code: /SCWM/82000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334RRD
```

### 3221. `/SCWM/82000014`

```
T-Code: /SCWM/82000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334RR
```

### 3222. `/SCWM/82000016`

```
T-Code: /SCWM/82000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPROCTYG
```

### 3223. `/SCWM/82000017`

```
T-Code: /SCWM/82000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCATG
```

### 3224. `/SCWM/82000021`

```
T-Code: /SCWM/82000021
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T305R
```

### 3225. `/SCWM/82000022`

```
T-Code: /SCWM/82000022
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T301G
```

### 3226. `/SCWM/82000023`

```
T-Code: /SCWM/82000023
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T301M
```

### 3227. `/SCWM/82000027`

```
T-Code: /SCWM/82000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T333
```

### 3228. `/SCWM/82000038`

```
T-Code: /SCWM/82000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STCKDTGR
```

### 3229. `/SCWM/82000039`

```
T-Code: /SCWM/82000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_STCKDT
```

### 3230. `/SCWM/82000040`

```
T-Code: /SCWM/82000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TACTTY
```

### 3231. `/SCWM/82000072`

```
T-Code: /SCWM/82000072
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRFILTER, /SCWM/TWCRLIMIT, /SCWM/V_TWCRFILT, /SCWM/V_TWCRPACK, /SCWM/V_TWCRPP, /SCWM/V_TWCRSORT, /SCWM/V_TWCRSRTO, /SCWM/V_TWCR_SUB
```

### 3232. `/SCWM/82000073`

```
T-Code: /SCWM/82000073
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPRCES
```

### 3233. `/SCWM/82000079`

```
T-Code: /SCWM/82000079
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WORKST
```

### 3234. `/SCWM/82000128`

```
T-Code: /SCWM/82000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T334Q
```

### 3235. `/SCWM/82000129`

```
T-Code: /SCWM/82000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T334QA
```

### 3236. `/SCWM/ABC`

```
T-Code: /SCWM/ABC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → AB
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3237. `/SCWM/ABC_SCR2`

```
T-Code: /SCWM/ABC_SCR2
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → AB
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3238. `/SCWM/ACC_IMP_ERP`

```
T-Code: /SCWM/ACC_IMP_ERP
├── /SCWM/AIMP
    ├── /SCWM/ACAT → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    └── ACTVT → 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3, F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/ACC
    └── ALG_SUBOBJ → /SCWM/ACC_ERP
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SCWM/RACC_IMP_ERP_UI
```

### 3239. `/SCWM/ACTIVE_LA`

```
T-Code: /SCWM/ACTIVE_LA
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LA_ACTIV
```

### 3240. `/SCWM/ACTLOG`

```
T-Code: /SCWM/ACTLOG
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/LOG_ACT, /SCWM/V_LOG_ACT
```

### 3241. `/SCWM/ADGI`

```
T-Code: /SCWM/ADGI
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → G1, G2, G3, G4, G5
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3, F4
├── F_LFA1_GRP
    ├── ACTVT → 3, F4
    └── KTOKK → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 1, 2, 3, 6, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T001L, T320, T340D
```

### 3242. `/SCWM/ADHOC`

```
T-Code: /SCWM/ADHOC
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, PWR, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3243. `/SCWM/ADHU`

```
T-Code: /SCWM/ADHU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, PWR, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── QIE_ELEM
    ├── ACTVT → 1, 2, 3, 6
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3244. `/SCWM/ADPROD`

```
T-Code: /SCWM/ADPROD
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, PWR, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 6
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── QIE_ELEM
    ├── ACTVT → 1, 2, 3, 6
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1
    ├── QIE_OBJTYP → PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3245. `/SCWM/ARC_DOC`

```
T-Code: /SCWM/ARC_DOC
└── /SCWM/DLV2
    ├── /SCDL/CAT → FDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3246. `/SCWM/BINMAT`

```
T-Code: /SCWM/BINMAT
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_FIXBIN
```

### 3247. `/SCWM/BSV`

```
T-Code: /SCWM/BSV
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3248. `/SCWM/BW01`

```
T-Code: /SCWM/BW01
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3249. `/SCWM/BW02`

```
T-Code: /SCWM/BW02
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3250. `/SCWM/BW03`

```
T-Code: /SCWM/BW03
└── S_ADMI_FCD
    └── S_ADMI_FCD → NADM
```

### 3251. `/SCWM/BW04`

```
T-Code: /SCWM/BW04
├── S_RO_OSOA
    ├── ACTVT → 23, 3
    ├── OLTPSOURCE → (empty — maintain in PFCG)
    ├── OSOAAPCO → (empty — maintain in PFCG)
    └── OSOAPART → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 3
    └── TTYPE → PIEC
```

### 3252. `/SCWM/BW05`

```
T-Code: /SCWM/BW05
├── S_RO_OSOA
    ├── ACTVT → 23, 3
    ├── OLTPSOURCE → (empty — maintain in PFCG)
    ├── OSOAAPCO → (empty — maintain in PFCG)
    └── OSOAPART → (empty — maintain in PFCG)
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 3
    └── TTYPE → ORDR, PIEC
```

### 3253. `/SCWM/BW10`

```
T-Code: /SCWM/BW10
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3254. `/SCWM/BW11`

```
T-Code: /SCWM/BW11
├── /SCWM/SFUN
    └── /SCWM/SFUN → BW
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3255. `/SCWM/BW12`

```
T-Code: /SCWM/BW12
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3256. `/SCWM/BW13`

```
T-Code: /SCWM/BW13
├── /SCWM/SFUN
    └── /SCWM/SFUN → BW
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3257. `/SCWM/BW14`

```
T-Code: /SCWM/BW14
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3258. `/SCWM/BW21`

```
T-Code: /SCWM/BW21
└── /SCWM/SFUN
    └── /SCWM/SFUN → BW
```

### 3259. `/SCWM/CANCPICK`

```
T-Code: /SCWM/CANCPICK
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 5
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → RP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → DELI, SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3260. `/SCWM/CAP`

```
T-Code: /SCWM/CAP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → HUP
    └── STSMA → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3261. `/SCWM/CAPDEL`

```
T-Code: /SCWM/CAPDEL
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 6
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3262. `/SCWM/CAPDLV`

```
T-Code: /SCWM/CAPDLV
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → DELI, SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── P_PKGBUILD
    └── ACTVT → 3
```

### 3263. `/SCWM/CAPPDET`

```
T-Code: /SCWM/CAPPDET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CAPPD
```

### 3264. `/SCWM/CAPWAVE`

```
T-Code: /SCWM/CAPWAVE
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3265. `/SCWM/CAP_PB_PKG_ASS`

```
T-Code: /SCWM/CAP_PB_PKG_ASS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CAP_PTAS
```

### 3266. `/SCWM/CCIND_MAINTAIN`

```
T-Code: /SCWM/CCIND_MAINTAIN
└── B_BUPA_RLT
    ├── ACTVT → 1, 2
    └── RLTYP → (empty — maintain in PFCG)
```

### 3267. `/SCWM/CDSTDET`

```
T-Code: /SCWM/CDSTDET
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_CDSTDET
```

### 3268. `/SCWM/CHM_LOG`

```
T-Code: /SCWM/CHM_LOG
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, PWR, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3269. `/SCWM/CHM_PRF`

```
T-Code: /SCWM/CHM_PRF
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/CHM_FRW
    └── ALG_SUBOBJ → CORRECTION_ACTION
```

### 3270. `/SCWM/CHNUM`

```
T-Code: /SCWM/CHNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/CHAR
```

### 3271. `/SCWM/CICO`

```
T-Code: /SCWM/CICO
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3272. `/SCWM/CLC_WIZARD`

```
T-Code: /SCWM/CLC_WIZARD
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 16, 2
├── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 2
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
└── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
```

### 3273. `/SCWM/CLC_WIZARD_CUS`

```
T-Code: /SCWM/CLC_WIZARD_CUS
└── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 16, 2, 6
```

### 3274. `/SCWM/CONV_BSKEY`

```
T-Code: /SCWM/CONV_BSKEY
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TBUSSYS, /SCMB/TOWNBS, /SCWM/V_SDESTDEF
```

### 3275. `/SCWM/CORR`

```
T-Code: /SCWM/CORR
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 3276. `/SCWM/CREATE_SUBBINS`

```
T-Code: /SCWM/CREATE_SUBBINS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
└── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
```

### 3277. `/SCWM/CTCTC1`

```
T-Code: /SCWM/CTCTC1
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3278. `/SCWM/CTCTC2`

```
T-Code: /SCWM/CTCTC2
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3279. `/SCWM/CTCTC3`

```
T-Code: /SCWM/CTCTC3
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3280. `/SCWM/CTFCC`

```
T-Code: /SCWM/CTFCC
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3281. `/SCWM/CT_ACC_SEQC1`

```
T-Code: /SCWM/CT_ACC_SEQC1
└── /SAPCND/CC
    ├── /SAPCND/AP → CON
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → PU
    └── ACTVT → 2
```

### 3282. `/SCWM/CT_ACC_SEQC2`

```
T-Code: /SCWM/CT_ACC_SEQC2
└── /SAPCND/CC
    ├── /SAPCND/AP → CON
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → BT
    └── ACTVT → 2
```

### 3283. `/SCWM/CT_ACC_SEQC3`

```
T-Code: /SCWM/CT_ACC_SEQC3
└── /SAPCND/CC
    ├── /SAPCND/AP → CON
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → SC
    └── ACTVT → 2
```

### 3284. `/SCWM/CT_DET_PROC1`

```
T-Code: /SCWM/CT_DET_PROC1
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3285. `/SCWM/CT_DET_PROC2`

```
T-Code: /SCWM/CT_DET_PROC2
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3286. `/SCWM/CT_DET_PROC3`

```
T-Code: /SCWM/CT_DET_PROC3
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3287. `/SCWM/CUST_DOOR_PPF1`

```
T-Code: /SCWM/CUST_DOOR_PPF1
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3288. `/SCWM/CUST_DOOR_PPF2`

```
T-Code: /SCWM/CUST_DOOR_PPF2
├── S_CTS_ADMI
    └── CTS_ADMFCT → 2
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3289. `/SCWM/CUST_TU_PPF1`

```
T-Code: /SCWM/CUST_TU_PPF1
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3290. `/SCWM/CUST_TU_PPF2`

```
T-Code: /SCWM/CUST_TU_PPF2
├── S_CTS_ADMI
    └── CTS_ADMFCT → 2
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3291. `/SCWM/CUST_VEH_PPF1`

```
T-Code: /SCWM/CUST_VEH_PPF1
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3292. `/SCWM/CUST_VEH_PPF2`

```
T-Code: /SCWM/CUST_VEH_PPF2
└── S_PPF_CONF
    └── ACTVT → 3
```

### 3293. `/SCWM/DCONS`

```
T-Code: /SCWM/DCONS
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → HU
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3, 6
    └── DOKAR → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3294. `/SCWM/DELETE_SUBBINS`

```
T-Code: /SCWM/DELETE_SUBBINS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_LOC
    ├── ACTVT → 10, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
```

### 3295. `/SCWM/DIFFERENCE_POST_RFC`

```
T-Code: /SCWM/DIFFERENCE_POST_RFC
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
└── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
```

### 3296. `/SCWM/DIFF_ANALYZER`

```
T-Code: /SCWM/DIFF_ANALYZER
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → DA
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3, F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 22, 23
├── C_TCLS_BER
    ├── KLART → 22
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3297. `/SCWM/DIFF_HUITM_CHANGE`

```
T-Code: /SCWM/DIFF_HUITM_CHANGE
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
└── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
```

### 3298. `/SCWM/DLVCONFCHECK`

```
T-Code: /SCWM/DLVCONFCHECK
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE, /SCWM/VITYPE
```

### 3299. `/SCWM/DLVDTYPE_EM_CL`

```
T-Code: /SCWM/DLVDTYPE_EM_CL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_EM
```

### 3300. `/SCWM/DLVPPFC`

```
T-Code: /SCWM/DLVPPFC
├── /SAPCND/CM
    ├── /SAPCND/AP → DPP
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → DP
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3301. `/SCWM/DLVPPFC1`

```
T-Code: /SCWM/DLVPPFC1
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3302. `/SCWM/DLVPPFC2`

```
T-Code: /SCWM/DLVPPFC2
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3303. `/SCWM/DLVPPFC3`

```
T-Code: /SCWM/DLVPPFC3
└── /SAPCND/CC
    ├── /SAPCND/AP → DPP
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → DP
    └── ACTVT → 2
```

### 3304. `/SCWM/DLVPPFC4`

```
T-Code: /SCWM/DLVPPFC4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3305. `/SCWM/DLVPPFC5`

```
T-Code: /SCWM/DLVPPFC5
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3306. `/SCWM/DLVPPFC6`

```
T-Code: /SCWM/DLVPPFC6
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CNT_GR
```

### 3307. `/SCWM/DLVPPFLOG`

```
T-Code: /SCWM/DLVPPFLOG
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TUSPPFLOG, /SCWM/TWHPPFLOG
```

### 3308. `/SCWM/DLV_ASYN`

```
T-Code: /SCWM/DLV_ASYN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DLV_ASYN
```

### 3309. `/SCWM/DLV_CONFIRM_QRFC`

```
T-Code: /SCWM/DLV_CONFIRM_QRFC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
```

### 3310. `/SCWM/DLV_CREATE_HU`

```
T-Code: /SCWM/DLV_CREATE_HU
└── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
```

### 3311. `/SCWM/DLV_DTYPE_EM`

```
T-Code: /SCWM/DLV_DTYPE_EM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDTYPE_EM
```

### 3312. `/SCWM/DLV_PRDETIN`

```
T-Code: /SCWM/DLV_PRDETIN
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRDETIN, /SCWM/V_SPRDETIN
```

### 3313. `/SCWM/DLV_PRDETOUT`

```
T-Code: /SCWM/DLV_PRDETOUT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRDETOUT, /SCWM/V_SPRDETOU
```

### 3314. `/SCWM/DOOR`

```
T-Code: /SCWM/DOOR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
```

### 3315. `/SCWM/DOOR_SCU`

```
T-Code: /SCWM/DOOR_SCU
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TDOOR_MD_V
```

### 3316. `/SCWM/DSAP`

```
T-Code: /SCWM/DSAP
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/DSAP
```

### 3317. `/SCWM/DSCONFDUR`

```
T-Code: /SCWM/DSCONFDUR
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDSDURPACK
```

### 3318. `/SCWM/DSDETLP`

```
T-Code: /SCWM/DSDETLP
├── /SCWM/DSLP
    └── ACTVT → 3
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VDSDETLP
```

### 3319. `/SCWM/DSGR`

```
T-Code: /SCWM/DSGR
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → DS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3320. `/SCWM/DSGR_ST`

```
T-Code: /SCWM/DSGR_ST
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → DS
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3321. `/SCWM/DSINT`

```
T-Code: /SCWM/DSINT
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DSSYST
```

### 3322. `/SCWM/DSLOG`

```
T-Code: /SCWM/DSLOG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DS_LOG
```

### 3323. `/SCWM/DS_APPOINTMENT`

```
T-Code: /SCWM/DS_APPOINTMENT
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → 1, 2, 3
├── /SCWM/DSLP
    └── ACTVT → 3
└── /SCWM/DSSL
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → 3
```

### 3324. `/SCWM/DS_UPDATE_STATUS`

```
T-Code: /SCWM/DS_UPDATE_STATUS
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → 2, 3
├── /SCWM/DSLP
    └── ACTVT → 3
└── /SCWM/DSSL
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → 3
```

### 3325. `/SCWM/EGF`

```
T-Code: /SCWM/EGF
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → A
    ├── /SCWM/OBCL → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 60, 61
├── /SCWM/EGF
    ├── /SCWM/EAGR → (empty — maintain in PFCG)
    └── /SCWM/EAPP → WHS_COCKPIT
└── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → (empty — maintain in PFCG)
    └── APO_TMOD → (empty — maintain in PFCG)
```

### 3326. `/SCWM/EGF_CHART`

```
T-Code: /SCWM/EGF_CHART
├── /SCWM/EGF2
    ├── /SCWM/EAGR → (empty — maintain in PFCG)
    ├── /SCWM/EAPP → (empty — maintain in PFCG)
    └── /SCWM/EFUN → TC
└── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
```

### 3327. `/SCWM/EGF_COCKPIT`

```
T-Code: /SCWM/EGF_COCKPIT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VAEGF_CINP, /SCWM/VAEGF_COBJ, /SCWM/VAEGF_COCH, /SCWM/VAEGF_COFI, /SCWM/VAEGF_COFN, /SCWM/VAEGF_COIN, /SCWM/VAEGF_CPT
```

### 3328. `/SCWM/EGF_OBJECT`

```
T-Code: /SCWM/EGF_OBJECT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VAEGF_OBJ, /SCWM/VAEGF_OCHT, /SCWM/VAEGF_OFIN, /SCWM/VAEGF_OFNC, /SCWM/VAEGF_OINP
```

### 3329. `/SCWM/EGF_TPL_TRANS`

```
T-Code: /SCWM/EGF_TPL_TRANS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCEGF_TEMP, /SCWM/VEGF_TEMPL
```

### 3330. `/SCWM/EGR`

```
T-Code: /SCWM/EGR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, GRN
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3331. `/SCWM/ELS`

```
T-Code: /SCWM/ELS
└── /SCWM/ELS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
```

### 3332. `/SCWM/ELS_UPLOAD`

```
T-Code: /SCWM/ELS_UPLOAD
└── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → /SCWM/ELS_UPLOAD
```

### 3333. `/SCWM/EPD_TRANSFER`

```
T-Code: /SCWM/EPD_TRANSFER
└── /SCMB/LMPD
    ├── /SCMB/PDST → D, F
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
```

### 3334. `/SCWM/EPERF`

```
T-Code: /SCWM/EPERF
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCMB/LMPD
    ├── /SCMB/PDST → A, B, C, E, G, H
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3, F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → LM0001, LM0002, LM0003
└── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3335. `/SCWM/ERPDOCNO`

```
T-Code: /SCWM/ERPDOCNO
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/DLNO
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → NRIV
```

### 3336. `/SCWM/ERPLOG`

```
T-Code: /SCWM/ERPLOG
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VERPLOG
```

### 3337. `/SCWM/ERPSONO`

```
T-Code: /SCWM/ERPSONO
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/SONO
```

### 3338. `/SCWM/ERP_EGR_DELETE`

```
T-Code: /SCWM/ERP_EGR_DELETE
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, GRN
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3339. `/SCWM/ERP_STOCKCHECK`

```
T-Code: /SCWM/ERP_STOCKCHECK
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SA, SD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/DLV_ERP
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 3340. `/SCWM/EWL_ASYNC`

```
T-Code: /SCWM/EWL_ASYNC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VEWLASYNC, /SCWM/VEWLASYNWO
```

### 3341. `/SCWM/EWL_COMPL`

```
T-Code: /SCWM/EWL_COMPL
├── /SCWM/EWLS
    ├── /SCWM/EWLS → A
    └── /SCWM/LGNU → $/SCWM/LGNU
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → EWL
```

### 3342. `/SCWM/EWL_PREPEOS`

```
T-Code: /SCWM/EWL_PREPEOS
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/EWLS
    ├── /SCWM/EWLS → C
    └── /SCWM/LGNU → $/SCWM/LGNU
├── /SCWM/ILT
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROC → (empty — maintain in PFCG)
    └── ACTVT → 1
├── /SCWM/TATT
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → LM0001, LM0002, LM0003
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → EWL
```

### 3343. `/SCWM/EWL_REDET`

```
T-Code: /SCWM/EWL_REDET
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/EWLS
    ├── /SCWM/EWLS → B
    └── /SCWM/LGNU → $/SCWM/LGNU
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → EWL
└── S_BDS_DS
    ├── ACTVT → 3
    ├── CLASSNAME → (empty — maintain in PFCG)
    └── CLASSTYPE → (empty — maintain in PFCG)
```

### 3344. `/SCWM/EXCODE_TEST`

```
T-Code: /SCWM/EXCODE_TEST
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3345. `/SCWM/EXCUSERID`

```
T-Code: /SCWM/EXCUSERID
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TEXCPRF, /SCWM/V_TEXCPRFD
```

### 3346. `/SCWM/EXECPR`

```
T-Code: /SCWM/EXECPR
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_LIN
    ├── ACTVT → 2, 3
    ├── ORG_CRIT → /SCWM/LGNUM
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VV_RSRCT, /SCWM/V_BINAT_PR, /SCWM/V_HUGRP_PR, /SCWM/V_T333, /SCWM/V_WO_RS_PR
```

### 3347. `/SCWM/FBINASN`

```
T-Code: /SCWM/FBINASN
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3348. `/SCWM/FBINDEL`

```
T-Code: /SCWM/FBINDEL
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3349. `/SCWM/FD`

```
T-Code: /SCWM/FD
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → R1, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── M_BEST_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 3350. `/SCWM/FLEXBINAREA`

```
T-Code: /SCWM/FLEXBINAREA
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DYBI_ARB, /SCWM/V_DYBI_ARE, /SCWM/V_T331_DYB
```

### 3351. `/SCWM/GCMC`

```
T-Code: /SCWM/GCMC
├── /SAPCND/CM
    ├── /SAPCND/AP → CON
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3352. `/SCWM/GENA`

```
T-Code: /SCWM/GENA
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3353. `/SCWM/GMBIN_DET`

```
T-Code: /SCWM/GMBIN_DET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TGMBIN_DTV
```

### 3354. `/SCWM/GMNUM`

```
T-Code: /SCWM/GMNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/TO
```

### 3355. `/SCWM/GR`

```
T-Code: /SCWM/GR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, IDR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 92
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3356. `/SCWM/GRN`

```
T-Code: /SCWM/GRN
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, GRN
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3357. `/SCWM/GRPE`

```
T-Code: /SCWM/GRPE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, GRN, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/DSLP
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3
    ├── QIE_OBJTYP → PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3358. `/SCWM/GRPI`

```
T-Code: /SCWM/GRPI
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6, 92
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/DSLP
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → INC, PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → INC, PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── QIE_ELEM
    ├── ACTVT → 1, 2, 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
```

### 3359. `/SCWM/GRWORK`

```
T-Code: /SCWM/GRWORK
├── /SCWM/DLV2
    ├── /SCDL/CAT → EGR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → F4
└── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
```

### 3360. `/SCWM/GTS_MAPWH`

```
T-Code: /SCWM/GTS_MAPWH
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GTSMAPWH
```

### 3361. `/SCWM/GWL`

```
T-Code: /SCWM/GWL
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3362. `/SCWM/GWLOBJ`

```
T-Code: /SCWM/GWLOBJ
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_GWLOBJ
```

### 3363. `/SCWM/HUID`

```
T-Code: /SCWM/HUID
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/HUID
```

### 3364. `/SCWM/HU_TRACK`

```
T-Code: /SCWM/HU_TRACK
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/HUHS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3365. `/SCWM/HU_TRACK_DEL`

```
T-Code: /SCWM/HU_TRACK_DEL
├── /SCWM/HUHS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3366. `/SCWM/IBGI`

```
T-Code: /SCWM/IBGI
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TIBGI
```

### 3367. `/SCWM/IDN`

```
T-Code: /SCWM/IDN
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── QIE_ELEM
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3368. `/SCWM/IGBI`

```
T-Code: /SCWM/IGBI
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → SCE
└── S_TABU_LIN
    ├── ACTVT → 2, 3
    ├── ORG_CRIT → /SCWM/LGNUM
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
```

### 3369. `/SCWM/ILHS`

```
T-Code: /SCWM/ILHS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── HSMA
    ├── ACTVT → 3
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3370. `/SCWM/ILT`

```
T-Code: /SCWM/ILT
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3371. `/SCWM/IMMED_LOAD`

```
T-Code: /SCWM/IMMED_LOAD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TIMMD_LOAD, /SCWM/VIMMD_LOAD, /SCWM/V_T3001_CU
```

### 3372. `/SCWM/IMMED_LOADC`

```
T-Code: /SCWM/IMMED_LOADC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TIMMDLOADC, /SCWM/VIMMDLOADC
```

### 3373. `/SCWM/IM_DR`

```
T-Code: /SCWM/IM_DR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → POR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 8
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── QIE_ELEM
    ├── ACTVT → 2, 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
```

### 3374. `/SCWM/IM_PC`

```
T-Code: /SCWM/IM_PC
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → POR, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3, F4
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── QIE_ELEM
    ├── ACTVT → 2, 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3375. `/SCWM/IM_ST`

```
T-Code: /SCWM/IM_ST
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3376. `/SCWM/INB_DLV_SAVEREPLICA`

```
T-Code: /SCWM/INB_DLV_SAVEREPLICA
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 23, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 1
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 22, 23
├── C_TCLS_BER
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── QIE_IRULE
    ├── ACTVT → 1
    ├── QIE_OBJTYP → PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T377, T377P, T377P_T
└── V_LIKP_VST
    ├── ACTVT → 1, 18, 3
    └── VSTEL → $VSTEL
```

### 3377. `/SCWM/INB_DLV_SAVEREP_YYY`

```
T-Code: /SCWM/INB_DLV_SAVEREP_YYY
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/DLV_ERP
    └── ALG_SUBOBJ → REPL
```

### 3378. `/SCWM/INDLNO`

```
T-Code: /SCWM/INDLNO
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/ILT
```

### 3379. `/SCWM/IPU`

```
T-Code: /SCWM/IPU
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 61, 7
```

### 3380. `/SCWM/ISU`

```
T-Code: /SCWM/ISU
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → IS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 1
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3381. `/SCWM/JIT_DOCMAP`

```
T-Code: /SCWM/JIT_DOCMAP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_DOCM
```

### 3382. `/SCWM/JIT_WPTDET`

```
T-Code: /SCWM/JIT_WPTDET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VJIT_WPTDT
```

### 3383. `/SCWM/JIT_WPTD_A`

```
T-Code: /SCWM/JIT_WPTD_A
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VJIT_WPT_A
```

### 3384. `/SCWM/LDP_VIEW_CUST`

```
T-Code: /SCWM/LDP_VIEW_CUST
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3385. `/SCWM/LGNBP`

```
T-Code: /SCWM/LGNBP
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
└── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3386. `/SCWM/LGNUM_COPY`

```
T-Code: /SCWM/LGNUM_COPY
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3387. `/SCWM/LM_CE`

```
T-Code: /SCWM/LM_CE
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3388. `/SCWM/LM_FE`

```
T-Code: /SCWM/LM_FE
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3389. `/SCWM/LOAD`

```
T-Code: /SCWM/LOAD
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → (empty — maintain in PFCG)
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── QIE_IRULE
    ├── ACTVT → 1, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3390. `/SCWM/LS01`

```
T-Code: /SCWM/LS01
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/LAGP, /SCWM/T302, /SCWM/T309, /SCWM/T337A, /SCWM/V_T331SC1
```

### 3391. `/SCWM/LS02`

```
T-Code: /SCWM/LS02
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_FIXBIN
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/T302
```

### 3392. `/SCWM/LS03`

```
T-Code: /SCWM/LS03
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── SDDLVIEW
    ├── ACTVT → 3
    ├── DDLNAME → (empty — maintain in PFCG)
    └── DDLSRCNAME → ESH_S_EWM_SBIN_2
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_FIXBIN
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/LAGP, /SCWM/T301, /SCWM/V_T3010
```

### 3393. `/SCWM/LS10`

```
T-Code: /SCWM/LS10
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T343XYZM
```

### 3394. `/SCWM/LS11`

```
T-Code: /SCWM/LS11
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/LAGP, /SCWM/T301, /SCWM/V_T3010
```

### 3395. `/SCWM/LTRC_ACT`

```
T-Code: /SCWM/LTRC_ACT
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_LTRC_ACT
```

### 3396. `/SCWM/LX45`

```
T-Code: /SCWM/LX45
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3397. `/SCWM/MAPRCLOC`

```
T-Code: /SCWM/MAPRCLOC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_LIN
    ├── ACTVT → 2, 3
    ├── ORG_CRIT → (empty — maintain in PFCG)
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
├── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TMAPRCLOC, /SCWM/V_TMAPRCL
└── W_BETR_USR
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 3398. `/SCWM/MAPSORG`

```
T-Code: /SCWM/MAPSORG
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TMAPSORG_V
```

### 3399. `/SCWM/MAT1`

```
T-Code: /SCWM/MAT1
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 1, 16, 2, 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RELH
    └── ACTVT → 1, 2, 3
├── C_APO_VERS
    ├── ACTVT → 1, 16, 2, 3, 6
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── SDDLVIEW
    ├── ACTVT → 3
    ├── DDLNAME → (empty — maintain in PFCG)
    └── DDLSRCNAME → ESH_S_EWM_PROD_2
```

### 3400. `/SCWM/MBINPSA`

```
T-Code: /SCWM/MBINPSA
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → (empty — maintain in PFCG)
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → (empty — maintain in PFCG)
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TMBINPSA, /SCWM/V_TMBINPSA
```

### 3401. `/SCWM/MDR_MSG_RESEND`

```
T-Code: /SCWM/MDR_MSG_RESEND
└── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → RR
```

### 3402. `/SCWM/MEDI_AQTY`

```
T-Code: /SCWM/MEDI_AQTY
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → MEDI
```

### 3403. `/SCWM/MES_PLANTS`

```
T-Code: /SCWM/MES_PLANTS
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMESPLNTS
```

### 3404. `/SCWM/MES_STAGE`

```
T-Code: /SCWM/MES_STAGE
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → MFG_STAGE
```

### 3405. `/SCWM/MFS_AKTION`

```
T-Code: /SCWM/MFS_AKTION
└── S_TABU_NAM
    ├── ACTVT → 2
    └── TABLE → /SCWM/V_TMFSACTA
```

### 3406. `/SCWM/MFS_APPSRV`

```
T-Code: /SCWM/MFS_APPSRV
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_T340MFSA
```

### 3407. `/SCWM/MFS_CCH`

```
T-Code: /SCWM/MFS_CCH
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_MFSCCH_A
```

### 3408. `/SCWM/MFS_CP`

```
T-Code: /SCWM/MFS_CP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_MFSCP
```

### 3409. `/SCWM/MFS_DEL_LOG`

```
T-Code: /SCWM/MFS_DEL_LOG
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3410. `/SCWM/MFS_GEN_APPDAT`

```
T-Code: /SCWM/MFS_GEN_APPDAT
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3411. `/SCWM/MFS_GEN_PLCOBJ`

```
T-Code: /SCWM/MFS_GEN_PLCOBJ
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2
├── /SCWM/MFS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PLC → CONSYS1, RACK1, RACK2, RACK3, RACK4
    └── ACTVT → 2
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3412. `/SCWM/MFS_OBJMAP`

```
T-Code: /SCWM/MFS_OBJMAP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_MFSOBMAP
```

### 3413. `/SCWM/MFS_PLC`

```
T-Code: /SCWM/MFS_PLC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TMFSPLCA
```

### 3414. `/SCWM/MFS_RSRC`

```
T-Code: /SCWM/MFS_RSRC
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3415. `/SCWM/MFS_RSRC_AISLE`

```
T-Code: /SCWM/MFS_RSRC_AISLE
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_MFSRSRCA
```

### 3416. `/SCWM/MIG_BIN`

```
T-Code: /SCWM/MIG_BIN
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 1
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3417. `/SCWM/MIG_CROSS`

```
T-Code: /SCWM/MIG_CROSS
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3418. `/SCWM/MIG_CUST_DEL`

```
T-Code: /SCWM/MIG_CUST_DEL
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 8
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3419. `/SCWM/MIG_MAP_ALTUOM`

```
T-Code: /SCWM/MIG_MAP_ALTUOM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MIGALTUO
```

### 3420. `/SCWM/MIG_MAP_SUT`

```
T-Code: /SCWM/MIG_MAP_SUT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/DMIGMAPSUT
```

### 3421. `/SCWM/MIG_PI_COMPL`

```
T-Code: /SCWM/MIG_PI_COMPL
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 2
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3422. `/SCWM/MIG_PRODUCT`

```
T-Code: /SCWM/MIG_PRODUCT
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_APO_PROD
    ├── ACTVT → 1, 2
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 3423. `/SCWM/MIG_STOCK`

```
T-Code: /SCWM/MIG_STOCK
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3, 8
    └── TABLE → /SCWM/VTMAPPLANT
```

### 3424. `/SCWM/MIG_STRATEGIES`

```
T-Code: /SCWM/MIG_STRATEGIES
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 7
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3425. `/SCWM/MIG_WH_LAYOUT`

```
T-Code: /SCWM/MIG_WH_LAYOUT
├── /SCWM/MIG
    ├── /SCWM/BS → (empty — maintain in PFCG)
    └── /SCWM/MIG → 5
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3426. `/SCWM/MON`

```
T-Code: /SCWM/MON
├── /SAPCND/CM
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCMB/WDCP
    ├── /SCMB/LGNU → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
├── /SCMB/WDEF
    ├── /SCMB/LGNU → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → /SCWM/ID, /SCWM/ODO, /SCWM/TUA, HU
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → B, C
    ├── /SCWM/OBCL → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 60, 61
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, PWR, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3, 8
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/HUHS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/ILT
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROC → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/LDPV
    ├── /SCWM/LDPV → (empty — maintain in PFCG)
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3
├── /SCWM/MFS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PLC → (empty — maintain in PFCG)
    └── ACTVT → 16, 2
├── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 1
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2, 3, 4
    └── ACTVT → 3, 4
├── /SCWM/PAS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6, 8
├── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 8
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/QFID
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/RSRC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/RSTY → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → CS, DA, MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TATT
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → READ
    └── APO_TMOD → (empty — maintain in PFCG)
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 43
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── /SPE/AAC
    ├── /SPE/AAC → 10, 11, 12
    └── ACTVT → 16
├── B_BUPA_RLT
    ├── ACTVT → 1, 2, 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_DEF
    ├── ACTVT → 1, 2, 3
    ├── APO_DEFN → (empty — maintain in PFCG)
    ├── APO_DEFT → (empty — maintain in PFCG)
    └── APO_PLNR → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 1, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_RES
    ├── ACTVT → 2
    ├── APO_LOC → (empty — maintain in PFCG)
    ├── APO_PLNR → (empty — maintain in PFCG)
    └── APO_RES → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3, 6
    └── DOKAR → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── JIT_PGD
    └── ACTVT → 3
├── JIT_S2C_CR
    ├── ACTVT → 3
    ├── NJIT_ACT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    ├── VTWEG → $VTWEG
    └── WERKS → $WERKS
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── Q_INSPLOT
    ├── QACT_ILOT → 10, 13, 3, F4
    ├── QMATAUTH → (empty — maintain in PFCG)
    ├── QPRUEFART → (empty — maintain in PFCG)
    └── WERKS → $WERKS
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/DLV_ERP, /SCWM/PACKSPEC, /SCWM/WME, /SPE/QUEUE, MATU, PPF, QM_TASK
    └── ALG_SUBOBJ → /SCWM/PWR, ABC, CAP, DATALOAD, DETERMINATION, DLV_GM_PP_SCHED, DLV_INBOUND_PRD, DLV_INTERNAL_MOVE, DLV_ORD_RED, DLV_OUTBOUND_PRD, DLV_OUTB_ODO_SPLIT, DLV_POSTING_CHANGE, GI_SCRAP, LDP, LOG_GENERAL, MFG_STAGE, MFS, MFS_RBD, PI, PMR, PROCESSING, QDEFECT_FUP, QINSP, QINSP_FIN_S4, QM_TASK_FOA, QUEUE_ALERTING, QUEUE_ERROR, REPL, REPLENISHMENT, ROUNDING_RESIDUAL, WAVE_REL, WHOEXCLOG, WHOLOG, WHRCNF, WHS_TASK, WIP_DEL, WI_WRITE, WORKCENTER, WZ_WHO_DETR
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SAPAPO/CD_RES, /SCWM/CD_FIXBIN
├── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /LIME/PI_DOCTYPE, /LIME/PI_DOC_IT, /LIME/PI_LOGHEAD, /LIME/PI_LOGITEM, /LIME/PI_PAR_BIZ, /SCWM/AQUA, /SCWM/D_BMR_ROOT, /SCWM/PI_PPF, /SCWM/T333, /SCWM/T334Q, /SCWM/T334QSD, /SCWM/TLMOBJTY, /SCWM/TSHIFTLEAD, /SCWM/TWF_PI, /SCWM/V_T334QS, APOC_I_OBJ_TYPET, APOC_I_OUTP_TYPE, APOC_I_OUTP_TYPT, APOC_I_OUTP_TYPV, T000, T001L, T320
├── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
└── W_BETR_USR
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 3427. `/SCWM/MONNAV`

```
T-Code: /SCWM/MONNAV
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3428. `/SCWM/MONNAV_AVSTOCK`

```
T-Code: /SCWM/MONNAV_AVSTOCK
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → AVSTO, PHYSTO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3429. `/SCWM/MONNAV_BIN`

```
T-Code: /SCWM/MONNAV_BIN
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PWR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → BIN
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── SDDLVIEW
    ├── ACTVT → 3
    ├── DDLNAME → (empty — maintain in PFCG)
    └── DDLSRCNAME → ESH_S_EWM_SBIN_2
```

### 3430. `/SCWM/MONNAV_BIN_SRT`

```
T-Code: /SCWM/MONNAV_BIN_SRT
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → BINSRT
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3431. `/SCWM/MONNAV_CR_HUWT`

```
T-Code: /SCWM/MONNAV_CR_HUWT
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → HU, PHYSTO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
```

### 3432. `/SCWM/MONNAV_CR_PC`

```
T-Code: /SCWM/MONNAV_CR_PC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PHYSTO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3433. `/SCWM/MONNAV_CR_P_WT`

```
T-Code: /SCWM/MONNAV_CR_P_WT
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → AVSTO, PHYSTO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLA_BKA
    └── KLART → 23
```

### 3434. `/SCWM/MONNAV_DOC_HU`

```
T-Code: /SCWM/MONNAV_DOC_HU
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PHYSTO
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3435. `/SCWM/MONNAV_FBINASN`

```
T-Code: /SCWM/MONNAV_FBINASN
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → FBOV, FIXBIN
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
```

### 3436. `/SCWM/MONNAV_HU`

```
T-Code: /SCWM/MONNAV_HU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → HU, HUIDT, HUPSTO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3437. `/SCWM/MONNAV_INB_HU`

```
T-Code: /SCWM/MONNAV_INB_HU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → /SCWM/ID, /SCWM/ODO, /SCWM/TUA, HU
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO, WO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → HU, HUIDT, WHRI
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 3
    └── DOKAR → SHU
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── F_KNA1_BED
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
```

### 3438. `/SCWM/MONNAV_INB_WT`

```
T-Code: /SCWM/MONNAV_INB_WT
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO, WO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO, WHRI, WHRITI
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHS_TASK
```

### 3439. `/SCWM/MONNAV_OUTB_HU`

```
T-Code: /SCWM/MONNAV_OUTB_HU
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → /SCWM/ID, /SCWM/ODO, /SCWM/TUA, HU
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO, WO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → HU, HUIDT, WHRO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SPE/AAC
    ├── /SPE/AAC → (empty — maintain in PFCG)
    └── ACTVT → 16
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3
    └── DOKAR → SHU
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → PPF
    └── ALG_SUBOBJ → DETERMINATION
```

### 3440. `/SCWM/MONNAV_OUTB_WT`

```
T-Code: /SCWM/MONNAV_OUTB_WT
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO, WO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO, WHRITO, WHRO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHS_TASK
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T300, T300T
```

### 3441. `/SCWM/MONNAV_PHSTOCK`

```
T-Code: /SCWM/MONNAV_PHSTOCK
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PHYSTO
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3442. `/SCWM/MONNAV_PI_CONF`

```
T-Code: /SCWM/MONNAV_PI_CONF
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PIRSLT
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 3
    └── ACTVT → 4
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLA_BKA
    └── KLART → 23
```

### 3443. `/SCWM/MONNAV_PMR`

```
T-Code: /SCWM/MONNAV_PMR
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → (empty — maintain in PFCG)
    ├── /SCWM/OBCL → (empty — maintain in PFCG)
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PWR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO, WHPCR, WHPCRI, WO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHOLOG, WHS_TASK
```

### 3444. `/SCWM/MONNAV_PROC_PC`

```
T-Code: /SCWM/MONNAV_PROC_PC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO, WHRPCI
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_TCLA_BKA
    └── KLART → 23
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHS_TASK
```

### 3445. `/SCWM/MONNAV_PRODUCT`

```
T-Code: /SCWM/MONNAV_PRODUCT
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PROST, PROWH
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 1, 2, 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
└── SDDLVIEW
    ├── ACTVT → 3
    ├── DDLNAME → (empty — maintain in PFCG)
    └── DDLSRCNAME → ESH_S_EWM_PROD_2
```

### 3446. `/SCWM/MONNAV_RES`

```
T-Code: /SCWM/MONNAV_RES
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → BIN
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/RSRC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/RSTY → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3447. `/SCWM/MONNAV_RES_GR`

```
T-Code: /SCWM/MONNAV_RES_GR
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → BIN
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3448. `/SCWM/MONNAV_STCKBIN`

```
T-Code: /SCWM/MONNAV_STCKBIN
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → PHYSTO
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3449. `/SCWM/MONNAV_TU_HU`

```
T-Code: /SCWM/MONNAV_TU_HU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO, WO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → HU, HUIDT, WHRO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → READ
    └── APO_TMOD → (empty — maintain in PFCG)
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
```

### 3450. `/SCWM/MONNAV_WMR`

```
T-Code: /SCWM/MONNAV_WMR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → (empty — maintain in PFCG)
    ├── /SCWM/OBCL → (empty — maintain in PFCG)
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → (empty — maintain in PFCG)
    └── /SCWM/OBCL → ALDITI, ALDITO, ALID, ALODWA, ALTO, ALWAVE, ALWO, ALWVIT, ALWVRM, APLOGD, APLOGH, AUOM01, AVSTO, BIN, CNT, CNTHU, CNTWC, CONS, COPOST, DCON, DCONCG, DCONHU, DCONWC, DDATES, DOOR, EGR, EGRI, EWL, EXCCD, FBDET, FBMUL, FBOO, FBOV, FBOW, FBOWC, FBOWW, FBWO, FIND, FIXBIN, FNDITM, FNDSAM, HU, HUCON, HUHIS, HUIDT, HUPSTO, HWL, HWLA, IDOC, ILT, INSP, INSPRO, INSPST, ITEM, KIT, LAMA_P, LOADRH, LOADRO, LOADRW, LUTIL, LUTILD, MFG, MFGGR, MFGHU, MFGHUC, MFGHUP, MFGPDI, MFSCCH, MFSCP, MFSCS, MFSCSG, MFSDY, MFSHU, MFSHUM, MFSRS, MFSTG, MFSTQ, MQUEUE, OPTPLN, PACKWA, PACKWC, PACKWH, PACKWR, PACKWS, PACWCC, PHYSTO, PICDOC, PICKAA, PICKRO, PICKWA, PICNTM, PICYC, PICYCH, PIDIFM, PIDOC, PIHIST, PISLOC, PISLOH, PISSTH, PISSTK, POD, PODWT, POHU, PROST, PROUOM, PROWH, PUTAA, PUTHU, PWL, QUEUE, ROUTE, RSRC, RSRCGR, SAMPLE, SERID, SHPRCV, SOAP, STOCK, TO, TU, TUSTA, UNLO, USER, VAS, VEHICL, VEHST, WAVAA, WAVDET, WAVE, WAVITM, WAVROU, WBBMH, WBBMI, WBMRH, WBMRI, WBPFDO, WBPPDI, WBPSTO, WBPWT, WBSNAP, WHPCR, WHPCRI, WHPCRS, WHRI, WHRITI, WHRITO, WHRO, WHRPC, WHRPCI, WHRST, WHRSTI, WO, WOHU, WOIDX, WTSN, YARD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02
```

### 3451. `/SCWM/MONNAV_WT`

```
T-Code: /SCWM/MONNAV_WT
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHS_TASK
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T001L, T320
```

### 3452. `/SCWM/MONNAV_WT_CONF`

```
T-Code: /SCWM/MONNAV_WT_CONF
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DATC
    ├── /SCWM/DCAP → C
    ├── /SCWM/OBCL → TO
    └── ACTVT → 2
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → SAP
    └── /SCWM/OBCL → TO
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, MW, SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_TCLA_BKA
    └── KLART → 23
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHS_TASK
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T001L, T320
```

### 3453. `/SCWM/MON_MAINT`

```
T-Code: /SCWM/MON_MAINT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MONITOR
```

### 3454. `/SCWM/MS_CLC_A`

```
T-Code: /SCWM/MS_CLC_A
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VMS_CLC_A
```

### 3455. `/SCWM/MS_RESULT`

```
T-Code: /SCWM/MS_RESULT
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3
├── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 16
├── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
└── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
```

### 3456. `/SCWM/MS_RESULT_DEL`

```
T-Code: /SCWM/MS_RESULT_DEL
├── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 16
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3457. `/SCWM/MS_TLR`

```
T-Code: /SCWM/MS_TLR
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_LIN
    ├── ACTVT → 3
    ├── ORG_CRIT → (empty — maintain in PFCG)
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMS_TLR_A
```

### 3458. `/SCWM/MS_TLR_A`

```
T-Code: /SCWM/MS_TLR_A
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VMS_TLR_A
```

### 3459. `/SCWM/MTR_DO`

```
T-Code: /SCWM/MTR_DO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_MTR_EX_V
```

### 3460. `/SCWM/NREPD`

```
T-Code: /SCWM/NREPD
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/EPD
```

### 3461. `/SCWM/NRTO`

```
T-Code: /SCWM/NRTO
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/TO
```

### 3462. `/SCWM/NRWRKL`

```
T-Code: /SCWM/NRWRKL
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WRKL
```

### 3463. `/SCWM/NR_MSGNR`

```
T-Code: /SCWM/NR_MSGNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/MSGN
```

### 3464. `/SCWM/NR_NODE`

```
T-Code: /SCWM/NR_NODE
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/NODE
```

### 3465. `/SCWM/NURA_WBM`

```
T-Code: /SCWM/NURA_WBM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WBBM
```

### 3466. `/SCWM/NURA_WBMI`

```
T-Code: /SCWM/NURA_WBMI
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WBRS
```

### 3467. `/SCWM/NURA_WBMR`

```
T-Code: /SCWM/NURA_WBMR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WBMR
```

### 3468. `/SCWM/OBDLV_CHNG_QUAN_MUL`

```
T-Code: /SCWM/OBDLV_CHNG_QUAN_MUL
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
```

### 3469. `/SCWM/ODR`

```
T-Code: /SCWM/ODR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → ODR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 8
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 4
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 43
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23, 230, 231, 400
├── C_TCLS_BER
    ├── KLART → 23, 230, 231, 400
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 23, 230, 231, 400
    └── SICHT → (empty — maintain in PFCG)
└── QIE_INSPDO
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3470. `/SCWM/ODUMMY`

```
T-Code: /SCWM/ODUMMY
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/DUMY
```

### 3471. `/SCWM/OUTB_DLV_CHANGE2`

```
T-Code: /SCWM/OUTB_DLV_CHANGE2
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
```

### 3472. `/SCWM/OUTB_DLV_GET_CHANGEABLE`

```
T-Code: /SCWM/OUTB_DLV_GET_CHANGEABLE
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
└── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
```

### 3473. `/SCWM/OUTB_DLV_SAVEREPLICA`

```
T-Code: /SCWM/OUTB_DLV_SAVEREPLICA
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 23, 3
├── /SPE/AAC
    ├── /SPE/AAC → 10, 11, 12
    └── ACTVT → 16
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 1
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W03
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 22, 23
├── C_TCLS_BER
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── JIT_PGD
    └── ACTVT → 3
├── JIT_S2C_CR
    ├── ACTVT → 3
    ├── NJIT_ACT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    ├── VTWEG → $VTWEG
    └── WERKS → $WERKS
├── M_BEST_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── QIE_IRULE
    ├── ACTVT → 1, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T001L, T320
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 3474. `/SCWM/OUTB_DLV_SAVEREP_YYY`

```
T-Code: /SCWM/OUTB_DLV_SAVEREP_YYY
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 3
    └── DOKOB → /SCWM/ODO
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 4
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MR, SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/DLV_ERP, /SPE/QUEUE
    └── ALG_SUBOBJ → QUEUE_ERROR, REPL
```

### 3475. `/SCWM/OUTB_SAVEREPLICA_REJ`

```
T-Code: /SCWM/OUTB_SAVEREPLICA_REJ
└── /SCWM/MO
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/METH → (empty — maintain in PFCG)
    ├── /SCWM/MONI → (empty — maintain in PFCG)
    └── /SCWM/OBCL → MQUEUE
```

### 3476. `/SCWM/PACK`

```
T-Code: /SCWM/PACK
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → HU
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2, 4
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → HUP
    └── STSMA → (empty — maintain in PFCG)
├── B_USERST_T
    ├── ACTVT → 1
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → HUP
    └── STSMA → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3, 6
    └── DOKAR → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3477. `/SCWM/PACKSPEC`

```
T-Code: /SCWM/PACKSPEC
├── /SAPCND/CM
    ├── /SAPCND/AP → PAK
    ├── /SAPCND/CT → SAPBDL01, SAPCAP01, SAPDKS01, SAPDKS02, SAPIBD01, SAPIBD02, SAPIBD03, SAPOBD01, SAPOBD02, SAPOBD03, SAPOBD04, SAPPAL01, SAPPSD01, SAPVSI01, SAPVSK01, SAPVSL01, SAPVSO01, SAPVSR01, SAPWHTA1, SAPWHTA2
    ├── /SAPCND/TY → 0AKT, 0BDL, 0CAP, 0DKS, 0GRP, 0GRV, 0IBD, 0OBD, 0PAL, 0PSD, 0RCV, 0VSI, 0VSK, 0VSL, 0VSO, 0VSR, 0WHT
    ├── /SAPCND/US → PS
    └── ACTVT → 1, 2, 3
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_APO_VERS
    ├── ACTVT → 3
    ├── APO_APKZ → (empty — maintain in PFCG)
    ├── APO_KENN → (empty — maintain in PFCG)
    ├── APO_MOD → (empty — maintain in PFCG)
    └── APO_VERS → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_NOH
    └── NOHIST → X
├── C_PVS_PATY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 61, 7
```

### 3478. `/SCWM/PACKSTDT`

```
T-Code: /SCWM/PACKSTDT
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_PACKSTDT
```

### 3479. `/SCWM/PACK_NAV_IBDL`

```
T-Code: /SCWM/PACK_NAV_IBDL
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 3, 4
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3480. `/SCWM/PACK_NAV_OBDL`

```
T-Code: /SCWM/PACK_NAV_OBDL
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 3, 6
    └── DOKOB → HU
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3, 6
    └── DOKAR → SHU
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3, F4
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3481. `/SCWM/PACK_ODLV_ASYN`

```
T-Code: /SCWM/PACK_ODLV_ASYN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PACKODLV
```

### 3482. `/SCWM/PI`

```
T-Code: /SCWM/PI
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 1, 2, 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3483. `/SCWM/PICUST_AR`

```
T-Code: /SCWM/PICUST_AR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /LIME/PIVCUST_AR, /LIME/PIVCUST_DC
```

### 3484. `/SCWM/PIDO`

```
T-Code: /SCWM/PIDO
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/PIDO
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → NRIV
```

### 3485. `/SCWM/PIIPO`

```
T-Code: /SCWM/PIIPO
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/IPON
```

### 3486. `/SCWM/PI_CC_CREATE`

```
T-Code: /SCWM/PI_CC_CREATE
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 3
    └── ACTVT → 3, 4
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLA_BKA
    └── KLART → 23
```

### 3487. `/SCWM/PI_COMPL_CHECK`

```
T-Code: /SCWM/PI_COMPL_CHECK
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
```

### 3488. `/SCWM/PI_COMPL_DEL`

```
T-Code: /SCWM/PI_COMPL_DEL
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3489. `/SCWM/PI_COUNT`

```
T-Code: /SCWM/PI_COUNT
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 5
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3490. `/SCWM/PI_COUNTLIST`

```
T-Code: /SCWM/PI_COUNTLIST
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3491. `/SCWM/PI_CREATE`

```
T-Code: /SCWM/PI_CREATE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3492. `/SCWM/PI_DOC_CREATE`

```
T-Code: /SCWM/PI_DOC_CREATE
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → SPC
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 3
    └── ACTVT → 3, 4
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3493. `/SCWM/PI_DOWNLOAD`

```
T-Code: /SCWM/PI_DOWNLOAD
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3494. `/SCWM/PI_PROCESS`

```
T-Code: /SCWM/PI_PROCESS
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── C_TCLA_BKA
    └── KLART → 230
```

### 3495. `/SCWM/PI_SAMP_CR`

```
T-Code: /SCWM/PI_SAMP_CR
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3496. `/SCWM/PI_SAMP_STOCK`

```
T-Code: /SCWM/PI_SAMP_STOCK
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3497. `/SCWM/PI_SAMP_UPDATE`

```
T-Code: /SCWM/PI_SAMP_UPDATE
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3498. `/SCWM/PI_UPLOAD`

```
T-Code: /SCWM/PI_UPLOAD
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3499. `/SCWM/PI_USER`

```
T-Code: /SCWM/PI_USER
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VM
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TOL_POST, /SCWM/V_TOL_RECO, /SCWM/V_USE_PI
```

### 3500. `/SCWM/PI_USER_DIFF`

```
T-Code: /SCWM/PI_USER_DIFF
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VM
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TOL_DIFF, /SCWM/V_USE_DIFF
```

### 3501. `/SCWM/PL`

```
T-Code: /SCWM/PL
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/MSTR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/MSGR → (empty — maintain in PFCG)
    └── ACTVT → 16
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/PL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3, 93
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3502. `/SCWM/PL_LOAD`

```
T-Code: /SCWM/PL_LOAD
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/PL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 93
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3503. `/SCWM/PMATCOHU`

```
T-Code: /SCWM/PMATCOHU
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PMATCOHU
```

### 3504. `/SCWM/PMR`

```
T-Code: /SCWM/PMR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PWR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23, 233
├── C_TCLS_BER
    ├── KLART → 23, 233
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 23, 233
    └── SICHT → (empty — maintain in PFCG)
```

### 3505. `/SCWM/PM_MTR`

```
T-Code: /SCWM/PM_MTR
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/PM_MTR_V
```

### 3506. `/SCWM/POD_IMP`

```
T-Code: /SCWM/POD_IMP
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → POD
```

### 3507. `/SCWM/POHUID`

```
T-Code: /SCWM/POHUID
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/POHU
```

### 3508. `/SCWM/POST`

```
T-Code: /SCWM/POST
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 4
├── /SCWM/SFUN
    └── /SCWM/SFUN → BW, DL, QC, SD
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3509. `/SCWM/POST_GI_ODO`

```
T-Code: /SCWM/POST_GI_ODO
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3510. `/SCWM/PPR`

```
T-Code: /SCWM/PPR
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3511. `/SCWM/PPR_W`

```
T-Code: /SCWM/PPR_W
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3512. `/SCWM/PRBIN`

```
T-Code: /SCWM/PRBIN
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3513. `/SCWM/PRC_MD`

```
T-Code: /SCWM/PRC_MD
├── /SAPCND/CM
    ├── /SAPCND/AP → DPP, PAK, PHU, PPI, PWO
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → DP, PH, PI, PS, PW
    └── ACTVT → 1, 2, 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → MP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 1, 2, 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 7
```

### 3514. `/SCWM/PRDI`

```
T-Code: /SCWM/PRDI
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/PAS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/QFID
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WCTA
    ├── ACTVT → 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → HUP
    └── STSMA → (empty — maintain in PFCG)
├── B_USERST_T
    ├── ACTVT → 1
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → HUP
    └── STSMA → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── QIE_ELEM
    ├── ACTVT → 1, 2, 3, 75
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_FIND
    ├── ACTVT → 3
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── QIE_IRULE
    ├── ACTVT → 1, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── QIE_SDRW
    ├── ACTVT → 3
    └── QIE_SDRW_T → (empty — maintain in PFCG)
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 3515. `/SCWM/PRDO`

```
T-Code: /SCWM/PRDO
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2, 4
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 6
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WCTA
    ├── ACTVT → 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23, 230, 231, 400
├── C_TCLS_BER
    ├── KLART → 23, 230, 231, 400
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 23, 230, 231, 400
    └── SICHT → (empty — maintain in PFCG)
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 3516. `/SCWM/PRDVC`

```
T-Code: /SCWM/PRDVC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRDV_C, /SCWM/V_PRDV_C_CLD
```

### 3517. `/SCWM/PRFIXBIN`

```
T-Code: /SCWM/PRFIXBIN
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3518. `/SCWM/PRHU0`

```
T-Code: /SCWM/PRHU0
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3519. `/SCWM/PRHU1`

```
T-Code: /SCWM/PRHU1
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3520. `/SCWM/PRHU2`

```
T-Code: /SCWM/PRHU2
└── /SAPCND/CC
    ├── /SAPCND/AP → PHU
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → PH
    └── ACTVT → 2
```

### 3521. `/SCWM/PRHU3`

```
T-Code: /SCWM/PRHU3
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3522. `/SCWM/PRHU4`

```
T-Code: /SCWM/PRHU4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3523. `/SCWM/PRHU5`

```
T-Code: /SCWM/PRHU5
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/VTPSCONDID
```

### 3524. `/SCWM/PRHU6`

```
T-Code: /SCWM/PRHU6
├── /SAPCND/CM
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
```

### 3525. `/SCWM/PRHUPPF1`

```
T-Code: /SCWM/PRHUPPF1
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3526. `/SCWM/PRHUPPF2`

```
T-Code: /SCWM/PRHUPPF2
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3527. `/SCWM/PRIOP_UPDATE`

```
T-Code: /SCWM/PRIOP_UPDATE
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3528. `/SCWM/PRODUCTION_WHR_MAINTAIN`

```
T-Code: /SCWM/PRODUCTION_WHR_MAINTAIN
├── /SCWM/DLV2
    ├── /SCDL/CAT → PWR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
```

### 3529. `/SCWM/PROD_CD_READ`

```
T-Code: /SCWM/PROD_CD_READ
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
```

### 3530. `/SCWM/PRPIAS`

```
T-Code: /SCWM/PRPIAS
└── /SAPCND/CC
    ├── /SAPCND/AP → PPI
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → PI
    └── ACTVT → 2
```

### 3531. `/SCWM/PRPICT`

```
T-Code: /SCWM/PRPICT
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3532. `/SCWM/PRPICTYPE`

```
T-Code: /SCWM/PRPICTYPE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V1_T685
```

### 3533. `/SCWM/PRPIDETPROC`

```
T-Code: /SCWM/PRPIDETPROC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3534. `/SCWM/PRPIFC`

```
T-Code: /SCWM/PRPIFC
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3535. `/SCWM/PRPIPPFC`

```
T-Code: /SCWM/PRPIPPFC
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3536. `/SCWM/PRPIPPFDET`

```
T-Code: /SCWM/PRPIPPFDET
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3537. `/SCWM/PRPI_DOC`

```
T-Code: /SCWM/PRPI_DOC
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3538. `/SCWM/PRPI_GCM`

```
T-Code: /SCWM/PRPI_GCM
├── /SAPCND/CM
    ├── /SAPCND/AP → PPI
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → PI
    └── ACTVT → 1, 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, F4
```

### 3539. `/SCWM/PRR_UPLOAD`

```
T-Code: /SCWM/PRR_UPLOAD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → PR
├── B_BUPA_GRP
    ├── ACTVT → 1, 2, 3, 6
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 1, 2, 3, F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → PRR
```

### 3540. `/SCWM/PRT_CT_EXT`

```
T-Code: /SCWM/PRT_CT_EXT
└── /SAPCND/CM
    ├── /SAPCND/AP → CON, DPP, PAK, PHU, PPI, PWO, SCH, WME
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
```

### 3541. `/SCWM/PRWO0`

```
T-Code: /SCWM/PRWO0
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3542. `/SCWM/PRWO1`

```
T-Code: /SCWM/PRWO1
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3543. `/SCWM/PRWO2`

```
T-Code: /SCWM/PRWO2
└── /SAPCND/CC
    ├── /SAPCND/AP → PWO
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → PW
    └── ACTVT → 2
```

### 3544. `/SCWM/PRWO3`

```
T-Code: /SCWM/PRWO3
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3545. `/SCWM/PRWO4`

```
T-Code: /SCWM/PRWO4
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3546. `/SCWM/PRWO5`

```
T-Code: /SCWM/PRWO5
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VTPSCONDID
```

### 3547. `/SCWM/PRWO6`

```
T-Code: /SCWM/PRWO6
├── /SAPCND/CM
    ├── /SAPCND/AP → PWO
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3548. `/SCWM/PRWOPPF1`

```
T-Code: /SCWM/PRWOPPF1
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3549. `/SCWM/PRWOPPF2`

```
T-Code: /SCWM/PRWOPPF2
├── S_PPF_CONF
    └── ACTVT → 2, 3
└── S_PPF_CUST
    └── ACTVT → 2, 3
```

### 3550. `/SCWM/PSA`

```
T-Code: /SCWM/PSA
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_PSASTG
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TDLVPPGIC, /SCWM/TDLVPPGIE, /SCWM/V_TPSA
```

### 3551. `/SCWM/PSAMAP`

```
T-Code: /SCWM/PSAMAP
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TMAPPSA, /SCWM/TMAPPSA_CLD
└── W_BETR_USR
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 3552. `/SCWM/PSASTAGE`

```
T-Code: /SCWM/PSASTAGE
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_PSASTG
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TPSASTAGE, /SCWM/V_TPSASTAG, /SCWM/V_TPSASTAG_CLD
```

### 3553. `/SCWM/PSASTAGE2`

```
T-Code: /SCWM/PSASTAGE2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── S_SCD0_OBJ
    ├── ACTVT → 8
    └── OBJECTCLAS → /SCWM/CD_PSASTG
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TPSASTGE, /SCWM/V_TPSASTGE_CLD
```

### 3554. `/SCWM/PSA_REPLICATE`

```
T-Code: /SCWM/PSA_REPLICATE
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TPSA, U_11008
```

### 3555. `/SCWM/PSCT0`

```
T-Code: /SCWM/PSCT0
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3556. `/SCWM/PSCT1`

```
T-Code: /SCWM/PSCT1
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64, 65
```

### 3557. `/SCWM/PSCT2`

```
T-Code: /SCWM/PSCT2
└── /SAPCND/CC
    ├── /SAPCND/AP → PAK
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → PS
    └── ACTVT → 2
```

### 3558. `/SCWM/PSCT3`

```
T-Code: /SCWM/PSCT3
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3559. `/SCWM/PSCT4`

```
T-Code: /SCWM/PSCT4
└── S_TABU_NAM
    ├── ACTVT → 2
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3560. `/SCWM/PSCT5`

```
T-Code: /SCWM/PSCT5
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/VTPSCONDID
```

### 3561. `/SCWM/PSCT6`

```
T-Code: /SCWM/PSCT6
├── /SAPCND/CM
    ├── /SAPCND/AP → PAK
    ├── /SAPCND/CT → SAPBDL01, SAPCAP01, SAPDKS01, SAPDKS02, SAPIBD01, SAPIBD02, SAPIBD03, SAPOBD01, SAPOBD02, SAPOBD03, SAPOBD04, SAPPAL01, SAPPSD01, SAPVSI01, SAPVSK01, SAPVSL01, SAPVSO01, SAPVSR01, SAPWHTA1, SAPWHTA2
    ├── /SAPCND/TY → 0AKT, 0BDL, 0CAP, 0DKS, 0GRP, 0GRV, 0IBD, 0OBD, 0PAL, 0PSD, 0RCV, 0VSI, 0VSK, 0VSL, 0VSO, 0VSR, 0WHT
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3562. `/SCWM/PSDISTRIBUTE`

```
T-Code: /SCWM/PSDISTRIBUTE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPSDISTRIB
```

### 3563. `/SCWM/PSEG`

```
T-Code: /SCWM/PSEG
├── /SAPCND/CM
    ├── /SAPCND/AP → PAK
    ├── /SAPCND/CT → SAPBDL01, SAPCAP01, SAPDKS01, SAPDKS02, SAPIBD01, SAPIBD02, SAPIBD03, SAPOBD01, SAPOBD02, SAPOBD03, SAPOBD04, SAPPAL01, SAPPSD01, SAPVSI01, SAPVSK01, SAPVSL01, SAPVSO01, SAPVSR01, SAPWHTA1, SAPWHTA2
    ├── /SAPCND/TY → 0AKT, 0BDL, 0CAP, 0DKS, 0GRP, 0GRV, 0IBD, 0OBD, 0PAL, 0PSD, 0RCV, 0VSI, 0VSK, 0VSL, 0VSO, 0VSR, 0WHT
    ├── /SAPCND/US → PS
    └── ACTVT → 1, 2, 3
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6, 61, 7
```

### 3564. `/SCWM/PSLOG`

```
T-Code: /SCWM/PSLOG
└── /SAPCND/CM
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → SAPBDL01, SAPCAP01, SAPDKS01, SAPDKS02, SAPIBD01, SAPIBD02, SAPIBD03, SAPOBD01, SAPOBD02, SAPOBD03, SAPOBD04, SAPPAL01, SAPPSD01, SAPVSI01, SAPVSK01, SAPVSL01, SAPVSO01, SAPVSR01, SAPWHTA1, SAPWHTA2
    ├── /SAPCND/TY → 0AKT, 0BDL, 0CAP, 0DKS, 0GRP, 0GRV, 0IBD, 0OBD, 0PAL, 0PSD, 0RCV, 0VSI, 0VSK, 0VSL, 0VSO, 0VSR, 0WHT
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3565. `/SCWM/PSWORKSTEP`

```
T-Code: /SCWM/PSWORKSTEP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── P_WORKSTEP
    ├── /SCWM/WSTY → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
```

### 3566. `/SCWM/PS_NR`

```
T-Code: /SCWM/PS_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/PS
```

### 3567. `/SCWM/PUT_TW_WT_AS`

```
T-Code: /SCWM/PUT_TW_WT_AS
└── /SAPCND/CC
    ├── /SAPCND/AP → TWP
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → TW
    └── ACTVT → 2
```

### 3568. `/SCWM/PUT_TW_WT_COPY`

```
T-Code: /SCWM/PUT_TW_WT_COPY
├── /SAPCND/CM
    ├── /SAPCND/AP → TWP
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → TW
    └── ACTVT → 1, 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3569. `/SCWM/PUT_TW_WT_CT`

```
T-Code: /SCWM/PUT_TW_WT_CT
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3570. `/SCWM/PUT_TW_WT_FC`

```
T-Code: /SCWM/PUT_TW_WT_FC
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64
```

### 3571. `/SCWM/PUT_TW_WT_GCM`

```
T-Code: /SCWM/PUT_TW_WT_GCM
├── /SAPCND/CM
    ├── /SAPCND/AP → TWP
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → TW
    └── ACTVT → 1, 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3572. `/SCWM/PUT_TW_WT_PRC`

```
T-Code: /SCWM/PUT_TW_WT_PRC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3573. `/SCWM/PUT_TW_WT_TB`

```
T-Code: /SCWM/PUT_TW_WT_TB
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64
```

### 3574. `/SCWM/PUT_TW_WT_TWP5`

```
T-Code: /SCWM/PUT_TW_WT_TWP5
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VTPSCONDID
```

### 3575. `/SCWM/QDECIDED_FUP_PROCESS`

```
T-Code: /SCWM/QDECIDED_FUP_PROCESS
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 6
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 22, 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── M_MATE_CHG
    ├── ACTVT → 2, 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAN
    └── ACTVT → 3
├── M_MATE_MAR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 2, 3
    └── WERKS → $WERKS
├── M_MSEG_BWA
    ├── ACTVT → 1, 3
    └── BWART → 551
├── M_MSEG_WWA
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
├── QIE_ELEM
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T300, T300T
```

### 3576. `/SCWM/QFUCODE_S4`

```
T-Code: /SCWM/QFUCODE_S4
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_QFUCO_S4, /SCWM/V_TQFU_S4
```

### 3577. `/SCWM/QFU_MASTER`

```
T-Code: /SCWM/QFU_MASTER
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_QFU
```

### 3578. `/SCWM/QIDDH`

```
T-Code: /SCWM/QIDDH
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3, 6, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → DLV
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QFID
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLA_BKA
    └── KLART → 230
```

### 3579. `/SCWM/QIDHU`

```
T-Code: /SCWM/QIDHU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3, 6, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → HU
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── QIE_E_CAT → 2
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/QFID
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_KLAH_BKL
    ├── ACTVT → 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLA_BKA
    └── KLART → 230, 400
```

### 3580. `/SCWM/QIDPR`

```
T-Code: /SCWM/QIDPR
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6, 75
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/QFID
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → QE1, QE2
    └── STSMA → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230, 400
├── QIE_ELEM
    ├── ACTVT → 1, 2, 3, 6, 75
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_FIND
    ├── ACTVT → 1, 2, 3, 6
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 1, 2, 3, 6, 75, 85
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── QIE_IRULE
    ├── ACTVT → 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── QIE_QLVL
    ├── ACTVT → 1, 2, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3581. `/SCWM/QINSP`

```
T-Code: /SCWM/QINSP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QELM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230, 400
├── QIE_ELEM
    ├── ACTVT → 1, 2, 3, 6, 75, 85
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_FIND
    ├── ACTVT → 1, 2, 3, 6
    └── QIE_FIN_TY → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 1, 2, 3, 6
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3582. `/SCWM/QINSP_S4`

```
T-Code: /SCWM/QINSP_S4
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → /SCWM/QINSP_S4
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 1, 2, 3, 6
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 1, 2, 3, 6
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PVTYPE → (empty — maintain in PFCG)
├── QIE_ELEM
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 2
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → T300, T300T
```

### 3583. `/SCWM/QLEVEL`

```
T-Code: /SCWM/QLEVEL
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → EXTER_VERS*, INBCK_VERS*, INBCT_VERS*, INBCU_VERS*
├── /SCWM/QLVL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → EXTER_VERS*, INBCK_VERS*, INBCT_VERS*, INBCU_VERS*
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
```

### 3584. `/SCWM/QLOT_SET_LOT_SIZE`

```
T-Code: /SCWM/QLOT_SET_LOT_SIZE
└── Q_INSPLOT
    ├── QACT_ILOT → 10
    ├── QMATAUTH → (empty — maintain in PFCG)
    ├── QPRUEFART → (empty — maintain in PFCG)
    └── WERKS → $WERKS
```

### 3585. `/SCWM/QM_GM_CREATE`

```
T-Code: /SCWM/QM_GM_CREATE
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SPE/AAC
    ├── /SPE/AAC → (empty — maintain in PFCG)
    └── ACTVT → 16
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── M_MSEG_BWA
    ├── ACTVT → 1
    └── BWART → (empty — maintain in PFCG)
└── M_MSEG_WWA
    ├── ACTVT → 1
    └── WERKS → $WERKS
```

### 3586. `/SCWM/QRSETUP`

```
T-Code: /SCWM/QRSETUP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → EXTER_VERS*, INBCK_VERS*, INBCT_VERS*, INBCU_VERS*
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── QIE_IRULE
    ├── ACTVT → 3, 6
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
└── QIE_SDRW
    ├── ACTVT → 3
    └── QIE_SDRW_T → (empty — maintain in PFCG)
```

### 3587. `/SCWM/QRSIM`

```
T-Code: /SCWM/QRSIM
└── /SCWM/QSUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 2, 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3588. `/SCWM/QSDRWP`

```
T-Code: /SCWM/QSDRWP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
└── QIE_SDRW
    ├── ACTVT → 1, 2, 3, 6
    └── QIE_SDRW_T → 1, 2, 3
```

### 3589. `/SCWM/QSEQ`

```
T-Code: /SCWM/QSEQ
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RSGR_QSQ, /SCWM/V_T346, /SCWM/V_T346MFS
```

### 3590. `/SCWM/QTSQ`

```
T-Code: /SCWM/QTSQ
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RS_TI_SQ
```

### 3591. `/SCWM/RACKTYPE`

```
T-Code: /SCWM/RACKTYPE
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3592. `/SCWM/RACK_GEN`

```
T-Code: /SCWM/RACK_GEN
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3593. `/SCWM/RCLOC`

```
T-Code: /SCWM/RCLOC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_TABU_LIN
    ├── ACTVT → 2, 3
    ├── ORG_CRIT → (empty — maintain in PFCG)
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TRCLOC, /SCWM/V_TRCLOC
```

### 3594. `/SCWM/RCLOCSTAGE`

```
T-Code: /SCWM/RCLOCSTAGE
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TRCLOCSTAG, /SCWM/V_TRCLOCST
```

### 3595. `/SCWM/RCLOC_REPL`

```
T-Code: /SCWM/RCLOC_REPL
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TRCLOC
```

### 3596. `/SCWM/REAR`

```
T-Code: /SCWM/REAR
├── /SCWM/DLV2
    ├── /SCDL/CAT → WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3597. `/SCWM/REAR_BATCH`

```
T-Code: /SCWM/REAR_BATCH
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3598. `/SCWM/REC_ACTIVATE`

```
T-Code: /SCWM/REC_ACTIVATE
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RECACTIV
```

### 3599. `/SCWM/REECCHECK`

```
T-Code: /SCWM/REECCHECK
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3600. `/SCWM/REPL`

```
T-Code: /SCWM/REPL
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 23
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3601. `/SCWM/RFID_POST`

```
T-Code: /SCWM/RFID_POST
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/RFID_POST
```

### 3602. `/SCWM/RFID_RFC`

```
T-Code: /SCWM/RFID_RFC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/RFID_RFC
```

### 3603. `/SCWM/RFID_RSRC`

```
T-Code: /SCWM/RFID_RSRC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_EPC_RSRC
```

### 3604. `/SCWM/RFLOGACT`

```
T-Code: /SCWM/RFLOGACT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RFLOGACT, /SCWM/V_RFLOGACT_CLD, /SCWM/V_RFLOGA_A, /SCWM/V_RFLOGA_A_CLD
```

### 3605. `/SCWM/RFUI`

```
T-Code: /SCWM/RFUI
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 5
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, PDI, PDO, PWR, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 92
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/DSLP
    └── ACTVT → 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/ILT
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROC → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/PIPR
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/RFAD
    └── /SCWM/RFAP → 1
├── /SCWM/RFLT
    ├── /SCWM/RFAP → 1
    └── /SCWM/RFLT → (empty — maintain in PFCG)
├── /SCWM/RSRC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/RSTY → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 78
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → RH
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TATT
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── M_MATE_MAN
    └── ACTVT → 3
├── M_MATE_MAR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 3606. `/SCWM/RFUI_PBV`

```
T-Code: /SCWM/RFUI_PBV
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 2
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 5
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, PDI, PDO, PWR, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 92
├── /SCWM/ILT
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROC → (empty — maintain in PFCG)
    └── ACTVT → 1
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3
├── /SCWM/RFAD
    └── /SCWM/RFAP → 1
├── /SCWM/RFLT
    ├── /SCWM/RFAP → 1
    └── /SCWM/RFLT → (empty — maintain in PFCG)
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → LM0001, LM0002, LM0003
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
└── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
```

### 3607. `/SCWM/RF_ASYNC_CTRL`

```
T-Code: /SCWM/RF_ASYNC_CTRL
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TCOMCTRL
```

### 3608. `/SCWM/RF_DEL_LOG`

```
T-Code: /SCWM/RF_DEL_LOG
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3609. `/SCWM/RGRP`

```
T-Code: /SCWM/RGRP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RSRC_G
```

### 3610. `/SCWM/RSCMSG_DEACT`

```
T-Code: /SCWM/RSCMSG_DEACT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/V_PRCRSRCM
```

### 3611. `/SCWM/RSRC`

```
T-Code: /SCWM/RSRC
├── /SCWM/RSRC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/RSTY → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_RSRC, /SCWM/V_RSRC_CLD
```

### 3612. `/SCWM/RSRC_PRN`

```
T-Code: /SCWM/RSRC_PRN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TRSRC_PRN, /SCWM/V_RSRC_PRN
```

### 3613. `/SCWM/RWOCR`

```
T-Code: /SCWM/RWOCR
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3614. `/SCWM/SBST`

```
T-Code: /SCWM/SBST
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3615. `/SCWM/SBUP`

```
T-Code: /SCWM/SBUP
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── S_DATASET
    ├── ACTVT → 33, A6
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → /SCWM/TLAGP_UPLOAD
```

### 3616. `/SCWM/SCALE`

```
T-Code: /SCWM/SCALE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/SCALE
```

### 3617. `/SCWM/SEBA`

```
T-Code: /SCWM/SEBA
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3618. `/SCWM/SER_CON_CHECK`

```
T-Code: /SCWM/SER_CON_CHECK
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3619. `/SCWM/SGI`

```
T-Code: /SCWM/SGI
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, ODR, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T333, /SCWM/TPTYPDET
```

### 3620. `/SCWM/SHIFT`

```
T-Code: /SCWM/SHIFT
├── /SCMB/WDCP
    ├── /SCMB/LGNU → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
├── /SCMB/WDEF
    ├── /SCMB/LGNU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3621. `/SCWM/SHIFT_WS`

```
T-Code: /SCWM/SHIFT_WS
├── /SCMB/WDCP
    ├── /SCMB/LGNU → (empty — maintain in PFCG)
    └── ACTVT → 2, 3, 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3622. `/SCWM/SLOT`

```
T-Code: /SCWM/SLOT
├── /SAPCND/CM
    ├── /SAPCND/AP → PAK
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → PS
    └── ACTVT → 3
├── /SCWM/SLT2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 16, 3, 7
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SCWM/CONCEPT
```

### 3623. `/SCWM/SLOTACT`

```
T-Code: /SCWM/SLOTACT
├── /SCWM/SLT2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 16, 7
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
```

### 3624. `/SCWM/SLOTOCC`

```
T-Code: /SCWM/SLOTOCC
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3625. `/SCWM/SLOTRNUM`

```
T-Code: /SCWM/SLOTRNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/CONR
```

### 3626. `/SCWM/SM30`

```
T-Code: /SCWM/SM30
├── /SCWM/COST
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VM
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3627. `/SCWM/SRTUP`

```
T-Code: /SCWM/SRTUP
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3628. `/SCWM/SR_ACT_RNUM`

```
T-Code: /SCWM/SR_ACT_RNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/SR_A
```

### 3629. `/SCWM/SR_BOL_RNUM`

```
T-Code: /SCWM/SR_BOL_RNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/BOL
```

### 3630. `/SCWM/SR_CREATE_VEH_TU_DAS`

```
T-Code: /SCWM/SR_CREATE_VEH_TU_DAS
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → CHG, READ
    └── APO_TMOD → (empty — maintain in PFCG)
└── C_LIME_HU
    ├── ACTVT → 1, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3631. `/SCWM/SR_INTDAS`

```
T-Code: /SCWM/SR_INTDAS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DASSYST
```

### 3632. `/SCWM/SR_PRINT`

```
T-Code: /SCWM/SR_PRINT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/T_SR_PRNT, /SCWM/T_SR_SMART, /SCWM/V_SR_PRPR2, /SCWM/V_SR_PRPRO, /SCWM/V_SR_QUEUE
```

### 3633. `/SCWM/SR_UPDATE_VEH_TU_DAS`

```
T-Code: /SCWM/SR_UPDATE_VEH_TU_DAS
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → CHG, READ
    └── APO_TMOD → (empty — maintain in PFCG)
└── C_LIME_HU
    ├── ACTVT → 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3634. `/SCWM/STADET_ASS`

```
T-Code: /SCWM/STADET_ASS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/STADET_ASS, /SCWM/STADET_A_V, /SCWM/STADET_A_V_CLD
```

### 3635. `/SCWM/STADET_ASS_IN`

```
T-Code: /SCWM/STADET_ASS_IN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/STADET_AIV, /SCWM/STADET_A_I_V_CLD
```

### 3636. `/SCWM/STADET_ASS_OUT`

```
T-Code: /SCWM/STADET_ASS_OUT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/STADET_AOV, /SCWM/STADET_A_O_V_CLD
```

### 3637. `/SCWM/STADET_IN`

```
T-Code: /SCWM/STADET_IN
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/STADET_I_V, /SCWM/STADET_I_V_CLD, /SCWM/T333, /SCWM/TDOOR_V, /SCWM/TDRDETGR, /SCWM/TPTYPDET
```

### 3638. `/SCWM/STADET_OUT`

```
T-Code: /SCWM/STADET_OUT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/STADET_OUT, /SCWM/STADET_O_V, /SCWM/STADET_O_V_CLD, /SCWM/V_T3001_CU
```

### 3639. `/SCWM/STAGE`

```
T-Code: /SCWM/STAGE
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PWR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/PROD
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → MFG_STAGE, WHOLOG, WHS_TASK
```

### 3640. `/SCWM/STOCK_ATT_CHG`

```
T-Code: /SCWM/STOCK_ATT_CHG
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SI
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_SI
    ├── ACTVT → 1, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → S
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → QINSP
```

### 3641. `/SCWM/STOCK_CONS`

```
T-Code: /SCWM/STOCK_CONS
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SC
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3642. `/SCWM/T331SC1P`

```
T-Code: /SCWM/T331SC1P
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_T331SC1P
```

### 3643. `/SCWM/T331SC2P`

```
T-Code: /SCWM/T331SC2P
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_T331SC2P
```

### 3644. `/SCWM/TATT_DELETE`

```
T-Code: /SCWM/TATT_DELETE
├── /SCWM/TATT
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → LM0001, LM0002, LM0003
```

### 3645. `/SCWM/TATT_UPLOAD`

```
T-Code: /SCWM/TATT_UPLOAD
├── /SCWM/TATT
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → TATT
```

### 3646. `/SCWM/TAVG`

```
T-Code: /SCWM/TAVG
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TAVG
```

### 3647. `/SCWM/TAVLGROUP`

```
T-Code: /SCWM/TAVLGROUP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TAVLGROUP
```

### 3648. `/SCWM/TCATLOCN`

```
T-Code: /SCWM/TCATLOCN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TCATLOCN
```

### 3649. `/SCWM/TDC_DOWNLOAD`

```
T-Code: /SCWM/TDC_DOWNLOAD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → TD
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3650. `/SCWM/TDC_EVALUATE`

```
T-Code: /SCWM/TDC_EVALUATE
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3651. `/SCWM/TDC_NET`

```
T-Code: /SCWM/TDC_NET
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → TD
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3652. `/SCWM/TDC_SETUP`

```
T-Code: /SCWM/TDC_SETUP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VEDGE_RSR2, /SCWM/VEDGE_RSRC, /SCWM/VNET_RSRCV, /SCWM/V_T331_DIS, /SCWM/V_TDC_EDGE, /SCWM/V_TDC_NET
```

### 3653. `/SCWM/TDC_UPLOAD`

```
T-Code: /SCWM/TDC_UPLOAD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → TD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → /SCWM/TDC_UPLOAD
```

### 3654. `/SCWM/TDPRF_STD`

```
T-Code: /SCWM/TDPRF_STD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_DPRF_S
```

### 3655. `/SCWM/TINC`

```
T-Code: /SCWM/TINC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCMB/TSINC
```

### 3656. `/SCWM/TJIT_WPTD`

```
T-Code: /SCWM/TJIT_WPTD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_WPTD
```

### 3657. `/SCWM/TJIT_WPT_A`

```
T-Code: /SCWM/TJIT_WPT_A
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTJIT_WPTA
```

### 3658. `/SCWM/TL5`

```
T-Code: /SCWM/TL5
└── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
```

### 3659. `/SCWM/TLR_WIZARD`

```
T-Code: /SCWM/TLR_WIZARD
├── /SCMB/LMEW
    ├── /SCMB/OWNE → (empty — maintain in PFCG)
    ├── /SCMB/PRR → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/PIMP
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_TCLA_BKA
    └── KLART → 230
└── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
```

### 3660. `/SCWM/TLR_WIZARD_CUS`

```
T-Code: /SCWM/TLR_WIZARD_CUS
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/QDOC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → DLV, HU, PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → (empty — maintain in PFCG)
└── C_TCLA_BKA
    └── KLART → 230
```

### 3661. `/SCWM/TMET`

```
T-Code: /SCWM/TMET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMET
```

### 3662. `/SCWM/TMFSTELTOI`

```
T-Code: /SCWM/TMFSTELTOI
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TMFSTELTOI
```

### 3663. `/SCWM/TODLV`

```
T-Code: /SCWM/TODLV
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_FDG
    ├── ACTVT → 3
    └── FLDGR → (empty — maintain in PFCG)
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 16, 3
├── C_PPE_PAID
    ├── ACTVT → 1, 2, 3, 6
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230, 400
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3664. `/SCWM/TODLV_I`

```
T-Code: /SCWM/TODLV_I
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3665. `/SCWM/TODLV_M`

```
T-Code: /SCWM/TODLV_M
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3666. `/SCWM/TODLV_NAV`

```
T-Code: /SCWM/TODLV_NAV
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3667. `/SCWM/TODLV_O`

```
T-Code: /SCWM/TODLV_O
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3668. `/SCWM/TODLV_O_NAV`

```
T-Code: /SCWM/TODLV_O_NAV
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3669. `/SCWM/TODLV_T`

```
T-Code: /SCWM/TODLV_T
├── /SCTM/AUTH
    ├── /SCTM/CAT → (empty — maintain in PFCG)
    ├── /SCTM/FRDT → (empty — maintain in PFCG)
    ├── /SCTM/SHPT → (empty — maintain in PFCG)
    ├── /SCTM/TPP → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
└── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3670. `/SCWM/TONUM`

```
T-Code: /SCWM/TONUM
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/TO
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → NRIV
```

### 3671. `/SCWM/TO_CONF`

```
T-Code: /SCWM/TO_CONF
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → IDR, ODR, PDI, PDO, POR, PWR, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31, 6
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → READ
    └── APO_TMOD → (empty — maintain in PFCG)
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1, 6
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → QE1, QE2
    └── STSMA → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
├── QIE_ELEM
    ├── ACTVT → 3
    ├── QIE_E_CAT → (empty — maintain in PFCG)
    ├── QIE_E_SCAT → (empty — maintain in PFCG)
    └── QIE_E_TYPE → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── QIE_IRULE
    ├── ACTVT → 3
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SCWM/WME
    └── ALG_SUBOBJ → WHOLOG
```

### 3672. `/SCWM/TO_DISP`

```
T-Code: /SCWM/TO_DISP
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → (empty — maintain in PFCG)
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 3, 4
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP, VD
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
```

### 3673. `/SCWM/TO_JIT_UPD_MULTIFIT_QRFC`

```
T-Code: /SCWM/TO_JIT_UPD_MULTIFIT_QRFC
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── JIT_S2P
    ├── ACTVT → 1
    ├── NJIT_ACT → S2_JC_STKT
    └── WERKS → $WERKS
└── JIT_S2P_CC
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 3674. `/SCWM/TPRCES`

```
T-Code: /SCWM/TPRCES
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TPRCES
```

### 3675. `/SCWM/TROUTP`

```
T-Code: /SCWM/TROUTP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TROUTP
```

### 3676. `/SCWM/TSPP`

```
T-Code: /SCWM/TSPP
└── /SCTM/RG
    └── ACTVT → 3
```

### 3677. `/SCWM/TU`

```
T-Code: /SCWM/TU
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/DSLP
    └── ACTVT → 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 4
    └── ACTVT → 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/QFUP
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/QFUP → (empty — maintain in PFCG)
    ├── ACTVT → 16, 2
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1, 6
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → TU
    └── STSMA → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 10, 2, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → (empty — maintain in PFCG)
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3678. `/SCWM/TUPROF_DET`

```
T-Code: /SCWM/TUPROF_DET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TUPROF_DET, /SCWM/V_TUPROFDE
```

### 3679. `/SCWM/TU_RNUM`

```
T-Code: /SCWM/TU_RNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/TU
```

### 3680. `/SCWM/TWCPRINT`

```
T-Code: /SCWM/TWCPRINT
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3681. `/SCWM/TWCRSORTD`

```
T-Code: /SCWM/TWCRSORTD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWCRSORTD
```

### 3682. `/SCWM/TWORKST`

```
T-Code: /SCWM/TWORKST
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWORKST, /SCWM/TWORKSTT, /SCWM/V_WORKST_A, /SCWM/V_WORKST_A_CLD
```

### 3683. `/SCWM/TWRKLDGR`

```
T-Code: /SCWM/TWRKLDGR
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/TWRKLDGR
```

### 3684. `/SCWM/TWSPACKMAT`

```
T-Code: /SCWM/TWSPACKMAT
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VM
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/TWSPACKMAT, /SCWM/V_WPACKMAT
```

### 3685. `/SCWM/UNBLK_PTWY`

```
T-Code: /SCWM/UNBLK_PTWY
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3686. `/SCWM/UNLOAD`

```
T-Code: /SCWM/UNLOAD
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/PAS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
├── QIE_INSPDO
    ├── ACTVT → 2, 3
    ├── QIE_AUTHGR → (empty — maintain in PFCG)
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1, 2, 3
    ├── QIE_OBJTYP → PROD
    ├── QIE_OWNER → SCM_EWM
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3687. `/SCWM/USER`

```
T-Code: /SCWM/USER
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_USER, /SCWM/V_USER_CLD
```

### 3688. `/SCWM/VALUATION_CHG`

```
T-Code: /SCWM/VALUATION_CHG
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VC
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3689. `/SCWM/VALUATION_SET`

```
T-Code: /SCWM/VALUATION_SET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
```

### 3690. `/SCWM/VAS`

```
T-Code: /SCWM/VAS
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → /SCWM/VAS
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUPR_BZT
    ├── ACTVT → 3
    └── RELTYP → (empty — maintain in PFCG)
└── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
```

### 3691. `/SCWM/VASEXEC`

```
T-Code: /SCWM/VASEXEC
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/ATTS
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── ACTVT → 1, 2, 3, 6
    └── DOKOB → HU
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 23, 3
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1
    └── ACTVT → 3, 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_DRAW_TCD
    ├── ACTVT → 1, 2, 3, 6
    └── DOKAR → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → (empty — maintain in PFCG)
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
```

### 3692. `/SCWM/VASID`

```
T-Code: /SCWM/VASID
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/VASO
```

### 3693. `/SCWM/VAS_I`

```
T-Code: /SCWM/VAS_I
├── /SAPCND/CM
    ├── /SAPCND/AP → (empty — maintain in PFCG)
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WCTA
    ├── ACTVT → 3
    └── TCD → /SCWM/VASEXEC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → (empty — maintain in PFCG)
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── P_PACKSPEC
    ├── /SCWM/PSGR → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3694. `/SCWM/VAS_INT`

```
T-Code: /SCWM/VAS_INT
├── /SCWM/DLV2
    ├── /SCDL/CAT → WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3695. `/SCWM/VAS_KTR`

```
T-Code: /SCWM/VAS_KTR
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WCTA
    ├── ACTVT → 2
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3696. `/SCWM/VAS_KTS`

```
T-Code: /SCWM/VAS_KTS
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 2
    └── ACTVT → 3, 4
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/WCTA
    ├── ACTVT → 2, 3
    └── TCD → (empty — maintain in PFCG)
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
└── QIE_IRULE
    ├── ACTVT → 1
    ├── QIE_OBJTYP → (empty — maintain in PFCG)
    ├── QIE_OWNER → (empty — maintain in PFCG)
    └── QIE_PROCES → (empty — maintain in PFCG)
```

### 3697. `/SCWM/VAS_O`

```
T-Code: /SCWM/VAS_O
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI, PDO, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → (empty — maintain in PFCG)
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_LIME_HU
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3698. `/SCWM/VCBD`

```
T-Code: /SCWM/VCBD
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3699. `/SCWM/VCTRLPR_DC`

```
T-Code: /SCWM/VCTRLPR_DC
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VCTRLPR_DC
```

### 3700. `/SCWM/VC_TSGMACT`

```
T-Code: /SCWM/VC_TSGMACT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TSGMMVTI, /SCWM/V_TSGMMVTO, /SCWM/V_TSGMMVTT, /SCWM/V_TSGMWHSE
```

### 3701. `/SCWM/VEH`

```
T-Code: /SCWM/VEH
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDI, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 3
├── /SCWM/DSAP
    ├── /SCWM/DSLP → (empty — maintain in PFCG)
    ├── /SCWM/DSPS → 1
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/DSLP
    └── ACTVT → 3
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3, 31, 6
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── B_USERSTAT
    ├── ACTVT → 1, 6
    ├── BERSL → (empty — maintain in PFCG)
    ├── OBTYP → VEH
    └── STSMA → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 3702. `/SCWM/VEH_RNUM`

```
T-Code: /SCWM/VEH_RNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/VEH
```

### 3703. `/SCWM/VMAPITMTYP_CLD`

```
T-Code: /SCWM/VMAPITMTYP_CLD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMAPITMTYP_CLD
```

### 3704. `/SCWM/VMAPSOFF`

```
T-Code: /SCWM/VMAPSOFF
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VMAPSOFF
```

### 3705. `/SCWM/VMEPSA`

```
T-Code: /SCWM/VMEPSA
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/V_MEPSA
```

### 3706. `/SCWM/VSETHUSPSH`

```
T-Code: /SCWM/VSETHUSPSH
├── S_TABU_LIN
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ORG_CRIT → /SCWM/LGNUM
    ├── ORG_FIELD1 → (empty — maintain in PFCG)
    ├── ORG_FIELD2 → (empty — maintain in PFCG)
    ├── ORG_FIELD3 → (empty — maintain in PFCG)
    ├── ORG_FIELD4 → (empty — maintain in PFCG)
    ├── ORG_FIELD5 → (empty — maintain in PFCG)
    ├── ORG_FIELD6 → (empty — maintain in PFCG)
    ├── ORG_FIELD7 → (empty — maintain in PFCG)
    └── ORG_FIELD8 → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VSETHUSPSH
```

### 3707. `/SCWM/VTIMREAGRP`

```
T-Code: /SCWM/VTIMREAGRP
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTIMREAGRP
```

### 3708. `/SCWM/VTIMREARQ`

```
T-Code: /SCWM/VTIMREARQ
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VTIMREARQ
```

### 3709. `/SCWM/V_EPD_CD`

```
T-Code: /SCWM/V_EPD_CD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_EPD_CD
```

### 3710. `/SCWM/V_MODE`

```
T-Code: /SCWM/V_MODE
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MODE
```

### 3711. `/SCWM/V_MODEDET2`

```
T-Code: /SCWM/V_MODEDET2
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_MODEDET2
```

### 3712. `/SCWM/V_PRR_CD`

```
T-Code: /SCWM/V_PRR_CD
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_PRR_CD
```

### 3713. `/SCWM/V_T3010`

```
T-Code: /SCWM/V_T3010
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T3010
```

### 3714. `/SCWM/V_T307`

```
T-Code: /SCWM/V_T307
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T307
```

### 3715. `/SCWM/V_T331_RF`

```
T-Code: /SCWM/V_T331_RF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T331_RF
```

### 3716. `/SCWM/V_T340DA`

```
T-Code: /SCWM/V_T340DA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340DA
```

### 3717. `/SCWM/V_T340D_RR`

```
T-Code: /SCWM/V_T340D_RR
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340D_RR
```

### 3718. `/SCWM/V_T340TATT`

```
T-Code: /SCWM/V_T340TATT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T340TATT
```

### 3719. `/SCWM/V_T346`

```
T-Code: /SCWM/V_T346
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346
```

### 3720. `/SCWM/V_T346A`

```
T-Code: /SCWM/V_T346A
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346A
```

### 3721. `/SCWM/V_T346L`

```
T-Code: /SCWM/V_T346L
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346L
```

### 3722. `/SCWM/V_T346L_PI`

```
T-Code: /SCWM/V_T346L_PI
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_T346L_PI
```

### 3723. `/SCWM/V_TAETS`

```
T-Code: /SCWM/V_TAETS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TAETS
```

### 3724. `/SCWM/V_TCAT`

```
T-Code: /SCWM/V_TCAT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TCAT
```

### 3725. `/SCWM/V_TILTNR`

```
T-Code: /SCWM/V_TILTNR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TILTNR
```

### 3726. `/SCWM/V_TIMCBUST`

```
T-Code: /SCWM/V_TIMCBUST
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TIMCBUST
```

### 3727. `/SCWM/V_TIMCBWA`

```
T-Code: /SCWM/V_TIMCBWA
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TIMCBWA
```

### 3728. `/SCWM/V_TL006`

```
T-Code: /SCWM/V_TL006
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TL006
```

### 3729. `/SCWM/V_TLAST`

```
T-Code: /SCWM/V_TLAST
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TLAST
```

### 3730. `/SCWM/V_TLASTA`

```
T-Code: /SCWM/V_TLASTA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TLASTA
```

### 3731. `/SCWM/V_TMATPPRF`

```
T-Code: /SCWM/V_TMATPPRF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMATPPRF
```

### 3732. `/SCWM/V_TMFSTETO`

```
T-Code: /SCWM/V_TMFSTETO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMFSTETO
```

### 3733. `/SCWM/V_TPRCESA`

```
T-Code: /SCWM/V_TPRCESA
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TPRCESA
```

### 3734. `/SCWM/V_TQIOT`

```
T-Code: /SCWM/V_TQIOT
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SCWM/V_TQIOT
```

### 3735. `/SCWM/V_TWCRSORT`

```
T-Code: /SCWM/V_TWCRSORT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWCRSORT
```

### 3736. `/SCWM/V_TWHONR`

```
T-Code: /SCWM/V_TWHONR
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TWHONR
```

### 3737. `/SCWM/V_WV_DIMI`

```
T-Code: /SCWM/V_WV_DIMI
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WV_DIMI
```

### 3738. `/SCWM/V_WV_DIMV`

```
T-Code: /SCWM/V_WV_DIMV
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_WV_DIMV
```

### 3739. `/SCWM/WAVE`

```
T-Code: /SCWM/WAVE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO, SPC, WMR
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 2, 3
├── /SCWM/HU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/LG
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/OC
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/OC_A → 1, 2
    └── ACTVT → 4
├── /SCWM/POHU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → SP
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/TR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/TRAC → READ
    └── APO_TMOD → READ
├── /SCWM/VAS
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 43
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 2
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → 3, F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_APO_PROD
    ├── ACTVT → 3
    ├── APO_LOC → (empty — maintain in PFCG)
    └── APO_PROD → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_PPE_PAID
    ├── ACTVT → 3
    ├── ALTNUM → (empty — maintain in PFCG)
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PATY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PATYPE → (empty — maintain in PFCG)
├── C_PVS_PNID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    └── PRODCLASS → (empty — maintain in PFCG)
├── C_PVS_PNTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PNTYPE → (empty — maintain in PFCG)
├── C_PVS_PVID
    ├── ACTVT → 3
    ├── PNAME → (empty — maintain in PFCG)
    ├── PRODCLASS → (empty — maintain in PFCG)
    └── PVNAME → (empty — maintain in PFCG)
├── C_PVS_PVTY
    ├── ACTVT → 3
    ├── APPLOBJTYP → PAK
    └── PVTYPE → (empty — maintain in PFCG)
└── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3740. `/SCWM/WAVENUM`

```
T-Code: /SCWM/WAVENUM
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WAVE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 3741. `/SCWM/WAVETMP`

```
T-Code: /SCWM/WAVETMP
├── /SCWM/WAV1
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_TMPLT_TM, /SCWM/V_WV_TMPLT
```

### 3742. `/SCWM/WAVE_CHANGE_DLV_EXT`

```
T-Code: /SCWM/WAVE_CHANGE_DLV_EXT
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
└── /SCWM/WAV2
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2
```

### 3743. `/SCWM/WB_BM_DIST`

```
T-Code: /SCWM/WB_BM_DIST
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3744. `/SCWM/WB_BM_GEN`

```
T-Code: /SCWM/WB_BM_GEN
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3745. `/SCWM/WB_SNAP`

```
T-Code: /SCWM/WB_SNAP
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 2, 61
```

### 3746. `/SCWM/WB_SNAP_DEL`

```
T-Code: /SCWM/WB_SNAP_DEL
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3747. `/SCWM/WB_SNAP_INB`

```
T-Code: /SCWM/WB_SNAP_INB
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDI
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_GUI
    └── ACTVT → 2, 61
```

### 3748. `/SCWM/WB_SNAP_OUTB`

```
T-Code: /SCWM/WB_SNAP_OUTB
├── /SCWM/DLV2
    ├── /SCDL/CAT → FDO, PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3749. `/SCWM/WB_SNAP_SET`

```
T-Code: /SCWM/WB_SNAP_SET
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/WBSNAP_ACT
```

### 3750. `/SCWM/WB_SNAP_STOCK`

```
T-Code: /SCWM/WB_SNAP_STOCK
├── /SCWM/STB2
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/LGTY → $/SCWM/LGTY
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
```

### 3751. `/SCWM/WB_SNAP_WT`

```
T-Code: /SCWM/WB_SNAP_WT
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3752. `/SCWM/WB_WBMR_DEL`

```
T-Code: /SCWM/WB_WBMR_DEL
├── /SCWM/WBMR
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 6
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3753. `/SCWM/WDAS`

```
T-Code: /SCWM/WDAS
└── /SAPCND/CC
    ├── /SAPCND/AP → WME
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/US → WD
    └── ACTVT → 2
```

### 3754. `/SCWM/WDCT`

```
T-Code: /SCWM/WDCT
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V1_T685
```

### 3755. `/SCWM/WDFC`

```
T-Code: /SCWM/WDFC
└── /SAPCND/CO
    └── ACTVT → 2, 21, 6, 64
```

### 3756. `/SCWM/WDGCM`

```
T-Code: /SCWM/WDGCM
├── /SAPCND/CM
    ├── /SAPCND/AP → WME
    ├── /SAPCND/CT → (empty — maintain in PFCG)
    ├── /SAPCND/TY → (empty — maintain in PFCG)
    ├── /SAPCND/US → WD
    └── ACTVT → 1, 2, 3
├── /SCWM/WAV1
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
```

### 3757. `/SCWM/WDGRP`

```
T-Code: /SCWM/WDGRP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SAPCND/V_GROUP, /SAPCND/V_GROUPI
```

### 3758. `/SCWM/WDPRC`

```
T-Code: /SCWM/WDPRC
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SAPCND/V_CLISTI, /SAPCND/V_CTLIST
```

### 3759. `/SCWM/WDTB`

```
T-Code: /SCWM/WDTB
└── /SAPCND/CO
    └── ACTVT → 2, 6, 64
```

### 3760. `/SCWM/WHCOST`

```
T-Code: /SCWM/WHCOST
├── /SCWM/COST
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3761. `/SCWM/WHONUM`

```
T-Code: /SCWM/WHONUM
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WHO
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → NRIV
```

### 3762. `/SCWM/WHO_DE`

```
T-Code: /SCWM/WHO_DE
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_TWHO_DD
```

### 3763. `/SCWM/WIPNR`

```
T-Code: /SCWM/WIPNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SCWM/WIP
```

### 3764. `/SCWM/WIP_GP`

```
T-Code: /SCWM/WIP_GP
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VWIPDETGP
```

### 3765. `/SCWM/WIP_GP_A`

```
T-Code: /SCWM/WIP_GP_A
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/VWIPDETGPA
```

### 3766. `/SCWM/WIP_INBDOCTYPE`

```
T-Code: /SCWM/WIP_INBDOCTYPE
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 1, 2, 3, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/VWIPDETINB
```

### 3767. `/SCWM/WM_ADJUST`

```
T-Code: /SCWM/WM_ADJUST
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/DLV2
    ├── /SCDL/CAT → PDO
    ├── /SCDL/TYPE → (empty — maintain in PFCG)
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    ├── /SCWM/ORG → (empty — maintain in PFCG)
    └── ACTVT → 3
├── /SCWM/SLFE
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → VD
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_GRP
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 2
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W03
├── C_LIME_SI
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_TCLS_BER
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 23
    └── SICHT → (empty — maintain in PFCG)
```

### 3768. `/SCWM/WM_ANA`

```
T-Code: /SCWM/WM_ANA
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3769. `/SCWM/WM_BATCH_MAINT`

```
T-Code: /SCWM/WM_BATCH_MAINT
├── /SCWM/CHG
    ├── /SCWM/PROD → (empty — maintain in PFCG)
    └── ACTVT → 1, 3
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 230
├── C_TCLS_BER
    ├── KLART → 230
    └── SICHT → (empty — maintain in PFCG)
└── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
```

### 3770. `/SCWM/WOHULOG`

```
T-Code: /SCWM/WOHULOG
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3771. `/SCWM/WOLOG`

```
T-Code: /SCWM/WOLOG
├── /SCWM/VM
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 3
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SCWM/V_T340WHOL, /SCWM/V_TAREAWOL, /SCWM/V_TWCRWHOL
```

### 3772. `/SCWM/WZACT`

```
T-Code: /SCWM/WZACT
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3, 8
    └── TABLE → /SCWM/ZONE_ACTVT
```

### 3773. `/SCWM/WZCAL`

```
T-Code: /SCWM/WZCAL
├── /SCWM/SLFU
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── /SCWM/SLFU → TZ
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3, 8
    └── TABLE → /SCWM/ZONE_DIST
```

### 3774. `/SCWM/WZDEF`

```
T-Code: /SCWM/WZDEF
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → 2, 3, 8
    └── TABLE → /SCWM/V_ZONE, /SCWM/ZONE, /SCWM/ZONET
```

### 3775. `/SCWM/YMOVE`

```
T-Code: /SCWM/YMOVE
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCWM/SFUN
    └── /SCWM/SFUN → QC
├── /SCWM/TOHA
    ├── /SCWM/AREA → $/SCWM/AREA
    ├── /SCWM/ENTL → $/SCWM/ENTL
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → 2, 3, 31, 6
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
├── B_BUPA_RLT
    ├── ACTVT → 3
    └── RLTYP → (empty — maintain in PFCG)
├── C_LIME_DOC
    └── LIME_PRCTP → DELI, SCWM
├── C_LIME_HU
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
├── C_LIME_SI
    ├── ACTVT → 1, 10, 2, 3, 6
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01, W02, W03, W04
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 3, 32
```

### 3776. `/SCWM/YM_CHKPT_BIN`

```
T-Code: /SCWM/YM_CHKPT_BIN
├── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SCWM/V_YM_CHKLP
```

### 3777. `/SCWM/YM_DOOR_BIN`

```
T-Code: /SCWM/YM_DOOR_BIN
└── /SCWM/WHF4
    ├── /SCWM/LGNU → $/SCWM/LGNU
    └── ACTVT → F4
```

### 3778. `/SCWM/ZONE`

```
T-Code: /SCWM/ZONE
├── /SCMB/LOCB
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── /SCMB/SFUN
    └── /SCMB/SFUN → QC
├── /SCTM/RG
    └── ACTVT → 1, 2, 3, 6
├── /SCTM/SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 33
├── C_APO_LOC
    ├── ACTVT → 3
    └── APO_LOC → (empty — maintain in PFCG)
└── C_MD_SCU
    ├── /SCMB/SCU → (empty — maintain in PFCG)
    └── ACTVT → 1, 2, 32
```

## `/SDF/` — SAP System Diagnostics & Focused Run
> T-codes: **58**


### 3779. `/SDF/AJM_CLEANUP`

```
T-Code: /SDF/AJM_CLEANUP
└── /SDF/S_OPS
    └── ACTVT → 7
```

### 3780. `/SDF/AJM_DISP_BWPC`

```
T-Code: /SDF/AJM_DISP_BWPC
├── S_BTCH_ADM
    └── BTCADMIN → Y
└── S_RS_PC
    ├── ACTVT → 3
    ├── RSPCAPPLNM → (empty — maintain in PFCG)
    ├── RSPCCHAIN → (empty — maintain in PFCG)
    └── RSPCPART → DEFINITION
```

### 3781. `/SDF/AJM_JOB_META_DATA`

```
T-Code: /SDF/AJM_JOB_META_DATA
└── /SDF/E2E
    └── ACTVT → 3
```

### 3782. `/SDF/AJM_METADATA`

```
T-Code: /SDF/AJM_METADATA
└── /SDF/E2E
    └── ACTVT → 3
```

### 3783. `/SDF/AJM_RUNTIME`

```
T-Code: /SDF/AJM_RUNTIME
└── /SDF/E2E
    └── ACTVT → 3
```

### 3784. `/SDF/AJM_SHOW_SM37`

```
T-Code: /SDF/AJM_SHOW_SM37
├── S_ALV_LAYO
    └── ACTVT → 23
├── S_ALV_LAYR
    ├── ACTVT → 23
    ├── HANDLE → (empty — maintain in PFCG)
    ├── LOG_GROUP → (empty — maintain in PFCG)
    └── REPORT → SAPLBTCH
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
└── S_GUI
    └── ACTVT → 61
```

### 3785. `/SDF/ALM_DIAGNOSTIC`

```
T-Code: /SDF/ALM_DIAGNOSTIC
├── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
└── S_ICF
    ├── ICF_FIELD → DEST
    └── ICF_VALUE → ALM
```

### 3786. `/SDF/ALM_SETUP`

```
T-Code: /SDF/ALM_SETUP
├── /SDF/S_OPS
    └── ACTVT → 3, 7
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_ICF
    ├── ICF_FIELD → DEST
    └── ICF_VALUE → ALM
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SDF/CALM_AUTO_DISCOVERY, /SDF/CALM_CDM_TSK_IMPORT, /SDF/CALM_CDM_TSK_PROCESS, /SDF/CALM_HEARTBEAT, /SDF/CALM_SCHEDULER, /SDF/CRBPA_AUTODISCOVERY, /SDF/CRBPA_DC_CONTROLLER
├── S_PSE_ADM
    ├── ACTVT → 1
    ├── PSEAPPLIC → (empty — maintain in PFCG)
    └── PSECONTEXT → SS
├── S_RFC_ADM
    ├── ACTVT → 1, 3
    ├── ICF_VALUE → ALM
    ├── RFCDEST → (empty — maintain in PFCG)
    └── RFCTYPE → G
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → SCUS
```

### 3787. `/SDF/ASR_READ_AGGR_ONE_INST`

```
T-Code: /SDF/ASR_READ_AGGR_ONE_INST
└── S_RZL_ADM
    └── ACTVT → 3
```

### 3788. `/SDF/CALM_SETUP`

```
T-Code: /SDF/CALM_SETUP
├── /SDF/E2E
    └── ACTVT → 3
├── /SDF/S_OPS
    └── ACTVT → 3, 7
├── S_ICF
    ├── ICF_FIELD → DEST
    └── ICF_VALUE → ALM
└── S_RFC_ADM
    ├── ACTVT → 1, 2
    ├── ICF_VALUE → (empty — maintain in PFCG)
    ├── RFCDEST → (empty — maintain in PFCG)
    └── RFCTYPE → G
```

### 3789. `/SDF/CCA_COLLECT`

```
T-Code: /SDF/CCA_COLLECT
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0M, ST0R
└── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3790. `/SDF/CD_CCA`

```
T-Code: /SDF/CD_CCA
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
└── S_TOOLS_EX
    └── AUTH → S_TOOLS_EX_A
```

### 3791. `/SDF/COF_PERFORM_EXTR`

```
T-Code: /SDF/COF_PERFORM_EXTR
├── /SDF/E2E
    └── ACTVT → 3
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
└── S_RZL_ADM
    └── ACTVT → 3
```

### 3792. `/SDF/DUMP_EXTRACTOR`

```
T-Code: /SDF/DUMP_EXTRACTOR
└── /SDF/E2E
    └── ACTVT → 3
```

### 3793. `/SDF/E2EEM_GET_DATA`

```
T-Code: /SDF/E2EEM_GET_DATA
├── /SDF/E2E
    └── ACTVT → 3
└── S_SRT_ELOG
    └── ACTVT → 3
```

### 3794. `/SDF/E2EEM_GET_SELOPT_PARAM`

```
T-Code: /SDF/E2EEM_GET_SELOPT_PARAM
└── /SDF/E2E
    └── ACTVT → 3
```

### 3795. `/SDF/E2EEM_GET_VH_FOR_PARAM`

```
T-Code: /SDF/E2EEM_GET_VH_FOR_PARAM
└── /SDF/E2E
    └── ACTVT → 3
```

### 3796. `/SDF/E2E_DISPATCHED_COLLECTOR`

```
T-Code: /SDF/E2E_DISPATCHED_COLLECTOR
├── /SDF/E2E
    └── ACTVT → 3
├── S_ADMI_FCD
    └── S_ADMI_FCD → PADM, ST0R
├── S_BTCH_ADM
    └── BTCADMIN → Y
└── S_RZL_ADM
    └── ACTVT → 3
```

### 3797. `/SDF/E2E_EFWKE_DATA_CONNECTOR`

```
T-Code: /SDF/E2E_EFWKE_DATA_CONNECTOR
└── /SDF/E2E
    └── ACTVT → 3
```

### 3798. `/SDF/E2E_TRACE`

```
T-Code: /SDF/E2E_TRACE
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0M, ST0R
├── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → (empty — maintain in PFCG)
├── S_GUI
    └── ACTVT → 61
├── S_PERSONAS
    ├── ACTVT → 16
    ├── FLAVOR_ID → SAP_PERSONAS
    └── TCODE → /SDF/E2E_TRACE
└── S_TOOLS_EX
    └── AUTH → S_TOOLS_EX_A
```

### 3799. `/SDF/GET_ALL_ATRA_FILE_NAMES`

```
T-Code: /SDF/GET_ALL_ATRA_FILE_NAMES
└── /SDF/E2E
    └── ACTVT → 3
```

### 3800. `/SDF/GET_DUMP_LOG`

```
T-Code: /SDF/GET_DUMP_LOG
└── /SDF/E2E
    └── ACTVT → 3
```

### 3801. `/SDF/GET_GW_STATS`

```
T-Code: /SDF/GET_GW_STATS
└── /SDF/E2E
    └── ACTVT → 3
```

### 3802. `/SDF/GET_IM_MESSAGES`

```
T-Code: /SDF/GET_IM_MESSAGES
└── /SDF/S_OPS
    └── ACTVT → 3, 7
```

### 3803. `/SDF/GET_SQLM_DATA`

```
T-Code: /SDF/GET_SQLM_DATA
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3804. `/SDF/GET_SSR_FESR`

```
T-Code: /SDF/GET_SSR_FESR
└── /SDF/E2E
    └── ACTVT → 3
```

### 3805. `/SDF/GET_SYS_LOG_INST_EPP`

```
T-Code: /SDF/GET_SYS_LOG_INST_EPP
└── /SDF/E2E
    └── ACTVT → 3
```

### 3806. `/SDF/MAINT_QGROUP`

```
T-Code: /SDF/MAINT_QGROUP
└── /SDF/S_OPS
    └── ACTVT → 3, 7
```

### 3807. `/SDF/MON`

```
T-Code: /SDF/MON
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3808. `/SDF/MON_GET_ANALYSES`

```
T-Code: /SDF/MON_GET_ANALYSES
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3809. `/SDF/ORADLD`

```
T-Code: /SDF/ORADLD
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3810. `/SDF/PINS_SETUP`

```
T-Code: /SDF/PINS_SETUP
├── /SDF/S_OPS
    └── ACTVT → 3, 7
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_ICF
    ├── ICF_FIELD → DEST
    └── ICF_VALUE → ALM
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SDF/CALM_AUTO_DISCOVERY, /SDF/CALM_CDM_TSK_IMPORT, /SDF/CALM_CDM_TSK_PROCESS, /SDF/CALM_HEARTBEAT, /SDF/CALM_SCHEDULER, /SDF/CRBPA_AUTODISCOVERY, /SDF/CRBPA_DC_CONTROLLER
└── S_RFC_ADM
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ICF_VALUE → ALM
    ├── RFCDEST → (empty — maintain in PFCG)
    └── RFCTYPE → G
```

### 3811. `/SDF/SCMON`

```
T-Code: /SDF/SCMON
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0M, ST0R
└── S_BTCH_JOB
    ├── JOBACTION → DELE, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3812. `/SDF/SCMOND`

```
T-Code: /SDF/SCMOND
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3813. `/SDF/SCMONL`

```
T-Code: /SDF/SCMONL
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3814. `/SDF/SLG1`

```
T-Code: /SDF/SLG1
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → (empty — maintain in PFCG)
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 3815. `/SDF/SM`

```
T-Code: /SDF/SM
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3816. `/SDF/SMON`

```
T-Code: /SDF/SMON
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3817. `/SDF/SMON_ANALYSIS_DECLUSTER`

```
T-Code: /SDF/SMON_ANALYSIS_DECLUSTER
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3818. `/SDF/SMON_ANALYSIS_DECLUSTER_R`

```
T-Code: /SDF/SMON_ANALYSIS_DECLUSTER_R
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3819. `/SDF/SMON_ANALYSIS_READ`

```
T-Code: /SDF/SMON_ANALYSIS_READ
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3820. `/SDF/SMON_ANALYSIS_START`

```
T-Code: /SDF/SMON_ANALYSIS_START
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0M
```

### 3821. `/SDF/SMON_CALLSTACK_CLEANUP`

```
T-Code: /SDF/SMON_CALLSTACK_CLEANUP
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3822. `/SDF/SMON_CALLSTACK_TRIGGER`

```
T-Code: /SDF/SMON_CALLSTACK_TRIGGER
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3823. `/SDF/SMON_COLLECT_DATA`

```
T-Code: /SDF/SMON_COLLECT_DATA
├── S_ADMI_FCD
    └── S_ADMI_FCD → PADM, ST0R
└── S_TCODE
    └── TCD → SM04, SM50, SM51
```

### 3824. `/SDF/SMON_DISPLAY`

```
T-Code: /SDF/SMON_DISPLAY
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3825. `/SDF/SMON_START_CM`

```
T-Code: /SDF/SMON_START_CM
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3826. `/SDF/SMON_STOP_CM`

```
T-Code: /SDF/SMON_STOP_CM
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3827. `/SDF/SMON_WPINFO_AGGR`

```
T-Code: /SDF/SMON_WPINFO_AGGR
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3828. `/SDF/SQL`

```
T-Code: /SDF/SQL
├── S_ADMI_FCD
    └── S_ADMI_FCD → SQMD
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3829. `/SDF/SQLM`

```
T-Code: /SDF/SQLM
├── S_ADMI_FCD
    └── S_ADMI_FCD → SQMA, SQMD
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3830. `/SDF/ST02`

```
T-Code: /SDF/ST02
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3831. `/SDF/ST03DISP`

```
T-Code: /SDF/ST03DISP
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3832. `/SDF/ST03DL`

```
T-Code: /SDF/ST03DL
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 3833. `/SDF/ST10`

```
T-Code: /SDF/ST10
└── S_ADMI_FCD
    └── S_ADMI_FCD → (empty — maintain in PFCG)
```

### 3834. `/SDF/TRCHECK`

```
T-Code: /SDF/TRCHECK
├── S_ADMI_FCD
    └── S_ADMI_FCD → PADM
├── S_BTCH_ADM
    └── BTCADMIN → Y
├── S_BTCH_JOB
    ├── JOBACTION → LIST, PLAN, PROT, RELE, SHOW
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_CTS_ADMI
    └── CTS_ADMFCT → EPS2, TABL
├── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → SAPLSTRF
├── S_GUI
    └── ACTVT → 61
├── S_RFC
    ├── ACTVT → 16
    ├── RFC_NAME → /SDF/EWA, /SDF/SWCM_GT700, /SDF/TEAP_SATELLITE, CNV_CDMC_CC_FUNCTIONS, CNV_CDMC_CTS_FUNCTIONS, SCTS_TRDIR, SDTB, SRFC, STRH, SUSR, SVRL, SVRS, SVRS_COMPARE, SVRW, TR_OBJECTS
    └── RFC_TYPE → FUGR
├── S_RFC_ADM
    ├── ACTVT → 3
    ├── ICF_VALUE → (empty — maintain in PFCG)
    ├── RFCDEST → (empty — maintain in PFCG)
    └── RFCTYPE → (empty — maintain in PFCG)
├── S_SDCC
    ├── SDCC_DEV → READ
    └── SDCC_RUN → ADMIN, READ, WRITE
├── S_SDCC_ADD
    ├── SDCC_DEV_N → READ
    └── SDCC_RUN_N → ADMIN, READ, WRITE
└── S_TRANSPRT
    ├── ACTVT → 1, 2, 3
    └── TTYPE → PATC
```

### 3835. `/SDF/ZQLM`

```
T-Code: /SDF/ZQLM
├── S_ADMI_FCD
    └── S_ADMI_FCD → SQMA, SQMD
└── S_BTCH_JOB
    ├── JOBACTION → DELE, LIST, RELE
    └── JOBGROUP → (empty — maintain in PFCG)
```

### 3836. `/SDF/ZQLMD`

```
T-Code: /SDF/ZQLMD
└── S_ADMI_FCD
    └── S_ADMI_FCD → SQMD
```

## `/SEHS/` — SAP Environment Health & Safety (EHS)
> T-codes: **82**


### 3837. `/SEHS/05000011`

```
T-Code: /SEHS/05000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG41
```

### 3838. `/SEHS/17000033`

```
T-Code: /SEHS/17000033
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_091
```

### 3839. `/SEHS/17000034`

```
T-Code: /SEHS/17000034
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_095
```

### 3840. `/SEHS/17000035`

```
T-Code: /SEHS/17000035
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_093
```

### 3841. `/SEHS/17000036`

```
T-Code: /SEHS/17000036
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_101
```

### 3842. `/SEHS/17000037`

```
T-Code: /SEHS/17000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_099
```

### 3843. `/SEHS/17000038`

```
T-Code: /SEHS/17000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_097
```

### 3844. `/SEHS/17000039`

```
T-Code: /SEHS/17000039
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_103
```

### 3845. `/SEHS/17000040`

```
T-Code: /SEHS/17000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_105
```

### 3846. `/SEHS/72000090`

```
T-Code: /SEHS/72000090
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCGENV, ESV_TCGENVEXT
```

### 3847. `/SEHS/72000091`

```
T-Code: /SEHS/72000091
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCGUEE, ESV_TCGUFU
```

### 3848. `/SEHS/72000092`

```
T-Code: /SEHS/72000092
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCGUEPA
```

### 3849. `/SEHS/72000094`

```
T-Code: /SEHS/72000094
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG61, ESV_TCG63
```

### 3850. `/SEHS/72000095`

```
T-Code: /SEHS/72000095
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG65
```

### 3851. `/SEHS/72000096`

```
T-Code: /SEHS/72000096
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG67
```

### 3852. `/SEHS/72000097`

```
T-Code: /SEHS/72000097
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → HSMC_015, HSMV_011, HSMV_013
```

### 3853. `/SEHS/72000098`

```
T-Code: /SEHS/72000098
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → HSMV_017
```

### 3854. `/SEHS/72000099`

```
T-Code: /SEHS/72000099
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → HSMV_019
```

### 3855. `/SEHS/72000102`

```
T-Code: /SEHS/72000102
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_039
```

### 3856. `/SEHS/72000103`

```
T-Code: /SEHS/72000103
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG91, ESV_TCG93
```

### 3857. `/SEHS/72000104`

```
T-Code: /SEHS/72000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_151
```

### 3858. `/SEHS/72000105`

```
T-Code: /SEHS/72000105
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG153
```

### 3859. `/SEHS/72000106`

```
T-Code: /SEHS/72000106
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_063
```

### 3860. `/SEHS/72000107`

```
T-Code: /SEHS/72000107
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_009, THM_011
```

### 3861. `/SEHS/72000108`

```
T-Code: /SEHS/72000108
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_159
```

### 3862. `/SEHS/72000109`

```
T-Code: /SEHS/72000109
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_003
```

### 3863. `/SEHS/72000110`

```
T-Code: /SEHS/72000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_031
```

### 3864. `/SEHS/72000112`

```
T-Code: /SEHS/72000112
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_075
```

### 3865. `/SEHS/72000113`

```
T-Code: /SEHS/72000113
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_073
```

### 3866. `/SEHS/72000114`

```
T-Code: /SEHS/72000114
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → THM_157
```

### 3867. `/SEHS/72000115`

```
T-Code: /SEHS/72000115
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG73
```

### 3868. `/SEHS/72000116`

```
T-Code: /SEHS/72000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG29
```

### 3869. `/SEHS/72000117`

```
T-Code: /SEHS/72000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG15
```

### 3870. `/SEHS/72000118`

```
T-Code: /SEHS/72000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG35
```

### 3871. `/SEHS/72000119`

```
T-Code: /SEHS/72000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG37
```

### 3872. `/SEHS/72000123`

```
T-Code: /SEHS/72000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDG81
```

### 3873. `/SEHS/72000124`

```
T-Code: /SEHS/72000124
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → CCUNV111
```

### 3874. `/SEHS/72000125`

```
T-Code: /SEHS/72000125
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → CCUNV117
```

### 3875. `/SEHS/72000126`

```
T-Code: /SEHS/72000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TVTR_COM
```

### 3876. `/SEHS/72000127`

```
T-Code: /SEHS/72000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDGD1, DGV_TDGD3
```

### 3877. `/SEHS/72000128`

```
T-Code: /SEHS/72000128
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → DGV_TDGD5, DGV_TDGD7
```

### 3878. `/SEHS/72000129`

```
T-Code: /SEHS/72000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_001
```

### 3879. `/SEHS/72000130`

```
T-Code: /SEHS/72000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_115
```

### 3880. `/SEHS/72000147`

```
T-Code: /SEHS/72000147
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_117
```

### 3881. `/SEHS/72000148`

```
T-Code: /SEHS/72000148
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_201, /SEHS/DGV_203
```

### 3882. `/SEHS/72000149`

```
T-Code: /SEHS/72000149
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCGUEE, ESV_TCGUFU
```

### 3883. `/SEHS/72000150`

```
T-Code: /SEHS/72000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_221
```

### 3884. `/SEHS/72000151`

```
T-Code: /SEHS/72000151
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_049
```

### 3885. `/SEHS/72000158`

```
T-Code: /SEHS/72000158
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG89
```

### 3886. `/SEHS/72000159`

```
T-Code: /SEHS/72000159
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → ESV_TCG91, ESV_TCG93
```

### 3887. `/SEHS/72000164`

```
T-Code: /SEHS/72000164
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SEHS/DGV_041
```

### 3888. `/SEHS/BAPI_BUS1091_CHANGE`

```
T-Code: /SEHS/BAPI_BUS1091_CHANGE
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3889. `/SEHS/BAPI_BUS1091_CREATE`

```
T-Code: /SEHS/BAPI_BUS1091_CREATE
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3890. `/SEHS/BAPI_BUS1091_DELETE`

```
T-Code: /SEHS/BAPI_BUS1091_DELETE
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3891. `/SEHS/BAPI_BUS1091_GETDETAIL`

```
T-Code: /SEHS/BAPI_BUS1091_GETDETAIL
└── C_EHSP_TPP
    ├── ACTVT → 3
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3892. `/SEHS/BAPI_BUS1091_GETLIST`

```
T-Code: /SEHS/BAPI_BUS1091_GETLIST
└── C_EHSP_TPP
    ├── ACTVT → 3
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3893. `/SEHS/BAPI_BUS1091_REPLICATE`

```
T-Code: /SEHS/BAPI_BUS1091_REPLICATE
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3894. `/SEHS/BAPI_BUS1091_SAVE_BUFFER`

```
T-Code: /SEHS/BAPI_BUS1091_SAVE_BUFFER
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3895. `/SEHS/BAPI_BUS1091_SAVREPMUL`

```
T-Code: /SEHS/BAPI_BUS1091_SAVREPMUL
└── C_EHSP_TPP
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    ├── ESEPHRGRP → (empty — maintain in PFCG)
    ├── LANGUAGE → (empty — maintain in PFCG)
    └── PPSTATE → (empty — maintain in PFCG)
```

### 3896. `/SEHS/BAPI_DANGEROUSGOOD_SAVRM`

```
T-Code: /SEHS/BAPI_DANGEROUSGOOD_SAVRM
└── M_MATE_DGM
    └── ACTVT → 1, 2, 3, 6
```

### 3897. `/SEHS/BAPI_HM1119_SAVE`

```
T-Code: /SEHS/BAPI_HM1119_SAVE
└── HSMA
    ├── ACTVT → 1, 2, 3, 6
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3898. `/SEHS/CGC1`

```
T-Code: /SEHS/CGC1
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → CSHE
```

### 3899. `/SEHS/DGD01`

```
T-Code: /SEHS/DGD01
└── /SEHS/DGAP
    └── ACTVT → 1, 2, 3, 6
```

### 3900. `/SEHS/DGD02`

```
T-Code: /SEHS/DGD02
└── /SEHS/DGAP
    └── ACTVT → 1, 2, 3, 6
```

### 3901. `/SEHS/DGD03`

```
T-Code: /SEHS/DGD03
└── /SEHS/DGAP
    └── ACTVT → 3
```

### 3902. `/SEHS/DGD04`

```
T-Code: /SEHS/DGD04
└── /SEHS/DGAP
    └── ACTVT → 3
```

### 3903. `/SEHS/DGR02`

```
T-Code: /SEHS/DGR02
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SEHS/ALE
    └── ALG_SUBOBJ → DG
```

### 3904. `/SEHS/DGR03`

```
T-Code: /SEHS/DGR03
└── S_APPL_LOG
    ├── ACTVT → 6
    ├── ALG_OBJECT → /SEHS/ALE, /SEHS/DG
    └── ALG_SUBOBJ → DG
```

### 3905. `/SEHS/DGR04`

```
T-Code: /SEHS/DGR04
├── C_SHEP_TPG
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    └── ESEPHRGRP → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → CSHE
```

### 3906. `/SEHS/DGR05`

```
T-Code: /SEHS/DGR05
├── C_SHEP_TPG
    ├── ACTVT → 1, 2, 3
    ├── ESECATPIN → (empty — maintain in PFCG)
    └── ESEPHRGRP → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 3907. `/SEHS/HM1119_CREATE`

```
T-Code: /SEHS/HM1119_CREATE
└── HSMA
    ├── ACTVT → 1
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3908. `/SEHS/HM1119_DELETE`

```
T-Code: /SEHS/HM1119_DELETE
└── HSMA
    ├── ACTVT → 6
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3909. `/SEHS/HM1119_GETDETAIL`

```
T-Code: /SEHS/HM1119_GETDETAIL
└── HSMA
    ├── ACTVT → 3
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3910. `/SEHS/HMD`

```
T-Code: /SEHS/HMD
└── HSMA
    ├── ACTVT → 3
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
```

### 3911. `/SEHS/HMR02`

```
T-Code: /SEHS/HMR02
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SEHS/ALE
    └── ALG_SUBOBJ → HM
```

### 3912. `/SEHS/HMR03`

```
T-Code: /SEHS/HMR03
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SEHS/ALE
    └── ALG_SUBOBJ → HM
```

### 3913. `/SEHS/HMR04`

```
T-Code: /SEHS/HMR04
├── C_SHEP_TPG
    ├── ACTVT → 2
    ├── ESECATPIN → (empty — maintain in PFCG)
    └── ESEPHRGRP → (empty — maintain in PFCG)
├── HSMA
    ├── ACTVT → 2
    ├── COUNTRY → (empty — maintain in PFCG)
    └── REGION → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 3914. `/SEHS/HMR05`

```
T-Code: /SEHS/HMR05
├── C_SHEP_TPG
    ├── ACTVT → 1, 3
    ├── ESECATPIN → (empty — maintain in PFCG)
    └── ESEPHRGRP → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → (empty — maintain in PFCG)
```

### 3915. `/SEHS/PHC05`

```
T-Code: /SEHS/PHC05
└── S_NUMBER
    ├── ACTVT → 11, 13, 17, 2, 3
    └── NROBJ → ESN_PHRID
```

### 3916. `/SEHS/PHC06`

```
T-Code: /SEHS/PHC06
├── C_SHEP_TPG
    ├── ACTVT → 1, 2, 3
    ├── ESECATPIN → (empty — maintain in PFCG)
    └── ESEPHRGRP → (empty — maintain in PFCG)
└── S_TABU_DIS
    ├── ACTVT → 2
    └── DICBERCLS → CSHE
```

### 3917. `/SEHS/PHC07`

```
T-Code: /SEHS/PHC07
└── S_TABU_DIS
    ├── ACTVT → 2, 3, BD
    └── DICBERCLS → CSHE
```

### 3918. `/SEHS/PHD23`

```
T-Code: /SEHS/PHD23
└── C_SHEG_GEN
    ├── ACTVT → 3
    └── TABLE → ESTPS
```

## `/SHCM/` — SAP Healthcare Management
> T-codes: **14**


### 3919. `/SHCM/52000194`

```
T-Code: /SHCM/52000194
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/RECON
```

### 3920. `/SHCM/52000897`

```
T-Code: /SHCM/52000897
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → PTRV_VENDOR_SYNC
```

### 3921. `/SHCM/52001185`

```
T-Code: /SHCM/52001185
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_001P_N_ALL
```

### 3922. `/SHCM/52002168`

```
T-Code: /SHCM/52002168
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/T77MDS_CL
```

### 3923. `/SHCM/52002169`

```
T-Code: /SHCM/52002169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_WFD_D_COMMDETS
```

### 3924. `/SHCM/52002170`

```
T-Code: /SHCM/52002170
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/T77MDS_CM
```

### 3925. `/SHCM/52002171`

```
T-Code: /SHCM/52002171
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/T77MDS_MAP
```

### 3926. `/SHCM/52002173`

```
T-Code: /SHCM/52002173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/T77MDS_STG
```

### 3927. `/SHCM/52002187`

```
T-Code: /SHCM/52002187
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_FAP_H2R_PM_MAP
```

### 3928. `/SHCM/52002256`

```
T-Code: /SHCM/52002256
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_001P_N_ALL
```

### 3929. `/SHCM/52002485`

```
T-Code: /SHCM/52002485
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SHCM/V_77MDS_ST
```

### 3930. `/SHCM/BUPA_SYNC_S`

```
T-Code: /SHCM/BUPA_SYNC_S
└── P_ORGIN
    ├── AUTHC → R
    ├── INFTY → 0, 1, 105, 2, 6, 9
    ├── PERSA → (empty — maintain in PFCG)
    ├── PERSG → (empty — maintain in PFCG)
    ├── PERSK → (empty — maintain in PFCG)
    ├── SUBTY → (empty — maintain in PFCG)
    └── VDSK1 → (empty — maintain in PFCG)
```

### 3931. `/SHCM/CHANGE_LOGS`

```
T-Code: /SHCM/CHANGE_LOGS
├── B_BUPA_GRP
    ├── ACTVT → 3, F4
    └── BEGRU → (empty — maintain in PFCG)
├── P_ORGIN
    ├── AUTHC → M, R
    ├── INFTY → (empty — maintain in PFCG)
    ├── PERSA → (empty — maintain in PFCG)
    ├── PERSG → (empty — maintain in PFCG)
    ├── PERSK → (empty — maintain in PFCG)
    ├── SUBTY → (empty — maintain in PFCG)
    └── VDSK1 → (empty — maintain in PFCG)
└── S_PROGRAM
    ├── P_ACTION → SUBMIT
    └── P_GROUP → RPUAUD00
```

### 3932. `/SHCM/RPUDELPP`

```
T-Code: /SHCM/RPUDELPP
└── S_WFAR_OBJ
    ├── ACTVT → 3, 6
    ├── OAARCHIV → A2
    ├── OADOKUMENT → HRICOLFOTO
    └── OAOBJEKTE → PREL
```

## `/SMB/` — SAP SMB Solutions
> T-codes: **33**


### 3933. `/SMB/ADMIN_GUIDE`

```
T-Code: /SMB/ADMIN_GUIDE
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /SMB/V_ADMIN_G_M
```

### 3934. `/SMB/AFTER_IMPORT`

```
T-Code: /SMB/AFTER_IMPORT
├── /SMB/ACTV
    └── ACTVT → 2
├── /SMB/PRTR
    └── ACTVT → 2
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_BTCH_NA1
    ├── BTCUNAME → (empty — maintain in PFCG)
    └── PROGRAM → /SMB/START_ACTIVATION
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SMB/ANALYZE_TDC_VARIANT_IDS, /SMB/ANALYZE_XX_BB_INCONS, /SMB/START_ACTIVATION
└── S_SMB_ACTV
    ├── ACTVT → 2
    └── ATYPE → R
```

### 3935. `/SMB/BBI`

```
T-Code: /SMB/BBI
├── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /SMB/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
├── S_BTCH_NA1
    ├── BTCUNAME → (empty — maintain in PFCG)
    └── PROGRAM → /SMB/Q2P_REPORT_DISPATCHER, /SMB/START_ACTIVATION
├── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SMB/START_ACTIVATION, RBANK_PROC_END, RBANK_PROC_START
└── S_SMB_ADM
    ├── ACTVT → 2
    └── SMB_ADMIN → PROC, SMGR
```

### 3936. `/SMB/BCPCOMPDIS`

```
T-Code: /SMB/BCPCOMPDIS
├── S_TABU_CLI
    └── CLIIDMAINT → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 3937. `/SMB/CCF`

```
T-Code: /SMB/CCF
├── S_BTCH_JOB
    ├── JOBACTION → RELE
    └── JOBGROUP → (empty — maintain in PFCG)
├── S_DATASET
    ├── ACTVT → 33, 34
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → CL_GUI_ABAPEDIT===============CP, CL_SEDI_TEMPORARY_STORAGE=====CP, SAPLSLOG, SAPLSTRF
└── S_PROGNAM
    ├── P_ACTION → BTCSUBMIT
    └── P_PROGNAM → /SMB/CCF_BG
```

### 3938. `/SMB/CDUI_MAINT_TF`

```
T-Code: /SMB/CDUI_MAINT_TF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/VM_CUST_SOL, /SMB/VM_SCOPE_SR
```

### 3939. `/SMB/CD_MAINT`

```
T-Code: /SMB/CD_MAINT
└── S_SMB_AUTH
    └── ACTVT → 2
```

### 3940. `/SMB/CR_METADATA`

```
T-Code: /SMB/CR_METADATA
├── S_SMB_ADM
    ├── ACTVT → 2, 3
    └── SMB_ADMIN → RSTR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/VCR_REF_CHG, /SMB/V_CR_FILE, /SMB/V_CR_HEAD, /SMB/V_CR_PARAM, /SMB/V_CR_PAR_AD, /SMB/V_CR_VAR
```

### 3941. `/SMB/FILE_STATE`

```
T-Code: /SMB/FILE_STATE
└── S_SMB_AUTH
    └── ACTVT → 2
```

### 3942. `/SMB/FIND_BCSET`

```
T-Code: /SMB/FIND_BCSET
└── S_BCSETS
    └── ACTVT → 3
```

### 3943. `/SMB/FIX_TDC_ENTRIES`

```
T-Code: /SMB/FIX_TDC_ENTRIES
└── S_SMB_AUTH
    └── ACTVT → 2
```

### 3944. `/SMB/GMD_ADMIN`

```
T-Code: /SMB/GMD_ADMIN
└── S_SMBGMDAD
    ├── /SMB/GMD → /SMB/GMD_ADMIN
    └── ACTVT → 3
```

### 3945. `/SMB/GMD_ADMIN_CST`

```
T-Code: /SMB/GMD_ADMIN_CST
├── S_SMBGMDAD
    ├── /SMB/GMD → /SMB/GMD_ADMIN_CST
    └── ACTVT → 3
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/V_GMDOPMODE, /SMB/V_GMDRFCDST
```

### 3946. `/SMB/GMD_REFRESH`

```
T-Code: /SMB/GMD_REFRESH
└── S_SMBGMDAD
    ├── /SMB/GMD → /SMB/GMD_REFRESH
    └── ACTVT → 2
```

### 3947. `/SMB/GMD_SYNC`

```
T-Code: /SMB/GMD_SYNC
└── S_SMBGMDAD
    ├── /SMB/GMD → /SMB/GMD_SYNC
    └── ACTVT → 2
```

### 3948. `/SMB/MAINTAIN_SCOPE`

```
T-Code: /SMB/MAINTAIN_SCOPE
└── S_SMB_ACTV
    ├── ACTVT → 2
    └── ATYPE → (empty — maintain in PFCG)
```

### 3949. `/SMB/MAINT_MIGR_OBJ`

```
T-Code: /SMB/MAINT_MIGR_OBJ
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/MIGR_OBJ
```

### 3950. `/SMB/PROC_4_LC`

```
T-Code: /SMB/PROC_4_LC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/TI_PRC_4_LC
```

### 3951. `/SMB/Q2P_EMAIL`

```
T-Code: /SMB/Q2P_EMAIL
├── /SMB/Q2P
    └── ACTVT → 16
└── S_SMB_ADM
    ├── ACTVT → 2
    └── SMB_ADMIN → SEML
```

### 3952. `/SMB/Q2P_ERROR`

```
T-Code: /SMB/Q2P_ERROR
└── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SMB/Q2P_MANAGE_FK_CHECK_ERROR
```

### 3953. `/SMB/Q2P_FKC`

```
T-Code: /SMB/Q2P_FKC
└── S_SMB_AUTH
    └── ACTVT → 3
```

### 3954. `/SMB/Q2P_FK_SRC`

```
T-Code: /SMB/Q2P_FK_SRC
└── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SMB/Q2P_UPDATE_FKERROR_SRC
```

### 3955. `/SMB/Q2P_PFILE`

```
T-Code: /SMB/Q2P_PFILE
└── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SMB/Q2P_FIND_FILES_FOR_PARAMS
```

### 3956. `/SMB/Q2P_RUN_CHECK`

```
T-Code: /SMB/Q2P_RUN_CHECK
└── S_SMB_AUTH
    └── ACTVT → 3
```

### 3957. `/SMB/Q2P_UPD_FKERR`

```
T-Code: /SMB/Q2P_UPD_FKERR
└── S_PROGNAM
    ├── P_ACTION → SUBMIT
    └── P_PROGNAM → /SMB/Q2P_UPDATE_FKERROR_INT
```

### 3958. `/SMB/REMOVE_ACT_STAT`

```
T-Code: /SMB/REMOVE_ACT_STAT
└── S_SMB_ACTV
    ├── ACTVT → 2
    └── ATYPE → (empty — maintain in PFCG)
```

### 3959. `/SMB/SBA1_ASSI_MAIN`

```
T-Code: /SMB/SBA1_ASSI_MAIN
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SMB/SBA1_V_ASMT
```

### 3960. `/SMB/SBA1_META_MAIN`

```
T-Code: /SMB/SBA1_META_MAIN
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /SMB/SBA1_V_META
```

### 3961. `/SMB/SCOPE`

```
T-Code: /SMB/SCOPE
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/VM_CUST_SOL, /SMB/VM_SCOPE_SR
```

### 3962. `/SMB/SOL_4_LC`

```
T-Code: /SMB/SOL_4_LC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/TI_SOL_4_LC
```

### 3963. `/SMB/TRANS_LOG`

```
T-Code: /SMB/TRANS_LOG
└── S_APPL_LOG
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ALG_OBJECT → /SMB/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 3964. `/SMB/V_WL_ACT`

```
T-Code: /SMB/V_WL_ACT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMB/V_WL_ACT
```

### 3965. `/SMB/XP_CR_RT_VA_DEL`

```
T-Code: /SMB/XP_CR_RT_VA_DEL
└── S_APPL_LOG
    ├── ACTVT → (empty — maintain in PFCG)
    ├── ALG_OBJECT → /SMB/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

## `/SMFND/` — SAP Service Management Foundation
> T-codes: **10**


### 3966. `/SMFND/CFG_4MAGENT`

```
T-Code: /SMFND/CFG_4MAGENT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SMFND/C_DFA01, /SMFND/C_DFA01A, /SMFND/C_DFA01B, /SMFND/C_DFA01E, /SMFND/C_DFA01T, /SMFND/C_DFA02I, /SMFND/C_DFA02X, /SMFND/C_DFA03D, /SMFND/C_DFA03M
```

### 3967. `/SMFND/CFG_OBJASGMNT`

```
T-Code: /SMFND/CFG_OBJASGMNT
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/CA000G
```

### 3968. `/SMFND/DS1_NR`

```
T-Code: /SMFND/DS1_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SMFND/DS1
```

### 3969. `/SMFND/DSF_LOADER`

```
T-Code: /SMFND/DSF_LOADER
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 16
```

### 3970. `/SMFND/IBQ_PROCESS`

```
T-Code: /SMFND/IBQ_PROCESS
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 16
```

### 3971. `/SMFND/IBQ_PURGE`

```
T-Code: /SMFND/IBQ_PURGE
└── S_MI_CCMS
    └── ACTVT → 6
```

### 3972. `/SMFND/IQ1_NR`

```
T-Code: /SMFND/IQ1_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SMFND/IQ1
```

### 3973. `/SMFND/MAPP_MIGRATE`

```
T-Code: /SMFND/MAPP_MIGRATE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 16
```

### 3974. `/SMFND/SY1_NR`

```
T-Code: /SMFND/SY1_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SMFND/SY1
```

### 3975. `/SMFND/V3_RUN`

```
T-Code: /SMFND/V3_RUN
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 16
```

## `/SOMO/` — SAP Social Media Order Management
> T-codes: **1**


### 3976. `/SOMO/MA16`

```
T-Code: /SOMO/MA16
└── S_PERSONAS
    ├── ACTVT → (empty — maintain in PFCG)
    ├── FLAVOR_ID → (empty — maintain in PFCG)
    └── TCODE → /SOMO/MA16
```

## `/SPE/` — SAP Shipment Processing & Event Management
> T-codes: **115**


### 3977. `/SPE/18000117`

```
T-Code: /SPE/18000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T001L_L
```

### 3978. `/SPE/23000636`

```
T-Code: /SPE/23000636
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T340_E
```

### 3979. `/SPE/23000637`

```
T-Code: /SPE/23000637
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_WME_QUEUE
```

### 3980. `/SPE/23000694`

```
T-Code: /SPE/23000694
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_QUEUE_LOG
```

### 3981. `/SPE/23000701`

```
T-Code: /SPE/23000701
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TVUVF_VAL, /SPE/V_TVUV_VAL
```

### 3982. `/SPE/23000703`

```
T-Code: /SPE/23000703
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/VAL_PROFL, /SPE/V_VAL_ROUT
```

### 3983. `/SPE/23000704`

```
T-Code: /SPE/23000704
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/VAL_CONTROL
```

### 3984. `/SPE/23000706`

```
T-Code: /SPE/23000706
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_CD_STKEEP
```

### 3985. `/SPE/24000094`

```
T-Code: /SPE/24000094
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_CTRL_SW
```

### 3986. `/SPE/36000401`

```
T-Code: /SPE/36000401
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TVSHP_HU
```

### 3987. `/SPE/36000439`

```
T-Code: /SPE/36000439
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T163Q
```

### 3988. `/SPE/40000043`

```
T-Code: /SPE/40000043
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_DIRDLV_TY
```

### 3989. `/SPE/40000044`

```
T-Code: /SPE/40000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_DIRITM_CA
```

### 3990. `/SPE/40000117`

```
T-Code: /SPE/40000117
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_HUSTAT
```

### 3991. `/SPE/40000119`

```
T-Code: /SPE/40000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/INB_V_TVLK
```

### 3992. `/SPE/40000122`

```
T-Code: /SPE/40000122
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_RET_SPLIP
```

### 3993. `/SPE/40000123`

```
T-Code: /SPE/40000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_HU_UPDATE
```

### 3994. `/SPE/40000125`

```
T-Code: /SPE/40000125
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_RET_EXC_C
```

### 3995. `/SPE/40000126`

```
T-Code: /SPE/40000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_DLV_BWA
```

### 3996. `/SPE/40000127`

```
T-Code: /SPE/40000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T001L_L
```

### 3997. `/SPE/40000155`

```
T-Code: /SPE/40000155
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T352B_B, V_T352C_B
```

### 3998. `/SPE/40000156`

```
T-Code: /SPE/40000156
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/RET_FOLLOW
```

### 3999. `/SPE/40000157`

```
T-Code: /SPE/40000157
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/RET_CATPROF
```

### 4000. `/SPE/40000166`

```
T-Code: /SPE/40000166
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/IBGI_IND
```

### 4001. `/SPE/40000167`

```
T-Code: /SPE/40000167
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/GI_RVRS_INV
```

### 4002. `/SPE/40000168`

```
T-Code: /SPE/40000168
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T3211
```

### 4003. `/SPE/40000307`

```
T-Code: /SPE/40000307
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_INBDLV_TY
```

### 4004. `/SPE/40000332`

```
T-Code: /SPE/40000332
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_KIT_STRUC
```

### 4005. `/SPE/40000333`

```
T-Code: /SPE/40000333
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/RET_SD_RMA
```

### 4006. `/SPE/40000371`

```
T-Code: /SPE/40000371
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/CD_SOURCE
```

### 4007. `/SPE/40000464`

```
T-Code: /SPE/40000464
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TVSHP
```

### 4008. `/SPE/40000653`

```
T-Code: /SPE/40000653
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_IBGI_PRPR
```

### 4009. `/SPE/52000462`

```
T-Code: /SPE/52000462
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_DLV_BWA
```

### 4010. `/SPE/52000481`

```
T-Code: /SPE/52000481
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_T352B_B, V_T352C_B
```

### 4011. `/SPE/52000512`

```
T-Code: /SPE/52000512
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_CD_STKEEP
```

### 4012. `/SPE/52000515`

```
T-Code: /SPE/52000515
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/RET_CATPROF
```

### 4013. `/SPE/52000516`

```
T-Code: /SPE/52000516
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/RET_FOLLOW
```

### 4014. `/SPE/52000526`

```
T-Code: /SPE/52000526
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/CTRL_SW
```

### 4015. `/SPE/52000529`

```
T-Code: /SPE/52000529
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_QUEUE_LOG
```

### 4016. `/SPE/52000539`

```
T-Code: /SPE/52000539
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_HUSTAT
```

### 4017. `/SPE/52000691`

```
T-Code: /SPE/52000691
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T161V
```

### 4018. `/SPE/52000715`

```
T-Code: /SPE/52000715
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_DIALOG_CONFIG
```

### 4019. `/SPE/52000716`

```
T-Code: /SPE/52000716
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_DIALOG_FILTER
```

### 4020. `/SPE/52000717`

```
T-Code: /SPE/52000717
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_BOR_PRO_COL2, V_BOR_PRO_COLUMN, V_BOR_PRO_COL_H
```

### 4021. `/SPE/52000718`

```
T-Code: /SPE/52000718
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_BOR_PROF_SEL, V_BOR_PROF_SELH
```

### 4022. `/SPE/52000719`

```
T-Code: /SPE/52000719
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_BOR_PROF_OBJ, V_BOR_PROF_OBJ_H
```

### 4023. `/SPE/52000720`

```
T-Code: /SPE/52000720
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_BOR_FC_PROF, V_BOR_FC_PROF_H
```

### 4024. `/SPE/52000722`

```
T-Code: /SPE/52000722
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_BOR_PROFILE
```

### 4025. `/SPE/52000787`

```
T-Code: /SPE/52000787
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TPOP_MAIL
```

### 4026. `/SPE/52000789`

```
T-Code: /SPE/52000789
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_RET_EXC_C
```

### 4027. `/SPE/52000809`

```
T-Code: /SPE/52000809
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MDLV, V_MDLG
```

### 4028. `/SPE/52000824`

```
T-Code: /SPE/52000824
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_KIT_GR
```

### 4029. `/SPE/52000838`

```
T-Code: /SPE/52000838
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_POD_STL
```

### 4030. `/SPE/52000839`

```
T-Code: /SPE/52000839
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_EXCP_REAS
```

### 4031. `/SPE/52000870`

```
T-Code: /SPE/52000870
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_POD_STL
```

### 4032. `/SPE/52000871`

```
T-Code: /SPE/52000871
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_EXCP_REAS
```

### 4033. `/SPE/52001226`

```
T-Code: /SPE/52001226
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T340D_EWM
```

### 4034. `/SPE/52001985`

```
T-Code: /SPE/52001985
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_EWM_DEST
```

### 4035. `/SPE/67000017`

```
T-Code: /SPE/67000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/CD_SOURCE
```

### 4036. `/SPE/67000097`

```
T-Code: /SPE/67000097
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/T_SRCSTKTYP
```

### 4037. `/SPE/79000012`

```
T-Code: /SPE/79000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/VAL_PROFL, /SPE/V_VAL_ROUT
```

### 4038. `/SPE/79000013`

```
T-Code: /SPE/79000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TVUVF_VAL, /SPE/V_TVUV_VAL
```

### 4039. `/SPE/79000014`

```
T-Code: /SPE/79000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/VAL_CONTROL
```

### 4040. `/SPE/79000017`

```
T-Code: /SPE/79000017
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TDELVAUTO
```

### 4041. `/SPE/79000018`

```
T-Code: /SPE/79000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_WME_QUEUE
```

### 4042. `/SPE/79000020`

```
T-Code: /SPE/79000020
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/CONS_V_STKT
```

### 4043. `/SPE/79000025`

```
T-Code: /SPE/79000025
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_HU_UPDATE
```

### 4044. `/SPE/79000027`

```
T-Code: /SPE/79000027
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_SHP_PNT
```

### 4045. `/SPE/79000028`

```
T-Code: /SPE/79000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_RET_SPLIP
```

### 4046. `/SPE/79000031`

```
T-Code: /SPE/79000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_LOC_SEQ
```

### 4047. `/SPE/79000037`

```
T-Code: /SPE/79000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MSG_RULES
```

### 4048. `/SPE/79000042`

```
T-Code: /SPE/79000042
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T186C
```

### 4049. `/SPE/79000044`

```
T-Code: /SPE/79000044
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/INB_V_TVLK
```

### 4050. `/SPE/79000045`

```
T-Code: /SPE/79000045
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/INB_OFFSET
```

### 4051. `/SPE/79000047`

```
T-Code: /SPE/79000047
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_TVLPAIP
```

### 4052. `/SPE/79000052`

```
T-Code: /SPE/79000052
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/OUTB_V_TVLK
```

### 4053. `/SPE/94000097`

```
T-Code: /SPE/94000097
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_PP_DLV_TY
```

### 4054. `/SPE/98000028`

```
T-Code: /SPE/98000028
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T340_VL60
```

### 4055. `/SPE/98000130`

```
T-Code: /SPE/98000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCWQTOLGR
```

### 4056. `/SPE/98000131`

```
T-Code: /SPE/98000131
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → TCWQPROC
```

### 4057. `/SPE/AAC_DETERMINATION`

```
T-Code: /SPE/AAC_DETERMINATION
└── /SPE/AAC
    ├── /SPE/AAC → (empty — maintain in PFCG)
    └── ACTVT → 16
```

### 4058. `/SPE/ASN01`

```
T-Code: /SPE/ASN01
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_KNA1_APP
    ├── ACTVT → F4
    └── APPKZ → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
├── F_KNA1_GRP
    ├── ACTVT → F4
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
├── F_LFA1_GEN
    └── ACTVT → F4
├── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
├── M_BEST_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_LFM1_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_STA
    ├── ACTVT → 8
    └── STATM → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
└── SPERETMON
    └── /SPE/MOSEL → ' ', 1, 2, 3
```

### 4059. `/SPE/ASN02`

```
T-Code: /SPE/ASN02
└── SPERETMON
    └── /SPE/MOSEL → 1
```

### 4060. `/SPE/BATCH_REPLICATE`

```
T-Code: /SPE/BATCH_REPLICATE
├── C_KLAH_BKL
    ├── ACTVT → 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 22
├── C_TCLS_BER
    ├── KLART → 22
    └── SICHT → (empty — maintain in PFCG)
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22
    └── SICHT → (empty — maintain in PFCG)
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 4061. `/SPE/BATCH_SAVE_REPLICA`

```
T-Code: /SPE/BATCH_SAVE_REPLICA
└── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 4062. `/SPE/BATCH_SAVE_REPLICA2`

```
T-Code: /SPE/BATCH_SAVE_REPLICA2
├── C_KLAH_BKL
    ├── ACTVT → 1
    └── BGRKL → 22, 23
├── C_TCLA_BKA
    └── KLART → 22, 23
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → 22, 23
    └── SICHT → (empty — maintain in PFCG)
├── M_MATE_CHG
    ├── ACTVT → 1, 2
    └── BEGRU → (empty — maintain in PFCG)
└── M_MATE_WRK
    ├── ACTVT → 1, 2
    └── WERKS → $WERKS
```

### 4063. `/SPE/CDMON`

```
T-Code: /SPE/CDMON
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → F4
└── V_LIKP_VST
    ├── ACTVT → 3, F4
    └── VSTEL → $VSTEL
```

### 4064. `/SPE/CRM_CREDIT_ACCOUNTS_GET`

```
T-Code: /SPE/CRM_CREDIT_ACCOUNTS_GET
└── F_KNKA_KKB
    ├── ACTVT → 3
    └── KKBER → $KKBER
```

### 4065. `/SPE/CRM_QUOT`

```
T-Code: /SPE/CRM_QUOT
└── S_GUI
    └── ACTVT → (empty — maintain in PFCG)
```

### 4066. `/SPE/DELTA01`

```
T-Code: /SPE/DELTA01
└── SPEDELTA
    └── ACTVT → 16
```

### 4067. `/SPE/EGR`

```
T-Code: /SPE/EGR
├── B_BUPA_GRP
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── B_BUPA_RLT
    ├── ACTVT → F4
    └── RLTYP → (empty — maintain in PFCG)
├── B_BUP_DCPD
    ├── ACTVT → F4
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── F_BKPF_BED
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_BKPF_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → F4
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_BUK
    ├── ACTVT → F4
    └── BUKRS → $BUKRS
├── F_LFA1_GEN
    └── ACTVT → F4
├── F_LFA1_GRP
    ├── ACTVT → F4
    └── KTOKK → (empty — maintain in PFCG)
├── M_ANFR_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANFR_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_ANFR_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_ANGB_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_ANGB_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_ANGB_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_BEST_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_LFM1_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_MATE_CHG
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_MAT
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WGR
    ├── ACTVT → F4
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → F4
    └── WERKS → $WERKS
├── M_QTN_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_QTN_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_QTN_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_RAHM_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_RAHM_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_RAHM_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
├── M_RFQ_BSA
    ├── ACTVT → F4
    └── BSART → (empty — maintain in PFCG)
├── M_RFQ_EKG
    ├── ACTVT → F4
    └── EKGRP → $EKGRP
├── M_RFQ_EKO
    ├── ACTVT → F4
    └── EKORG → $EKORG
└── V_LIKP_VST
    ├── ACTVT → 1, 3, F4
    └── VSTEL → $VSTEL
```

### 4068. `/SPE/GET_SINGLE_DELIVERY`

```
T-Code: /SPE/GET_SINGLE_DELIVERY
└── V_LIKP_VST
    ├── ACTVT → 3
    └── VSTEL → $VSTEL
```

### 4069. `/SPE/GOODSMVT_CREATE`

```
T-Code: /SPE/GOODSMVT_CREATE
├── CA_ATP_OVD
    ├── ACTVT → 3
    └── USAGE_ID → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 2
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_TCLA_BKA
    └── KLART → 23
├── C_TCLS_MNT
    ├── ACTVT → 23
    ├── KLART → (empty — maintain in PFCG)
    └── SICHT → (empty — maintain in PFCG)
├── F_BKPF_BUK
    ├── ACTVT → 1, 10, 3
    └── BUKRS → $BUKRS
├── F_BKPF_FKB
    ├── ACTVT → 1, 3
    └── FKBER → $FKBER
├── F_BKPF_GSB
    ├── ACTVT → 1, 3
    └── GSBER → $GSBER
├── F_BKPF_KOA
    ├── ACTVT → 1, 3
    └── KOART → $KOART
├── F_FAGL_LDR
    ├── ACTVT → 1, 2, 3
    ├── BUKRS → $BUKRS
    ├── GLRLDNR → (empty — maintain in PFCG)
    ├── GLRRCTY → (empty — maintain in PFCG)
    └── GLRVERS → (empty — maintain in PFCG)
├── F_FAGL_SEG
    ├── ACTVT → 1, 2, 3
    ├── GLRRCTY → (empty — maintain in PFCG)
    └── SEGMENT → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 2
    └── WERKS → $WERKS
├── M_MSEG_BWA
    ├── ACTVT → 1, 3
    └── BWART → (empty — maintain in PFCG)
├── M_MSEG_BWF
    ├── ACTVT → 1, 3
    └── BWART → (empty — maintain in PFCG)
├── M_MSEG_LGO
    ├── ACTVT → 1, 3
    ├── BWART → (empty — maintain in PFCG)
    ├── LGORT → (empty — maintain in PFCG)
    └── WERKS → $WERKS
├── M_MSEG_WMB
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
├── M_MSEG_WWA
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
├── M_MSEG_WWF
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
└── V_LIKP_VST
    ├── ACTVT → 1, 2, 3, 4, 85
    └── VSTEL → $VSTEL
```

### 4070. `/SPE/INB_DELIVERY_CONFIRM_DEC`

```
T-Code: /SPE/INB_DELIVERY_CONFIRM_DEC
├── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 1, 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3
    └── BGRKP → (empty — maintain in PFCG)
├── C_LIME_HU
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LIME_LOC
    ├── ACTVT → 3
    ├── BEGRU → (empty — maintain in PFCG)
    └── LIME_IDX → W01
├── C_LO_HU
    ├── ACTVT → 3
    └── WERKS → $WERKS
├── C_PROJ_KOK
    ├── KOKRS → $KOKRS
    └── PS_ACTVT → 3
├── C_PROJ_PRC
    ├── PRCTR → $PRCTR
    └── PS_ACTVT → 3
├── C_TCLA_BKA
    └── KLART → 1, 2, 200, 23, 300
├── FLOG_SPITM
    ├── ACTVT → 2
    ├── FL_PLANT_B → (empty — maintain in PFCG)
    ├── FL_PLANT_R → (empty — maintain in PFCG)
    ├── FL_SPITMST → (empty — maintain in PFCG)
    └── SPITM_TYPE → (empty — maintain in PFCG)
├── F_BKPF_BEK
    ├── ACTVT → 1, 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_BKPF_BUK
    ├── ACTVT → 1, 3
    └── BUKRS → $BUKRS
├── F_BKPF_FKB
    ├── ACTVT → 1, 3
    └── FKBER → $FKBER
├── F_BKPF_GSB
    ├── ACTVT → 1, 3
    └── GSBER → $GSBER
├── F_BKPF_KOA
    ├── ACTVT → 1, 3
    └── KOART → $KOART
├── F_FAGL_LDR
    ├── ACTVT → 1, 3
    ├── BUKRS → $BUKRS
    ├── GLRLDNR → (empty — maintain in PFCG)
    ├── GLRRCTY → (empty — maintain in PFCG)
    └── GLRVERS → (empty — maintain in PFCG)
├── F_FAGL_SEG
    ├── ACTVT → 1, 3
    ├── GLRRCTY → (empty — maintain in PFCG)
    └── SEGMENT → (empty — maintain in PFCG)
├── F_KNA1_BED
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_KNA1_GEN
    └── ACTVT → 3
├── F_KNA1_GRP
    ├── ACTVT → 3
    └── KTOKD → (empty — maintain in PFCG)
├── F_LFA1_BEK
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3
├── F_LFA1_GRP
    ├── ACTVT → 3
    └── KTOKK → (empty — maintain in PFCG)
├── JIT_S2C_CR
    ├── ACTVT → 16
    ├── NJIT_ACT → (empty — maintain in PFCG)
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    ├── VTWEG → $VTWEG
    └── WERKS → $WERKS
├── JIT_S_ACTN
    ├── ACTVT → 16
    ├── S_ACTION → (empty — maintain in PFCG)
    └── WERKS → $WERKS
├── M_MATE_CHG
    ├── ACTVT → 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_STA
    ├── ACTVT → 3
    └── STATM → (empty — maintain in PFCG)
├── M_MSEG_BWA
    ├── ACTVT → 1, 2, 3
    └── BWART → (empty — maintain in PFCG)
├── M_MSEG_BWE
    ├── ACTVT → 1, 2, 3
    └── BWART → (empty — maintain in PFCG)
├── M_MSEG_BWF
    ├── ACTVT → 1, 2, 3
    └── BWART → (empty — maintain in PFCG)
├── M_MSEG_LGO
    ├── ACTVT → 1, 3
    ├── BWART → (empty — maintain in PFCG)
    ├── LGORT → (empty — maintain in PFCG)
    └── WERKS → $WERKS
├── M_MSEG_WMB
    ├── ACTVT → 1, 2, 3, 4
    └── WERKS → $WERKS
├── M_MSEG_WWA
    ├── ACTVT → 1, 2, 3, 4
    └── WERKS → $WERKS
├── M_MSEG_WWE
    ├── ACTVT → 1, 2, 3, 4
    └── WERKS → $WERKS
├── M_MSEG_WWF
    ├── ACTVT → 1, 2, 3, 4
    └── WERKS → $WERKS
├── V_LIKP_VST
    ├── ACTVT → 1, 2, 3, 4, 85
    └── VSTEL → $VSTEL
├── V_VBAK_AAT
    ├── ACTVT → 2
    └── AUART → (empty — maintain in PFCG)
└── V_VBAK_VKO
    ├── ACTVT → 2
    ├── SPART → $SPART
    ├── VKORG → $VKORG
    └── VTWEG → $VTWEG
```

### 4071. `/SPE/INB_DELIVERY_REPLACE`

```
T-Code: /SPE/INB_DELIVERY_REPLACE
├── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3, F4
    └── BGRKP → (empty — maintain in PFCG)
├── F_BKPF_BEK
    ├── ACTVT → 1, 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_BKPF_KOA
    ├── ACTVT → 3
    └── KOART → $KOART
├── F_LFA1_BEK
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3
├── F_LFA1_GRP
    ├── ACTVT → 3
    └── KTOKK → (empty — maintain in PFCG)
├── M_BEST_BSA
    ├── ACTVT → 1, 2, 3
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 1, 2, 3
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 1, 2, 3
    └── EKORG → $EKORG
├── M_BEST_WRK
    ├── ACTVT → 1, 2, 3
    └── WERKS → $WERKS
├── M_MATE_CHG
    ├── ACTVT → 1, 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
├── S_BDS_DS
    ├── ACTVT → 25, 3, 30, 4
    ├── CLASSNAME → (empty — maintain in PFCG)
    └── CLASSTYPE → (empty — maintain in PFCG)
└── V_LIKP_VST
    ├── ACTVT → 1, 18, 2, 3, 4, F4
    └── VSTEL → $VSTEL
```

### 4072. `/SPE/INB_DELIVERY_RESPONSE`

```
T-Code: /SPE/INB_DELIVERY_RESPONSE
├── C_KLAH_BKL
    ├── ACTVT → 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3, F4
    └── BGRKP → (empty — maintain in PFCG)
├── F_BKPF_KOA
    ├── ACTVT → 3
    └── KOART → $KOART
├── M_BEST_BSA
    ├── ACTVT → 1, 2, 3
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 1, 2, 3
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 1, 2, 3
    └── EKORG → $EKORG
├── M_BEST_WRK
    ├── ACTVT → 1, 2, 3
    └── WERKS → $WERKS
├── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
└── V_LIKP_VST
    ├── ACTVT → 1, 2, 3, 4, 85
    └── VSTEL → $VSTEL
```

### 4073. `/SPE/INB_DELIVERY_SAVEREPLICA`

```
T-Code: /SPE/INB_DELIVERY_SAVEREPLICA
├── B_BUP_DCPD
    ├── ACTVT → 3
    ├── DATA_CTRLR → (empty — maintain in PFCG)
    └── PURPSE → (empty — maintain in PFCG)
├── C_KLAH_BKL
    ├── ACTVT → 2, 3
    └── BGRKL → (empty — maintain in PFCG)
├── C_KLAH_BKP
    ├── ACTVT → 2, 3, F4
    └── BGRKP → (empty — maintain in PFCG)
├── C_TCLA_BKA
    └── KLART → 26
├── F_BKPF_BEK
    ├── ACTVT → 1, 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_BKPF_KOA
    ├── ACTVT → 3
    └── KOART → $KOART
├── F_LFA1_BEK
    ├── ACTVT → 3
    └── BRGRU → (empty — maintain in PFCG)
├── F_LFA1_GEN
    └── ACTVT → 3
├── F_LFA1_GRP
    ├── ACTVT → 3
    └── KTOKK → (empty — maintain in PFCG)
├── M_BEST_BSA
    ├── ACTVT → 1, 3
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 1, 3
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 1, 3
    └── EKORG → $EKORG
├── M_BEST_WRK
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
├── M_MATE_CHG
    ├── ACTVT → 1, 3
    └── BEGRU → (empty — maintain in PFCG)
├── M_MATE_WRK
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
└── V_LIKP_VST
    ├── ACTVT → 1, 3
    └── VSTEL → $VSTEL
```

### 4074. `/SPE/INB_DELIVERY_SPLIT`

```
T-Code: /SPE/INB_DELIVERY_SPLIT
├── M_BEST_BSA
    ├── ACTVT → 1, 3
    └── BSART → (empty — maintain in PFCG)
├── M_BEST_EKG
    ├── ACTVT → 1, 3
    └── EKGRP → $EKGRP
├── M_BEST_EKO
    ├── ACTVT → 1, 3
    └── EKORG → $EKORG
├── M_BEST_WRK
    ├── ACTVT → 1, 3
    └── WERKS → $WERKS
└── V_LIKP_VST
    ├── ACTVT → 1, 3, 85
    └── VSTEL → $VSTEL
```

### 4075. `/SPE/INB_EGR_CREATE_POSA`

```
T-Code: /SPE/INB_EGR_CREATE_POSA
└── V_LIKP_VST
    ├── ACTVT → 1, 3, 4, 85
    └── VSTEL → $VSTEL
```

### 4076. `/SPE/INB_EGR_CREATE_PROD`

```
T-Code: /SPE/INB_EGR_CREATE_PROD
└── V_LIKP_VST
    ├── ACTVT → 1, 18, 2, 3, 85
    └── VSTEL → $VSTEL
```

### 4077. `/SPE/INSP_MAINTAIN_MULTIPLE`

```
T-Code: /SPE/INSP_MAINTAIN_MULTIPLE
└── INSPRESULT
    └── ACTVT → 1, 2, 3
```

### 4078. `/SPE/MATERIAL_STOCK_READ`

```
T-Code: /SPE/MATERIAL_STOCK_READ
└── M_MATE_WRK
    ├── ACTVT → 3
    └── WERKS → $WERKS
```

### 4079. `/SPE/MBEW_GEN_ARRAY_READ`

```
T-Code: /SPE/MBEW_GEN_ARRAY_READ
├── M_MATE_MAN
    └── ACTVT → 1, 2, 3
└── M_MATE_WRK
    ├── ACTVT → 1, 2, 3
    └── WERKS → $WERKS
```

### 4080. `/SPE/MQWL_APPL`

```
T-Code: /SPE/MQWL_APPL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MQWLCMP_A, /SPE/V_MQWLPAR_A, /SPE/V_MQWLRFC_A
```

### 4081. `/SPE/MQWL_CUS`

```
T-Code: /SPE/MQWL_CUS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MQWLCMP_C, /SPE/V_MQWLPAR_C, /SPE/V_MQWLRFC_C
```

### 4082. `/SPE/MQWL_SYS`

```
T-Code: /SPE/MQWL_SYS
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MQWLCMP_S, /SPE/V_MQWLPAR_S, /SPE/V_MQWLRFC_S
```

### 4083. `/SPE/OUTB_DELIVERY_SAVEREPLICA`

```
T-Code: /SPE/OUTB_DELIVERY_SAVEREPLICA
└── V_LIKP_VST
    ├── ACTVT → 1, 3
    └── VSTEL → $VSTEL
```

### 4084. `/SPE/PRINT_BILLING_DOC`

```
T-Code: /SPE/PRINT_BILLING_DOC
└── V_VBRK_FKA
    ├── ACTVT → 4
    └── FKART → (empty — maintain in PFCG)
```

### 4085. `/SPE/REDIR`

```
T-Code: /SPE/REDIR
└── V_LIKP_VST
    ├── ACTVT → (empty — maintain in PFCG)
    └── VSTEL → $VSTEL
```

### 4086. `/SPE/RETINSPNO`

```
T-Code: /SPE/RETINSPNO
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SPE/RETIN
```

### 4087. `/SPE/RETINSP_DISPLAY`

```
T-Code: /SPE/RETINSP_DISPLAY
└── INSPRESULT
    └── ACTVT → 3
```

### 4088. `/SPE/VL10CUC`

```
T-Code: /SPE/VL10CUC
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T186C
```

### 4089. `/SPE/V_MVT_ACCGR`

```
T-Code: /SPE/V_MVT_ACCGR
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MVT_ACCGR
```

### 4090. `/SPE/V_MVT_DETER`

```
T-Code: /SPE/V_MVT_DETER
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_MVT_DETER
```

### 4091. `/SPE/V_T340_3PL`

```
T-Code: /SPE/V_T340_3PL
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SPE/V_T340_3PL
```

## `/SRMERP/` — SAP SRM ERP Integration
> T-codes: **9**


### 4092. `/SRMERP/44000008`

```
T-Code: /SRMERP/44000008
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_ATP_SC, /SRMERP/V_DTP_SC, /SRMERP/V_FDT_SC, /SRMERP/V_TXCPI
```

### 4093. `/SRMERP/44000197`

```
T-Code: /SRMERP/44000197
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_WF_R
```

### 4094. `/SRMERP/44000216`

```
T-Code: /SRMERP/44000216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_MSG_C
```

### 4095. `/SRMERP/44000217`

```
T-Code: /SRMERP/44000217
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_SRC
```

### 4096. `/SRMERP/44000300`

```
T-Code: /SRMERP/44000300
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → MMPUROCI_CONN_V, MMPUROCI_ENTIT_V, WSI_CALL_MV
```

### 4097. `/SRMERP/44000475`

```
T-Code: /SRMERP/44000475
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_CATCST
```

### 4098. `/SRMERP/44000484`

```
T-Code: /SRMERP/44000484
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMERP/V_MSG_F
```

### 4099. `/SRMERP/44000490`

```
T-Code: /SRMERP/44000490
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → SWN_CAT2MSGV, SWN_CATEGORYV, SWN_COLSCHED, SWN_DELPROVIV, SWN_DELSCHED, SWN_DELTYPE, SWN_FILTERV, SWN_FILTVALV, SWN_MSGTMPL, SWN_REMAP, SWN_SCENARIO, SWN_SETTINGS, SWN_SUBSV, SWN_SUBSVALV
```

### 4100. `/SRMERP/SNUM`

```
T-Code: /SRMERP/SNUM
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMERP/SC
```

## `/SRMSMC/` — SAP SRM Supplier Management
> T-codes: **124**


### 4101. `/SRMSMC/52000123`

```
T-Code: /SRMSMC/52000123
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TBDLS
```

### 4102. `/SRMSMC/52000461`

```
T-Code: /SRMSMC/52000461
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_S_EVAL
```

### 4103. `/SRMSMC/52000559`

```
T-Code: /SRMSMC/52000559
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/VREM_LVL
```

### 4104. `/SRMSMC/52004347`

```
T-Code: /SRMSMC/52004347
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → (empty — maintain in PFCG)
```

### 4105. `/SRMSMC/62000011`

```
T-Code: /SRMSMC/62000011
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → V_TB001
```

### 4106. `/SRMSMC/62000012`

```
T-Code: /SRMSMC/62000012
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_S_NRIV
```

### 4107. `/SRMSMC/62000013`

```
T-Code: /SRMSMC/62000013
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/D_S_NRIV
```

### 4108. `/SRMSMC/62000014`

```
T-Code: /SRMSMC/62000014
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_C_NRIV
```

### 4109. `/SRMSMC/62000015`

```
T-Code: /SRMSMC/62000015
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SYSCHR, /SRMSMC/V_TRDEF
```

### 4110. `/SRMSMC/62000016`

```
T-Code: /SRMSMC/62000016
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_QL
```

### 4111. `/SRMSMC/62000018`

```
T-Code: /SRMSMC/62000018
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → SSTN_D_SUP_AG_C, SSTN_D_SUP_AG_MV
```

### 4112. `/SRMSMC/62000019`

```
T-Code: /SRMSMC/62000019
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → SSTN_D_SUP_OT_MV
```

### 4113. `/SRMSMC/62000030`

```
T-Code: /SRMSMC/62000030
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ATF
```

### 4114. `/SRMSMC/62000031`

```
T-Code: /SRMSMC/62000031
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ROS
```

### 4115. `/SRMSMC/62000037`

```
T-Code: /SRMSMC/62000037
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/C_S3Q
```

### 4116. `/SRMSMC/62000038`

```
T-Code: /SRMSMC/62000038
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/C_S3Q
```

### 4117. `/SRMSMC/62000040`

```
T-Code: /SRMSMC/62000040
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_S3Q
```

### 4118. `/SRMSMC/62000049`

```
T-Code: /SRMSMC/62000049
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4119. `/SRMSMC/62000055`

```
T-Code: /SRMSMC/62000055
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_WF_APP
```

### 4120. `/SRMSMC/62000056`

```
T-Code: /SRMSMC/62000056
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_RFCDST
```

### 4121. `/SRMSMC/62000060`

```
T-Code: /SRMSMC/62000060
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SYSCHR
```

### 4122. `/SRMSMC/62000061`

```
T-Code: /SRMSMC/62000061
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_RFCDST
```

### 4123. `/SRMSMC/62000064`

```
T-Code: /SRMSMC/62000064
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4124. `/SRMSMC/62000065`

```
T-Code: /SRMSMC/62000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4125. `/SRMSMC/62000067`

```
T-Code: /SRMSMC/62000067
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ACTION
```

### 4126. `/SRMSMC/62000070`

```
T-Code: /SRMSMC/62000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_EVAL
```

### 4127. `/SRMSMC/62000082`

```
T-Code: /SRMSMC/62000082
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/VEXT_SFC
```

### 4128. `/SRMSMC/62000083`

```
T-Code: /SRMSMC/62000083
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/VEXT_SFC
```

### 4129. `/SRMSMC/62000086`

```
T-Code: /SRMSMC/62000086
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_OBJ_TY
```

### 4130. `/SRMSMC/62000089`

```
T-Code: /SRMSMC/62000089
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_BS_COM
```

### 4131. `/SRMSMC/62000091`

```
T-Code: /SRMSMC/62000091
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_BS_COM
```

### 4132. `/SRMSMC/62000092`

```
T-Code: /SRMSMC/62000092
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4133. `/SRMSMC/62000093`

```
T-Code: /SRMSMC/62000093
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/VREM_LEV
```

### 4134. `/SRMSMC/62000100`

```
T-Code: /SRMSMC/62000100
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_MCASS
```

### 4135. `/SRMSMC/62000101`

```
T-Code: /SRMSMC/62000101
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_PCHIER
```

### 4136. `/SRMSMC/62000104`

```
T-Code: /SRMSMC/62000104
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4137. `/SRMSMC/62000106`

```
T-Code: /SRMSMC/62000106
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ALIAS
```

### 4138. `/SRMSMC/62000107`

```
T-Code: /SRMSMC/62000107
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ATTR
```

### 4139. `/SRMSMC/62000116`

```
T-Code: /SRMSMC/62000116
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_EVKEY
```

### 4140. `/SRMSMC/62000119`

```
T-Code: /SRMSMC/62000119
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ASQUAL
```

### 4141. `/SRMSMC/62000120`

```
T-Code: /SRMSMC/62000120
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_CHGMOD
```

### 4142. `/SRMSMC/62000121`

```
T-Code: /SRMSMC/62000121
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DUPCHK
```

### 4143. `/SRMSMC/62000122`

```
T-Code: /SRMSMC/62000122
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SETDEL
```

### 4144. `/SRMSMC/62000125`

```
T-Code: /SRMSMC/62000125
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4145. `/SRMSMC/62000126`

```
T-Code: /SRMSMC/62000126
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SQKEY
```

### 4146. `/SRMSMC/62000127`

```
T-Code: /SRMSMC/62000127
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ATTSIZ
```

### 4147. `/SRMSMC/62000129`

```
T-Code: /SRMSMC/62000129
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4148. `/SRMSMC/62000130`

```
T-Code: /SRMSMC/62000130
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SQKEY
```

### 4149. `/SRMSMC/62000133`

```
T-Code: /SRMSMC/62000133
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_EVKEY
```

### 4150. `/SRMSMC/62000136`

```
T-Code: /SRMSMC/62000136
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4151. `/SRMSMC/62000137`

```
T-Code: /SRMSMC/62000137
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_PLANT
```

### 4152. `/SRMSMC/62000140`

```
T-Code: /SRMSMC/62000140
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DUPCHK
```

### 4153. `/SRMSMC/62000142`

```
T-Code: /SRMSMC/62000142
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_MAXDUP
```

### 4154. `/SRMSMC/62000143`

```
T-Code: /SRMSMC/62000143
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_MAXDUP
```

### 4155. `/SRMSMC/62000145`

```
T-Code: /SRMSMC/62000145
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_BPHTYP, /SRMSMC/V_BPHTYT
```

### 4156. `/SRMSMC/62000146`

```
T-Code: /SRMSMC/62000146
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4157. `/SRMSMC/62000148`

```
T-Code: /SRMSMC/62000148
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4158. `/SRMSMC/62000149`

```
T-Code: /SRMSMC/62000149
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4159. `/SRMSMC/62000150`

```
T-Code: /SRMSMC/62000150
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4160. `/SRMSMC/62000160`

```
T-Code: /SRMSMC/62000160
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_ACTTYP, /SRMSMC/V_ACTTYT
```

### 4161. `/SRMSMC/62000162`

```
T-Code: /SRMSMC/62000162
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /BOBF/C_ATFMIME
```

### 4162. `/SRMSMC/62000166`

```
T-Code: /SRMSMC/62000166
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_TSK_S
```

### 4163. `/SRMSMC/62000167`

```
T-Code: /SRMSMC/62000167
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_TSK
```

### 4164. `/SRMSMC/62000168`

```
T-Code: /SRMSMC/62000168
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4165. `/SRMSMC/62000169`

```
T-Code: /SRMSMC/62000169
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4166. `/SRMSMC/62000173`

```
T-Code: /SRMSMC/62000173
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4167. `/SRMSMC/62000174`

```
T-Code: /SRMSMC/62000174
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4168. `/SRMSMC/62000177`

```
T-Code: /SRMSMC/62000177
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DNBRCO, /SRMSMC/V_DNBRCT
```

### 4169. `/SRMSMC/62000178`

```
T-Code: /SRMSMC/62000178
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DNBSIC, /SRMSMC/V_DNBSIT
```

### 4170. `/SRMSMC/62000179`

```
T-Code: /SRMSMC/62000179
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DNBLAC, /SRMSMC/V_DNBLAT
```

### 4171. `/SRMSMC/62000180`

```
T-Code: /SRMSMC/62000180
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4172. `/SRMSMC/62000181`

```
T-Code: /SRMSMC/62000181
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DNB_CR
```

### 4173. `/SRMSMC/62000182`

```
T-Code: /SRMSMC/62000182
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_DNBFRS, /SRMSMC/V_DNBFRT
```

### 4174. `/SRMSMC/62000183`

```
T-Code: /SRMSMC/62000183
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_QSTCHK
```

### 4175. `/SRMSMC/62000184`

```
T-Code: /SRMSMC/62000184
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4176. `/SRMSMC/62000185`

```
T-Code: /SRMSMC/62000185
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4177. `/SRMSMC/62000189`

```
T-Code: /SRMSMC/62000189
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4178. `/SRMSMC/62000190`

```
T-Code: /SRMSMC/62000190
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4179. `/SRMSMC/62000191`

```
T-Code: /SRMSMC/62000191
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4180. `/SRMSMC/62000193`

```
T-Code: /SRMSMC/62000193
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_CLSTYP, /SRMSMC/V_CLSTYT
```

### 4181. `/SRMSMC/62000195`

```
T-Code: /SRMSMC/62000195
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4182. `/SRMSMC/62000196`

```
T-Code: /SRMSMC/62000196
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4183. `/SRMSMC/62000197`

```
T-Code: /SRMSMC/62000197
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4184. `/SRMSMC/62000198`

```
T-Code: /SRMSMC/62000198
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4185. `/SRMSMC/62000199`

```
T-Code: /SRMSMC/62000199
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4186. `/SRMSMC/62000200`

```
T-Code: /SRMSMC/62000200
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4187. `/SRMSMC/62000201`

```
T-Code: /SRMSMC/62000201
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4188. `/SRMSMC/62000202`

```
T-Code: /SRMSMC/62000202
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4189. `/SRMSMC/62000203`

```
T-Code: /SRMSMC/62000203
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4190. `/SRMSMC/62000205`

```
T-Code: /SRMSMC/62000205
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_QSTCHK
```

### 4191. `/SRMSMC/62000206`

```
T-Code: /SRMSMC/62000206
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4192. `/SRMSMC/62000207`

```
T-Code: /SRMSMC/62000207
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4193. `/SRMSMC/62000210`

```
T-Code: /SRMSMC/62000210
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_XTF
```

### 4194. `/SRMSMC/62000211`

```
T-Code: /SRMSMC/62000211
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4195. `/SRMSMC/62000212`

```
T-Code: /SRMSMC/62000212
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4196. `/SRMSMC/62000213`

```
T-Code: /SRMSMC/62000213
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4197. `/SRMSMC/62000214`

```
T-Code: /SRMSMC/62000214
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4198. `/SRMSMC/62000215`

```
T-Code: /SRMSMC/62000215
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4199. `/SRMSMC/62000216`

```
T-Code: /SRMSMC/62000216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4200. `/SRMSMC/62000217`

```
T-Code: /SRMSMC/62000217
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_SWITCH
```

### 4201. `/SRMSMC/62000218`

```
T-Code: /SRMSMC/62000218
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SRMSMC/V_XTF
```

### 4202. `/SRMSMC/CREA_QLB_EVL`

```
T-Code: /SRMSMC/CREA_QLB_EVL
├── /SRMSMC/BO
    ├── /BOFU/BO → /SRMSMC/MO_QLB
    └── ACTVT → 1, 2, 3
└── S_PROGRAM
    ├── P_ACTION → SUBMIT
    └── P_GROUP → /SRMSMC/
```

### 4203. `/SRMSMC/CREA_QLB_QUA`

```
T-Code: /SRMSMC/CREA_QLB_QUA
└── S_IMG_ACTV
    ├── ACTVT → 2
    ├── IMG_ACTIV → ACT
    └── PROJAUTH → (empty — maintain in PFCG)
```

### 4204. `/SRMSMC/NR_ACT`

```
T-Code: /SRMSMC/NR_ACT
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/AC
```

### 4205. `/SRMSMC/NR_BPH`

```
T-Code: /SRMSMC/NR_BPH
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/BH
```

### 4206. `/SRMSMC/NR_CRT`

```
T-Code: /SRMSMC/NR_CRT
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/CT
```

### 4207. `/SRMSMC/NR_PUC`

```
T-Code: /SRMSMC/NR_PUC
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/PC
```

### 4208. `/SRMSMC/NR_Q`

```
T-Code: /SRMSMC/NR_Q
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/Q
```

### 4209. `/SRMSMC/NR_QG`

```
T-Code: /SRMSMC/NR_QG
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QG
```

### 4210. `/SRMSMC/NR_QNR`

```
T-Code: /SRMSMC/NR_QNR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QN
```

### 4211. `/SRMSMC/NR_QNRE`

```
T-Code: /SRMSMC/NR_QNRE
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QE
```

### 4212. `/SRMSMC/NR_QS`

```
T-Code: /SRMSMC/NR_QS
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QS
```

### 4213. `/SRMSMC/NR_SCS`

```
T-Code: /SRMSMC/NR_SCS
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/CS
```

### 4214. `/SRMSMC/NR_SEP`

```
T-Code: /SRMSMC/NR_SEP
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/EP
```

### 4215. `/SRMSMC/NR_SES`

```
T-Code: /SRMSMC/NR_SES
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/SS
```

### 4216. `/SRMSMC/NR_SEV`

```
T-Code: /SRMSMC/NR_SEV
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/EV
```

### 4217. `/SRMSMC/NR_SQP`

```
T-Code: /SRMSMC/NR_SQP
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QP
```

### 4218. `/SRMSMC/NR_SQQ`

```
T-Code: /SRMSMC/NR_SQQ
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QQ
```

### 4219. `/SRMSMC/NR_SQR`

```
T-Code: /SRMSMC/NR_SQR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QR
```

### 4220. `/SRMSMC/NR_SQS`

```
T-Code: /SRMSMC/NR_SQS
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/QC
```

### 4221. `/SRMSMC/NR_SRS`

```
T-Code: /SRMSMC/NR_SRS
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/RS
```

### 4222. `/SRMSMC/NR_TSK`

```
T-Code: /SRMSMC/NR_TSK
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SRMSMC/TS
```

### 4223. `/SRMSMC/UPLOAD_MGPCT`

```
T-Code: /SRMSMC/UPLOAD_MGPCT
└── S_DATASET
    ├── ACTVT → 33
    ├── FILENAME → (empty — maintain in PFCG)
    └── PROGRAM → /SRMSMC/UPLOAD_MGPCT
```

### 4224. `/SRMSMC/UPLOAD_SPC`

```
T-Code: /SRMSMC/UPLOAD_SPC
├── S_GUI
    └── ACTVT → 2, 4, 60, 61
└── S_TCODE
    └── TCD → COMM_HIERARCHY, SE38
```

## `/SSF/` — SAP Secure Store & Forward
> T-codes: **5**


### 4225. `/SSF/AB`

```
T-Code: /SSF/AB
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 4226. `/SSF/CALL_SUBROUTINE_RFC`

```
T-Code: /SSF/CALL_SUBROUTINE_RFC
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 4227. `/SSF/COLLECT_DATA_FOR_SDCC`

```
T-Code: /SSF/COLLECT_DATA_FOR_SDCC
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 4228. `/SSF/COLLECT_DATA_FOR_SDCC_B`

```
T-Code: /SSF/COLLECT_DATA_FOR_SDCC_B
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

### 4229. `/SSF/PB`

```
T-Code: /SSF/PB
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

## `/STMC/` — SAP Store Management & Commerce
> T-codes: **4**


### 4230. `/STMC/A`

```
T-Code: /STMC/A
├── /STMC/TMSY
    ├── /STMC/TMSY → (empty — maintain in PFCG)
    ├── /STMC/TMTI → (empty — maintain in PFCG)
    ├── /STMC/TMTY → (empty — maintain in PFCG)
    └── ACTVT → 3
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
├── S_DBCON
    ├── ACTVT → 3
    ├── DBA_DBHOST → (empty — maintain in PFCG)
    ├── DBA_DBSID → (empty — maintain in PFCG)
    └── DBA_DBUSER → (empty — maintain in PFCG)
├── S_DOTM_SYS
    ├── ACTVT → 3
    ├── DOTM_BP_ID → (empty — maintain in PFCG)
    ├── DOTM_SUBTY → (empty — maintain in PFCG)
    ├── DOTM_SYSTY → (empty — maintain in PFCG)
    └── DOTM_TECID → (empty — maintain in PFCG)
└── S_RZL_ADM
    └── ACTVT → 1
```

### 4231. `/STMC/B`

```
T-Code: /STMC/B
├── /STMC/TMSY
    ├── /STMC/TMSY → (empty — maintain in PFCG)
    ├── /STMC/TMTI → (empty — maintain in PFCG)
    ├── /STMC/TMTY → (empty — maintain in PFCG)
    └── ACTVT → 3
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
├── S_DBCON
    ├── ACTVT → 3
    ├── DBA_DBHOST → (empty — maintain in PFCG)
    ├── DBA_DBSID → (empty — maintain in PFCG)
    └── DBA_DBUSER → (empty — maintain in PFCG)
├── S_DOTM_SYS
    ├── ACTVT → 3
    ├── DOTM_BP_ID → (empty — maintain in PFCG)
    ├── DOTM_SUBTY → (empty — maintain in PFCG)
    ├── DOTM_SYSTY → (empty — maintain in PFCG)
    └── DOTM_TECID → (empty — maintain in PFCG)
└── S_RZL_ADM
    └── ACTVT → 1
```

### 4232. `/STMC/S`

```
T-Code: /STMC/S
├── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
├── S_DBCON
    ├── ACTVT → 3
    ├── DBA_DBHOST → (empty — maintain in PFCG)
    ├── DBA_DBSID → (empty — maintain in PFCG)
    └── DBA_DBUSER → (empty — maintain in PFCG)
├── S_DOTM_SYS
    ├── ACTVT → 3
    ├── DOTM_BP_ID → (empty — maintain in PFCG)
    ├── DOTM_SUBTY → (empty — maintain in PFCG)
    ├── DOTM_SYSTY → (empty — maintain in PFCG)
    └── DOTM_TECID → (empty — maintain in PFCG)
└── S_RZL_ADM
    └── ACTVT → 1
```

### 4233. `/STMC/UI5_S`

```
T-Code: /STMC/UI5_S
└── S_ADMI_FCD
    └── S_ADMI_FCD → ST0R
```

## `/STTPEC/` — SAP Time & Attendance Management
> T-codes: **12**


### 4234. `/STTPEC/BATCH_CLCORR`

```
T-Code: /STTPEC/BATCH_CLCORR
└── /STTPEC/IN
    ├── /STTPEC/OT → LOT
    └── ACTVT → 16
```

### 4235. `/STTPEC/CUST`

```
T-Code: /STTPEC/CUST
├── /CDBASIS/C
    ├── /CDBASIS/A → /STTPEC/
    ├── /CDBASIS/S → CUS, OPR, SET
    └── ACTVT → 2, 21, 3
└── /STTPEC/CO
    └── ACTVT → 65
```

### 4236. `/STTPEC/CUSTGEN`

```
T-Code: /STTPEC/CUSTGEN
├── /CDBASIS/C
    ├── /CDBASIS/A → /STTPEC/
    ├── /CDBASIS/S → CUS, OPR
    └── ACTVT → 2, 21, 3
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /CDBASIS/CUSGEN*, /CDBASIS/CUSOPR*
```

### 4237. `/STTPEC/INT_BATCH`

```
T-Code: /STTPEC/INT_BATCH
└── /STTPEC/IN
    ├── /STTPEC/OT → LOT
    └── ACTVT → 16
```

### 4238. `/STTPEC/INT_BUP`

```
T-Code: /STTPEC/INT_BUP
└── /STTPEC/IN
    ├── /STTPEC/OT → BUP
    └── ACTVT → 16
```

### 4239. `/STTPEC/INT_LOC`

```
T-Code: /STTPEC/INT_LOC
└── /STTPEC/IN
    ├── /STTPEC/OT → LOC
    └── ACTVT → 16
```

### 4240. `/STTPEC/INT_MAT`

```
T-Code: /STTPEC/INT_MAT
└── /STTPEC/IN
    ├── /STTPEC/OT → MAT
    └── ACTVT → 16
```

### 4241. `/STTPEC/INT_TRN`

```
T-Code: /STTPEC/INT_TRN
└── /STTPEC/IN
    ├── /STTPEC/OT → TRN
    └── ACTVT → 16
```

### 4242. `/STTPEC/LOG_D`

```
T-Code: /STTPEC/LOG_D
└── S_APPL_LOG
    ├── ACTVT → 3
    ├── ALG_OBJECT → /STTPEC/
    └── ALG_SUBOBJ → ANY, INT, MD, TOOLBOX, WHS
```

### 4243. `/STTPEC/MD_TEST`

```
T-Code: /STTPEC/MD_TEST
└── /STTPEC/IN
    ├── /STTPEC/OT → BUP
    └── ACTVT → 16
```

### 4244. `/STTPEC/NCODE_UNIQUE`

```
T-Code: /STTPEC/NCODE_UNIQUE
├── /CDBASIS/C
    ├── /CDBASIS/A → /STTPEC/
    ├── /CDBASIS/S → CUS, OPR
    └── ACTVT → 2, 21, 3
├── S_TABU_DIS
    ├── ACTVT → 2, 3
    └── DICBERCLS → (empty — maintain in PFCG)
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /CDBASIS/CUSGEN*, /CDBASIS/CUSOPR*
```

### 4245. `/STTPEC/WHS_TOOLBOX`

```
T-Code: /STTPEC/WHS_TOOLBOX
└── /STTPEC/TS
    └── ACTVT → 16
```

## `/SYCLO/` — SAP Syclo — Mobile Applications
> T-codes: **18**


### 4246. `/SYCLO/AP1_NR`

```
T-Code: /SYCLO/AP1_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SYCLO/AP1
```

### 4247. `/SYCLO/CACHE_PURGE`

```
T-Code: /SYCLO/CACHE_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

### 4248. `/SYCLO/CFG_EFI`

```
T-Code: /SYCLO/CFG_EFI
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/EFI01
```

### 4249. `/SYCLO/CFG_EXCHOBJ`

```
T-Code: /SYCLO/CFG_EXCHOBJ
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/CA002, /SYCLO/CA002D, /SYCLO/CA002F, /SYCLO/CA002L, /SYCLO/CA002T, /SYCLO/CA003
```

### 4250. `/SYCLO/CFG_MOBILEAPP`

```
T-Code: /SYCLO/CFG_MOBILEAPP
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/CA000, /SYCLO/CA000A, /SYCLO/CA000D, /SYCLO/CA000F, /SYCLO/CA000FT, /SYCLO/CA000G, /SYCLO/CA000P, /SYCLO/CA000PT, /SYCLO/CA000R, /SYCLO/CA000S, /SYCLO/CA000ST, /SYCLO/CA000U, /SYCLO/CA000UT, /SYCLO/CA000X
```

### 4251. `/SYCLO/CFG_OTRIGDEF`

```
T-Code: /SYCLO/CFG_OTRIGDEF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/OT001, /SYCLO/OT001D, /SYCLO/OT001M, /SYCLO/OT001T
```

### 4252. `/SYCLO/CFG_PUSHDEF`

```
T-Code: /SYCLO/CFG_PUSHDEF
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/PSH00, /SYCLO/PSH00OT
```

### 4253. `/SYCLO/CFG_SYSTEM`

```
T-Code: /SYCLO/CFG_SYSTEM
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /SYCLO/CA001
```

### 4254. `/SYCLO/CORE_PURGE`

```
T-Code: /SYCLO/CORE_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

### 4255. `/SYCLO/C_1_NR`

```
T-Code: /SYCLO/C_1_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SYCLO/C_1
```

### 4256. `/SYCLO/C_2_NR`

```
T-Code: /SYCLO/C_2_NR
└── S_NUMBER
    ├── ACTVT → (empty — maintain in PFCG)
    └── NROBJ → /SYCLO/C_2
```

### 4257. `/SYCLO/EX_PURGE`

```
T-Code: /SYCLO/EX_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

### 4258. `/SYCLO/MTRAHIS_PURGE`

```
T-Code: /SYCLO/MTRAHIS_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

### 4259. `/SYCLO/PUSH_PURGE`

```
T-Code: /SYCLO/PUSH_PURGE
└── S_MI_CCMS
    └── ACTVT → 6
```

### 4260. `/SYCLO/SLG1`

```
T-Code: /SYCLO/SLG1
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SYCLO/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 4261. `/SYCLO/SLG2`

```
T-Code: /SYCLO/SLG2
└── S_APPL_LOG
    ├── ACTVT → 3, 6
    ├── ALG_OBJECT → /SYCLO/
    └── ALG_SUBOBJ → (empty — maintain in PFCG)
```

### 4262. `/SYCLO/SUBQ_PURGE`

```
T-Code: /SYCLO/SUBQ_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

### 4263. `/SYCLO/SYSLOG_PURGE`

```
T-Code: /SYCLO/SYSLOG_PURGE
└── /SMFND/A01
    ├── /SMFND/APP → (empty — maintain in PFCG)
    └── ACTVT → 6
```

## `/TMF/` — SAP TM Foundation
> T-codes: **45**


### 4264. `/TMF/64000001`

```
T-Code: /TMF/64000001
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_REP_STAT
```

### 4265. `/TMF/70000063`

```
T-Code: /TMF/70000063
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_CENEFD_BP, /TMF/D_CENTR_EFD, /TMF/D_CNPJ, /TMF/D_CNPJ_ROOT, /TMF/D_ESTABELEC, /TMF/D_IE, /TMF/D_SOFTHOUSE
```

### 4266. `/TMF/70000065`

```
T-Code: /TMF/70000065
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_SIGNATAR
```

### 4267. `/TMF/70000070`

```
T-Code: /TMF/70000070
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_QUESTION
```

### 4268. `/TMF/70000077`

```
T-Code: /TMF/70000077
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_LOG_TYPE, /TMF/D_REG_PROFI, /TMF/D_REP_CUST, /TMF/D_REP_VERS, /TMF/D_VIEW_VERS
```

### 4269. `/TMF/70000085`

```
T-Code: /TMF/70000085
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_COD_INF_D, /TMF/D_COD_OBS_A, /TMF/D_COD_OBS_D
```

### 4270. `/TMF/70000086`

```
T-Code: /TMF/70000086
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_MAT_CONF
```

### 4271. `/TMF/70000087`

```
T-Code: /TMF/70000087
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_CFOP_PCO
```

### 4272. `/TMF/70000088`

```
T-Code: /TMF/70000088
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_CONF_BOM
```

### 4273. `/TMF/70000089`

```
T-Code: /TMF/70000089
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_TIPOORDEM
```

### 4274. `/TMF/70000118`

```
T-Code: /TMF/70000118
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_R_FILTER
```

### 4275. `/TMF/70000121`

```
T-Code: /TMF/70000121
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → J_1BPIS_V_CFOP, J_1BPIS_V_CFOP2, J_1BTAXSITCOFV, J_1BTAXSITPISV
```

### 4276. `/TMF/70000145`

```
T-Code: /TMF/70000145
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_ACC_ANA_C
```

### 4277. `/TMF/70000198`

```
T-Code: /TMF/70000198
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_R_FILTER
```

### 4278. `/TMF/70000230`

```
T-Code: /TMF/70000230
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_WHTX_TYPE
```

### 4279. `/TMF/70000252`

```
T-Code: /TMF/70000252
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_CCCFGAC_V, /TMF/D_CCCFGCC_V, /TMF/D_CCCFG_V
```

### 4280. `/TMF/70000268`

```
T-Code: /TMF/70000268
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_REG_PRIO
```

### 4281. `/TMF/70000299`

```
T-Code: /TMF/70000299
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_LEDG_CURR
```

### 4282. `/TMF/70000363`

```
T-Code: /TMF/70000363
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/D_WH_TAXGRP
```

### 4283. `/TMF/70000369`

```
T-Code: /TMF/70000369
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /TMF/V_CCPRCSTCT
```

### 4284. `/TMF/COCKPIT_EFIN`

```
T-Code: /TMF/COCKPIT_EFIN
├── /TMF/M_EVT
    ├── /TMF/M_EVT → F*
    └── ACTVT → 3
├── S_SERVICE
    ├── SRV_NAME → 6F6F9965017B6A1DC086EC4AF433BC, FD5E055B21E5E7FBD95211793EE65F
    └── SRV_TYPE → HT
└── S_USER_GRP
    ├── ACTVT → 3, 5
    └── CLASS → ADDON_TST
```

### 4285. `/TMF/COCKPIT_ESOCIAL`

```
T-Code: /TMF/COCKPIT_ESOCIAL
├── /TMF/M_EVT
    ├── /TMF/M_EVT → S*
    └── ACTVT → 3
├── S_SERVICE
    ├── SRV_NAME → 6F6F9965017B6A1DC086EC4AF433BC, FD5E055B21E5E7FBD95211793EE65F
    └── SRV_TYPE → HT
└── S_USER_GRP
    ├── ACTVT → 3, 5
    └── CLASS → ADDON_TST
```

### 4286. `/TMF/COCKPIT_REINF`

```
T-Code: /TMF/COCKPIT_REINF
├── /TMF/M_EVT
    ├── /TMF/M_EVT → R*
    └── ACTVT → 3
├── S_SERVICE
    ├── SRV_NAME → 6F6F9965017B6A1DC086EC4AF433BC, FD5E055B21E5E7FBD95211793EE65F
    └── SRV_TYPE → HT
└── S_USER_GRP
    ├── ACTVT → 3, 5
    └── CLASS → ADDON_TST
```

### 4287. `/TMF/COD_INF`

```
T-Code: /TMF/COD_INF
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /TMF/D_COD_INF_D
```

### 4288. `/TMF/COD_OBS`

```
T-Code: /TMF/COD_OBS
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /TMF/D_COD_OBS_D
```

### 4289. `/TMF/COD_OBS_P`

```
T-Code: /TMF/COD_OBS_P
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /TMF/D_COD_OBS_A
```

### 4290. `/TMF/ECD`

```
T-Code: /TMF/ECD
├── /TMF/ECD
    └── DUMMY → X
└── F_SRF_RNTM
    ├── ACTVT → 1
    ├── COUNTRY → BR
    └── SRF_REPCAT → BR_SPED_ECD
```

### 4291. `/TMF/ECF`

```
T-Code: /TMF/ECF
├── /TMF/ECF01
    ├── /TMF/CNPJR → (empty — maintain in PFCG)
    └── /TMF/REPUS → 2
├── F_SRF_RNTM
    ├── ACTVT → 1
    ├── COUNTRY → BR
    └── SRF_REPCAT → BR_SPED_ECF
└── S_ICF
    ├── ICF_FIELD → SERVICE
    └── ICF_VALUE → ECF01
```

### 4292. `/TMF/EFD`

```
T-Code: /TMF/EFD
├── /TMF/EFD
    └── DUMMY → X
└── F_SRF_RNTM
    ├── ACTVT → 1
    ├── COUNTRY → BR
    └── SRF_REPCAT → BR_SPED_EFD
```

### 4293. `/TMF/IMPDELEXL_GUI`

```
T-Code: /TMF/IMPDELEXL_GUI
└── /TMF/SHAEX
    ├── ACTVT → 2
    └── SPEDREPORT → (empty — maintain in PFCG)
```

### 4294. `/TMF/IMPEXL_GUI`

```
T-Code: /TMF/IMPEXL_GUI
└── /TMF/SHAEX
    ├── ACTVT → 2
    └── SPEDREPORT → (empty — maintain in PFCG)
```

### 4295. `/TMF/INITIAL_LOAD`

```
T-Code: /TMF/INITIAL_LOAD
└── /TMF/INIL
    └── DUMMY → X
```

### 4296. `/TMF/MAT_CONFI`

```
T-Code: /TMF/MAT_CONFI
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /TMF/D_MAT_CONF
```

### 4297. `/TMF/MF_FLOW_EXEC`

```
T-Code: /TMF/MF_FLOW_EXEC
└── /TMF/M_FLW
    └── FLOW_ID → BATCHF, BATCHR, BATCHS, EVENTF, EVENTR, EVENTS
```

### 4298. `/TMF/MGOVS`

```
T-Code: /TMF/MGOVS
└── S_NUMBER
    ├── ACTVT → 2, 3
    └── NROBJ → /TMF/MGOVS
```

### 4299. `/TMF/M_BAT`

```
T-Code: /TMF/M_BAT
└── S_NUMBER
    ├── ACTVT → 11, 13, 17, 2, 3
    └── NROBJ → /TMF/M_BAT
```

### 4300. `/TMF/PCO`

```
T-Code: /TMF/PCO
├── /TMF/PCO
    └── DUMMY → X
└── F_SRF_RNTM
    ├── ACTVT → 1
    ├── COUNTRY → BR
    └── SRF_REPCAT → BR_SPED_PCO
```

### 4301. `/TMF/REINF`

```
T-Code: /TMF/REINF
└── /TMF/REINF
    ├── /TMF/EMPR → (empty — maintain in PFCG)
    ├── /TMF/FILI → (empty — maintain in PFCG)
    ├── /TMF/R_ACT → (empty — maintain in PFCG)
    ├── /TMF/R_EVT → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 4302. `/TMF/REINF_PRE_PROC`

```
T-Code: /TMF/REINF_PRE_PROC
└── /TMF/REIPP
    ├── /TMF/EMPR → (empty — maintain in PFCG)
    ├── /TMF/FILI → (empty — maintain in PFCG)
    ├── /TMF/R_EVT → (empty — maintain in PFCG)
    └── ACTVT → 3
```

### 4303. `/TMF/REINF_TABCONTR`

```
T-Code: /TMF/REINF_TABCONTR
└── /TMF/REINF
    ├── /TMF/EMPR → (empty — maintain in PFCG)
    ├── /TMF/FILI → (empty — maintain in PFCG)
    ├── /TMF/R_ACT → (empty — maintain in PFCG)
    ├── /TMF/R_EVT → R-1000
    └── ACTVT → 3
```

### 4304. `/TMF/REINF_TABPROC`

```
T-Code: /TMF/REINF_TABPROC
└── /TMF/REINF
    ├── /TMF/EMPR → (empty — maintain in PFCG)
    ├── /TMF/FILI → (empty — maintain in PFCG)
    ├── /TMF/R_ACT → (empty — maintain in PFCG)
    ├── /TMF/R_EVT → R-1070
    └── ACTVT → 3
```

### 4305. `/TMF/SHEET_GEN`

```
T-Code: /TMF/SHEET_GEN
└── /TMF/SHAEX
    ├── ACTVT → 2
    └── SPEDREPORT → (empty — maintain in PFCG)
```

### 4306. `/TMF/SHEET_GEN_ESO`

```
T-Code: /TMF/SHEET_GEN_ESO
└── /TMF/M_SH
    └── DUMMY → X
```

### 4307. `/TMF/SYS_INF`

```
T-Code: /TMF/SYS_INF
└── S_TABU_NAM
    ├── ACTVT → 3
    └── TABLE → /TMF/D_SYS_INF
```

### 4308. `/TMF/TRACE_LOG`

```
T-Code: /TMF/TRACE_LOG
└── S_TABU_NAM
    ├── ACTVT → 2, 3
    └── TABLE → /TMF/D_TRACE_LOG
```

## `/VSO/` — SAP Vehicle Scheduling & Optimization
> T-codes: **21**


### 4309. `/VSO/06000109`

```
T-Code: /VSO/06000109
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/R_V_REL
```

### 4310. `/VSO/06000110`

```
T-Code: /VSO/06000110
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/B_FLEET_V, /VSO/B_SUBVCL_V
```

### 4311. `/VSO/06000111`

```
T-Code: /VSO/06000111
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_MSG_STAT
```

### 4312. `/VSO/91000206`

```
T-Code: /VSO/91000206
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/P_V_PRTYP
```

### 4313. `/VSO/91000207`

```
T-Code: /VSO/91000207
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/P_V_F_PRTYP
```

### 4314. `/VSO/91000208`

```
T-Code: /VSO/91000208
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PLT_TP
```

### 4315. `/VSO/91000209`

```
T-Code: /VSO/91000209
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PLT_TP
```

### 4316. `/VSO/91000211`

```
T-Code: /VSO/91000211
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PROFIL
```

### 4317. `/VSO/91000212`

```
T-Code: /VSO/91000212
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PALVCL
```

### 4318. `/VSO/91000213`

```
T-Code: /VSO/91000213
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PKG
```

### 4319. `/VSO/91000214`

```
T-Code: /VSO/91000214
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PACKTYP
```

### 4320. `/VSO/91000215`

```
T-Code: /VSO/91000215
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PKG_PKTYP
```

### 4321. `/VSO/91000216`

```
T-Code: /VSO/91000216
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PKGPAL
```

### 4322. `/VSO/91000219`

```
T-Code: /VSO/91000219
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/B_SUBVCL_FT
```

### 4323. `/VSO/91000245`

```
T-Code: /VSO/91000245
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_VSO_SETUP
```

### 4324. `/VSO/91000246`

```
T-Code: /VSO/91000246
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/R_DPOINT
```

### 4325. `/VSO/91000247`

```
T-Code: /VSO/91000247
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/P_V_HU_CRT
```

### 4326. `/VSO/91000248`

```
T-Code: /VSO/91000248
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_PROF_W_S
```

### 4327. `/VSO/91000250`

```
T-Code: /VSO/91000250
└── S_TABU_NAM
    ├── ACTVT → (empty — maintain in PFCG)
    └── TABLE → /VSO/M_BAY_CAT
```

### 4328. `/VSO/M_REORG`

```
T-Code: /VSO/M_REORG
└── V_SO_TCODE
    ├── ACTVT → 65
    └── TCD → /VSO/M_REORG
```

### 4329. `/VSO/P_PICK`

```
T-Code: /VSO/P_PICK
└── L_TCODE
    └── TCD → /VSO/P_PICK
```

---
*📝 Generated from USOBT_C (SU24) — SAP S/4HANA System*